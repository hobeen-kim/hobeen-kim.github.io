import{_ as s,c as e,e as a,o as t}from"./app-dO2BZptS.js";const o={};function r(i,n){return t(),e("div",null,n[0]||(n[0]=[a(`<h1 id="컨테이너란-무엇인가" tabindex="-1"><a class="header-anchor" href="#컨테이너란-무엇인가"><span>컨테이너란 무엇인가</span></a></h1><div class="hint-container info"><p class="hint-container-title">학습 목표</p><ul><li>하이퍼바이저 기반 가상 머신과 컨테이너의 구조적 차이를 격리 수준·오버헤드 관점에서 설명할 수 있다.</li><li>리눅스 namespace(pid/net/mnt/uts/ipc/user)가 무엇을 격리하는지 각각 이해한다.</li><li>cgroup이 CPU·메모리 등 리소스를 어떻게 제한하는지, v1과 v2의 차이를 안다.</li><li>Union 파일시스템(OverlayFS)으로 이미지 레이어와 컨테이너의 쓰기 계층이 어떻게 구성되는지 설명할 수 있다.</li><li>컨테이너 격리가 VM보다 약한 이유와 그에 따른 보안 관점을 이해한다.</li></ul></div><h2 id="_1-가상화-vs-컨테이너-—-무엇을-격리하는가" tabindex="-1"><a class="header-anchor" href="#_1-가상화-vs-컨테이너-—-무엇을-격리하는가"><span>1. 가상화 vs 컨테이너 — 무엇을 격리하는가</span></a></h2><p><strong>컨테이너(container)</strong>는 &quot;별도의 작은 운영체제&quot;가 아니다. 컨테이너는 <strong>호스트 커널을 공유하면서, 프로세스가 마치 독립된 시스템 위에서 도는 것처럼 보이게 만든 격리된 프로세스</strong>다. 이 한 문장이 컨테이너의 본질이며, 이번 장의 모든 설명이 이 문장으로 수렴한다.</p><p>전통적인 <strong>하이퍼바이저 기반 가상 머신(VM)</strong>과 비교하면 차이가 분명해진다.</p><table><thead><tr><th>구분</th><th>가상 머신(VM)</th><th>컨테이너</th></tr></thead><tbody><tr><td>격리 단위</td><td>가상 하드웨어 + 게스트 OS 전체</td><td>호스트 커널을 공유하는 프로세스</td></tr><tr><td>커널</td><td>게스트마다 독립 커널</td><td>호스트 커널 1개 공유</td></tr><tr><td>부팅 시간</td><td>수십 초 (OS 부팅)</td><td>수십 ms (프로세스 시작)</td></tr><tr><td>이미지 크기</td><td>수 GB (OS 포함)</td><td>수 MB ~ 수백 MB</td></tr><tr><td>오버헤드</td><td>하이퍼바이저 + 게스트 커널</td><td>거의 없음 (네이티브 수준)</td></tr><tr><td>격리 강도</td><td>강함 (하드웨어 경계)</td><td>상대적으로 약함 (커널 공유)</td></tr></tbody></table><p>VM은 <strong>하이퍼바이저(hypervisor)</strong>(예: KVM, ESXi, Hyper-V)가 물리 하드웨어를 가상화해 각 게스트에 가상 CPU·메모리·디스크·NIC를 제공한다. 게스트는 자기만의 커널을 부팅하므로 격리가 매우 강하지만, OS를 통째로 들고 다녀야 해 무겁고 느리다.</p><p>컨테이너는 게스트 커널을 부팅하지 않는다. 호스트 커널의 기능(namespace, cgroup)만으로 &quot;격리된 것처럼&quot; 보이게 한다. 따라서 가볍고 빠르지만, <strong>커널을 공유하므로 커널 취약점이 곧 컨테이너 탈출(container escape)로 이어질 수 있다</strong>는 약점을 안고 간다.</p><pre class="mermaid">flowchart TB
    subgraph VM[&quot;가상 머신 모델&quot;]
        HW1[&quot;물리 하드웨어&quot;] --&gt; HV[&quot;하이퍼바이저&quot;]
        HV --&gt; G1[&quot;게스트 OS / 커널 A&quot;]
        HV --&gt; G2[&quot;게스트 OS / 커널 B&quot;]
        G1 --&gt; AppA[&quot;App A&quot;]
        G2 --&gt; AppB[&quot;App B&quot;]
    end
    subgraph CT[&quot;컨테이너 모델&quot;]
        HW2[&quot;물리 하드웨어&quot;] --&gt; HK[&quot;호스트 커널 (공유)&quot;]
        HK --&gt; C1[&quot;컨테이너 A (격리된 프로세스)&quot;]
        HK --&gt; C2[&quot;컨테이너 B (격리된 프로세스)&quot;]
        C1 --&gt; AppC[&quot;App A&quot;]
        C2 --&gt; AppD[&quot;App B&quot;]
    end
</pre><div class="hint-container tip"><p class="hint-container-title">VM과 컨테이너는 대립 관계가 아니다</p><p>실무에서는 둘을 함께 쓴다. 클라우드의 워커 노드는 대개 VM이고, 그 VM 안에서 컨테이너가 돈다. 멀티테넌시 보안이 중요하면 VM으로 1차 경계를, 배포 단위로는 컨테이너를 쓴다. 뒤에서 다룰 <a href="/study/kubernetes/05-container-runtime">gVisor·Kata</a> 같은 샌드박스 런타임은 이 둘의 장점을 합치려는 시도다.</p></div><h2 id="_2-리눅스-namespace-—-독립된-세계-의-착시" tabindex="-1"><a class="header-anchor" href="#_2-리눅스-namespace-—-독립된-세계-의-착시"><span>2. 리눅스 namespace — &quot;독립된 세계&quot;의 착시</span></a></h2><p>컨테이너가 &quot;독립된 시스템처럼&quot; 보이는 핵심 장치가 <strong>namespace</strong>다. namespace는 커널이 관리하는 전역 리소스(프로세스 트리, 네트워크 스택, 마운트 테이블 등)를 <strong>프로세스 그룹별로 분리된 뷰</strong>로 보여준다. 같은 커널이지만, 컨테이너 안에서 보면 &quot;나만의 시스템&quot;처럼 느껴지는 이유다.</p><p>주요 namespace는 다음과 같다. 자세한 명세는 <a href="https://man7.org/linux/man-pages/man7/namespaces.7.html" target="_blank" rel="noopener noreferrer">Linux namespaces 매뉴얼</a>을 참고한다.</p><table><thead><tr><th>namespace</th><th>격리 대상</th><th>효과</th></tr></thead><tbody><tr><td><code>pid</code></td><td>프로세스 ID 공간</td><td>컨테이너 내부에서 자기 프로세스만 보이고, 내부 PID 1이 따로 존재</td></tr><tr><td><code>net</code></td><td>네트워크 스택</td><td>독립된 인터페이스·라우팅 테이블·iptables·포트 공간</td></tr><tr><td><code>mnt</code></td><td>마운트 포인트</td><td>자기만의 파일시스템 트리(루트 <code>/</code>가 분리됨)</td></tr><tr><td><code>uts</code></td><td>호스트명·도메인명</td><td>컨테이너마다 다른 hostname 설정 가능</td></tr><tr><td><code>ipc</code></td><td>System V IPC, POSIX 메시지 큐</td><td>공유 메모리·세마포어를 격리</td></tr><tr><td><code>user</code></td><td>UID/GID 매핑</td><td>컨테이너 내부 root(UID 0)를 호스트의 비특권 UID로 매핑</td></tr></tbody></table><p>가장 직관적인 것이 <code>pid</code> namespace다. 컨테이너 안에서 <code>ps</code>를 실행하면 PID 1부터 시작하는 자기 프로세스만 보인다. 하지만 호스트에서 보면 그 프로세스는 전혀 다른 큰 PID를 가진다. 즉 <strong>같은 프로세스가 두 개의 PID를 갖는다</strong> — 컨테이너 내부의 PID와 호스트의 실제 PID.</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line"><span class="token comment"># 새 UTS·PID·mount namespace를 만들고 그 안에서 셸을 실행</span></span>
<span class="line"><span class="token function">sudo</span> unshare <span class="token parameter variable">--uts</span> <span class="token parameter variable">--pid</span> --mount-proc <span class="token parameter variable">--fork</span> <span class="token function">bash</span></span>
<span class="line"></span>
<span class="line"><span class="token comment"># 컨테이너처럼 hostname을 바꿔도 호스트에는 영향 없음</span></span>
<span class="line"><span class="token function">hostname</span> mini-container</span>
<span class="line"></span>
<span class="line"><span class="token comment"># 내부에서는 PID가 1부터 시작한다</span></span>
<span class="line"><span class="token function">ps</span> <span class="token parameter variable">-ef</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><code>user</code> namespace는 보안상 특히 중요하다. 컨테이너 내부에서 root여도, 실제 호스트에서는 비특권 사용자(예: UID 100000)로 매핑하면, 컨테이너가 탈출하더라도 호스트에서 root 권한을 얻지 못한다. 이를 <strong>rootless 컨테이너</strong>의 기반이라 부른다.</p><pre class="mermaid">sequenceDiagram
    participant App as 컨테이너 내부 프로세스
    participant NS as PID namespace
    participant Kernel as 호스트 커널
    App-&gt;&gt;NS: getpid() 호출
    NS--&gt;&gt;App: PID 1 (격리된 뷰)
    App-&gt;&gt;Kernel: 실제 스케줄링 요청
    Kernel--&gt;&gt;Kernel: 호스트 PID 24817로 관리
    Note over App,Kernel: 같은 프로세스가 내부 PID 1, 호스트 PID 24817
</pre><h2 id="_3-cgroup-—-리소스를-제한하는-손잡이" tabindex="-1"><a class="header-anchor" href="#_3-cgroup-—-리소스를-제한하는-손잡이"><span>3. cgroup — 리소스를 제한하는 손잡이</span></a></h2><p>namespace가 &quot;무엇을 볼 수 있는가&quot;를 격리한다면, <strong>cgroup(control group)</strong>은 &quot;얼마나 쓸 수 있는가&quot;를 제한한다. CPU·메모리·디스크 I/O·네트워크 대역폭·PID 개수 등을 그룹 단위로 제한·계량·우선순위 부여한다. cgroup이 없으면 컨테이너 하나가 메모리를 모두 먹어 호스트 전체를 마비시킬 수 있다.</p><p>cgroup은 두 가지 버전이 공존한다.</p><ul><li><strong>cgroup v1</strong>: 리소스 종류(cpu, memory, blkio 등)마다 별도의 계층 트리를 가진다. 컨트롤러가 분산돼 있어 설정이 복잡하다.</li><li><strong>cgroup v2</strong>: <strong>단일 통합 계층(unified hierarchy)</strong>으로 모든 컨트롤러를 한 트리에서 관리한다. 메모리·CPU 압박 지표(PSI), 더 정교한 제어를 제공한다. 최신 배포판과 쿠버네티스는 v2를 표준으로 권장한다.</li></ul><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line"><span class="token comment"># cgroup v2 환경에서 메모리 200MB로 제한하는 그룹 생성</span></span>
<span class="line"><span class="token function">sudo</span> <span class="token function">mkdir</span> /sys/fs/cgroup/demo</span>
<span class="line"><span class="token builtin class-name">echo</span> <span class="token string">&quot;200M&quot;</span> <span class="token operator">|</span> <span class="token function">sudo</span> <span class="token function">tee</span> /sys/fs/cgroup/demo/memory.max</span>
<span class="line"></span>
<span class="line"><span class="token comment"># 현재 셸을 이 그룹에 넣으면, 이후 프로세스는 200MB를 넘으면 OOM kill 된다</span></span>
<span class="line"><span class="token builtin class-name">echo</span> <span class="token variable">$$</span> <span class="token operator">|</span> <span class="token function">sudo</span> <span class="token function">tee</span> /sys/fs/cgroup/demo/cgroup.procs</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>도커는 이 동작을 명령행 플래그로 추상화한다.</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line"><span class="token comment"># CPU는 1.5코어, 메모리는 512MB로 제한</span></span>
<span class="line"><span class="token function">docker</span> run <span class="token parameter variable">--cpus</span><span class="token operator">=</span><span class="token string">&quot;1.5&quot;</span> <span class="token parameter variable">--memory</span><span class="token operator">=</span><span class="token string">&quot;512m&quot;</span> nginx</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p>쿠버네티스의 <code>resources.requests</code>/<code>resources.limits</code>도 결국 노드에서 cgroup으로 변환되어 적용된다. cgroup 명세는 <a href="https://man7.org/linux/man-pages/man7/cgroups.7.html" target="_blank" rel="noopener noreferrer">cgroups 매뉴얼</a>에서, 도커 적용 방식은 <a href="https://docs.docker.com/engine/containers/resource_constraints/" target="_blank" rel="noopener noreferrer">Runtime resource constraints</a>에서 확인할 수 있다.</p><div class="hint-container warning"><p class="hint-container-title">limit과 OOM</p><p>메모리 limit을 넘은 컨테이너는 커널의 OOM killer에 의해 강제 종료된다(<code>OOMKilled</code>). limit을 너무 빡빡하게 잡으면 JVM·Node 같은 런타임이 예고 없이 죽는다. 반대로 limit을 안 잡으면 노이지 네이버(noisy neighbor) 문제로 노드 전체가 흔들린다. 적정값 산정은 [리소스 관리] 챕터에서 다룬다.</p></div><h2 id="_4-union-파일시스템과-이미지-레이어" tabindex="-1"><a class="header-anchor" href="#_4-union-파일시스템과-이미지-레이어"><span>4. Union 파일시스템과 이미지 레이어</span></a></h2><p>컨테이너 이미지가 가볍고 빠르게 공유되는 비밀은 <strong>Union 파일시스템</strong>, 그중에서도 리눅스 표준인 <strong>OverlayFS</strong>에 있다. 이미지는 여러 개의 <strong>읽기 전용 레이어(read-only layer)</strong>가 쌓인 구조이고, 컨테이너를 실행하면 그 위에 <strong>쓰기 가능한 얇은 레이어(writable layer)</strong>가 한 장 올라간다.</p><pre class="mermaid">flowchart TB
    L0[&quot;베이스 레이어: ubuntu rootfs (RO)&quot;] --&gt; L1[&quot;레이어 1: apt 패키지 설치 (RO)&quot;]
    L1 --&gt; L2[&quot;레이어 2: 앱 바이너리 COPY (RO)&quot;]
    L2 --&gt; L3[&quot;레이어 3: 설정 파일 COPY (RO)&quot;]
    L3 --&gt; RW[&quot;컨테이너 쓰기 레이어 (RW, container별)&quot;]
    RW --&gt; View[&quot;병합된 뷰 = 컨테이너가 보는 / 파일시스템&quot;]
</pre><p>OverlayFS는 여러 디렉터리를 겹쳐(union) 하나의 뷰로 보여준다.</p><ul><li><strong>lowerdir</strong>: 읽기 전용 이미지 레이어들 (아래에서 위로 쌓임)</li><li><strong>upperdir</strong>: 컨테이너가 쓰는 가변 레이어</li><li><strong>merged</strong>: 사용자가 실제로 보는 통합된 파일시스템</li></ul><p>핵심은 <strong>Copy-on-Write(CoW)</strong>다. 컨테이너가 읽기 전용 레이어의 파일을 <strong>수정</strong>하면, 그 파일이 upperdir로 복사된 뒤 수정된다. 원본 레이어는 그대로 남아 다른 컨테이너와 안전하게 공유된다. 그래서 같은 이미지로 컨테이너 100개를 띄워도 이미지 레이어는 디스크에 한 번만 저장된다.</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line"><span class="token comment"># OverlayFS를 수동으로 마운트해 동작을 직접 확인</span></span>
<span class="line"><span class="token function">mkdir</span> <span class="token parameter variable">-p</span> lower upper work merged</span>
<span class="line"><span class="token builtin class-name">echo</span> <span class="token string">&quot;from image&quot;</span> <span class="token operator">&gt;</span> lower/file.txt</span>
<span class="line"></span>
<span class="line"><span class="token function">sudo</span> <span class="token function">mount</span> <span class="token parameter variable">-t</span> overlay overlay <span class="token punctuation">\\</span></span>
<span class="line">  <span class="token parameter variable">-o</span> <span class="token assign-left variable">lowerdir</span><span class="token operator">=</span>lower,upperdir<span class="token operator">=</span>upper,workdir<span class="token operator">=</span>work merged</span>
<span class="line"></span>
<span class="line"><span class="token comment"># merged에서 수정하면 원본(lower)은 그대로, upper에 복사본이 생긴다</span></span>
<span class="line"><span class="token builtin class-name">echo</span> <span class="token string">&quot;modified&quot;</span> <span class="token operator">&gt;&gt;</span> merged/file.txt</span>
<span class="line"><span class="token function">ls</span> upper/   <span class="token comment"># file.txt (Copy-on-Write 결과)</span></span>
<span class="line"><span class="token function">cat</span> lower/file.txt   <span class="token comment"># 원본은 &quot;from image&quot; 그대로</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>이 레이어 구조 덕분에 이미지는 <strong>증분(delta)</strong>만 주고받으면 된다. <code>docker pull</code> 시 이미 가진 레이어는 건너뛰고 새 레이어만 받는 이유다. 도커의 스토리지 드라이버 동작은 <a href="https://docs.docker.com/engine/storage/drivers/" target="_blank" rel="noopener noreferrer">About storage drivers</a>에서 자세히 다룬다.</p><h2 id="_5-컨테이너-격리의-한계와-보안-관점" tabindex="-1"><a class="header-anchor" href="#_5-컨테이너-격리의-한계와-보안-관점"><span>5. 컨테이너 격리의 한계와 보안 관점</span></a></h2><p>컨테이너는 강력하지만 <strong>VM 수준의 격리가 아니다</strong>. 모든 컨테이너가 <strong>같은 호스트 커널</strong>을 공유하기 때문에, 다음과 같은 한계가 있다.</p><ul><li><strong>커널 공유 위험</strong>: 커널에 권한 상승 취약점이 있으면, 한 컨테이너에서 호스트 전체를 장악할 수 있다(container escape). VM이라면 게스트 커널이 뚫려도 하이퍼바이저 경계가 한 겹 더 있다.</li><li><strong>특권 컨테이너의 위험</strong>: <code>--privileged</code>로 실행하면 거의 모든 커널 capability와 디바이스 접근을 얻어, 사실상 호스트와 동급 권한이 된다. 운영 환경에서는 피해야 한다.</li><li><strong>공유 자원을 통한 누수</strong>: cgroup 설정이 허술하면 사이드 채널·자원 고갈 공격에 노출될 수 있다.</li></ul><p>이를 완화하는 표준 방어선들이 있다.</p><pre class="mermaid">stateDiagram-v2
    [*] --&gt; Default
    Default --&gt; Hardened: 보안 강화 적용
    Hardened --&gt; Sandboxed: 더 강한 격리
    Default: 기본 컨테이너&lt;br&gt;(root, 모든 capability)
    Hardened: 강화 컨테이너&lt;br&gt;non-root + capability drop&lt;br&gt;seccomp + read-only rootfs
    Sandboxed: 샌드박스 런타임&lt;br&gt;gVisor / Kata&lt;br&gt;커널 경계 추가
    Sandboxed --&gt; [*]
</pre><p>실무에서 권장되는 최소 방어 조합은 다음과 같다.</p><ul><li><strong>non-root 실행</strong>: 컨테이너 내부에서도 root를 쓰지 않는다(<code>USER</code> 지정, user namespace 활용).</li><li><strong>capability drop</strong>: <code>--cap-drop=ALL</code> 후 꼭 필요한 capability만 추가한다.</li><li><strong>seccomp 프로파일</strong>: 위험한 시스템 콜을 커널 진입 전에 차단한다.</li><li><strong>read-only rootfs</strong>: 루트 파일시스템을 읽기 전용으로 마운트해 침해 시 변조를 막는다.</li></ul><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line"><span class="token comment"># 권한을 최소화한 컨테이너 실행 예</span></span>
<span class="line"><span class="token function">docker</span> run <span class="token punctuation">\\</span></span>
<span class="line">  <span class="token parameter variable">--user</span> <span class="token number">1000</span>:1000 <span class="token punctuation">\\</span></span>
<span class="line">  --cap-drop<span class="token operator">=</span>ALL <span class="token punctuation">\\</span></span>
<span class="line">  --security-opt<span class="token operator">=</span>no-new-privileges <span class="token punctuation">\\</span></span>
<span class="line">  --read-only <span class="token punctuation">\\</span></span>
<span class="line">  nginx</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>이 주제들은 쿠버네티스의 <a href="/study/kubernetes/35-pod-security">Pod 보안과 seccomp</a> 챕터에서 SecurityContext·Pod Security Standards와 함께 다시 깊이 다룬다.</p><div class="hint-container tip"><p class="hint-container-title">핵심 정리</p><ul><li>컨테이너는 호스트 커널을 공유하는 <strong>격리된 프로세스</strong>일 뿐, 별도 OS가 아니다. 그래서 가볍지만 격리는 VM보다 약하다.</li><li><strong>namespace</strong>는 &quot;무엇을 볼 수 있는가&quot;를, <strong>cgroup</strong>은 &quot;얼마나 쓸 수 있는가&quot;를 격리·제한한다. 이 둘이 컨테이너의 양대 기둥이다.</li><li>이미지는 <strong>OverlayFS</strong> 위에 읽기 전용 레이어를 쌓고, 컨테이너는 그 위에 Copy-on-Write 쓰기 레이어를 얹어 디스크와 네트워크를 절약한다.</li><li>커널 공유라는 본질적 한계 때문에 non-root·capability drop·seccomp·샌드박스 런타임 같은 다층 방어가 필요하다.</li></ul></div><h2 id="다음-챕터" tabindex="-1"><a class="header-anchor" href="#다음-챕터"><span>다음 챕터</span></a></h2><p>컨테이너의 원리를 이해했으니, 이제 이 원리를 사용 가능한 도구로 묶은 <a href="/study/kubernetes/02-docker-basics">Docker 기초</a>로 넘어가 이미지·컨테이너·레지스트리와 핵심 명령어를 다룬다.</p>`,47)]))}const p=s(o,[["render",r],["__file","01-container-basics.html.vue"]]),c=JSON.parse('{"path":"/study/kubernetes/01-container-basics.html","title":"컨테이너란 무엇인가","lang":"en-US","frontmatter":{"title":"컨테이너란 무엇인가","description":"VM과 컨테이너의 차이, 리눅스 namespace·cgroup·UnionFS로 본 격리의 원리와 보안 한계까지 컨테이너의 본질을 파고든다.","date":"2026-06-15T00:00:00.000Z","tags":["Container","Linux","namespace","cgroup","OverlayFS","Security"],"prev":"/study/kubernetes/","next":"/study/kubernetes/02-docker-basics"},"headers":[{"level":1,"title":"컨테이너란 무엇인가","slug":"컨테이너란-무엇인가","link":"#컨테이너란-무엇인가","children":[{"level":2,"title":"1. 가상화 vs 컨테이너 — 무엇을 격리하는가","slug":"_1-가상화-vs-컨테이너-—-무엇을-격리하는가","link":"#_1-가상화-vs-컨테이너-—-무엇을-격리하는가","children":[]},{"level":2,"title":"2. 리눅스 namespace — \\"독립된 세계\\"의 착시","slug":"_2-리눅스-namespace-—-독립된-세계-의-착시","link":"#_2-리눅스-namespace-—-독립된-세계-의-착시","children":[]},{"level":2,"title":"3. cgroup — 리소스를 제한하는 손잡이","slug":"_3-cgroup-—-리소스를-제한하는-손잡이","link":"#_3-cgroup-—-리소스를-제한하는-손잡이","children":[]},{"level":2,"title":"4. Union 파일시스템과 이미지 레이어","slug":"_4-union-파일시스템과-이미지-레이어","link":"#_4-union-파일시스템과-이미지-레이어","children":[]},{"level":2,"title":"5. 컨테이너 격리의 한계와 보안 관점","slug":"_5-컨테이너-격리의-한계와-보안-관점","link":"#_5-컨테이너-격리의-한계와-보안-관점","children":[]},{"level":2,"title":"다음 챕터","slug":"다음-챕터","link":"#다음-챕터","children":[]}]}],"git":{},"filePathRelative":"_study/kubernetes/01-container-basics.md"}');export{p as comp,c as data};
