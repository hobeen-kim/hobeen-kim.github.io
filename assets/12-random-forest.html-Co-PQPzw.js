import{_ as s,c as a,f as e,o as t}from"./app-g4YXskd6.js";const o="/images/study-supervised-learning/12-double-randomness-light.png",p="/images/study-supervised-learning/12-double-randomness-dark.png",r="/images/study-supervised-learning/12-oob-evaluation-light.png",i="/images/study-supervised-learning/12-oob-evaluation-dark.png",l={};function c(u,n){return t(),a("div",null,n[0]||(n[0]=[e('<h1 id="랜덤-포레스트" tabindex="-1"><a class="header-anchor" href="#랜덤-포레스트"><span>랜덤 포레스트</span></a></h1><div class="hint-container info"><p class="hint-container-title">학습 목표</p><ul><li>랜덤 포레스트가 배깅에 피처 무작위성을 더한 이중 무작위화임을 이해하고, 그것이 왜 분산을 더 줄이는지 설명한다.</li><li>OOB(out-of-bag) 평가가 어떻게 공짜 검증셋을 제공하는지 안다.</li><li>트리 수는 많을수록 안정적이며 과적합의 원인이 아니라는 점(수익 체감)을 이해한다.</li><li><code>n_estimators</code>, <code>max_features</code>, <code>max_depth</code> 등 주요 하이퍼파라미터의 역할을 안다.</li><li>impurity 기반 feature importance의 고카디널리티 편향을 인지하고, 대안은 <a href="/study/supervised-learning/18-model-interpretation">18장</a>에서 다룸을 예고받는다.</li><li>랜덤 포레스트가 병렬 학습에 유리한 이유와 실무에서 강한 상황을 파악한다.</li></ul></div><h2 id="_1-배깅에-무작위성을-하나-더" tabindex="-1"><a class="header-anchor" href="#_1-배깅에-무작위성을-하나-더"><span>1. 배깅에 무작위성을 하나 더</span></a></h2><p>랜덤 포레스트(random forest)는 한마디로 <strong>배깅한 결정 트리에 피처 무작위성을 더한</strong> 모델이다. <a href="/study/supervised-learning/11-ensemble-basics">11장</a>에서 배깅은 부트스트랩 표본으로 여러 트리를 병렬 학습해 분산을 줄인다고 했다. 랜덤 포레스트는 여기에 한 겹의 무작위화를 추가한다 — <strong>각 노드에서 분할 기준을 고를 때, 전체 피처가 아니라 무작위로 뽑은 일부 피처(max_features)만 후보로 본다.</strong></p><p><img src="'+o+'" alt="랜덤 포레스트의 이중 무작위화 — 원본 데이터에서 트리마다 서로 다른 부트스트랩 행 표본을 뽑고(첫 번째 무작위), 각 트리는 노드마다 서로 다른 피처 부분집합만 후보로 보아 분할하며(두 번째 무작위), 세 트리의 예측을 다수결/평균으로 집계해 최종 예측을 낸다"><img src="'+p+'" alt="랜덤 포레스트의 이중 무작위화 — 원본 데이터에서 트리마다 서로 다른 부트스트랩 행 표본을 뽑고(첫 번째 무작위), 각 트리는 노드마다 서로 다른 피처 부분집합만 후보로 보아 분할하며(두 번째 무작위), 세 트리의 예측을 다수결/평균으로 집계해 최종 예측을 낸다"></p><p>집계 방식은 문제 유형에 따라 갈린다. <strong>분류</strong>에서는 각 트리가 한 표씩 던져 다수결로 정하거나(hard voting), 트리별 클래스 확률을 평균한다(soft voting). <strong>회귀</strong>에서는 트리들의 예측값을 산술 평균한다. 어느 쪽이든 여러 독립적 예측을 모아 하나로 합치는 <a href="/study/supervised-learning/11-ensemble-basics">11장</a>의 집계 그대로다.</p><p>왜 굳이 후보 피처를 제한할까. 순수 배깅만 하면 트리들이 <strong>서로 너무 비슷해지는</strong> 문제가 있기 때문이다. 데이터에 유난히 강한 피처가 하나 있으면, 모든 트리가 뿌리 근처에서 그 피처로 먼저 분할한다. 부트스트랩으로 표본을 바꿔도 첫 분할이 다들 똑같으니 트리들의 상관이 높고, 상관이 높은 예측을 평균 내면 분산 감소 효과가 약해진다.</p><p>노드마다 후보 피처를 무작위로 제한하면 이 강한 피처가 후보에서 빠지는 노드가 생기고, 그러면 다른 피처로 분할하는 트리가 나온다. 결과적으로 트리들이 <strong>서로 다르게(덜 상관되게)</strong> 자라 <a href="/study/supervised-learning/11-ensemble-basics">11장</a>에서 강조한 다양성이 커진다. 개별 트리 하나하나는 최선의 피처를 못 쓰니 약간 나빠지지만, 상관이 낮아진 덕에 <strong>평균의 분산은 더 크게 줄어든다.</strong></p><p>두 겹의 무작위를 정리하면 이렇다.</p><ul><li><strong>행(row) 무작위</strong> — 트리마다 서로 다른 부트스트랩 표본으로 학습한다(배깅에서 이어받은 층).</li><li><strong>열(column) 무작위</strong> — 각 노드에서 후보 피처를 <code>max_features</code>개만 무작위로 본다(랜덤 포레스트가 추가한 층).</li></ul><p>이 이중 무작위화(부트스트랩 + 피처 무작위)가 순수 배깅보다 트리 간 상관을 더 강하게 끊는 것이 랜덤 포레스트의 핵심이다. 무작위성을 한 걸음 더 밀어붙인 변형으로 <strong>익스트림 랜덤 트리(Extra-Trees)</strong>가 있는데, 분할 지점(threshold)까지 무작위로 뽑아 트리를 더 다양하게 만든다. 편향이 약간 늘지만 분산이 더 줄고 학습이 빨라, sklearn의 <code>ExtraTreesClassifier</code>로 대안 삼아 볼 만하다.</p><h2 id="_2-oob-—-공짜-검증셋" tabindex="-1"><a class="header-anchor" href="#_2-oob-—-공짜-검증셋"><span>2. OOB — 공짜 검증셋</span></a></h2><p>부트스트랩은 복원 추출이라 한 트리를 학습할 때 원본 샘플 중 일부는 한 번도 뽑히지 않는다. 표본 크기를 원본과 같게 잡으면, 뽑히지 않을 확률은 대략 $(1 - 1/n)^n \\approx 1/e \\approx 0.37$이다. 즉 <strong>각 트리마다 약 37%의 샘플은 학습에 쓰이지 않는다.</strong> 이렇게 빠진 샘플을 그 트리의 out-of-bag(OOB) 샘플이라 한다.</p><p><img src="'+r+'" alt="OOB 평가의 원리 — 한 그루의 부트스트랩 복원 추출에서 뽑힌 in-bag 샘플과 한 번도 안 뽑힌 OOB 샘플(약 37%)이 갈리고, 어떤 샘플의 OOB 예측은 그 샘플을 학습에 쓰지 않은 트리들만 모아 계산하므로 별도 검증셋 없이 공짜로 일반화 성능을 추정한다"><img src="'+i+`" alt="OOB 평가의 원리 — 한 그루의 부트스트랩 복원 추출에서 뽑힌 in-bag 샘플과 한 번도 안 뽑힌 OOB 샘플(약 37%)이 갈리고, 어떤 샘플의 OOB 예측은 그 샘플을 학습에 쓰지 않은 트리들만 모아 계산하므로 별도 검증셋 없이 공짜로 일반화 성능을 추정한다"></p><p>여기서 공짜 검증셋이 나온다. 어떤 샘플 하나를 놓고 보면, 전체 트리 중 <strong>그 샘플을 학습에 쓰지 않은 트리들</strong>(그 샘플이 OOB였던 트리들)만 모아 예측하면 된다. 그 트리들에게 이 샘플은 사실상 처음 보는 데이터이므로, 이 예측을 모으면 별도의 검증셋을 떼어 두지 않고도 일반화 성능을 추정할 수 있다. 이것이 OOB 점수(OOB score)다.</p><p>교차 검증과 비슷한 효과를 내지만 추가 학습이 필요 없다 — 어차피 만든 트리들을 재활용하기 때문이다. 실제로 OOB 오차는 대체로 k-fold 교차 검증 추정치와 비슷하게 나오며, 데이터가 적어 검증셋을 떼기 아까울 때 특히 유용하다. sklearn에서는 <code>oob_score=True</code> 한 줄이면 된다.</p><div class="language-python line-numbers-mode" data-highlighter="prismjs" data-ext="py" data-title="py"><pre><code><span class="line"><span class="token keyword">from</span> sklearn<span class="token punctuation">.</span>datasets <span class="token keyword">import</span> load_breast_cancer</span>
<span class="line"><span class="token keyword">from</span> sklearn<span class="token punctuation">.</span>ensemble <span class="token keyword">import</span> RandomForestClassifier</span>
<span class="line"></span>
<span class="line">X<span class="token punctuation">,</span> y <span class="token operator">=</span> load_breast_cancer<span class="token punctuation">(</span>return_X_y<span class="token operator">=</span><span class="token boolean">True</span><span class="token punctuation">)</span></span>
<span class="line"></span>
<span class="line">rf <span class="token operator">=</span> RandomForestClassifier<span class="token punctuation">(</span></span>
<span class="line">    n_estimators<span class="token operator">=</span><span class="token number">300</span><span class="token punctuation">,</span> oob_score<span class="token operator">=</span><span class="token boolean">True</span><span class="token punctuation">,</span> random_state<span class="token operator">=</span><span class="token number">0</span><span class="token punctuation">,</span> n_jobs<span class="token operator">=</span><span class="token operator">-</span><span class="token number">1</span><span class="token punctuation">,</span></span>
<span class="line"><span class="token punctuation">)</span></span>
<span class="line">rf<span class="token punctuation">.</span>fit<span class="token punctuation">(</span>X<span class="token punctuation">,</span> y<span class="token punctuation">)</span></span>
<span class="line"><span class="token keyword">print</span><span class="token punctuation">(</span><span class="token string">&quot;OOB 점수:&quot;</span><span class="token punctuation">,</span> <span class="token builtin">round</span><span class="token punctuation">(</span>rf<span class="token punctuation">.</span>oob_score_<span class="token punctuation">,</span> <span class="token number">3</span><span class="token punctuation">)</span><span class="token punctuation">)</span>  <span class="token comment"># 별도 검증셋 없이 얻은 추정치</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>단, OOB는 편리한 <strong>추정치</strong>일 뿐 정식 교차 검증을 완전히 대체하지는 않는다. OOB 점수는 각 샘플을 그것이 OOB였던 트리들(전체의 약 37%)로만 예측하므로, 트리 수가 적으면 표본이 부족해 추정이 불안정하고, 시계열처럼 분할 방식 자체가 중요한 문제에는 맞지 않는다(<a href="/study/supervised-learning/04-cross-validation-tuning">04장</a>). 그래서 빠른 감을 잡을 때는 OOB로 충분하되, 하이퍼파라미터 튜닝의 최종 판단은 교차 검증으로 확정하는 것이 안전하다.</p><h2 id="_3-트리-수는-많을수록-안정적이다" tabindex="-1"><a class="header-anchor" href="#_3-트리-수는-많을수록-안정적이다"><span>3. 트리 수는 많을수록 안정적이다</span></a></h2><p>랜덤 포레스트에서 트리 수(<code>n_estimators</code>)는 흔한 오해의 대상이다. &quot;트리를 많이 쌓으면 과적합하지 않나?&quot;라는 질문이 자주 나오는데, 랜덤 포레스트에서는 <strong>그렇지 않다.</strong></p><p>이유는 <a href="/study/supervised-learning/11-ensemble-basics">11장 3절</a>에서 정리한 배깅의 성질에 있다. 트리를 더하는 것은 독립적인 예측을 하나 더 평균에 넣는 일이라, 분산을 더 줄일 뿐 편향을 건드리지 않는다. 트리를 아무리 많이 넣어도 모델이 훈련 데이터에 더 들러붙지는 않는다. 다만 분산 감소 효과는 <strong>수익이 체감</strong>한다 — 트리 100개에서 200개로 늘릴 때의 개선이 10개에서 100개로 늘릴 때보다 훨씬 작다.</p><p>그래서 <code>n_estimators</code>는 &quot;과적합을 걱정하며 조절할 하이퍼파라미터&quot;가 아니라 <strong>&quot;계산 비용이 허락하는 만큼 크게, 성능이 평평해질 때까지&quot;</strong> 두는 값이다. 보통 수백 개면 충분하고, 더 늘려도 성능은 정체된 채 학습·예측 시간만 길어진다.</p><p>충분한지 확인하려면 OOB 점수나 검증 점수를 트리 수에 따라 그려 보면 된다. 곡선이 평평해지는 지점이 실용적인 하한이다. 이 점은 <a href="/study/supervised-learning/13-gradient-boosting">13장</a>의 부스팅과 정반대다. 부스팅에서는 트리 수가 과적합을 좌우하는 핵심 손잡이여서, 곡선이 평평해지는 게 아니라 다시 올라가는 지점을 찾아 멈춰야 한다.</p><h2 id="_4-주요-하이퍼파라미터" tabindex="-1"><a class="header-anchor" href="#_4-주요-하이퍼파라미터"><span>4. 주요 하이퍼파라미터</span></a></h2><p>랜덤 포레스트에서 실제로 성능을 좌우하는 손잡이는 다음과 같다.</p><ul><li><strong><code>n_estimators</code></strong> — 트리 수. 3절대로 클수록 안정적이며 계산 비용과의 타협점을 찾는다. 기본값보다 크게 두는 편이 낫다.</li><li><strong><code>max_features</code></strong> — 각 분할에서 후보로 볼 피처 수. 랜덤 포레스트의 핵심 손잡이다. 작을수록 트리 간 상관이 낮아져 다양성이 커지지만 개별 트리는 약해진다. 분류는 보통 <code>&quot;sqrt&quot;</code>(피처 수의 제곱근), 회귀는 <code>1.0</code>이나 <code>1/3</code> 근방이 기본값으로 좋다.</li><li><strong><code>max_depth</code> / <code>min_samples_leaf</code></strong> — 개별 트리의 성장 제한. 랜덤 포레스트는 보통 트리를 <strong>깊게(제한 없이) 자라게</strong> 둔다. 배깅이 분산을 잡아 주므로 개별 트리는 저편향 상태로 강하게 두는 것이 <a href="/study/supervised-learning/11-ensemble-basics">11장 3절</a>의 원칙에 맞다. 다만 데이터가 매우 잡음이 많으면 <code>min_samples_leaf</code>를 키워 각 트리를 약간 규제하기도 한다.</li><li><strong><code>bootstrap</code></strong> — 부트스트랩 표본 사용 여부. 끄면 각 트리가 전체 데이터로 학습되어 트리 간 다양성이 행(row) 차원에서는 사라진다. 기본값 <code>True</code>를 유지하는 것이 보통이며, OOB 평가도 이때만 가능하다.</li><li><strong><code>class_weight</code></strong> — 클래스 불균형이 있을 때 소수 클래스에 가중치를 주어 분할·예측이 다수 클래스로 쏠리는 것을 완화한다(<code>&quot;balanced&quot;</code> 등). 불균형 처리 전반은 <a href="/study/supervised-learning/17-imbalanced-calibration">17장</a>에서 다룬다.</li></ul><p>튜닝 우선순위는 대체로 <code>max_features</code> → 트리 규제(<code>max_depth</code>, <code>min_samples_leaf</code>) 순이고, <code>n_estimators</code>는 튜닝이라기보다 충분히 크게 고정하는 값에 가깝다. 랜덤 포레스트는 대체로 기본값이 견고해, 이 손잡이들만 소폭 조정해도 대부분의 이득을 얻는다.</p><h2 id="_5-feature-importance와-그-함정" tabindex="-1"><a class="header-anchor" href="#_5-feature-importance와-그-함정"><span>5. Feature importance와 그 함정</span></a></h2><p>랜덤 포레스트는 <strong>어떤 피처가 중요한가</strong>를 손쉽게 내놓는다. sklearn의 <code>feature_importances_</code>는 각 피처가 트리들의 분할에서 불순도(impurity, <a href="/study/supervised-learning/10-decision-tree">10장</a>)를 얼마나 줄였는지 합산·정규화한 값이다. 이를 impurity 기반(또는 MDI, mean decrease in impurity) 중요도라 한다.</p><div class="language-python line-numbers-mode" data-highlighter="prismjs" data-ext="py" data-title="py"><pre><code><span class="line"><span class="token keyword">import</span> numpy <span class="token keyword">as</span> np</span>
<span class="line"></span>
<span class="line">rf<span class="token punctuation">.</span>fit<span class="token punctuation">(</span>X<span class="token punctuation">,</span> y<span class="token punctuation">)</span></span>
<span class="line">order <span class="token operator">=</span> np<span class="token punctuation">.</span>argsort<span class="token punctuation">(</span>rf<span class="token punctuation">.</span>feature_importances_<span class="token punctuation">)</span><span class="token punctuation">[</span><span class="token punctuation">:</span><span class="token punctuation">:</span><span class="token operator">-</span><span class="token number">1</span><span class="token punctuation">]</span></span>
<span class="line"><span class="token keyword">for</span> i <span class="token keyword">in</span> order<span class="token punctuation">[</span><span class="token punctuation">:</span><span class="token number">5</span><span class="token punctuation">]</span><span class="token punctuation">:</span></span>
<span class="line">    <span class="token keyword">print</span><span class="token punctuation">(</span><span class="token string-interpolation"><span class="token string">f&quot;feature </span><span class="token interpolation"><span class="token punctuation">{</span>i<span class="token punctuation">:</span><span class="token format-spec">&gt;2</span><span class="token punctuation">}</span></span><span class="token string">: </span><span class="token interpolation"><span class="token punctuation">{</span>rf<span class="token punctuation">.</span>feature_importances_<span class="token punctuation">[</span>i<span class="token punctuation">]</span><span class="token punctuation">:</span><span class="token format-spec">.3f</span><span class="token punctuation">}</span></span><span class="token string">&quot;</span></span><span class="token punctuation">)</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><code>feature_importances_</code>는 모든 피처에 대해 합이 1이 되도록 정규화되어 있어, 각 값은 &quot;전체 불순도 감소 중 이 피처의 몫&quot;으로 읽으면 된다. 절대값보다 피처 간 상대적 크기와 순위를 보는 것이 실용적이다.</p><p>편리하지만 <strong>함정</strong>이 있다. impurity 기반 중요도는 <strong>고카디널리티(high-cardinality) 피처, 즉 고유값이 많은 피처를 과대평가</strong>한다. 값의 종류가 많은 연속형이나 ID에 가까운 피처는 분할 후보가 많아, 순전히 우연으로라도 불순도를 조금 줄이는 분할을 찾기 쉽다. 그래서 실제로는 별 정보가 없는데도 중요도가 부풀려진다. 극단적으로, 무작위로 채운 고유 ID 컬럼을 넣어도 impurity 중요도가 꽤 높게 나올 수 있다.</p><p>또한 이 중요도는 <strong>훈련 데이터 기준</strong>으로 계산되어, 훈련 성능에는 기여하지만 일반화에는 도움이 안 되는 피처까지 높게 평가할 수 있다. 상관이 높은 피처들끼리 중요도가 임의로 나뉘어 분산되는 문제도 있다.</p><p>이 편향을 넘어서는 대안 — 데이터를 섞어 성능 하락을 재는 <strong>permutation importance</strong>와, 예측에 대한 각 피처의 기여를 게임 이론으로 배분하는 <strong>SHAP</strong> — 은 <a href="/study/supervised-learning/18-model-interpretation">18장 모델 해석</a>에서 본격적으로 다룬다.</p><p>지금 기억할 실전 수칙은 이렇다. <code>feature_importances_</code>는 &quot;어느 피처가 트리 분할에 많이 쓰였나&quot;를 보여 줄 뿐 인과나 일반화 기여를 보장하지 않으니 그대로 믿지 말 것, 특히 고유값이 많은 연속형·ID성 피처가 상위에 있으면 의심할 것, 그리고 중요한 판단이 걸린 해석은 permutation·SHAP 같은 검증 방법으로 교차 확인할 것.</p><h2 id="_6-병렬-학습과-랜덤-포레스트가-강한-상황" tabindex="-1"><a class="header-anchor" href="#_6-병렬-학습과-랜덤-포레스트가-강한-상황"><span>6. 병렬 학습과 랜덤 포레스트가 강한 상황</span></a></h2><p>랜덤 포레스트의 트리들은 <a href="/study/supervised-learning/11-ensemble-basics">11장</a>에서 본 배깅의 성질 그대로 <strong>서로 독립적으로 학습</strong>된다. 트리 A를 만들 때 트리 B의 결과가 전혀 필요 없다. 덕분에 트리들을 여러 CPU 코어에 나눠 <strong>병렬로</strong> 학습할 수 있고, sklearn에서는 <code>n_jobs=-1</code>로 모든 코어를 쓴다. 이는 트리를 순차적으로 쌓아야 해서 본질적으로 병렬화가 어려운 <a href="/study/supervised-learning/13-gradient-boosting">13장</a>의 부스팅과 대비되는 실무적 장점이다.</p><p>랜덤 포레스트가 특히 강한 상황은 다음과 같다.</p><ul><li><strong>튜닝 없이 좋은 베이스라인이 필요할 때</strong> — 기본값만으로도 대체로 견고한 성능을 내, 새 문제의 첫 모델로 안전하다.</li><li><strong>피처 스케일이 제각각이고 전처리를 최소화하고 싶을 때</strong> — 트리 기반이라 스케일링·정규화가 필요 없고 이상치에도 비교적 둔감하다.</li><li><strong>과적합을 크게 걱정하지 않고 안정적인 예측을 원할 때</strong> — 분산이 낮아 결과가 재현성 있게 안정적이다.</li><li><strong>빠른 탐색적 분석이 필요할 때</strong> — OOB 점수와 feature importance를 별도 작업 없이 곧바로 내주므로, 어떤 피처가 신호를 담는지 초기 감을 잡기에 좋다(단 5절의 편향은 유념).</li></ul><p>반면 <strong>최고의 정확도</strong>가 목표라면 표 형태 데이터에서는 대개 부스팅 계열(GBDT, <a href="/study/supervised-learning/13-gradient-boosting">13장</a>~<a href="/study/supervised-learning/14-xgboost-lightgbm-catboost">14장</a>)이 한 수 위다. 랜덤 포레스트는 &quot;빠르고 튼튼한 기본기&quot;, 부스팅은 &quot;잘 튜닝하면 더 높은 천장&quot;이라는 역할 분담으로 이해하면 좋다.</p><p>반대로 랜덤 포레스트가 <strong>약한 상황</strong>도 알아 두면 좋다. 회귀에서 <strong>외삽(extrapolation)</strong>이 필요할 때, 즉 훈련 데이터의 값 범위를 벗어난 입력을 예측해야 할 때 트리 기반 모델은 훈련에서 본 잎(leaf)의 평균값밖에 낼 수 없어 범위 밖으로 뻗어 나가지 못한다(선형 모델과 대비되는 근본적 한계다). 또 트리를 수백 그루 저장하므로 <strong>모델 크기와 예측 지연</strong>이 단일 트리나 선형 모델보다 크고, 텍스트처럼 <strong>매우 고차원·희소한</strong> 데이터에서는 선형 모델·나이브 베이즈(<a href="/study/supervised-learning/09-naive-bayes">09장</a>)가 더 잘 맞는 경우가 많다.</p><div class="hint-container tip"><p class="hint-container-title">핵심 정리</p><ul><li>랜덤 포레스트는 배깅에 <strong>피처 무작위성(max_features)</strong>을 더한 이중 무작위화로, 트리 간 상관을 낮춰 분산을 더 크게 줄인다.</li><li>OOB 샘플(부트스트랩에서 빠진 약 37%)로 별도 검증셋 없이 일반화 성능을 <strong>공짜로</strong> 추정할 수 있다.</li><li>트리 수는 많을수록 안정적이며 과적합의 원인이 아니다 — 수익이 체감하니 비용이 허락하는 만큼 크게 둔다.</li><li>핵심 손잡이는 <code>max_features</code>이고, 개별 트리는 배깅이 분산을 잡아 주므로 보통 깊게 둔다.</li><li>impurity 기반 <code>feature_importances_</code>는 고카디널리티 피처를 과대평가하니 주의하고, permutation·SHAP는 <a href="/study/supervised-learning/18-model-interpretation">18장</a>에서 다룬다.</li><li>트리가 독립적이라 병렬 학습이 쉽고, 튜닝 없이 견고한 베이스라인이 필요할 때 강하다.</li></ul></div><h2 id="다음-챕터" tabindex="-1"><a class="header-anchor" href="#다음-챕터"><span>다음 챕터</span></a></h2><p>랜덤 포레스트가 분산을 줄이는 배깅의 대표라면, 이제 편향을 줄이는 부스팅으로 넘어간다. <a href="/study/supervised-learning/13-gradient-boosting">그래디언트 부스팅 원리</a>에서는 AdaBoost의 가중치에서 잔차 학습으로의 전환, 함수 공간 경사하강이라는 관점, 그리고 학습률과 트리 수의 트레이드오프를 다룬다.</p>`,44)]))}const g=s(l,[["render",c],["__file","12-random-forest.html.vue"]]),m=JSON.parse('{"path":"/study/supervised-learning/12-random-forest.html","title":"랜덤 포레스트","lang":"en-US","frontmatter":{"title":"랜덤 포레스트","description":"배깅에 피처 무작위성(max_features)을 더한 이중 무작위화가 트리들의 상관을 끊어 분산을 더 줄이는 원리를 설명하고, 부트스트랩에서 빠진 샘플로 공짜 검증셋을 얻는 OOB 평가, 트리 수는 많을수록 안정적이라는 성질과 주요 하이퍼파라미터, impurity 기반 feature importance의 고카디널리티 편향, 병렬 학습 가능성과 랜덤 포레스트가 강한 상황을 정리한다.","date":"2026-07-03T00:00:00.000Z","tags":["Machine Learning","Supervised Learning","Random Forest","Bagging","OOB","Feature Importance"],"prev":"/study/supervised-learning/11-ensemble-basics","next":"/study/supervised-learning/13-gradient-boosting"},"headers":[{"level":1,"title":"랜덤 포레스트","slug":"랜덤-포레스트","link":"#랜덤-포레스트","children":[{"level":2,"title":"1. 배깅에 무작위성을 하나 더","slug":"_1-배깅에-무작위성을-하나-더","link":"#_1-배깅에-무작위성을-하나-더","children":[]},{"level":2,"title":"2. OOB — 공짜 검증셋","slug":"_2-oob-—-공짜-검증셋","link":"#_2-oob-—-공짜-검증셋","children":[]},{"level":2,"title":"3. 트리 수는 많을수록 안정적이다","slug":"_3-트리-수는-많을수록-안정적이다","link":"#_3-트리-수는-많을수록-안정적이다","children":[]},{"level":2,"title":"4. 주요 하이퍼파라미터","slug":"_4-주요-하이퍼파라미터","link":"#_4-주요-하이퍼파라미터","children":[]},{"level":2,"title":"5. Feature importance와 그 함정","slug":"_5-feature-importance와-그-함정","link":"#_5-feature-importance와-그-함정","children":[]},{"level":2,"title":"6. 병렬 학습과 랜덤 포레스트가 강한 상황","slug":"_6-병렬-학습과-랜덤-포레스트가-강한-상황","link":"#_6-병렬-학습과-랜덤-포레스트가-강한-상황","children":[]},{"level":2,"title":"다음 챕터","slug":"다음-챕터","link":"#다음-챕터","children":[]}]}],"git":{},"filePathRelative":"_study/supervised-learning/12-random-forest.md"}');export{g as comp,m as data};
