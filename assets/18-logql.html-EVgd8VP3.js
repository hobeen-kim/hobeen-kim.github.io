import{_ as t,c as o,e as a,o as l}from"./app-WxxISwYx.js";const s="/images/study-observability/18-parser-pipeline-light.png",n="/images/study-observability/18-parser-pipeline-dark.png",i="/images/study-observability/18-performance-stages-light.png",d="/images/study-observability/18-performance-stages-dark.png",r={};function c(p,e){return l(),o("div",null,e[0]||(e[0]=[a(`<h1 id="logql" tabindex="-1"><a class="header-anchor" href="#logql"><span>LogQL</span></a></h1><div class="hint-container info"><p class="hint-container-title">학습 목표</p><ul><li>log stream selector로 대상 스트림을 좁히고 라인 필터(<code>|=</code>, <code>!=</code>, <code>|~</code>, <code>!~</code>)로 텍스트를 거르는 방법을 익힌다.</li><li>logfmt, json, pattern, regexp 네 파서의 사용 시점과 차이를 구분한다.</li><li>파서 이후 label filter·line_format·label_format으로 구조화된 필드를 다루는 방법을 안다.</li><li>rate, count_over_time, unwrap 기반 메트릭 쿼리로 로그에서 시계열을 뽑아내는 방법을 이해한다.</li><li>라벨 우선 필터링과 파서 비용을 고려한 LogQL 성능 튜닝 원칙을 적용할 수 있다.</li></ul></div><h2 id="_1-log-stream-selector-—-라벨-매처" tabindex="-1"><a class="header-anchor" href="#_1-log-stream-selector-—-라벨-매처"><span>1. Log Stream Selector — 라벨 매처</span></a></h2><p>모든 LogQL 쿼리는 <strong>스트림 셀렉터</strong>로 시작한다. 중괄호 안에 라벨 매처를 나열해 대상 스트림 집합을 지정한다.</p><div class="language-logql line-numbers-mode" data-highlighter="prismjs" data-ext="logql" data-title="logql"><pre><code><span class="line">{app=&quot;checkout&quot;, env=&quot;prod&quot;}</span>
<span class="line">{app=&quot;checkout&quot;, env=~&quot;prod|staging&quot;}</span>
<span class="line">{app=&quot;checkout&quot;, env!=&quot;dev&quot;}</span>
<span class="line">{app=~&quot;checkout.*&quot;}</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>지원 연산자는 Prometheus 라벨 매처와 동일하다.</p><table><thead><tr><th>연산자</th><th>의미</th></tr></thead><tbody><tr><td><code>=</code></td><td>정확히 일치</td></tr><tr><td><code>!=</code></td><td>불일치</td></tr><tr><td><code>=~</code></td><td>정규식 일치</td></tr><tr><td><code>!~</code></td><td>정규식 불일치</td></tr></tbody></table><p>스트림 셀렉터는 인덱스만으로 해석되므로 비용이 거의 없다. 반대로 셀렉터가 스트림을 충분히 좁히지 못하면(예: 라벨 하나만 지정하고 값이 넓게 매칭됨) 뒤 단계에서 스캔해야 할 청크가 폭증한다. LogQL 성능의 첫 단추는 항상 스트림 셀렉터다.</p><h2 id="_2-라인-필터-—" tabindex="-1"><a class="header-anchor" href="#_2-라인-필터-—"><span>2. 라인 필터 — <code>|=</code>, <code>!=</code>, <code>|~</code>, <code>!~</code></span></a></h2><p>스트림 셀렉터 뒤에 라인 필터를 체이닝해 로그 라인 텍스트 자체를 거른다.</p><div class="language-logql line-numbers-mode" data-highlighter="prismjs" data-ext="logql" data-title="logql"><pre><code><span class="line">{app=&quot;checkout&quot;} |= &quot;timeout&quot;</span>
<span class="line">{app=&quot;checkout&quot;} != &quot;healthcheck&quot;</span>
<span class="line">{app=&quot;checkout&quot;} |~ &quot;error|panic|fatal&quot;</span>
<span class="line">{app=&quot;checkout&quot;} !~ &quot;DEBUG.*&quot;</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><table><thead><tr><th>연산자</th><th>의미</th></tr></thead><tbody><tr><td><code>|=</code></td><td>문자열 포함</td></tr><tr><td><code>!=</code></td><td>문자열 미포함</td></tr><tr><td><code>|~</code></td><td>정규식 매치</td></tr><tr><td><code>!~</code></td><td>정규식 미매치</td></tr></tbody></table><p>라인 필터는 여러 개를 체이닝할 수 있고(<code>{app=&quot;checkout&quot;} |= &quot;error&quot; != &quot;timeout&quot; |~ &quot;5\\\\d\\\\d&quot;</code>), 왼쪽부터 순서대로 적용된다. <code>|=</code>/<code>!=</code>는 정규식 엔진을 거치지 않는 단순 바이트 비교라 <code>|~</code>/<code>!~</code>보다 훨씬 빠르다 — 가능하면 문자열 포함 필터를 먼저 배치해 후보 라인 수를 줄인 뒤 정규식 필터를 적용하는 순서가 유리하다.</p><h2 id="_3-파서-—-logfmt-json-pattern-regexp" tabindex="-1"><a class="header-anchor" href="#_3-파서-—-logfmt-json-pattern-regexp"><span>3. 파서 — logfmt, json, pattern, regexp</span></a></h2><p>라인 필터로 좁힌 뒤에는 파서로 로그 라인을 구조화된 필드(라벨처럼 다룰 수 있는 추출 값)로 분해한다.</p><p><img src="`+s+'" alt="LogQL 파이프라인 흐름 — 스트림 셀렉터로 대상을 좁히고 라인 필터로 텍스트를 거른 뒤 파서(json 등)로 status·path·duration 같은 구조화된 필드를 추출"><img src="'+n+`" alt="LogQL 파이프라인 흐름 — 스트림 셀렉터로 대상을 좁히고 라인 필터로 텍스트를 거른 뒤 파서(json 등)로 status·path·duration 같은 구조화된 필드를 추출"></p><ul><li><strong>logfmt</strong>: <code>key=value key2=&quot;value 2&quot;</code> 형식 로그를 파싱한다. <code>| logfmt</code>만 쓰면 전체 필드를 추출하고, <code>| logfmt status, path</code>처럼 필드를 지정할 수도 있다.</li><li><strong>json</strong>: JSON 로그 라인을 파싱한다. 중첩 필드는 <code>| json request_method=&quot;request.method&quot;</code>처럼 JMESPath 유사 표기로 평탄화해 뽑는다.</li><li><strong>pattern</strong>: 사람이 읽기 쉬운 캡처 문법(<code>&lt;필드명&gt;</code>)으로 정형화되지 않은 텍스트를 파싱한다. 정규식보다 가독성이 좋다.</li><li><strong>regexp</strong>: 명명 캡처 그룹(<code>(?P&lt;name&gt;...)</code>)을 쓰는 정규식 파서. 가장 유연하지만 표현식 작성·유지보수 비용이 가장 크다.</li></ul><div class="language-logql line-numbers-mode" data-highlighter="prismjs" data-ext="logql" data-title="logql"><pre><code><span class="line">{app=&quot;checkout&quot;} | logfmt</span>
<span class="line">{app=&quot;checkout&quot;} | json</span>
<span class="line">{app=&quot;nginx&quot;}    | pattern &quot;&lt;ip&gt; - - [&lt;_&gt;] \\&quot;&lt;method&gt; &lt;path&gt; &lt;_&gt;\\&quot; &lt;status&gt; &lt;size&gt;&quot;</span>
<span class="line">{app=&quot;nginx&quot;}    | regexp &quot;(?P&lt;method&gt;\\\\w+) (?P&lt;path&gt;\\\\S+) HTTP.*(?P&lt;status&gt;\\\\d{3})&quot;</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>파서가 실패하면(예: JSON이 아닌 라인에 <code>| json</code> 적용) 해당 라인에 <code>__error__</code> 라벨이 붙는다. <code>| json | __error__=&quot;&quot;</code>처럼 필터링해 파싱 실패 라인을 제외하거나, 반대로 <code>__error__!=&quot;&quot;</code>로 파싱 실패 라인만 골라 원인을 조사할 수 있다.</p><h2 id="_4-label-filter-·-line-format-·-label-format" tabindex="-1"><a class="header-anchor" href="#_4-label-filter-·-line-format-·-label-format"><span>4. Label Filter · line_format · label_format</span></a></h2><p>파서로 필드를 추출한 다음에는 세 가지 후처리 연산을 쓴다.</p><p><strong>label filter</strong>는 추출된 필드 값으로 라인을 다시 거른다. 문자열뿐 아니라 숫자·기간 비교도 지원한다.</p><div class="language-logql line-numbers-mode" data-highlighter="prismjs" data-ext="logql" data-title="logql"><pre><code><span class="line">{app=&quot;checkout&quot;} | json | status_code &gt;= 500</span>
<span class="line">{app=&quot;checkout&quot;} | json | duration &gt; 5s</span>
<span class="line">{app=&quot;checkout&quot;} | logfmt | level=&quot;error&quot; | status_code &gt;= 500</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><strong>line_format</strong>은 Go 템플릿 문법으로 출력 라인 자체를 재구성한다. 대시보드에 필요한 필드만 뽑아 읽기 쉬운 형태로 바꿀 때 쓴다.</p><div class="language-logql line-numbers-mode" data-highlighter="prismjs" data-ext="logql" data-title="logql"><pre><code><span class="line">{app=&quot;checkout&quot;} | json | line_format &quot;{{.status_code}} {{.method}} {{.path}} ({{.duration}})&quot;</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p><strong>label_format</strong>은 추출된 필드나 기존 라벨의 이름을 바꾸거나 템플릿으로 값을 재계산해 결과 라벨로 승격한다. 뒤에 나올 메트릭 쿼리에서 <code>by (...)</code> 집계 기준으로 쓰려면 원하는 필드를 라벨 형태로 만들어둬야 한다.</p><div class="language-logql line-numbers-mode" data-highlighter="prismjs" data-ext="logql" data-title="logql"><pre><code><span class="line">{app=&quot;checkout&quot;} | json | label_format http_status=status_code</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><div class="hint-container warning"><p class="hint-container-title">label_format으로 만든 라벨도 카디널리티에 영향을 준다</p><p>label_format이 만드는 라벨은 인덱스에는 들어가지 않지만, 메트릭 쿼리의 <code>by ()</code> 집계 기준으로 쓰이면 결과 시계열 수를 그만큼 늘린다. <code>request_id</code>처럼 고카디널리티 필드를 그대로 <code>by ()</code>에 넣으면 집계 결과 자체가 폭발한다.</p></div><h2 id="_5-메트릭-쿼리-—-rate-count-over-time-unwrap-집계" tabindex="-1"><a class="header-anchor" href="#_5-메트릭-쿼리-—-rate-count-over-time-unwrap-집계"><span>5. 메트릭 쿼리 — rate, count_over_time, unwrap, 집계</span></a></h2><p>LogQL은 로그 스트림에서 바로 시계열을 뽑아내는 <strong>메트릭 쿼리</strong>를 지원한다. 로그 라인의 존재 빈도를 세는 방식과, 라인에서 숫자 값을 추출해 집계하는 방식 두 가지가 있다.</p><p>라인 개수 기반 함수는 range vector(<code>[5m]</code> 같은 구간)를 받는다.</p><div class="language-logql line-numbers-mode" data-highlighter="prismjs" data-ext="logql" data-title="logql"><pre><code><span class="line">count_over_time({app=&quot;checkout&quot;} |= &quot;error&quot; [5m])</span>
<span class="line">rate({app=&quot;checkout&quot;} |= &quot;error&quot; [5m])</span>
<span class="line">bytes_over_time({app=&quot;checkout&quot;} [5m])</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><code>count_over_time</code>은 구간 내 매칭된 라인 수, <code>rate</code>는 초당 라인 발생률(구간 내 라인 수를 초로 나눈 값)이다. Prometheus의 <code>rate()</code>와 이름은 같지만 카운터 증가율이 아니라 로그 라인 발생률이라는 점이 다르다.</p><p>숫자 값 자체를 집계하려면 <strong>unwrap</strong>으로 필드를 숫자 값으로 변환한 뒤 range vector 함수를 적용한다.</p><div class="language-logql line-numbers-mode" data-highlighter="prismjs" data-ext="logql" data-title="logql"><pre><code><span class="line">sum by (path) (</span>
<span class="line">  sum_over_time({app=&quot;checkout&quot;} | json | unwrap duration_ms [5m])</span>
<span class="line">)</span>
<span class="line"></span>
<span class="line">quantile_over_time(0.99,</span>
<span class="line">  {app=&quot;checkout&quot;} | json | unwrap duration_ms [5m]</span>
<span class="line">) by (path)</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><code>unwrap</code>은 라인 수가 아니라 추출된 필드 값(예: 응답 시간 <code>duration_ms</code>)을 시계열 샘플로 취급한다. 이후 <code>sum_over_time</code>, <code>avg_over_time</code>, <code>max_over_time</code>, <code>min_over_time</code>, <code>quantile_over_time</code>, <code>rate</code> 등을 적용해 실제 수치 기반 집계를 계산한다.</p><p>바깥쪽에는 Prometheus와 동일한 집계 연산자(<code>sum</code>, <code>avg</code>, <code>max</code>, <code>min</code>, <code>count</code>, <code>topk</code>, <code>bottomk</code>)를 <code>by (...)</code>/<code>without (...)</code>와 함께 쓸 수 있다.</p><div class="language-logql line-numbers-mode" data-highlighter="prismjs" data-ext="logql" data-title="logql"><pre><code><span class="line">topk(5,</span>
<span class="line">  sum by (path) (rate({app=&quot;checkout&quot;} | json | status_code &gt;= 500 [5m]))</span>
<span class="line">)</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h2 id="_6-성능-—-라벨로-먼저-좁히기-파서-비용" tabindex="-1"><a class="header-anchor" href="#_6-성능-—-라벨로-먼저-좁히기-파서-비용"><span>6. 성능 — 라벨로 먼저 좁히기, 파서 비용</span></a></h2><p>LogQL 성능은 파이프라인 앞단에서 데이터를 얼마나 줄이느냐에 좌우된다. 각 단계의 비용이 크게 다르기 때문이다.</p><p><img src="`+i+'" alt="LogQL 성능 단계별 비용 — 스트림 셀렉터(매우 저렴) → 라인 필터 |= !=(저렴) → 라인 필터 |~ !~(중간) → 파서(비쌈) → 라벨 필터·집계 순으로 갈수록 비용이 커지므로 저렴한 단계로 먼저 후보를 줄인다"><img src="'+d+'" alt="LogQL 성능 단계별 비용 — 스트림 셀렉터(매우 저렴) → 라인 필터 |= !=(저렴) → 라인 필터 |~ !~(중간) → 파서(비쌈) → 라벨 필터·집계 순으로 갈수록 비용이 커지므로 저렴한 단계로 먼저 후보를 줄인다"></p><p>원칙은 단순하다.</p><ul><li><strong>스트림 셀렉터를 최대한 좁힌다.</strong> 라벨로 스트림 수를 줄이지 못하면 뒤 단계가 다뤄야 할 청크 자체가 많아 어떤 최적화도 소용없다.</li><li><strong>파서보다 라인 필터를 먼저 적용한다.</strong> <code>{app=&quot;checkout&quot;} | json | message |= &quot;timeout&quot;</code>보다 <code>{app=&quot;checkout&quot;} |= &quot;timeout&quot; | json</code>이 훨씬 빠르다 — 전자는 모든 라인을 JSON 파싱한 뒤 거르지만, 후자는 문자열 필터로 후보를 줄인 뒤에만 파싱한다.</li><li><strong>필요한 필드만 파싱한다.</strong> <code>| logfmt</code>로 전체 필드를 뽑기보다 <code>| logfmt status, path</code>처럼 필요한 필드만 지정하면 파싱 비용이 줄어든다.</li><li><strong>정규식은 최후 수단으로.</strong> <code>pattern</code>이나 <code>logfmt</code>/<code>json</code>으로 표현 가능하면 <code>regexp</code>보다 그쪽이 낫다. 정규식은 백트래킹 패턴에 따라 비용이 예측하기 어렵게 커질 수 있다.</li></ul><h2 id="다음-챕터" tabindex="-1"><a class="header-anchor" href="#다음-챕터"><span>다음 챕터</span></a></h2><p>LogQL로 쿼리하는 법을 익혔다면, 이제 그 로그가 애초에 어떻게 수집·라벨링돼 Loki까지 도달하는지 볼 차례다. 다음 챕터 <a href="/study/observability/19-log-pipeline-storage">로그 파이프라인과 스토리지</a>에서는 Alloy/Promtail 수집 에이전트, 파이프라인 스테이지, 구조화 메타데이터, 라벨 설계, 오브젝트 스토리지 운영을 다룬다.</p>',45)]))}const g=t(r,[["render",c],["__file","18-logql.html.vue"]]),m=JSON.parse('{"path":"/study/observability/18-logql.html","title":"LogQL","lang":"en-US","frontmatter":{"title":"LogQL","description":"Loki 쿼리 언어 LogQL의 스트림 셀렉터, 라인 필터, 파서(logfmt/json/pattern/regexp), 라벨 필터·포맷, 메트릭 쿼리와 성능 최적화 원칙을 다룬다.","date":"2026-07-02T00:00:00.000Z","tags":["Loki","LogQL"],"prev":"/study/observability/17-loki-read-write-path","next":"/study/observability/19-log-pipeline-storage"},"headers":[{"level":1,"title":"LogQL","slug":"logql","link":"#logql","children":[{"level":2,"title":"1. Log Stream Selector — 라벨 매처","slug":"_1-log-stream-selector-—-라벨-매처","link":"#_1-log-stream-selector-—-라벨-매처","children":[]},{"level":2,"title":"2. 라인 필터 — |=, !=, |~, !~","slug":"_2-라인-필터-—","link":"#_2-라인-필터-—","children":[]},{"level":2,"title":"3. 파서 — logfmt, json, pattern, regexp","slug":"_3-파서-—-logfmt-json-pattern-regexp","link":"#_3-파서-—-logfmt-json-pattern-regexp","children":[]},{"level":2,"title":"4. Label Filter · line_format · label_format","slug":"_4-label-filter-·-line-format-·-label-format","link":"#_4-label-filter-·-line-format-·-label-format","children":[]},{"level":2,"title":"5. 메트릭 쿼리 — rate, count_over_time, unwrap, 집계","slug":"_5-메트릭-쿼리-—-rate-count-over-time-unwrap-집계","link":"#_5-메트릭-쿼리-—-rate-count-over-time-unwrap-집계","children":[]},{"level":2,"title":"6. 성능 — 라벨로 먼저 좁히기, 파서 비용","slug":"_6-성능-—-라벨로-먼저-좁히기-파서-비용","link":"#_6-성능-—-라벨로-먼저-좁히기-파서-비용","children":[]},{"level":2,"title":"다음 챕터","slug":"다음-챕터","link":"#다음-챕터","children":[]}]}],"git":{},"filePathRelative":"_study/observability/18-logql.md"}');export{g as comp,m as data};
