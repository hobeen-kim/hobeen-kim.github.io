import{_ as s,c as o,e as n,o as t}from"./app-J_vrPzUq.js";const a="/images/study-android-internals/07-boot-chain-light.png",i="/images/study-android-internals/07-boot-chain-dark.png",d="/images/study-android-internals/07-first-second-init-light.png",l="/images/study-android-internals/07-first-second-init-dark.png",r={};function c(p,e){return t(),o("div",null,e[0]||(e[0]=[n('<h1 id="ch7-부트로더와-커널-부팅" tabindex="-1"><a class="header-anchor" href="#ch7-부트로더와-커널-부팅"><span>CH7. 부트로더와 커널 부팅</span></a></h1><h2 id="학습-목표" tabindex="-1"><a class="header-anchor" href="#학습-목표"><span>학습 목표</span></a></h2><ul><li>Boot ROM(PBL)부터 init까지 이어지는 부트체인과 신뢰 체인(chain of trust)의 구조를 이해한다.</li><li>Little Kernel(LK/ABL)의 역할, fastboot 모드 진입, boot image 로드 과정을 안다.</li><li>부트로더 락/언락이 AVB·사용자 데이터에 미치는 영향을 파악한다.</li><li>커널 cmdline(<code>androidboot.*</code>)과 DTB 전달, initramfs와 first/second-stage init 분리를 이해한다.</li><li>Boot Control HAL의 A/B 슬롯 관리와 부팅 실패 시 롤백 메커니즘을 안다.</li></ul><h2 id="_1-부트체인-개관-—-전원부터-init까지" tabindex="-1"><a class="header-anchor" href="#_1-부트체인-개관-—-전원부터-init까지"><span>1. 부트체인 개관 — 전원부터 init까지</span></a></h2><p>전원 버튼을 누른 순간부터 안드로이드 홈 화면이 뜨기까지, 실행 주체는 여러 번 바뀐다. 각 단계는 자기 다음에 실행할 코드의 서명을 검증한 뒤에만 제어를 넘긴다. 이 사슬을 <strong>신뢰 체인(chain of trust)</strong>이라 부르고, 그 시작점은 SoC 안에 물리적으로 구워진 불변 코드다.</p><p><img src="'+a+'" alt="Boot ROM부터 2차 부트로더, Android Bootloader, 커널, init까지 각 단계가 다음 단계 서명을 검증하며 실행을 넘기는 부트체인"><img src="'+i+`" alt="Boot ROM부터 2차 부트로더, Android Bootloader, 커널, init까지 각 단계가 다음 단계 서명을 검증하며 실행을 넘기는 부트체인"></p><ul><li><strong>Boot ROM(PBL, Primary Bootloader)</strong>은 SoC 제조 시점에 마스크 ROM으로 박힌 코드다. 수정도 삭제도 불가능하며, 이것이 신뢰 체인의 <strong>루트 오브 트러스트(hardware root of trust)</strong>다. PBL은 SoC 내부 eFuse에 구워진 공개키 해시로 다음 단계(2차 부트로더)의 서명을 검증한다. 검증에 실패하면 그 자리에서 부팅을 멈춘다.</li><li><strong>2차 부트로더</strong>는 SoC 벤더마다 이름이 다르다. Qualcomm은 XBL(eXtensible Bootloader, UEFI 기반), Samsung Exynos는 S-BOOT, MediaTek은 Preloader라 부른다. 이 단계에서 DRAM 초기화, 클럭·전원 설정, 저장장치(UFS/eMMC) 드라이버 초기화 같은 저수준 하드웨어 준비가 끝난다. Qualcomm 계열은 XBL이 다시 여러 하위 이미지(ABL, keymaster TA 등)를 로드하는 다단계 구조다.</li><li><strong>Android Bootloader(ABL 또는 LK)</strong>가 안드로이드 특화 로직을 담당한다. <code>boot</code> 파티션에서 커널+ramdisk를 읽어 메모리에 올리고, cmdline을 구성하고, fastboot 모드나 recovery 진입을 처리한다. 뒤 2절에서 자세히 다룬다.</li><li><strong>Linux Kernel</strong>이 압축 해제돼 실행되면 메모리 관리·스케줄러·드라이버가 올라오고, 마지막에 initramfs 안의 <code>/init</code>을 PID 1로 실행한다.</li><li><strong>init(PID 1)</strong>이 유저스페이스의 첫 프로세스로, 여기서부터 <a href="/study/android-internals/08-init-zygote">CH8</a>의 세계가 시작된다.</li></ul><div class="hint-container info"><p class="hint-container-title">신뢰 체인이 끊기면</p><p>서명 검증에 실패하면 단계에 따라 동작이 다르다. PBL/XBL 단계 실패는 대개 벽돌(brick)에 가깝고, ABL이 boot 이미지 검증에 실패하면 기기 상태에 따라 부팅 거부, 경고 후 진행(언락 상태), recovery 진입 중 하나로 갈린다. 이 검증 로직이 곧 <a href="/study/android-internals/16-selinux-avb">CH16</a>에서 다룰 Android Verified Boot(AVB)다.</p></div><h2 id="_2-little-kernel-lk-과-android-bootloader" tabindex="-1"><a class="header-anchor" href="#_2-little-kernel-lk-과-android-bootloader"><span>2. Little Kernel(LK)과 Android Bootloader</span></a></h2><p><code>LK</code>(Little Kernel)는 원래 임베디드용 경량 커널/부트로더 프로젝트다. Qualcomm이 이를 안드로이드 부트로더로 채택하면서 사실상 표준처럼 퍼졌고, 오늘날 ABL(Android Boot Loader, UEFI 앱 형태로 빌드된 LK)로 진화했다. 이름이 무엇이든 이 단계가 하는 일은 공통적이다.</p><p><strong>부트 이미지 로드.</strong> <code>boot</code> 파티션의 이미지는 단순 커널 바이너리가 아니라 정해진 헤더 포맷을 가진 컨테이너다. <code>boot.img</code>는 헤더 + 커널 + ramdisk(+ 예전엔 second-stage 정보)로 구성되고, 헤더에 각 섹션의 크기와 로드 주소, cmdline이 들어 있다. 부트 이미지 버전은 안드로이드 릴리스마다 진화해왔다.</p><ul><li><strong>v0~v1</strong>: 커널 + ramdisk가 <code>boot</code>에 함께.</li><li><strong>v2</strong>: DTB/DTBO 관련 필드 추가.</li><li><strong>v3~v4</strong>(Android 11+): ramdisk가 <strong>generic ramdisk</strong>(<code>boot</code>)와 <strong>vendor ramdisk</strong>(<code>vendor_boot</code>)로 분리됐다. GKI(Generic Kernel Image) 정책에 따라 Google이 서명한 공통 커널과 벤더 모듈을 분리하기 위한 구조다. <a href="/study/android-internals/14-building-aosp">CH14</a>에서 GKI를 다룬다.</li></ul><p><code>unpack_bootimg</code>, <code>mkbootimg</code> 같은 AOSP 도구로 이미지를 뜯어보고 다시 조립할 수 있다.</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line"><span class="token comment"># boot.img를 뜯어 헤더·커널·ramdisk를 분리</span></span>
<span class="line">unpack_bootimg <span class="token parameter variable">--boot_img</span> boot.img <span class="token parameter variable">--out</span> ./out</span>
<span class="line"><span class="token comment"># 헤더 필드(버전·cmdline·페이지 크기 등) 확인</span></span>
<span class="line">unpack_bootimg <span class="token parameter variable">--boot_img</span> boot.img <span class="token parameter variable">--format</span><span class="token operator">=</span>mkbootimg</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><strong>fastboot 모드 진입.</strong> ABL은 부팅 초기에 특정 조건을 만족하면 커널로 넘어가는 대신 fastboot 프로토콜 데몬으로 들어간다. 진입 조건은 보통 세 가지다 — 볼륨 다운 등 물리 키 조합, <code>boot</code> 파티션이 없거나 손상됨, 또는 실행 중인 안드로이드에서 <code>adb reboot bootloader</code>로 진입. fastboot 모드에서는 USB로 파티션을 직접 플래시하거나 이미지를 임시로 부팅할 수 있다.</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line">fastboot devices                 <span class="token comment"># 연결된 fastboot 기기 확인</span></span>
<span class="line">fastboot getvar all              <span class="token comment"># 부트로더 변수 덤프 (슬롯·언락 상태 등)</span></span>
<span class="line">fastboot boot custom_boot.img    <span class="token comment"># 플래시 없이 이미지로 1회 부팅</span></span>
<span class="line">fastboot flash boot boot.img     <span class="token comment"># boot 파티션에 실제 기록</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><div class="hint-container info"><p class="hint-container-title">bootloader vs fastbootd</p><p>전통적 <code>fastboot</code>는 부트로더 안에서 도는 저수준 모드다. Android 10부터는 <code>fastbootd</code>가 추가됐는데, 이건 <strong>userspace fastboot</strong>로 recovery/ramdisk 안에서 실행된다. 동적 파티션(super) 안의 논리 파티션은 부트로더가 이해하지 못하므로, <code>fastboot flash system</code> 같은 명령은 <code>fastboot reboot fastboot</code>로 fastbootd에 진입한 뒤에 처리한다. 동적 파티션은 <a href="/study/android-internals/05-storage-management">CH5</a>에서 다뤘다.</p></div><h2 id="_3-부트로더-락-언락" tabindex="-1"><a class="header-anchor" href="#_3-부트로더-락-언락"><span>3. 부트로더 락/언락</span></a></h2><p>출고 상태의 부트로더는 <strong>락(locked)</strong>돼 있다. 락 상태에서는 AVB 검증에 통과한, 즉 OEM이 서명한 <code>boot</code>/<code>vbmeta</code> 이미지만 실행된다. 커스텀 이미지를 올리려면 언락이 필요하다.</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line">fastboot flashing unlock          <span class="token comment"># 언락 요청 → 기기 화면에서 물리 확인</span></span>
<span class="line">fastboot flashing lock            <span class="token comment"># 다시 락</span></span>
<span class="line">fastboot flashing get_unlock_ability   <span class="token comment"># 언락 허용 여부(1/0)</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>언락은 단순히 &quot;커스텀 이미지 허용&quot; 스위치가 아니다. <strong>보안·데이터에 세 가지 영향</strong>을 준다.</p><ul><li><strong>강제 데이터 초기화.</strong> <code>flashing unlock</code>을 실행하면 기기는 반드시 <code>/data</code>를 완전히 wipe한다. 도난당한 기기를 언락해 잠금화면을 우회하고 데이터를 빼내는 공격을 막기 위한 설계다. 이 wipe는 우회할 수 없다.</li><li><strong>AVB 상태 변경.</strong> 언락하면 부팅 시 노란색/주황색 경고 화면(<code>androidboot.verifiedbootstate</code>가 <code>orange</code>)이 뜬다. 락 + 정품 서명은 <code>green</code>, 락 + 커스텀 키는 <code>yellow</code>, 언락은 <code>orange</code>, 검증 실패는 <code>red</code>로 상태가 구분된다. 이 상태는 <a href="/study/android-internals/16-selinux-avb">CH16</a>에서 Keystore 어테스테이션에도 반영된다.</li><li><strong>롤백 보호와 무결성 신호.</strong> 언락 상태에서는 dm-verity 강제가 완화되거나 꺼질 수 있어, SafetyNet/Play Integrity 같은 무결성 판정에서 기기가 실패로 분류된다. 결제·뱅킹 앱이 언락 기기에서 동작을 거부하는 이유다.</li></ul><p>임베디드 기기를 개발할 때는 개발 단계에서 언락 상태로 커스텀 이미지를 반복 플래시하다가, 양산 시점에 자체 서명 키로 락(<code>avb_custom_key</code>)을 걸어 무결성을 확보하는 흐름을 밟는다.</p><h2 id="_4-커널-부팅-—-cmdline-dtb-그리고-init까지" tabindex="-1"><a class="header-anchor" href="#_4-커널-부팅-—-cmdline-dtb-그리고-init까지"><span>4. 커널 부팅 — cmdline, DTB, 그리고 init까지</span></a></h2><p>ABL이 커널로 제어를 넘길 때 두 가지를 함께 전달한다 — <strong>커널 cmdline</strong>과 <strong>DTB(Device Tree Blob)</strong>다.</p><p><strong>커널 cmdline</strong>에는 부팅 파라미터가 공백으로 구분돼 들어간다. 안드로이드 특화 파라미터는 <code>androidboot.</code> 접두어를 가지며, 커널이 이를 읽어 나중에 유저스페이스 프로퍼티로 승격시킨다. 실행 중인 기기에서 확인할 수 있다.</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line">adb shell <span class="token function">cat</span> /proc/cmdline</span>
<span class="line"><span class="token comment"># 예시:</span></span>
<span class="line"><span class="token comment"># androidboot.hardware=qcom androidboot.slot_suffix=_a \\</span></span>
<span class="line"><span class="token comment"># androidboot.verifiedbootstate=green androidboot.serialno=... \\</span></span>
<span class="line"><span class="token comment"># androidboot.boot_devices=soc/1d84000.ufshc console=ttyMSM0,115200</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><code>androidboot.*</code> 파라미터는 <a href="/study/android-internals/08-init-zygote">CH8</a>에서 다룰 <code>ro.boot.*</code> 프로퍼티로 변환된다. 예를 들어 <code>androidboot.hardware=qcom</code>은 <code>ro.boot.hardware=qcom</code>이 되고, 이는 다시 <code>ro.hardware</code>로 굳어져 HAL 라이브러리 이름 결정 등에 쓰인다. Android 12부터는 이 커널 cmdline 대신 <code>bootconfig</code>(별도 영역에 담긴 부팅 설정)로 옮겨가는 추세이며, <code>/proc/bootconfig</code>에서 볼 수 있다.</p><p><strong>DTB(Device Tree)</strong>는 SoC의 하드웨어 구성(메모리 맵, 인터럽트, 주변장치 노드)을 커널에 알려주는 이진 트리다. GKI 정책 아래에서 커널은 특정 보드를 몰라야 하므로, 보드별 하드웨어 정보는 코드가 아닌 데이터(DTB)로 분리돼 <code>dtb</code>/<code>dtbo</code> 파티션이나 <code>vendor_boot</code>에 담긴다. ABL이 실행 중인 보드에 맞는 DTB를 골라 커널에 넘긴다. CAN 컨트롤러 같은 <a href="/study/can/13-socketcan-basics">주변장치</a>를 SPI로 붙일 때도 이 device tree에 노드를 추가해 커널이 드라이버를 바인딩하게 만든다.</p><p>커널은 초기화를 마치면 initramfs를 루트 파일시스템(rootfs)으로 마운트하고 <code>/init</code>을 <code>execve</code>한다. 이 순간 커널의 역할은 끝나고 유저스페이스가 시작된다.</p><h2 id="_5-initramfs와-first-second-stage-init-분리" tabindex="-1"><a class="header-anchor" href="#_5-initramfs와-first-second-stage-init-분리"><span>5. initramfs와 first/second-stage init 분리</span></a></h2><p><strong>initramfs</strong>(initial RAM filesystem)는 커널이 진짜 저장장치를 마운트하기 전에 메모리 위에 올려두는 초기 루트 파일시스템이다. ramdisk 안에는 최소한의 것만 들어 있다.</p><ul><li><code>/init</code> — 정적 링크된 init 실행 파일(공유 라이브러리 없이 혼자 뜰 수 있어야 하므로).</li><li><code>*.rc</code> — first-stage에서 필요한 최소 rc 스크립트.</li><li><code>/system/etc/ramdisk/</code> 계열 fstab, AVB용 <code>vbmeta</code> 관련 정보.</li><li>부팅에 필요한 커널 모듈(<code>vendor_boot</code>의 vendor ramdisk에 위치).</li></ul><p>과거에는 initramfs가 곧 최종 루트였지만, Android 10 무렵부터 <strong>system-as-root</strong>와 함께 init이 두 단계로 뚜렷이 나뉘었다.</p><p><img src="`+d+'" alt="initramfs 안에서 도는 first-stage init이 파일시스템을 마운트하고 SELinux를 켠 뒤 execv로 second-stage init에 실행을 넘기는 구조"><img src="'+l+`" alt="initramfs 안에서 도는 first-stage init이 파일시스템을 마운트하고 SELinux를 켠 뒤 execv로 second-stage init에 실행을 넘기는 구조"></p><ul><li><strong>first-stage init</strong>은 ramdisk 안의 정적 링크 바이너리다. 하는 일은 &quot;진짜 파일시스템을 마운트할 수 있는 상태를 만드는 것&quot;에 집중돼 있다 — <code>/dev</code>·<code>/proc</code>·<code>/sys</code> 마운트, early property 초기화, fstab을 읽어 <code>/system</code>·<code>/vendor</code> 등 핵심 파티션 마운트, dm-verity(AVB) 설정, SELinux 정책 로드 후 enforcing 전환. 마지막으로 자기 자신을 <code>/system/bin/init</code>으로 다시 <code>execve</code>한다.</li><li><strong>second-stage init</strong>은 방금 마운트한 <code>/system</code> 위의 동적 링크 init이다. 여기서 <code>libbase</code>, <code>libselinux</code> 등 정상적인 공유 라이브러리를 쓸 수 있고, <code>property_service</code>를 띄우고, ueventd로 <code>/dev</code> 노드를 만들고, 본격적으로 <code>*.rc</code> 파일을 파싱해 서비스들을 기동한다.</li></ul><p>두 단계로 나눈 이유는 명확하다. 파일시스템과 SELinux가 준비되기 전에는 쓸 수 있는 자원이 극히 제한되므로, 그 최소 환경에서 돌 정적 바이너리(first-stage)와 완전한 환경에서 돌 동적 바이너리(second-stage)를 갈라놓은 것이다. 중요한 점은 <strong>execve로 이어지므로 PID는 계속 1</strong>이라는 것 — 새 프로세스를 fork하는 게 아니라 같은 프로세스가 자기 이미지만 바꿔 낀다.</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line"><span class="token comment"># 실행 중 기기에서 init이 second-stage로 넘어간 뒤의 상태</span></span>
<span class="line">adb shell <span class="token function">ls</span> <span class="token parameter variable">-l</span> /proc/1/exe     <span class="token comment"># → /system/bin/init 를 가리킴</span></span>
<span class="line">adb shell <span class="token function">cat</span> /proc/1/cmdline   <span class="token comment"># → /system/bin/init second_stage</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h2 id="_6-boot-control-hal과-a-b-슬롯-롤백" tabindex="-1"><a class="header-anchor" href="#_6-boot-control-hal과-a-b-슬롯-롤백"><span>6. Boot Control HAL과 A/B 슬롯 롤백</span></a></h2><p>현대 안드로이드 기기 대부분은 <strong>A/B(seamless) 업데이트</strong> 구조를 쓴다. <code>boot</code>, <code>system</code>, <code>vendor</code> 같은 핵심 파티션이 <code>_a</code>/<code>_b</code> 두 벌 존재하고, 한쪽(active)으로 부팅하는 동안 다른 쪽(inactive)에 OTA를 적용한다. A/B 자체는 <a href="/study/android-internals/06-images-updates">CH6</a>에서 다뤘고, 여기서는 <strong>어느 슬롯으로 부팅할지와 실패 시 롤백을 관리하는 계층</strong>인 Boot Control HAL을 본다.</p><p><strong>Boot Control HAL</strong>(과거 <code>android.hardware.boot@1.x</code> HIDL, 현재 <code>android.hardware.boot</code> AIDL)은 부트로더가 관리하는 슬롯 메타데이터를 유저스페이스에서 읽고 쓰는 인터페이스다. 슬롯마다 세 가지 상태 플래그가 있다.</p><ul><li><strong>active</strong> — 다음에 부팅을 시도할 슬롯.</li><li><strong>bootable</strong> — 부팅 가능하다고 표시된 슬롯. 남은 시도 횟수(retry count)를 가진다.</li><li><strong>successful</strong> — 부팅에 성공해 안정 상태로 표시된 슬롯.</li></ul><p><strong>롤백 시나리오</strong>는 이렇게 흐른다. OTA가 inactive 슬롯(예: <code>_b</code>)에 새 이미지를 쓰고 그 슬롯을 active로, retry count를 N(보통 7)으로 설정한다. 재부팅하면 부트로더는 <code>_b</code>로 부팅을 시도하며 retry count를 하나 줄인다. 부팅이 끝까지 성공해 안드로이드가 <code>markBootSuccessful()</code>을 호출하면 슬롯이 <code>successful</code>로 굳는다. 그러나 부팅이 계속 실패해 retry count가 0에 도달하면, 부트로더는 <code>_b</code>를 unbootable로 표시하고 <strong>이전에 successful이던 <code>_a</code> 슬롯으로 자동 롤백</strong>한다. 이 덕분에 망가진 OTA가 기기를 영구 벽돌로 만들지 않는다.</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line"><span class="token comment"># 현재 슬롯 확인</span></span>
<span class="line">adb shell getprop ro.boot.slot_suffix     <span class="token comment"># → _a 또는 _b</span></span>
<span class="line"></span>
<span class="line"><span class="token comment"># bootctl로 슬롯 메타데이터 조회·조작 (root/AOSP 빌드)</span></span>
<span class="line">adb shell bootctl get-current-slot        <span class="token comment"># 현재 부팅한 슬롯 번호</span></span>
<span class="line">adb shell bootctl get-suffix <span class="token number">1</span>            <span class="token comment"># 슬롯 1의 접미사(_b)</span></span>
<span class="line">adb shell bootctl set-active-boot-slot <span class="token number">0</span>  <span class="token comment"># 다음 부팅 슬롯을 강제 지정</span></span>
<span class="line">adb shell bootctl mark-boot-successful    <span class="token comment"># 현재 슬롯을 성공으로 표시</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><code>update_engine</code>이 OTA 적용 중에 이 HAL을 호출해 대상 슬롯을 active로 세팅하고, 부팅 성공 판정 로직이 <code>markBootSuccessful</code>을 부른다. 커스텀 임베디드 기기를 A/B로 구성하면, 현장에서 잘못된 업데이트가 배포돼도 기기가 스스로 이전 버전으로 돌아오므로 원격 복구 부담이 크게 준다.</p><div class="hint-container warning"><p class="hint-container-title">virtual A/B와 non-A/B</p><p>저가·초기 기기 중에는 슬롯이 하나뿐인 non-A/B 구조(별도 <code>recovery</code> 파티션 사용)도 있다. 반대로 Android 11+ 다수 기기는 <strong>Virtual A/B</strong>를 쓰는데, 물리적으로 파티션을 두 벌 두는 대신 snapshot(dm-snapshot)으로 A/B 효과를 낸다. 어느 구조든 Boot Control HAL 인터페이스 자체는 동일하게 유지돼, 상위 <code>update_engine</code> 로직은 바뀌지 않는다.</p></div><div class="hint-container tip"><p class="hint-container-title">핵심 정리</p><ul><li>부팅은 Boot ROM(PBL, 하드웨어 루트 오브 트러스트) → 2차 부트로더(XBL/S-BOOT/Preloader) → ABL/LK → 커널 → init 순서로, 각 단계가 다음 단계 서명을 검증하는 신뢰 체인이다.</li><li>ABL/LK는 <code>boot</code> 이미지를 로드하고 cmdline·DTB를 커널에 넘기며, 조건에 따라 fastboot/fastbootd 모드로 진입한다.</li><li><code>fastboot flashing unlock</code>은 커스텀 이미지를 허용하는 대신 강제로 <code>/data</code>를 wipe하고 AVB 상태를 orange로 바꿔 무결성 판정을 실패시킨다.</li><li>커널은 initramfs의 <code>/init</code>을 PID 1로 실행하고, init은 정적 first-stage(파일시스템·SELinux 준비) → <code>execve</code> → 동적 second-stage(서비스 기동)로 나뉘며 PID 1을 유지한다.</li><li>Boot Control HAL은 A/B 슬롯의 active/bootable/successful 상태를 관리해, OTA 부팅이 retry count 안에 성공하지 못하면 직전 successful 슬롯으로 자동 롤백한다.</li></ul></div><h2 id="다음-챕터" tabindex="-1"><a class="header-anchor" href="#다음-챕터"><span>다음 챕터</span></a></h2><p><a href="/study/android-internals/08-init-zygote">CH8. init과 Zygote</a>에서는 PID 1 init의 rc 파일 문법과 서비스 정의, System Properties 심층 구조, 그리고 모든 앱의 부모가 되는 Zygote fork 모델을 파헤친다.</p>`,49)]))}const b=s(r,[["render",c],["__file","07-boot-process.html.vue"]]),g=JSON.parse('{"path":"/study/android-internals/07-boot-process.html","title":"CH7. 부트로더와 커널 부팅","lang":"en-US","frontmatter":{"title":"CH7. 부트로더와 커널 부팅","description":"Boot ROM부터 2차 부트로더, ABL/LK, 커널, initramfs, first/second-stage init까지 이어지는 안드로이드 신뢰 체인과 부트 컨트롤 HAL의 슬롯 롤백을 다룬다.","date":"2026-07-13T00:00:00.000Z","tags":["android","aosp","boot","bootloader","kernel"]},"headers":[{"level":1,"title":"CH7. 부트로더와 커널 부팅","slug":"ch7-부트로더와-커널-부팅","link":"#ch7-부트로더와-커널-부팅","children":[{"level":2,"title":"학습 목표","slug":"학습-목표","link":"#학습-목표","children":[]},{"level":2,"title":"1. 부트체인 개관 — 전원부터 init까지","slug":"_1-부트체인-개관-—-전원부터-init까지","link":"#_1-부트체인-개관-—-전원부터-init까지","children":[]},{"level":2,"title":"2. Little Kernel(LK)과 Android Bootloader","slug":"_2-little-kernel-lk-과-android-bootloader","link":"#_2-little-kernel-lk-과-android-bootloader","children":[]},{"level":2,"title":"3. 부트로더 락/언락","slug":"_3-부트로더-락-언락","link":"#_3-부트로더-락-언락","children":[]},{"level":2,"title":"4. 커널 부팅 — cmdline, DTB, 그리고 init까지","slug":"_4-커널-부팅-—-cmdline-dtb-그리고-init까지","link":"#_4-커널-부팅-—-cmdline-dtb-그리고-init까지","children":[]},{"level":2,"title":"5. initramfs와 first/second-stage init 분리","slug":"_5-initramfs와-first-second-stage-init-분리","link":"#_5-initramfs와-first-second-stage-init-분리","children":[]},{"level":2,"title":"6. Boot Control HAL과 A/B 슬롯 롤백","slug":"_6-boot-control-hal과-a-b-슬롯-롤백","link":"#_6-boot-control-hal과-a-b-슬롯-롤백","children":[]},{"level":2,"title":"다음 챕터","slug":"다음-챕터","link":"#다음-챕터","children":[]}]}],"git":{},"filePathRelative":"_study/android-internals/07-boot-process.md"}');export{b as comp,g as data};
