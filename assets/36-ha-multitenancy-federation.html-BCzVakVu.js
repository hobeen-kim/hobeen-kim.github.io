import{_ as e,c as n,e as a,o as t}from"./app-J_vrPzUq.js";const i="/images/study-observability/36-ha-dedup-light.png",l="/images/study-observability/36-ha-dedup-dark.png",r="/images/study-observability/36-gateway-auth-light.png",p="/images/study-observability/36-gateway-auth-dark.png",o={};function c(u,s){return t(),n("div",null,s[0]||(s[0]=[a(`<h1 id="ha·멀티테넌시·페더레이션" tabindex="-1"><a class="header-anchor" href="#ha·멀티테넌시·페더레이션"><span>HA·멀티테넌시·페더레이션</span></a></h1><div class="hint-container info"><p class="hint-container-title">학습 목표</p><ul><li>Prometheus를 동일한 스크레이프 쌍으로 이중화하는 HA 패턴과, 다운스트림에서 중복을 제거하는 방식을 이해한다.</li><li><code>/federate</code> 엔드포인트를 이용한 계층형(hierarchical) 페더레이션과 교차 서비스(cross-service) 페더레이션의 용도 차이를 구분한다.</li><li><code>X-Scope-OrgID</code> 헤더 기반으로 Mimir/Loki/Tempo가 멀티테넌시를 구현하는 방식을 안다.</li><li>테넌트별 limit과 격리가 왜 필요한지, noisy neighbor 문제를 어떻게 방지하는지 이해한다.</li><li>게이트웨이 계층에서 인증·인가를 처리하고 컴포넌트 간 mTLS를 적용하는 보안 패턴을 익힌다.</li><li>멀티클러스터 환경에서 하나의 대시보드로 전체 시스템을 조망하는 글로벌 뷰 구성 방법을 안다.</li></ul></div><h2 id="_1-prometheus-ha" tabindex="-1"><a class="header-anchor" href="#_1-prometheus-ha"><span>1. Prometheus HA</span></a></h2><p>단일 Prometheus 서버는 <a href="/study/observability/35-mimir-longterm-storage">장기 저장 — Mimir</a>로 데이터 유실 문제를 완화할 수 있지만, 그 자체가 죽으면 죽어있는 동안의 스크레이프와 알림 평가가 통째로 비는 <strong>가용성 공백</strong>이 생긴다. Prometheus의 표준 HA 패턴은 <strong>동일한 스크레이프 설정을 가진 Prometheus 두 대(또는 그 이상)를 완전히 독립적으로</strong> 운영하는 것이다. 둘 다 같은 타깃을 같은 주기로 긁고, 같은 알림 규칙을 평가한다.</p><div class="language-yaml line-numbers-mode" data-highlighter="prismjs" data-ext="yml" data-title="yml"><pre><code><span class="line"><span class="token comment"># 두 레플리카를 external_labels로만 구분</span></span>
<span class="line"><span class="token key atrule">global</span><span class="token punctuation">:</span></span>
<span class="line">  <span class="token key atrule">external_labels</span><span class="token punctuation">:</span></span>
<span class="line">    <span class="token key atrule">replica</span><span class="token punctuation">:</span> replica<span class="token punctuation">-</span>a   <span class="token comment"># 다른 레플리카는 replica-b</span></span>
<span class="line">    <span class="token key atrule">cluster</span><span class="token punctuation">:</span> prod<span class="token punctuation">-</span>us<span class="token punctuation">-</span>east</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>레플리카는 스크레이프 타이밍이 완전히 동기화되지 않으므로 같은 시계열이라도 타임스탬프가 미세하게 어긋난 별개의 데이터로 저장된다. 이 중복을 처리하는 책임은 다운스트림에 있다.</p><ul><li><strong>Mimir/Thanos 쿼리 계층</strong>은 <code>replica</code> 라벨을 기준으로 동일 시계열의 여러 레플리카 데이터를 병합하며 중복 제거한다. Thanos Querier의 <code>--query.replica-label=replica</code> 옵션이나 Mimir 쿼리 시 벡터 셀렉터에서 <code>replica</code> 라벨을 무시하고 조인하는 방식이 대표적이다.</li><li><strong>Alertmanager</strong>는 자체 gossip 기반 클러스터링으로 동일한 알림이 여러 Prometheus 레플리카에서 동시에 들어와도 하나로 묶어 한 번만 발송한다. 즉 두 Prometheus가 각각 같은 알림을 Alertmanager로 보내도, Alertmanager 클러스터가 알림의 fingerprint를 공유해 중복 알림을 억제한다.</li></ul><p><img src="`+i+'" alt="Prometheus A(replica=a)와 Prometheus B(replica=b)가 독립적으로 타깃을 스크레이프하고 룰을 평가한 뒤 각각 동일한 Alert(fingerprint 동일)를 Alertmanager 클러스터로 발화하면 gossip으로 중복을 인지해 1건만 발송하고, 두 레플리카가 각각 remote_write(replica=a/b)로 Mimir에 보내면 쿼리 시 replica 라벨 기준으로 중복을 제거하는 Prometheus HA 순서 다이어그램"><img src="'+l+`" alt="Prometheus A(replica=a)와 Prometheus B(replica=b)가 독립적으로 타깃을 스크레이프하고 룰을 평가한 뒤 각각 동일한 Alert(fingerprint 동일)를 Alertmanager 클러스터로 발화하면 gossip으로 중복을 인지해 1건만 발송하고, 두 레플리카가 각각 remote_write(replica=a/b)로 Mimir에 보내면 쿼리 시 replica 라벨 기준으로 중복을 제거하는 Prometheus HA 순서 다이어그램"></p><p>레플리카는 완전히 독립적이어야 한다는 점이 중요하다 — 같은 랙, 같은 가용영역, 같은 전원 계통에 두면 HA의 의미가 없다. 가능하면 서로 다른 가용영역(AZ)에 배치한다.</p><h2 id="_2-페더레이션" tabindex="-1"><a class="header-anchor" href="#_2-페더레이션"><span>2. 페더레이션</span></a></h2><p><strong>페더레이션(federation)</strong>은 한 Prometheus가 다른 Prometheus의 <code>/federate</code> HTTP 엔드포인트를 스크레이프해 일부 시계열만 가져오는 기능이다.</p><div class="language-yaml line-numbers-mode" data-highlighter="prismjs" data-ext="yml" data-title="yml"><pre><code><span class="line"><span class="token key atrule">scrape_configs</span><span class="token punctuation">:</span></span>
<span class="line"><span class="token punctuation">-</span> <span class="token key atrule">job_name</span><span class="token punctuation">:</span> <span class="token string">&#39;federate&#39;</span></span>
<span class="line">  <span class="token key atrule">scrape_interval</span><span class="token punctuation">:</span> 30s</span>
<span class="line">  <span class="token key atrule">honor_labels</span><span class="token punctuation">:</span> <span class="token boolean important">true</span></span>
<span class="line">  <span class="token key atrule">metrics_path</span><span class="token punctuation">:</span> <span class="token string">&#39;/federate&#39;</span></span>
<span class="line">  <span class="token key atrule">params</span><span class="token punctuation">:</span></span>
<span class="line">    <span class="token key atrule">&#39;match[]&#39;</span><span class="token punctuation">:</span></span>
<span class="line">      <span class="token punctuation">-</span> <span class="token string">&#39;{job=&quot;checkout-service&quot;}&#39;</span></span>
<span class="line">      <span class="token punctuation">-</span> <span class="token string">&#39;up&#39;</span></span>
<span class="line">  <span class="token key atrule">static_configs</span><span class="token punctuation">:</span></span>
<span class="line">    <span class="token punctuation">-</span> <span class="token key atrule">targets</span><span class="token punctuation">:</span></span>
<span class="line">      <span class="token punctuation">-</span> <span class="token string">&#39;prometheus-team-a:9090&#39;</span></span>
<span class="line">      <span class="token punctuation">-</span> <span class="token string">&#39;prometheus-team-b:9090&#39;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><code>honor_labels: true</code>는 필수에 가깝다 — 이게 없으면 하위 Prometheus가 붙인 <code>instance</code>, <code>job</code> 라벨이 상위 Prometheus의 스크레이프 라벨로 덮어씌워져 원본 출처를 잃는다.</p><p>용도는 크게 두 가지로 나뉜다.</p><ul><li><strong>계층형 페더레이션(hierarchical)</strong> — 글로벌 Prometheus가 각 클러스터/리전 Prometheus로부터 이미 <strong>집계된</strong> 요약 지표(예: recording rule로 사전 계산한 클러스터별 가용성)만 가져와 전사 대시보드를 구성한다. 원본 고카디널리티 데이터는 하위 레벨에 그대로 두고 요약만 끌어올리는 패턴이다.</li><li><strong>교차 서비스 페더레이션(cross-service)</strong> — 팀 A의 Prometheus가 팀 B의 특정 서비스 상태(<code>up</code>, 에러율 등)만 자기 알림 규칙에서 참조하고 싶을 때 부분적으로 끌어온다.</li></ul><p>페더레이션의 근본적 한계는 <strong>스케일하지 않는다</strong>는 것이다. <code>match[]</code>로 걸러도 결국 HTTP 풀링이고, 가져오는 시계열이 많아지면 상위 Prometheus가 병목이 된다. 전체 원본 데이터를 중앙에서 질의 가능하게 만드는 목적이라면 페더레이션이 아니라 <code>remote_write</code>로 Mimir에 밀어넣는 구조(<a href="/study/observability/12-tsdb-remote-write">TSDB와 remote_write</a>)가 정답이다. 페더레이션은 어디까지나 &quot;이미 축약된 소규모 요약 지표를 계층적으로 끌어올리는&quot; 좁은 용도에 최적화돼 있다고 이해하는 게 정확하다.</p><h2 id="_3-멀티테넌시" tabindex="-1"><a class="header-anchor" href="#_3-멀티테넌시"><span>3. 멀티테넌시</span></a></h2><p>여러 팀·고객이 같은 물리 인프라를 공유하면서도 데이터는 완전히 격리해야 하는 경우, Mimir·Loki·Tempo는 공통적으로 <strong><code>X-Scope-OrgID</code></strong> HTTP 헤더로 테넌트를 식별하는 멀티테넌시 모델을 쓴다.</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line"><span class="token comment"># 쓰기: 테넌트 헤더 포함</span></span>
<span class="line"><span class="token function">curl</span> <span class="token parameter variable">-X</span> POST <span class="token parameter variable">-H</span> <span class="token string">&quot;X-Scope-OrgID: team-checkout&quot;</span> <span class="token punctuation">\\</span></span>
<span class="line">  --data-binary @metrics.pb <span class="token punctuation">\\</span></span>
<span class="line">  http://mimir/api/v1/push</span>
<span class="line"></span>
<span class="line"><span class="token comment"># 읽기: 동일 헤더로 자기 테넌트 데이터만 조회</span></span>
<span class="line"><span class="token function">curl</span> <span class="token parameter variable">-H</span> <span class="token string">&quot;X-Scope-OrgID: team-checkout&quot;</span> <span class="token punctuation">\\</span></span>
<span class="line">  <span class="token string">&quot;http://mimir/prometheus/api/v1/query?query=up&quot;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>각 컴포넌트는 이 헤더 값을 기준으로 오브젝트 스토리지 상의 경로를 분리(<code>&lt;bucket&gt;/&lt;tenant-id&gt;/...</code>)하거나, ingester 안에서 테넌트별 in-memory 구조를 나눈다. 즉 물리 클러스터는 공유하되 논리적으로 완전히 분리된 데이터 공간을 갖는다. Prometheus의 <code>remote_write</code>에는 <code>headers</code> 설정으로 이 헤더를 붙이면 되고, Alloy나 OpenTelemetry Collector도 exporter 설정에서 동일한 헤더를 주입할 수 있다.</p><h2 id="_4-테넌트-limit·격리" tabindex="-1"><a class="header-anchor" href="#_4-테넌트-limit·격리"><span>4. 테넌트 limit·격리</span></a></h2><p>멀티테넌시의 핵심 리스크는 <strong>noisy neighbor</strong>다 — 한 테넌트가 급격히 카디널리티를 늘리거나 쓰기량을 폭증시키면, 같은 클러스터를 쓰는 다른 테넌트의 성능까지 저하될 수 있다. Mimir는 이를 막기 위해 테넌트별 limit을 세밀하게 설정한다.</p><div class="language-yaml line-numbers-mode" data-highlighter="prismjs" data-ext="yml" data-title="yml"><pre><code><span class="line"><span class="token comment"># runtime_config로 테넌트별 오버라이드 (핫 리로드 가능)</span></span>
<span class="line"><span class="token key atrule">overrides</span><span class="token punctuation">:</span></span>
<span class="line">  <span class="token key atrule">team-checkout</span><span class="token punctuation">:</span></span>
<span class="line">    <span class="token key atrule">ingestion_rate</span><span class="token punctuation">:</span> <span class="token number">50000</span>        <span class="token comment"># 초당 샘플 수</span></span>
<span class="line">    <span class="token key atrule">max_series_per_metric</span><span class="token punctuation">:</span> <span class="token number">100000</span></span>
<span class="line">    <span class="token key atrule">max_global_series_per_user</span><span class="token punctuation">:</span> <span class="token number">5000000</span></span>
<span class="line">    <span class="token key atrule">max_samples_per_query</span><span class="token punctuation">:</span> <span class="token number">50000000</span></span>
<span class="line">  <span class="token key atrule">team-batch-analytics</span><span class="token punctuation">:</span></span>
<span class="line">    <span class="token key atrule">ingestion_rate</span><span class="token punctuation">:</span> <span class="token number">5000</span></span>
<span class="line">    <span class="token key atrule">max_series_per_metric</span><span class="token punctuation">:</span> <span class="token number">10000</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>이 <code>runtime_config</code>는 별도 파일로 관리되며 Mimir 프로세스 재시작 없이 핫 리로드된다 — 온콜 중 특정 테넌트가 폭주할 때 즉시 limit을 조여 다른 테넌트를 보호할 수 있다. limit을 넘는 쓰기는 429(Too Many Requests)로 거부되고, 이는 <a href="/study/observability/34-cardinality-cost">카디널리티 관리와 비용</a>에서 다룬 카디널리티 탐지·통제 절차와 맞물려 운영된다 — limit에 걸린 테넌트는 곧 어딘가에서 라벨 설계가 잘못됐다는 신호다. 쿼리 측면에서도 테넌트별 동시 쿼리 수·쿼리당 최대 조회 시계열 수를 제한해, 한 테넌트의 무거운 쿼리가 querier 전체를 독점하지 못하게 한다.</p><h2 id="_5-인증·인가·tls" tabindex="-1"><a class="header-anchor" href="#_5-인증·인가·tls"><span>5. 인증·인가·TLS</span></a></h2><p><code>X-Scope-OrgID</code>는 그 자체로는 인증 수단이 아니다 — 헤더 값을 임의로 바꾸면 다른 테넌트인 척할 수 있으므로, 신뢰할 수 없는 클라이언트가 직접 이 헤더를 붙이게 두면 안 된다. 실무에서는 Mimir/Loki/Tempo 앞에 <strong>게이트웨이</strong>(nginx, Mimir 자체 gateway 컴포넌트, 또는 클라우드 로드밸런서 + 인증 미들웨어)를 두고, 게이트웨이가 실제 인증(API 키, OAuth2/JWT, mTLS 클라이언트 인증서)을 수행한 뒤 검증된 신원에 대응하는 <code>X-Scope-OrgID</code>를 게이트웨이가 직접 주입한다. 백엔드 컴포넌트는 게이트웨이를 거친 요청만 신뢰하도록 네트워크 정책으로 직접 접근을 차단한다.</p><p><img src="`+r+'" alt="클라이언트(Prometheus/Grafana)가 mTLS와 Bearer Token으로 Gateway에 접속하면 Gateway가 OAuth2/JWT를 검증하고 X-Scope-OrgID를 주입해 내부 네트워크의 Mimir distributor와 query-frontend로 전달하고, 내부에서는 distributor가 ingester로, query-frontend가 store-gateway로 각각 mTLS로 통신하는 게이트웨이 인증·인가와 mTLS 다이어그램"><img src="'+p+`" alt="클라이언트(Prometheus/Grafana)가 mTLS와 Bearer Token으로 Gateway에 접속하면 Gateway가 OAuth2/JWT를 검증하고 X-Scope-OrgID를 주입해 내부 네트워크의 Mimir distributor와 query-frontend로 전달하고, 내부에서는 distributor가 ingester로, query-frontend가 store-gateway로 각각 mTLS로 통신하는 게이트웨이 인증·인가와 mTLS 다이어그램"></p><p>컴포넌트 간 통신(distributor↔ingester, querier↔store-gateway 등)에는 <strong>mTLS</strong>를 적용해 내부 네트워크가 뚫려도 트래픽을 가로채거나 위조하지 못하게 한다. Mimir는 각 gRPC 클라이언트/서버 설정에 <code>tls_cert_path</code>, <code>tls_key_path</code>, <code>tls_ca_path</code>를 지정해 컴포넌트 간 상호 인증을 강제할 수 있다.</p><h2 id="_6-글로벌-뷰" tabindex="-1"><a class="header-anchor" href="#_6-글로벌-뷰"><span>6. 글로벌 뷰</span></a></h2><p>멀티클러스터·멀티리전 환경에서는 각 클러스터의 로컬 상태를 빠르게 보는 것과, 전사 관점에서 전체를 조망하는 것 둘 다 필요하다. 일반적인 패턴은 각 클러스터에 로컬 Prometheus(또는 Alloy)를 두어 그 클러스터의 스크레이프·룰 평가·단기 알림을 자체적으로 처리하게 하고, <code>cluster</code> 라벨을 붙여 중앙 Mimir로 <code>remote_write</code>하는 것이다.</p><div class="language-yaml line-numbers-mode" data-highlighter="prismjs" data-ext="yml" data-title="yml"><pre><code><span class="line"><span class="token key atrule">global</span><span class="token punctuation">:</span></span>
<span class="line">  <span class="token key atrule">external_labels</span><span class="token punctuation">:</span></span>
<span class="line">    <span class="token key atrule">cluster</span><span class="token punctuation">:</span> eu<span class="token punctuation">-</span>west<span class="token punctuation">-</span><span class="token number">1</span></span>
<span class="line">    <span class="token key atrule">replica</span><span class="token punctuation">:</span> replica<span class="token punctuation">-</span>a</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Grafana는 중앙 Mimir 하나만 데이터소스로 연결하면 <code>cluster</code> 라벨로 특정 리전을 필터링하거나, <code>sum by (cluster)</code>로 전체 리전을 조인한 글로벌 대시보드를 동시에 그릴 수 있다. 멀티테넌시와 결합하면, 테넌트별로 격리하면서도 동일 대시보드 안에서 <code>cluster</code> 차원으로 드릴다운하는 구조가 가능해진다. 이 글로벌 뷰는 어디까지나 저장·질의 계층(Mimir)의 수평 확장과 멀티테넌시 격리가 뒷받침돼야 실용적으로 동작하며, 실제로 이 모든 컴포넌트(로컬 Prometheus, Alloy, 게이트웨이, Mimir 마이크로서비스)를 클러스터 안에 배치·운영하는 구체적인 매니페스트는 다음 챕터에서 이어진다.</p><div class="hint-container tip"><p class="hint-container-title">핵심 정리</p><ul><li>Prometheus HA는 동일한 스크레이프 쌍을 완전히 독립적으로 운영하고, <code>replica</code> 라벨로 다운스트림(Mimir/Thanos)에서 dedup하며 Alertmanager는 gossip으로 중복 알림을 억제한다.</li><li><code>/federate</code>는 <code>honor_labels: true</code>와 함께 계층형(집계 지표 상향) 또는 교차 서비스(부분 참조) 용도로만 쓰고, 전체 원본 데이터 중앙화는 remote_write로 해결한다.</li><li>Mimir/Loki/Tempo는 <code>X-Scope-OrgID</code> 헤더로 물리 인프라를 공유하면서 논리적으로 완전히 격리된 테넌트 데이터 공간을 만든다.</li><li><code>runtime_config</code> 기반 테넌트별 limit(ingestion rate, series 수, 쿼리 동시성)이 noisy neighbor를 막는 핵심 장치이며 핫 리로드로 즉시 조정 가능하다.</li><li><code>X-Scope-OrgID</code>는 인증 수단이 아니므로 게이트웨이에서 실제 인증(OAuth2/JWT/mTLS)을 마친 뒤 신뢰된 값으로 주입해야 하고, 컴포넌트 간에는 mTLS를 적용한다.</li><li>멀티클러스터 글로벌 뷰는 로컬 Prometheus/Alloy가 <code>cluster</code> 라벨을 붙여 중앙 Mimir로 원격 저장하고, Grafana 한 곳에서 조인·드릴다운하는 구조로 만든다.</li></ul></div><h2 id="다음-챕터" tabindex="-1"><a class="header-anchor" href="#다음-챕터"><span>다음 챕터</span></a></h2><p>지금까지 다룬 HA·멀티테넌시·페더레이션 패턴은 모두 실제로는 Kubernetes 클러스터 위에 구체적인 워크로드로 배치돼야 동작한다. 다음 챕터 <a href="/study/observability/37-kubernetes-deployment">Kubernetes 배포</a>에서는 kube-prometheus-stack과 Prometheus Operator, ServiceMonitor/PodMonitor를 이용해 지금까지의 아키텍처를 실제 클러스터에 선언적으로 배포하는 방법을 다룬다.</p>`,35)]))}const m=e(o,[["render",c],["__file","36-ha-multitenancy-federation.html.vue"]]),h=JSON.parse('{"path":"/study/observability/36-ha-multitenancy-federation.html","title":"HA·멀티테넌시·페더레이션","lang":"en-US","frontmatter":{"title":"HA·멀티테넌시·페더레이션","description":"동일 스크레이프 쌍으로 구성하는 Prometheus HA와 Mimir/Alertmanager의 중복 제거 방식, federate 엔드포인트를 이용한 계층형·교차 서비스 페더레이션, X-Scope-OrgID 기반 Mimir/Loki/Tempo 멀티테넌시와 테넌트별 limit·격리, 게이트웨이 인증과 mTLS, 멀티클러스터 글로벌 뷰 구성까지 대규모 관측성 스택 운영 패턴을 다룬다.","date":"2026-07-02T00:00:00.000Z","tags":["Observability","HA","Multitenancy","Federation","SRE"],"prev":"/study/observability/35-mimir-longterm-storage","next":"/study/observability/37-kubernetes-deployment"},"headers":[{"level":1,"title":"HA·멀티테넌시·페더레이션","slug":"ha·멀티테넌시·페더레이션","link":"#ha·멀티테넌시·페더레이션","children":[{"level":2,"title":"1. Prometheus HA","slug":"_1-prometheus-ha","link":"#_1-prometheus-ha","children":[]},{"level":2,"title":"2. 페더레이션","slug":"_2-페더레이션","link":"#_2-페더레이션","children":[]},{"level":2,"title":"3. 멀티테넌시","slug":"_3-멀티테넌시","link":"#_3-멀티테넌시","children":[]},{"level":2,"title":"4. 테넌트 limit·격리","slug":"_4-테넌트-limit·격리","link":"#_4-테넌트-limit·격리","children":[]},{"level":2,"title":"5. 인증·인가·TLS","slug":"_5-인증·인가·tls","link":"#_5-인증·인가·tls","children":[]},{"level":2,"title":"6. 글로벌 뷰","slug":"_6-글로벌-뷰","link":"#_6-글로벌-뷰","children":[]},{"level":2,"title":"다음 챕터","slug":"다음-챕터","link":"#다음-챕터","children":[]}]}],"git":{},"filePathRelative":"_study/observability/36-ha-multitenancy-federation.md"}');export{m as comp,h as data};
