import{_ as n,c as e,e as a,o as t}from"./app-uUo1GAi4.js";const i={};function l(o,s){return t(),e("div",null,s[0]||(s[0]=[a(`<h1 id="dockerfile과-이미지-빌드" tabindex="-1"><a class="header-anchor" href="#dockerfile과-이미지-빌드"><span>Dockerfile과 이미지 빌드</span></a></h1><div class="hint-container info"><p class="hint-container-title">학습 목표</p><ul><li>FROM·RUN·COPY·CMD·ENTRYPOINT·ARG·ENV 등 주요 Dockerfile 명령어의 역할과 차이를 설명할 수 있다.</li><li>각 명령어가 레이어를 만든다는 사실과, 빌드 캐시가 무효화되는 조건을 이해한다.</li><li>멀티스테이지 빌드로 빌드 도구를 최종 이미지에서 분리하는 패턴을 익힌다.</li><li>베이스 이미지 선택·distroless·.dockerignore로 이미지를 작고 안전하게 최적화한다.</li><li>BuildKit이 무엇이고 왜 기본 빌더가 됐는지 안다.</li></ul></div><h2 id="_1-dockerfile-명령어-—-이미지의-설계도" tabindex="-1"><a class="header-anchor" href="#_1-dockerfile-명령어-—-이미지의-설계도"><span>1. Dockerfile 명령어 — 이미지의 설계도</span></a></h2><p><strong>Dockerfile</strong>은 이미지를 어떻게 만들지 한 줄씩 적은 텍스트 파일이다. 각 명령은 위에서 아래로 순서대로 실행된다. 전체 명세는 <a href="https://docs.docker.com/reference/dockerfile/" target="_blank" rel="noopener noreferrer">Dockerfile reference</a>에 있다. 핵심 명령어를 먼저 정리한다.</p><table><thead><tr><th>명령어</th><th>역할</th></tr></thead><tbody><tr><td><code>FROM</code></td><td>베이스 이미지 지정. 모든 Dockerfile의 시작점</td></tr><tr><td><code>RUN</code></td><td>빌드 시점에 셸 명령 실행 (패키지 설치, 컴파일 등)</td></tr><tr><td><code>COPY</code></td><td>호스트의 파일을 이미지로 복사</td></tr><tr><td><code>ADD</code></td><td>COPY + URL 다운로드 + tar 자동 해제 (특별한 이유 없으면 COPY 권장)</td></tr><tr><td><code>WORKDIR</code></td><td>이후 명령의 작업 디렉터리 설정</td></tr><tr><td><code>ENV</code></td><td>이미지·컨테이너에 영구적으로 남는 환경변수</td></tr><tr><td><code>ARG</code></td><td>빌드 시점에만 쓰는 변수 (이미지에 남지 않음)</td></tr><tr><td><code>EXPOSE</code></td><td>문서화 목적의 포트 선언 (실제 매핑은 run의 -p)</td></tr><tr><td><code>USER</code></td><td>이후 명령과 컨테이너 실행 사용자 지정</td></tr><tr><td><code>CMD</code></td><td>컨테이너 시작 시 실행할 기본 명령(덮어쓰기 가능)</td></tr><tr><td><code>ENTRYPOINT</code></td><td>컨테이너의 고정 실행 진입점</td></tr></tbody></table><p>가장 헷갈리는 것이 <strong>CMD와 ENTRYPOINT</strong>다.</p><ul><li><code>ENTRYPOINT</code>는 &quot;이 컨테이너는 무슨 프로그램인가&quot;를 고정한다.</li><li><code>CMD</code>는 그 프로그램에 넘길 &quot;기본 인자&quot;를 제공하며, <code>docker run</code> 뒤에 인자를 주면 덮어써진다.</li></ul><div class="language-docker line-numbers-mode" data-highlighter="prismjs" data-ext="docker" data-title="docker"><pre><code><span class="line"><span class="token comment"># ENTRYPOINT로 실행 파일을 고정, CMD로 기본 인자를 제공</span></span>
<span class="line"><span class="token instruction"><span class="token keyword">ENTRYPOINT</span> [<span class="token string">&quot;python&quot;</span>, <span class="token string">&quot;app.py&quot;</span>]</span></span>
<span class="line"><span class="token instruction"><span class="token keyword">CMD</span> [<span class="token string">&quot;--port&quot;</span>, <span class="token string">&quot;8080&quot;</span>]</span></span>
<span class="line"><span class="token comment"># docker run img            → python app.py --port 8080</span></span>
<span class="line"><span class="token comment"># docker run img --port 9090 → python app.py --port 9090</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>또한 <code>ARG</code>와 <code>ENV</code>의 차이도 중요하다. <code>ARG</code>는 빌드 중에만 살아 있고 최종 이미지에 흔적이 남지 않는다. <code>ENV</code>는 이미지에 박혀 컨테이너 런타임까지 따라간다. 따라서 빌드용 토큰 같은 민감 값은 절대 <code>ENV</code>로 넣지 않는다.</p><div class="language-docker line-numbers-mode" data-highlighter="prismjs" data-ext="docker" data-title="docker"><pre><code><span class="line"><span class="token instruction"><span class="token keyword">ARG</span> APP_VERSION=1.0.0      # 빌드 시점에만 유효</span></span>
<span class="line"><span class="token instruction"><span class="token keyword">ENV</span> NODE_ENV=production    # 컨테이너 런타임까지 유지</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><h2 id="_2-레이어와-빌드-캐시-—-빠른-빌드의-핵심" tabindex="-1"><a class="header-anchor" href="#_2-레이어와-빌드-캐시-—-빠른-빌드의-핵심"><span>2. 레이어와 빌드 캐시 — 빠른 빌드의 핵심</span></a></h2><p><code>FROM</code>, <code>RUN</code>, <code>COPY</code> 같은 명령어는 <strong>각각 하나의 레이어</strong>를 만든다. 도커는 빌드 시 각 명령의 결과를 캐시해 두고, 다음 빌드에서 <strong>변경되지 않은 명령은 캐시를 재사용</strong>한다. 이 캐시 동작을 이해하는 것이 빌드 속도의 전부다.</p><p>핵심 규칙: <strong>한 레이어의 캐시가 깨지면, 그 아래(이후)의 모든 레이어 캐시도 함께 깨진다.</strong></p><pre class="mermaid">flowchart TB
    A[&quot;FROM node:20-slim ✅ 캐시&quot;] --&gt; B[&quot;WORKDIR /app ✅ 캐시&quot;]
    B --&gt; C[&quot;COPY package.json ./ ✅ 캐시&quot;]
    C --&gt; D[&quot;RUN npm ci ✅ 캐시&quot;]
    D --&gt; E[&quot;COPY . . ❌ 소스 변경 → 캐시 무효&quot;]
    E --&gt; F[&quot;RUN npm run build ❌ 재실행&quot;]
    Note[&quot;package.json이 안 바뀌면 npm ci는 캐시 재사용,&lt;br&gt;소스만 바뀌면 의존성 설치는 건너뛴다&quot;]
</pre><p>여기서 <strong>의존성 설치와 소스 복사를 분리</strong>하는 패턴이 나온다. 의존성 정의 파일을 먼저 복사·설치한 뒤, 자주 바뀌는 소스 코드를 나중에 복사하면, 소스만 고쳤을 때 무거운 의존성 설치 단계를 캐시로 건너뛸 수 있다.</p><div class="language-docker line-numbers-mode" data-highlighter="prismjs" data-ext="docker" data-title="docker"><pre><code><span class="line"><span class="token comment"># 나쁜 예: 소스가 한 글자만 바뀌어도 npm ci가 매번 재실행</span></span>
<span class="line"><span class="token instruction"><span class="token keyword">COPY</span> . .</span></span>
<span class="line"><span class="token instruction"><span class="token keyword">RUN</span> npm ci</span></span>
<span class="line"></span>
<span class="line"><span class="token comment"># 좋은 예: 의존성 정의를 먼저 복사해 캐시를 살린다</span></span>
<span class="line"><span class="token instruction"><span class="token keyword">COPY</span> package.json package-lock.json ./</span></span>
<span class="line"><span class="token instruction"><span class="token keyword">RUN</span> npm ci</span></span>
<span class="line"><span class="token instruction"><span class="token keyword">COPY</span> . .</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>또 <code>RUN</code>을 무분별하게 나누면 레이어가 늘어난다. 관련 명령은 <code>&amp;&amp;</code>로 묶고, 패키지 캐시는 같은 레이어에서 정리한다.</p><div class="language-docker line-numbers-mode" data-highlighter="prismjs" data-ext="docker" data-title="docker"><pre><code><span class="line"><span class="token instruction"><span class="token keyword">RUN</span> apt-get update <span class="token operator">\\</span></span>
<span class="line"> &amp;&amp; apt-get install -y --no-install-recommends curl ca-certificates <span class="token operator">\\</span></span>
<span class="line"> &amp;&amp; rm -rf /var/lib/apt/lists/*</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h2 id="_3-멀티스테이지-빌드-—-빌드-도구를-버리고-결과만-남기기" tabindex="-1"><a class="header-anchor" href="#_3-멀티스테이지-빌드-—-빌드-도구를-버리고-결과만-남기기"><span>3. 멀티스테이지 빌드 — 빌드 도구를 버리고 결과만 남기기</span></a></h2><p>컴파일 언어나 번들링이 필요한 앱은 빌드에 컴파일러·SDK·node_modules 같은 무거운 도구가 필요하다. 하지만 <strong>실행에는 결과물(바이너리, 정적 파일)만 필요하다</strong>. 이 둘을 분리하는 것이 <a href="https://docs.docker.com/build/building/multi-stage/" target="_blank" rel="noopener noreferrer">멀티스테이지 빌드(multi-stage build)</a>다.</p><pre class="mermaid">flowchart LR
    subgraph Build[&quot;builder 스테이지 (무거움)&quot;]
        SDK[&quot;golang:1.22 SDK&quot;] --&gt; Compile[&quot;go build → app 바이너리&quot;]
    end
    subgraph Final[&quot;최종 스테이지 (가벼움)&quot;]
        Base[&quot;distroless/static&quot;] --&gt; Copy[&quot;COPY --from=builder app&quot;]
    end
    Compile -.-&gt;|바이너리만 복사| Copy
    Copy --&gt; Img[&quot;수십 MB 최종 이미지&quot;]
</pre><p>Go 애플리케이션 예시다. 빌드 스테이지에서 컴파일하고, 최종 스테이지에는 바이너리 하나만 가져온다.</p><div class="language-docker line-numbers-mode" data-highlighter="prismjs" data-ext="docker" data-title="docker"><pre><code><span class="line"><span class="token comment"># 1단계: 빌드 (SDK 포함, 무거움)</span></span>
<span class="line"><span class="token instruction"><span class="token keyword">FROM</span> golang:1.22 <span class="token keyword">AS</span> builder</span></span>
<span class="line"><span class="token instruction"><span class="token keyword">WORKDIR</span> /src</span></span>
<span class="line"><span class="token instruction"><span class="token keyword">COPY</span> go.mod go.sum ./</span></span>
<span class="line"><span class="token instruction"><span class="token keyword">RUN</span> go mod download</span></span>
<span class="line"><span class="token instruction"><span class="token keyword">COPY</span> . .</span></span>
<span class="line"><span class="token instruction"><span class="token keyword">RUN</span> CGO_ENABLED=0 go build -o /app ./cmd/server</span></span>
<span class="line"></span>
<span class="line"><span class="token comment"># 2단계: 실행 (바이너리만, 매우 가벼움)</span></span>
<span class="line"><span class="token instruction"><span class="token keyword">FROM</span> gcr.io/distroless/static-debian12</span></span>
<span class="line"><span class="token instruction"><span class="token keyword">COPY</span> <span class="token options"><span class="token property">--from</span><span class="token punctuation">=</span><span class="token string">builder</span></span> /app /app</span></span>
<span class="line"><span class="token instruction"><span class="token keyword">USER</span> nonroot:nonroot</span></span>
<span class="line"><span class="token instruction"><span class="token keyword">ENTRYPOINT</span> [<span class="token string">&quot;/app&quot;</span>]</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><code>COPY --from=builder</code>가 핵심이다. 앞 스테이지의 산출물만 골라 가져오므로, SDK·소스·캐시는 최종 이미지에 전혀 포함되지 않는다. 수백 MB짜리 빌드 환경이 수십 MB 이하 이미지로 줄어든다.</p><p>Node 앱이라면 빌드 스테이지에서 번들하고, 실행 스테이지에서 production 의존성만 설치하는 식으로 응용한다.</p><div class="language-docker line-numbers-mode" data-highlighter="prismjs" data-ext="docker" data-title="docker"><pre><code><span class="line"><span class="token instruction"><span class="token keyword">FROM</span> node:20 <span class="token keyword">AS</span> build</span></span>
<span class="line"><span class="token instruction"><span class="token keyword">WORKDIR</span> /app</span></span>
<span class="line"><span class="token instruction"><span class="token keyword">COPY</span> package*.json ./</span></span>
<span class="line"><span class="token instruction"><span class="token keyword">RUN</span> npm ci</span></span>
<span class="line"><span class="token instruction"><span class="token keyword">COPY</span> . .</span></span>
<span class="line"><span class="token instruction"><span class="token keyword">RUN</span> npm run build</span></span>
<span class="line"></span>
<span class="line"><span class="token instruction"><span class="token keyword">FROM</span> node:20-slim <span class="token keyword">AS</span> runtime</span></span>
<span class="line"><span class="token instruction"><span class="token keyword">WORKDIR</span> /app</span></span>
<span class="line"><span class="token instruction"><span class="token keyword">ENV</span> NODE_ENV=production</span></span>
<span class="line"><span class="token instruction"><span class="token keyword">COPY</span> package*.json ./</span></span>
<span class="line"><span class="token instruction"><span class="token keyword">RUN</span> npm ci --omit=dev</span></span>
<span class="line"><span class="token instruction"><span class="token keyword">COPY</span> <span class="token options"><span class="token property">--from</span><span class="token punctuation">=</span><span class="token string">build</span></span> /app/dist ./dist</span></span>
<span class="line"><span class="token instruction"><span class="token keyword">USER</span> node</span></span>
<span class="line"><span class="token instruction"><span class="token keyword">CMD</span> [<span class="token string">&quot;node&quot;</span>, <span class="token string">&quot;dist/main.js&quot;</span>]</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h2 id="_4-이미지-최적화-—-작고-안전하게" tabindex="-1"><a class="header-anchor" href="#_4-이미지-최적화-—-작고-안전하게"><span>4. 이미지 최적화 — 작고 안전하게</span></a></h2><p>이미지를 작게 만들면 <strong>풀 속도·배포 시간·공격 표면(attack surface)</strong>이 모두 좋아진다. 핵심 기법을 정리한다.</p><p><strong>(1) 베이스 이미지 선택</strong>: 같은 런타임이라도 베이스에 따라 크기가 크게 다르다.</p><table><thead><tr><th>베이스</th><th>특징</th></tr></thead><tbody><tr><td><code>ubuntu</code> / <code>debian</code></td><td>풀 OS. 디버깅 편하지만 크고 패키지 취약점이 많다</td></tr><tr><td><code>*-slim</code></td><td>불필요한 패키지를 뺀 경량 변형</td></tr><tr><td><code>alpine</code></td><td>musl libc 기반, 수 MB대. 단 glibc 호환 이슈 주의</td></tr><tr><td><code>distroless</code></td><td>셸·패키지 매니저조차 없는 최소 런타임. 가장 안전</td></tr></tbody></table><p><strong>(2) distroless</strong>: 구글의 <a href="https://github.com/GoogleContainerTools/distroless" target="_blank" rel="noopener noreferrer">distroless</a> 이미지는 셸, apt, busybox 같은 것 없이 애플리케이션과 런타임 의존성만 담는다. 침입자가 셸을 못 잡으니 공격이 어렵고, 취약점이 들어올 표면이 작다. 대신 <code>docker exec ... sh</code>로 들어갈 수 없어 디버깅 방식이 달라진다(임시 디버그 컨테이너 사용).</p><p><strong>(3) .dockerignore</strong>: 빌드 컨텍스트(<code>docker build</code>가 데몬에 보내는 파일들)에서 불필요한 것을 제외한다. <code>.git</code>, <code>node_modules</code>, 빌드 산출물, 비밀 파일을 빼면 컨텍스트 전송이 빨라지고 비밀이 이미지에 새는 사고를 막는다.</p><div class="language-gitignore line-numbers-mode" data-highlighter="prismjs" data-ext="gitignore" data-title="gitignore"><pre><code><span class="line"><span class="token comment"># .dockerignore</span></span>
<span class="line"><span class="token entry string">.git</span></span>
<span class="line"><span class="token entry string">node_modules</span></span>
<span class="line"><span class="token entry string">dist</span></span>
<span class="line"><span class="token entry string"><span class="token operator">*</span>.log</span></span>
<span class="line"><span class="token entry string">.env</span></span>
<span class="line"><span class="token entry string">Dockerfile</span></span>
<span class="line"><span class="token entry string"><span class="token operator">**</span><span class="token punctuation">/</span>.DS_Store</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><div class="hint-container warning"><p class="hint-container-title">비밀(secret)을 이미지에 굽지 말 것</p><p><code>COPY .env</code>나 <code>ARG</code>로 받은 토큰은 레이어 히스토리에 남아 <code>docker history</code>로 들여다보일 수 있다. 빌드 타임 비밀이 필요하면 BuildKit의 <code>--secret</code> 마운트를 써서 레이어에 흔적을 남기지 않는다.</p></div><h2 id="_5-buildkit-—-현대적-빌드-엔진" tabindex="-1"><a class="header-anchor" href="#_5-buildkit-—-현대적-빌드-엔진"><span>5. BuildKit — 현대적 빌드 엔진</span></a></h2><p><strong>BuildKit</strong>은 현재 도커의 기본 빌드 엔진이다(<code>docker build</code>가 내부적으로 사용). 자세한 내용은 <a href="https://docs.docker.com/build/buildkit/" target="_blank" rel="noopener noreferrer">BuildKit 문서</a>에 있다. 과거 레거시 빌더 대비 다음 장점이 있다.</p><ul><li><strong>병렬 빌드</strong>: 의존성 그래프를 분석해 독립적인 스테이지를 동시에 빌드한다.</li><li><strong>효율적 캐시</strong>: 변경된 부분만 정밀하게 다시 빌드하고, 원격 캐시(레지스트리) 공유를 지원한다.</li><li><strong>빌드 시크릿</strong>: <code>--secret</code>으로 비밀을 레이어에 남기지 않고 주입한다.</li><li><strong>캐시 마운트</strong>: <code>RUN --mount=type=cache</code>로 패키지 매니저 캐시를 빌드 간에 재사용한다.</li></ul><div class="language-docker line-numbers-mode" data-highlighter="prismjs" data-ext="docker" data-title="docker"><pre><code><span class="line"><span class="token comment"># syntax=docker/dockerfile:1</span></span>
<span class="line"></span>
<span class="line"><span class="token comment"># 빌드 시크릿: 레이어에 남지 않게 토큰을 주입</span></span>
<span class="line"><span class="token instruction"><span class="token keyword">RUN</span> <span class="token options"><span class="token property">--mount</span><span class="token punctuation">=</span><span class="token string">type=secret,id=npmrc,target=/root/.npmrc</span></span> <span class="token operator">\\</span></span>
<span class="line">    npm ci</span></span>
<span class="line"></span>
<span class="line"><span class="token comment"># 캐시 마운트: pip 다운로드 캐시를 빌드끼리 공유</span></span>
<span class="line"><span class="token instruction"><span class="token keyword">RUN</span> <span class="token options"><span class="token property">--mount</span><span class="token punctuation">=</span><span class="token string">type=cache,target=/root/.cache/pip</span></span> <span class="token operator">\\</span></span>
<span class="line">    pip install -r requirements.txt</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line"><span class="token comment"># 시크릿을 전달하며 빌드 (값이 이미지에 남지 않는다)</span></span>
<span class="line"><span class="token function">docker</span> build <span class="token parameter variable">--secret</span> <span class="token assign-left variable">id</span><span class="token operator">=</span>npmrc,src<span class="token operator">=</span><span class="token environment constant">$HOME</span>/.npmrc <span class="token parameter variable">-t</span> myapp:1.0 <span class="token builtin class-name">.</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><pre class="mermaid">sequenceDiagram
    participant CLI as docker build
    participant BK as BuildKit
    participant Cache as 캐시 저장소
    participant Reg as 레지스트리
    CLI-&gt;&gt;BK: Dockerfile + 컨텍스트 전달
    BK-&gt;&gt;BK: 의존성 그래프 분석 (병렬화)
    BK-&gt;&gt;Cache: 변경 안 된 스테이지 캐시 조회
    Cache--&gt;&gt;BK: 캐시 히트 → 재사용
    BK-&gt;&gt;BK: 변경된 스테이지만 재빌드
    BK-&gt;&gt;Reg: 이미지 push (--push)
</pre><p><code>docker buildx</code>를 쓰면 한 번의 빌드로 여러 아키텍처(amd64/arm64) 이미지를 만드는 멀티플랫폼 빌드도 가능하다.</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line"><span class="token function">docker</span> buildx build <span class="token parameter variable">--platform</span> linux/amd64,linux/arm64 <span class="token parameter variable">-t</span> myapp:1.0 <span class="token parameter variable">--push</span> <span class="token builtin class-name">.</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><div class="hint-container tip"><p class="hint-container-title">핵심 정리</p><ul><li>Dockerfile의 각 명령은 레이어를 만들고, <strong>한 레이어 캐시가 깨지면 이후 레이어가 모두 깨진다</strong>. 의존성 설치와 소스 복사를 분리해 캐시를 살린다.</li><li><code>ENTRYPOINT</code>는 &quot;무슨 프로그램인가&quot;를 고정하고, <code>CMD</code>는 덮어쓸 수 있는 기본 인자를 준다. <code>ARG</code>는 빌드용, <code>ENV</code>는 런타임까지 남는다.</li><li><strong>멀티스테이지 빌드</strong>로 빌드 도구를 버리고 산출물만 남겨 이미지를 극적으로 줄인다.</li><li><strong>distroless·slim·.dockerignore</strong>로 크기와 공격 표면을 줄이고, 비밀은 BuildKit <code>--secret</code>으로 레이어에 남기지 않는다.</li></ul></div><h2 id="다음-챕터" tabindex="-1"><a class="header-anchor" href="#다음-챕터"><span>다음 챕터</span></a></h2><p>이미지를 만들었으니 컨테이너끼리, 그리고 바깥세상과 어떻게 연결되는지 볼 차례다. <a href="/study/kubernetes/04-container-network-volume">컨테이너 네트워크와 볼륨</a>에서 네트워크 드라이버·포트 매핑·볼륨·Docker Compose를 다룬다.</p>`,45)]))}const p=n(i,[["render",l],["__file","03-dockerfile-image.html.vue"]]),r=JSON.parse('{"path":"/study/kubernetes/03-dockerfile-image.html","title":"Dockerfile과 이미지 빌드","lang":"en-US","frontmatter":{"title":"Dockerfile과 이미지 빌드","description":"Dockerfile 명령어와 레이어·빌드 캐시, 멀티스테이지 빌드, distroless·.dockerignore·BuildKit으로 가볍고 안전한 이미지를 만드는 법을 다룬다.","date":"2026-06-15T00:00:00.000Z","tags":["Docker","Dockerfile","BuildKit","Multistage","distroless"],"prev":"/study/kubernetes/02-docker-basics","next":"/study/kubernetes/04-container-network-volume"},"headers":[{"level":1,"title":"Dockerfile과 이미지 빌드","slug":"dockerfile과-이미지-빌드","link":"#dockerfile과-이미지-빌드","children":[{"level":2,"title":"1. Dockerfile 명령어 — 이미지의 설계도","slug":"_1-dockerfile-명령어-—-이미지의-설계도","link":"#_1-dockerfile-명령어-—-이미지의-설계도","children":[]},{"level":2,"title":"2. 레이어와 빌드 캐시 — 빠른 빌드의 핵심","slug":"_2-레이어와-빌드-캐시-—-빠른-빌드의-핵심","link":"#_2-레이어와-빌드-캐시-—-빠른-빌드의-핵심","children":[]},{"level":2,"title":"3. 멀티스테이지 빌드 — 빌드 도구를 버리고 결과만 남기기","slug":"_3-멀티스테이지-빌드-—-빌드-도구를-버리고-결과만-남기기","link":"#_3-멀티스테이지-빌드-—-빌드-도구를-버리고-결과만-남기기","children":[]},{"level":2,"title":"4. 이미지 최적화 — 작고 안전하게","slug":"_4-이미지-최적화-—-작고-안전하게","link":"#_4-이미지-최적화-—-작고-안전하게","children":[]},{"level":2,"title":"5. BuildKit — 현대적 빌드 엔진","slug":"_5-buildkit-—-현대적-빌드-엔진","link":"#_5-buildkit-—-현대적-빌드-엔진","children":[]},{"level":2,"title":"다음 챕터","slug":"다음-챕터","link":"#다음-챕터","children":[]}]}],"git":{},"filePathRelative":"_study/kubernetes/03-dockerfile-image.md"}');export{p as comp,r as data};
