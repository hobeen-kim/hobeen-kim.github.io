import{_ as s,c as t,e as n,o as d}from"./app-CZKs0mhl.js";const a="/images/study-android-internals/08-property-system-light.png",o="/images/study-android-internals/08-property-system-dark.png",i="/images/study-android-internals/08-boot-stages-light.png",r="/images/study-android-internals/08-boot-stages-dark.png",c="/images/study-android-internals/08-zygote-fork-light.png",l="/images/study-android-internals/08-zygote-fork-dark.png",p={};function g(v,e){return d(),t("div",null,e[0]||(e[0]=[n(`<h1 id="ch8-init과-zygote" tabindex="-1"><a class="header-anchor" href="#ch8-init과-zygote"><span>CH8. init과 Zygote</span></a></h1><h2 id="학습-목표" tabindex="-1"><a class="header-anchor" href="#학습-목표"><span>학습 목표</span></a></h2><ul><li>PID 1 init이 하는 일(프로세스 reaping, 파일시스템 마운트, ueventd)을 이해한다.</li><li>System Properties의 네임스페이스·로드 순서·공유 메모리 구조와 property trigger를 파악한다.</li><li>rc 파일 문법(import, on 트리거, 명령어, service 옵션)을 완전히 읽고 쓸 수 있다.</li><li>커스텀 네이티브 데몬(CAN 데몬)을 위한 rc 파일을 작성할 수 있다.</li><li>Zygote fork 모델(preload + COW)과 왜 이 구조인지, 그리고 주요 네이티브 데몬들의 역할을 안다.</li></ul><p>이 챕터는 시리즈의 심장이다. 커스텀 안드로이드에 네이티브 데몬을 서비스로 올리는 실제 작업(<a href="/study/android-internals/23-native-daemon-case-study">CH23</a>)의 문법적·개념적 토대가 전부 여기서 나온다.</p><h2 id="_1-init의-역할-—-pid-1이-짊어지는-것" tabindex="-1"><a class="header-anchor" href="#_1-init의-역할-—-pid-1이-짊어지는-것"><span>1. init의 역할 — PID 1이 짊어지는 것</span></a></h2><p><a href="/study/android-internals/07-boot-process">CH7</a>에서 봤듯 커널은 마지막에 <code>/init</code>을 PID 1로 실행한다. 리눅스에서 PID 1은 특별하다. 모든 프로세스의 조상이고, 죽으면 커널 패닉(kernel panic)이 난다. 안드로이드 init은 이 자리에서 다음을 책임진다.</p><ul><li><strong>프로세스 reaping.</strong> 유닉스에서 부모가 먼저 죽거나 자식을 명시적으로 회수하지 않으면 자식은 <strong>좀비(zombie)</strong>가 되고, 고아 프로세스는 PID 1로 입양된다. init은 <code>SIGCHLD</code>를 받아 죽은 자식을 <code>waitpid</code>로 회수해 좀비를 청소한다. <code>oneshot</code>이 아닌 서비스가 죽으면 여기서 감지해 재시작 정책을 적용한다.</li><li><strong>파일시스템 마운트.</strong> first-stage에서 핵심 파티션을, second-stage의 <code>on fs</code>/<code>on post-fs</code>/<code>on post-fs-data</code> 단계에서 나머지 마운트를 처리한다(4절 트리거 참고).</li><li><strong>watchdogd.</strong> init은 하드웨어 워치독(<code>/dev/watchdog</code>)을 주기적으로 쓰다듬어(pet) 시스템이 살아있음을 알린다. init 자신이 멈추면 pet이 끊기고 하드웨어가 기기를 리셋한다. 소프트웨어가 완전히 굳는 상황의 최후 방어선이다.</li><li><strong>서브 reaper / 서비스 관리.</strong> <code>start</code>/<code>stop</code>/<code>restart</code>로 서비스 생명주기를 관리하고, 프로퍼티 변화·트리거에 반응해 액션 큐를 실행한다.</li></ul><h3 id="ueventd-—-dev-노드와-콜드-핫플러그" tabindex="-1"><a class="header-anchor" href="#ueventd-—-dev-노드와-콜드-핫플러그"><span>ueventd — /dev 노드와 콜드/핫플러그</span></a></h3><p>리눅스 커널은 장치가 나타나거나 사라질 때 <strong>uevent</strong>를 netlink 소켓으로 브로드캐스트한다. 데스크톱 리눅스는 <code>udev</code>가 이를 받지만, 안드로이드는 init이 <code>ueventd</code> 모드로 동작해 직접 처리한다(<code>/system/bin/ueventd</code>는 init의 심볼릭 링크다).</p><ul><li><strong>콜드플러그(cold plug).</strong> 부팅 시 이미 존재하는 장치들은 uevent를 이미 흘려보낸 뒤다. ueventd는 <code>/sys</code>를 훑어 기존 장치에 대한 uevent를 재생(replay)시켜 <code>/dev</code> 노드를 만든다.</li><li><strong>핫플러그(hot plug).</strong> 부팅 후 USB·SD카드처럼 나중에 붙는 장치는 실시간 uevent로 처리한다.</li><li><strong>노드 생성과 권한.</strong> <code>ueventd.rc</code>(및 <code>/vendor/ueventd.rc</code>)에 장치 경로별 소유자·그룹·권한을 정의한다. 예를 들어 CAN 컨트롤러를 문자 장치나 네트워크 인터페이스로 노출할 때, 그 노드에 어떤 uid/gid가 접근할 수 있는지를 여기서 정한다.</li></ul><div class="language-text line-numbers-mode" data-highlighter="prismjs" data-ext="text" data-title="text"><pre><code><span class="line"># /vendor/ueventd.rc 예시 — 장치노드 권한 규칙</span>
<span class="line"># subsystem/devpath      mode   uid       gid</span>
<span class="line">/dev/spidev0.0           0660   system    can</span>
<span class="line">/dev/can0                0660   system    can</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><div class="hint-container info"><p class="hint-container-title">uevent를 직접 관찰</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line"><span class="token comment"># 커널 uevent를 실시간으로 덤프 (root)</span></span>
<span class="line">adb shell <span class="token string">&#39;toybox nc -lu ...&#39;</span>   <span class="token comment"># 실제로는 아래가 간단</span></span>
<span class="line">adb shell <span class="token string">&#39;stat /dev/can0&#39;</span>      <span class="token comment"># ueventd가 만든 노드의 권한 확인</span></span>
<span class="line">adb shell <span class="token string">&#39;ls -lZ /dev/&#39;</span>        <span class="token comment"># SELinux 레이블까지 확인</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div></div><h2 id="_2-system-properties-심층" tabindex="-1"><a class="header-anchor" href="#_2-system-properties-심층"><span>2. System Properties 심층</span></a></h2><p><strong>System Properties</strong>는 안드로이드의 전역 key-value 설정 저장소다. 유닉스의 환경변수와 비슷하지만, 프로세스 로컬이 아니라 <strong>시스템 전역이고 공유 메모리 기반</strong>이라 어떤 프로세스에서 읽어도 같은 값을 본다.</p><p><img src="`+a+'" alt="prop 소스 파일들이 property_service를 통해 공유 메모리 property store에 로드되고, 클라이언트는 mmap으로 읽고 setprop은 소켓으로 쓰는 프로퍼티 시스템 구조"><img src="'+o+`" alt="prop 소스 파일들이 property_service를 통해 공유 메모리 property store에 로드되고, 클라이언트는 mmap으로 읽고 setprop은 소켓으로 쓰는 프로퍼티 시스템 구조"></p><h3 id="네임스페이스-—-접두어가-곧-의미다" tabindex="-1"><a class="header-anchor" href="#네임스페이스-—-접두어가-곧-의미다"><span>네임스페이스 — 접두어가 곧 의미다</span></a></h3><p>프로퍼티 이름의 접두어는 관례가 아니라 <strong>동작·권한을 규정하는 네임스페이스</strong>다.</p><ul><li><strong><code>ro.</code></strong> — read-only. 부팅 중 한 번 설정되면 다시 못 바꾼다(예: <code>ro.build.version.sdk</code>, <code>ro.product.model</code>). 커널 cmdline의 <code>androidboot.*</code>는 <code>ro.boot.*</code>로 들어온다.</li><li><strong><code>persist.</code></strong> — <code>/data/property/</code>에 파일로 저장돼 재부팅 후에도 유지된다(예: <code>persist.sys.timezone</code>). 단 <code>/data</code>가 마운트된 뒤(<code>post-fs-data</code> 이후)에만 로드·저장된다.</li><li><strong><code>sys.</code></strong> — 런타임 시스템 상태(예: <code>sys.boot_completed</code>, <code>sys.usb.state</code>). 휘발성이다.</li><li><strong><code>ctl.</code></strong> — 특수 제어 네임스페이스. <code>setprop ctl.start &lt;svc&gt;</code>는 서비스를 시작시키는 명령으로 해석된다(뒤 5절).</li><li><strong><code>vendor.</code></strong> — 벤더 파티션 소유 프로퍼티. Treble 이후 vendor/system 프로퍼티 소유권이 분리됐고, SELinux로 누가 쓸 수 있는지가 엄격히 통제된다.</li><li><strong><code>debug.</code></strong>, <strong><code>dalvik.</code></strong>, <strong><code>init.svc.</code></strong> 등도 각각 디버그 플래그, ART 튜닝, 서비스 상태(<code>init.svc.&lt;name&gt;</code> = running/stopped) 용도로 쓰인다.</li></ul><h3 id="prop-파일-로드-순서" tabindex="-1"><a class="header-anchor" href="#prop-파일-로드-순서"><span>prop 파일 로드 순서</span></a></h3><p>부팅 시 init은 여러 파티션의 <code>build.prop</code> 계열 파일을 <strong>정해진 순서</strong>로 읽어 property store를 채운다. 대략적인 순서는 이렇다.</p><ol><li>ramdisk의 <code>default.prop</code>(현재는 <code>/system/etc/prop.default</code>) — 가장 먼저, 기본값.</li><li><code>/system/build.prop</code> — 시스템 이미지 빌드 정보.</li><li><code>/system_ext/</code>, <code>/product/</code> 계열.</li><li><code>/vendor/build.prop</code>, <code>/odm/etc/build.prop</code> — 벤더/ODM.</li><li><code>post-fs-data</code> 이후 <code>/data/property/persistent_properties</code> — persist.* 복원.</li></ol><p>같은 이름이 여러 곳에 있으면 로드 순서상 나중 값이 이긴다(단 <code>ro.</code>는 최초 설정만 유효). <code>PropertyInit()</code>이 이 로딩을 수행하고, 이후 프로퍼티 접근은 모두 <code>property_service</code>를 거친다.</p><h3 id="property-store-—-공유-메모리-구조" tabindex="-1"><a class="header-anchor" href="#property-store-—-공유-메모리-구조"><span>property store — 공유 메모리 구조</span></a></h3><p>프로퍼티는 <code>/dev/__properties__/</code> 아래 여러 파일에 <strong>tmpfs + mmap</strong>으로 올라간다. 핵심 자료구조는 프로퍼티 이름을 문자 단위로 쪼갠 <strong>trie(prefix tree)</strong>다. 이렇게 하면 <code>ro.build.</code> 같은 접두어 공유가 자연스럽고, SELinux 컨텍스트별로 프로퍼티를 별도 backing 파일에 분리해 접근 제어를 걸 수 있다.</p><ul><li><strong>읽기</strong>는 각 프로세스가 이 공유 메모리를 자기 주소공간에 mmap해서 <code>libc</code>의 <code>__system_property_get()</code>으로 직접 읽는다. 즉 <strong>읽기에는 IPC가 없다</strong> — 그래서 매우 빠르다.</li><li><strong>쓰기</strong>는 아무나 못 한다. 클라이언트가 <code>property_service</code>의 유닉스 도메인 소켓(<code>/dev/socket/property_service</code>)에 요청을 보내면, init 안의 property_service가 SELinux 정책으로 <strong>보내는 쪽이 이 프로퍼티를 쓸 권한이 있는지</strong> 검사한 뒤에만 store를 갱신한다. 쓰기를 init 한 곳으로 몰아 감사·권한 통제를 집중시킨 구조다.</li></ul><h3 id="실습-—-getprop-setprop-watchprops" tabindex="-1"><a class="header-anchor" href="#실습-—-getprop-setprop-watchprops"><span>실습 — getprop / setprop / watchprops</span></a></h3><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line">adb shell getprop                       <span class="token comment"># 전체 프로퍼티 덤프</span></span>
<span class="line">adb shell getprop ro.build.version.sdk  <span class="token comment"># 특정 프로퍼티 읽기</span></span>
<span class="line">adb shell getprop <span class="token operator">|</span> <span class="token function">grep</span> init.svc       <span class="token comment"># 서비스 상태 일괄 확인</span></span>
<span class="line">adb shell setprop persist.sys.mylog <span class="token number">1</span>   <span class="token comment"># 쓰기 (SELinux 허용 시)</span></span>
<span class="line">adb shell watchprops                    <span class="token comment"># 프로퍼티 변화를 실시간 스트리밍</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><code>watchprops</code>로 부팅 후 <code>sys.boot_completed</code>가 0→1로 바뀌는 순간이나, 서비스가 뜰 때 <code>init.svc.&lt;name&gt;</code>이 바뀌는 것을 직접 볼 수 있다.</p><h3 id="property-triggers" tabindex="-1"><a class="header-anchor" href="#property-triggers"><span>property triggers</span></a></h3><p>프로퍼티는 단순 저장소를 넘어 <strong>이벤트 소스</strong>다. rc 파일에서 <code>on property:&lt;name&gt;=&lt;value&gt;</code>로 트리거를 걸면, 그 프로퍼티가 해당 값이 되는 순간 액션이 실행된다.</p><div class="language-text line-numbers-mode" data-highlighter="prismjs" data-ext="text" data-title="text"><pre><code><span class="line"># 특정 프로퍼티가 원하는 값이 되면 데몬을 시작</span>
<span class="line">on property:vendor.can.ready=1</span>
<span class="line">    start can_daemon</span>
<span class="line"></span>
<span class="line"># sys.boot_completed가 뜨면 후처리</span>
<span class="line">on property:sys.boot_completed=1</span>
<span class="line">    setprop vendor.can.autostart 1</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>이 메커니즘 덕에 &quot;vold가 키를 언락하면&quot;, &quot;부팅이 끝나면&quot;, &quot;특정 HAL이 준비되면&quot; 같은 조건부 시작을 선언적으로 표현할 수 있다.</p><h2 id="_3-rc-파일-문법-완전-정복" tabindex="-1"><a class="header-anchor" href="#_3-rc-파일-문법-완전-정복"><span>3. rc 파일 문법 완전 정복</span></a></h2><p>init의 동작은 대부분 <code>*.rc</code> 파일에 <strong>선언적으로</strong> 기술된다. 문법은 Android Init Language라 부르며, 크게 <strong>action(트리거+명령)</strong>, <strong>service(데몬 정의)</strong>, <strong>import</strong>, <strong>on/command</strong> 요소로 이뤄진다.</p><h3 id="import와-로드-위치" tabindex="-1"><a class="header-anchor" href="#import와-로드-위치"><span>import와 로드 위치</span></a></h3><p>init은 부팅 시 <code>/system/etc/init/hw/init.rc</code>를 진입점으로 읽고, 거기서 여러 디렉토리를 <code>import</code>한다. 오늘날 서비스 정의는 하나의 거대한 파일이 아니라, 각 파티션의 <strong>init 디렉토리에 파일별로 흩어져</strong> 있고 init이 자동으로 모두 로드한다.</p><ul><li><code>/system/etc/init/</code> — 시스템 데몬.</li><li><code>/vendor/etc/init/</code> — 벤더 데몬(여기가 커스텀 CAN 데몬이 들어갈 자리다).</li><li><code>/odm/etc/init/</code>, <code>/product/etc/init/</code> 등.</li></ul><div class="language-text line-numbers-mode" data-highlighter="prismjs" data-ext="text" data-title="text"><pre><code><span class="line"># init.rc 상단의 import 예시 (AOSP)</span>
<span class="line">import /init.environ.rc</span>
<span class="line">import /system/etc/init/hw/init.usb.rc</span>
<span class="line">import /vendor/etc/init/hw/init.\${ro.hardware}.rc</span>
<span class="line">import /init.\${ro.zygote}.rc</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><code>import</code>는 파싱 시점에 즉시 펼쳐지지 않고 큐에 쌓였다가 순서대로 처리되며, 같은 트리거의 액션은 <strong>여러 파일에 나뉘어 있어도 파싱된 순서대로 이어 실행</strong>된다.</p><h3 id="on-트리거와-부트-단계" tabindex="-1"><a class="header-anchor" href="#on-트리거와-부트-단계"><span>on 트리거와 부트 단계</span></a></h3><p><code>on &lt;trigger&gt;</code> 블록은 트리거가 발생하면 그 안의 명령들을 순서대로 실행한다. 트리거는 부트 단계(<code>boot</code>, <code>fs</code> 등), 프로퍼티(<code>property:x=y</code>), 또는 커스텀 트리거(<code>trigger &lt;name&gt;</code>으로 발동)일 수 있다. 부트 단계 트리거는 <strong>정해진 순서</strong>로 발동한다.</p><p><img src="`+i+'" alt="early-init부터 init, late-init, fs, post-fs, post-fs-data, zygote-start, boot까지 순서대로 발동하는 init 부트 단계 트리거 흐름"><img src="'+r+`" alt="early-init부터 init, late-init, fs, post-fs, post-fs-data, zygote-start, boot까지 순서대로 발동하는 init 부트 단계 트리거 흐름"></p><ul><li><strong>early-init</strong> — 가장 이른 단계. ueventd 콜드플러그, SELinux 준비.</li><li><strong>init</strong> — 기본 디렉토리·심볼릭 링크 생성, 커널 파라미터 설정.</li><li><strong>late-init</strong> — 이후 단계들을 순서대로 큐잉하는 관문(<code>trigger fs</code>, <code>trigger post-fs</code> ...).</li><li><strong>fs</strong> — fstab 기반 파일시스템 마운트.</li><li><strong>post-fs</strong> — <code>/vendor</code> 등이 마운트된 뒤 접근 가능해지는 단계.</li><li><strong>post-fs-data</strong> — vold가 <code>/data</code>를 복호화·마운트한 뒤. 여기서부터 <code>/data</code>에 쓸 수 있고 <code>persist.*</code>가 로드된다.</li><li><strong>zygote-start</strong> — servicemanager·Zygote 등 코어 프로세스를 시작.</li><li><strong>boot</strong> — 부팅이 거의 끝난 시점의 마무리 액션.</li></ul><p><strong>커스텀 데몬을 어느 단계에서 시작할지</strong>가 실전에서 중요하다. <code>/data</code>에 로그를 써야 하면 <code>post-fs-data</code> 이후여야 하고, 다른 HAL/서비스에 의존하면 그게 준비된 뒤여야 한다. 보통은 <code>class_start</code>나 프로퍼티 트리거로 늦게 띄운다.</p><h3 id="명령어-세트" tabindex="-1"><a class="header-anchor" href="#명령어-세트"><span>명령어 세트</span></a></h3><p><code>on</code> 블록 안에서 쓰는 대표 명령들이다.</p><div class="language-text line-numbers-mode" data-highlighter="prismjs" data-ext="text" data-title="text"><pre><code><span class="line">on post-fs-data</span>
<span class="line">    mkdir /data/vendor/can 0770 system can   # 디렉토리 생성(+권한/소유자)</span>
<span class="line">    chown system can /dev/can0               # 소유자 변경</span>
<span class="line">    chmod 0660 /dev/can0                      # 권한 변경</span>
<span class="line">    write /sys/class/net/can0/tx_queue_len 1000  # sysfs에 값 쓰기</span>
<span class="line">    setprop vendor.can.ready 1                # 프로퍼티 설정</span>
<span class="line">    start can_daemon                          # 서비스 시작</span>
<span class="line">    exec_start wait_for_bus                    # 동기 실행(끝날 때까지 대기)</span>
<span class="line">    copy /vendor/etc/can/default.cfg /data/vendor/can/cfg</span>
<span class="line">    rm /data/vendor/can/stale.lock</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><code>exec</code>/<code>exec_start</code>는 명령이 끝날 때까지 init을 블로킹하므로 남용하면 부팅이 느려진다. 대부분은 <code>start</code>(비동기 서비스 시작)를 쓴다.</p><h3 id="service-정의-—-옵션-총정리" tabindex="-1"><a class="header-anchor" href="#service-정의-—-옵션-총정리"><span>service 정의 — 옵션 총정리</span></a></h3><p><code>service</code> 블록은 init이 관리할 데몬을 정의한다. 형식은 <code>service &lt;이름&gt; &lt;실행경로&gt; [인자...]</code>이고, 그 아래 들여쓴 줄에 옵션을 나열한다. 주요 옵션은 다음과 같다.</p><table><thead><tr><th>옵션</th><th>의미</th></tr></thead><tbody><tr><td><code>class &lt;name&gt;</code></td><td>서비스 그룹. <code>class_start &lt;name&gt;</code>으로 그룹 단위 시작(예: <code>class main</code>, <code>class hal</code>)</td></tr><tr><td><code>user</code> / <code>group</code></td><td>실행 uid/gid. 보조 그룹을 여럿 나열 가능</td></tr><tr><td><code>capabilities &lt;CAP...&gt;</code></td><td>root 없이 특정 리눅스 capability만 부여(예: <code>NET_ADMIN</code>, <code>NET_RAW</code>)</td></tr><tr><td><code>disabled</code></td><td>자동 시작 안 함. <code>start</code>/<code>class_start</code>로 명시적 기동 필요</td></tr><tr><td><code>oneshot</code></td><td>한 번 실행하고 끝. 죽어도 재시작 안 함</td></tr><tr><td><code>restart_period</code> / <code>restart</code></td><td>재시작 정책. <code>oneshot</code>이 아니면 죽을 때 자동 재시작</td></tr><tr><td><code>socket &lt;name&gt; &lt;type&gt; &lt;perm&gt; [uid gid]</code></td><td>유닉스 소켓을 미리 만들어 <code>/dev/socket/&lt;name&gt;</code>으로 전달</td></tr><tr><td><code>file &lt;path&gt; &lt;mode&gt;</code></td><td>파일 디스크립터를 열어 서비스에 상속</td></tr><tr><td><code>writepid &lt;path...&gt;</code></td><td>시작 시 자기 PID를 파일에 기록(cgroup 배치 등)</td></tr><tr><td><code>seclabel &lt;label&gt;</code></td><td>이 서비스가 실행될 SELinux 도메인 지정</td></tr><tr><td><code>override</code></td><td>같은 이름의 앞선 정의를 덮어씀(벤더가 시스템 서비스 재정의)</td></tr><tr><td><code>task_profiles &lt;p...&gt;</code></td><td>cgroup 기반 task profile(스케줄·메모리 제한) 적용</td></tr><tr><td><code>priority &lt;n&gt;</code> / <code>oom_score_adjust &lt;n&gt;</code></td><td>nice 값 / LMK OOM 점수 조정</td></tr><tr><td><code>setenv &lt;k&gt; &lt;v&gt;</code></td><td>환경변수 주입</td></tr><tr><td><code>onrestart &lt;cmd&gt;</code></td><td>재시작 시 실행할 명령</td></tr></tbody></table><p><strong>keychords</strong>는 물리 키 조합에 서비스를 묶는 특수 기능이다(<code>keycodes &lt;k1&gt; &lt;k2&gt; ...</code>). 지정한 키를 동시에 누르면 서비스가 시작돼, 진단 모드나 로그 수집 데몬 진입에 쓰인다.</p><p>아래는 AOSP <code>init.rc</code>에 실제로 들어 있는 <code>ueventd</code> 정의 조각이다. 옵션이 어떻게 조합되는지 보라.</p><div class="language-text line-numbers-mode" data-highlighter="prismjs" data-ext="text" data-title="text"><pre><code><span class="line">service ueventd /system/bin/ueventd</span>
<span class="line">    class core</span>
<span class="line">    critical            # 죽으면 시스템에 치명적 → 반복 실패 시 재부팅</span>
<span class="line">    seclabel u:r:ueventd:s0</span>
<span class="line">    shutdown critical    # 셧다운 시에도 늦게까지 유지</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><code>critical</code>은 서비스가 정해진 시간 창(window) 안에 여러 번 죽으면 기기를 recovery/재부팅시키는 강한 플래그다. 함부로 붙이면 데몬 하나 때문에 부팅 루프에 빠질 수 있으니, 커스텀 데몬에는 신중히 쓴다.</p><h2 id="_4-커스텀-서비스-rc-실전-—-can-데몬" tabindex="-1"><a class="header-anchor" href="#_4-커스텀-서비스-rc-실전-—-can-데몬"><span>4. 커스텀 서비스 rc 실전 — CAN 데몬</span></a></h2><p>이제 실제 목표를 구체화하자. AgIsoStack++ 기반 네이티브 CAN 통신 데몬을 <code>/vendor/bin/can_daemon</code>으로 빌드해 올렸다고 하자. 이를 서비스로 등록하는 <code>/vendor/etc/init/can_daemon.rc</code>는 다음처럼 쓴다(본격 케이스 스터디는 <a href="/study/android-internals/23-native-daemon-case-study">CH23</a>에서 다룬다).</p><div class="language-text line-numbers-mode" data-highlighter="prismjs" data-ext="text" data-title="text"><pre><code><span class="line"># /vendor/etc/init/can_daemon.rc</span>
<span class="line">service can_daemon /vendor/bin/can_daemon --iface can0 --config /vendor/etc/can/agiso.cfg</span>
<span class="line">    class hal                       # HAL 그룹으로 묶어 class_start hal 시 함께</span>
<span class="line">    user system                     # root 대신 system uid로 실행</span>
<span class="line">    group system can inet           # CAN·네트워크 접근용 보조 그룹</span>
<span class="line">    capabilities NET_ADMIN NET_RAW  # SocketCAN 인터페이스 제어에 필요한 최소 권한</span>
<span class="line">    seclabel u:r:can_daemon:s0      # 전용 SELinux 도메인 (정책은 CH16)</span>
<span class="line">    disabled                        # 자동 시작 금지 → 아래 트리거로 제어</span>
<span class="line">    oom_score_adjust -800           # LMK가 쉽게 죽이지 못하게</span>
<span class="line">    writepid /dev/cpuset/system-background/tasks</span>
<span class="line"></span>
<span class="line"># can0 인터페이스가 준비되면 데몬을 올린다</span>
<span class="line">on property:vendor.can.iface_up=1</span>
<span class="line">    start can_daemon</span>
<span class="line"></span>
<span class="line"># /data가 준비된 뒤 런타임 디렉토리·권한 세팅</span>
<span class="line">on post-fs-data</span>
<span class="line">    mkdir /data/vendor/can 0770 system can</span>
<span class="line">    chown system can /dev/can0</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>여기서 설계 포인트는 세 가지다. (1) root로 돌리지 않고 <code>user system</code> + 필요한 <code>capabilities</code>만 부여해 최소 권한을 지킨다. (2) <code>disabled</code> + 프로퍼티 트리거로 &quot;인터페이스가 실제 올라온 뒤&quot;에만 시작해 경합을 피한다. (3) 전용 <code>seclabel</code> 도메인을 둬 SELinux로 이 데몬이 만질 수 있는 것을 좁힌다. SocketCAN 자체의 동작 원리는 <a href="/study/can/13-socketcan-basics">CAN 스터디 CH13</a>을 참고하라.</p><h2 id="_5-zygote-—-왜-fork-모델인가" tabindex="-1"><a class="header-anchor" href="#_5-zygote-—-왜-fork-모델인가"><span>5. Zygote — 왜 fork 모델인가</span></a></h2><p>안드로이드의 모든 앱 프로세스와 <code>system_server</code>는 <strong>Zygote</strong>라는 하나의 부모에서 <code>fork</code>돼 나온다. Zygote(수정란)라는 이름 그대로다.</p><p><img src="`+c+'" alt="Zygote가 프레임워크 클래스·리소스를 preload한 뒤 fork로 system_server와 앱 프로세스를 만들고 preload 페이지를 COW로 공유하는 모델"><img src="'+l+`" alt="Zygote가 프레임워크 클래스·리소스를 preload한 뒤 fork로 system_server와 앱 프로세스를 만들고 preload 페이지를 COW로 공유하는 모델"></p><p><strong>왜 매번 새 VM을 띄우지 않고 fork하는가.</strong> 안드로이드 앱은 ART 위에서 돌고, 프레임워크 클래스(수천 개)와 리소스를 로드하는 비용이 크다. Zygote는 부팅 시 이 <strong>공통 프레임워크 클래스·리소스를 미리 로드(preload)</strong>해두고 소켓에서 대기한다. 새 앱이 필요하면 Zygote를 <code>fork</code>만 하면 되는데, 리눅스 <code>fork</code>는 <strong>COW(Copy-on-Write)</strong>라 부모의 메모리 페이지를 실제로 복사하지 않고 공유한다. 즉 모든 앱이 같은 preload된 프레임워크 페이지를 물리적으로 공유하다가, 자기가 쓴 페이지만 그때 복제한다. 결과적으로 <strong>앱 시작이 빠르고 전체 메모리 사용이 크게 준다</strong>.</p><ul><li><strong>app_process.</strong> Zygote의 실체는 <code>/system/bin/app_process</code>(64비트는 <code>app_process64</code>)다. init이 이를 <code>--zygote --start-system-server</code> 인자로 띄운다.</li><li><strong>Zygote32/64.</strong> 32/64비트 앱을 모두 지원하려면 두 Zygote가 필요하다. <code>ro.zygote</code> 프로퍼티가 <code>zygote64_32</code>(64 주 + 32 보조) 같은 값으로 구성을 결정하고, init은 그에 맞는 <code>init.zygote64_32.rc</code>를 import한다.</li><li><strong>zygote 소켓 프로토콜.</strong> Zygote는 <code>/dev/socket/zygote</code> 유닉스 소켓에서 대기한다. AMS(ActivityManagerService)가 앱을 띄우려면 이 소켓으로 &quot;이런 uid/gid/클래스명으로 fork해라&quot;는 인자를 보내고, Zygote가 fork한 자식이 지정된 앱 클래스로 진입한다.</li><li><strong>webview_zygote.</strong> WebView 렌더러는 보안 격리를 위해 별도의 <code>webview_zygote</code>에서 fork돼 나온다.</li><li><strong>USAP 풀(Unspecialized App Process pool).</strong> Android 10+는 미리 fork만 해둔(아직 특정 앱으로 특수화되지 않은) 예비 프로세스 풀을 유지한다. 앱 시작 요청이 오면 fork를 기다리지 않고 풀에서 하나를 꺼내 특수화만 하므로 지연이 더 준다.</li></ul><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line">adb shell getprop ro.zygote                 <span class="token comment"># zygote 구성 확인 (예: zygote64_32)</span></span>
<span class="line">adb shell <span class="token function">ps</span> <span class="token parameter variable">-A</span> <span class="token operator">|</span> <span class="token function">grep</span> zygote               <span class="token comment"># 실행 중인 zygote 프로세스</span></span>
<span class="line">adb shell <span class="token string">&#39;cat /proc/$(pidof zygote64)/cmdline&#39;</span>  <span class="token comment"># app_process 인자 확인</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><code>system_server</code>는 Zygote가 부팅 직후 처음으로 fork하는 특별한 자식이다. 그 내부는 <a href="/study/android-internals/09-service-architecture">CH9</a>에서 해부한다.</p><h2 id="_6-안드로이드-데몬-총람" tabindex="-1"><a class="header-anchor" href="#_6-안드로이드-데몬-총람"><span>6. 안드로이드 데몬 총람</span></a></h2><p>init이 관리하는 네이티브 데몬들은 안드로이드 시스템의 뼈대다. 자주 마주치는 것들을 정리한다.</p><table><thead><tr><th>데몬</th><th>위치</th><th>역할</th></tr></thead><tbody><tr><td><code>servicemanager</code></td><td>/system/bin</td><td>framework binder 서비스 레지스트리(<a href="/study/android-internals/09-service-architecture">CH9</a>)</td></tr><tr><td><code>hwservicemanager</code></td><td>/system/bin</td><td>HIDL HAL용 레지스트리(hwbinder). AIDL 전환으로 축소 중</td></tr><tr><td><code>vold</code></td><td>/system/bin</td><td>Volume Daemon. 저장소 마운트·암호화·<code>/data</code> 복호화</td></tr><tr><td><code>netd</code></td><td>/system/bin</td><td>네트워크 설정(라우팅·iptables·DNS) 데몬</td></tr><tr><td><code>logd</code></td><td>/system/bin</td><td>로그 버퍼 데몬. <code>logcat</code>이 여기서 읽는다(<a href="/study/android-internals/12-logging-monitoring">CH12</a>)</td></tr><tr><td><code>lmkd</code></td><td>/system/bin</td><td>Low Memory Killer Daemon. 메모리 압박 시 프로세스 종료</td></tr><tr><td><code>installd</code></td><td>/system/bin</td><td>APK 설치·dexopt·앱 데이터 디렉토리 관리(<a href="/study/android-internals/17-package-management">CH17</a>)</td></tr><tr><td><code>statsd</code></td><td>/system/bin</td><td>통계·메트릭 수집(statsdmetrics)</td></tr><tr><td><code>ueventd</code></td><td>/system/bin</td><td>(init 링크) <code>/dev</code> 노드 생성·핫플러그(1절)</td></tr><tr><td><code>gatekeeperd</code></td><td>/system/bin</td><td>잠금화면 인증(PIN/패턴) 검증, HAL 연동</td></tr><tr><td><code>keystore2</code></td><td>/system/bin</td><td>하드웨어 지원 키 저장·서명(Keymint 연동)</td></tr><tr><td><code>adbd</code></td><td>/system/bin</td><td>ADB 데몬. USB/TCP로 셸·파일 전송 제공</td></tr><tr><td><code>init</code></td><td>/system/bin</td><td>PID 1. 나머지 전부의 부모</td></tr></tbody></table><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line">adb shell <span class="token function">ps</span> <span class="token parameter variable">-A</span> <span class="token parameter variable">-o</span> PID,<span class="token environment constant">USER</span>,NAME <span class="token operator">|</span> <span class="token function">grep</span> <span class="token parameter variable">-E</span> <span class="token string">&#39;vold|netd|logd|lmkd|servicemanager&#39;</span></span>
<span class="line">adb shell getprop <span class="token operator">|</span> <span class="token function">grep</span> init.svc.vold    <span class="token comment"># vold 서비스 상태</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p>커스텀 CAN 데몬도 결국 이 표에 한 줄 추가되는 것과 같다 — init이 관리하고, 전용 uid/SELinux 도메인을 갖고, 필요한 데몬(예: <code>vold</code>가 <code>/data</code>를 열어준 뒤)에 순서를 맞춰 뜨는 하나의 네이티브 서비스가 된다.</p><div class="hint-container tip"><p class="hint-container-title">핵심 정리</p><ul><li>init은 PID 1로서 프로세스 reaping, 파일시스템 마운트, watchdogd, 서비스 관리를 맡고, ueventd 모드로 <code>/dev</code> 노드와 핫플러그·권한을 처리한다.</li><li>System Properties는 공유 메모리(trie) 기반 전역 key-value로, 읽기는 mmap으로 IPC 없이 빠르고 쓰기는 property_service 소켓 + SELinux로 통제된다. <code>ro./persist./sys./vendor.</code> 등 접두어가 동작을 규정하고, <code>on property:</code> 트리거로 이벤트 소스가 된다.</li><li>rc 파일은 <code>import</code>, <code>on &lt;trigger&gt;</code>(early-init→...→post-fs-data→zygote-start→boot 순), 명령어, <code>service</code>(class/user/capabilities/disabled/oneshot/socket/seclabel...)로 데몬을 선언적으로 정의한다.</li><li>커스텀 CAN 데몬은 <code>user system</code>+최소 <code>capabilities</code>, <code>disabled</code>+프로퍼티 트리거 시작, 전용 <code>seclabel</code> 도메인으로 최소 권한 원칙에 맞춰 <code>/vendor/etc/init/</code>에 등록한다.</li><li>Zygote는 프레임워크를 preload한 뒤 fork(COW)로 앱·system_server를 만들어 시작 속도와 메모리를 아끼며, USAP 풀로 지연을 더 줄인다.</li></ul></div><h2 id="다음-챕터" tabindex="-1"><a class="header-anchor" href="#다음-챕터"><span>다음 챕터</span></a></h2><p><a href="/study/android-internals/09-service-architecture">CH9. 서비스 아키텍처</a>에서는 &quot;서비스&quot;의 세 가지 의미를 구분하고, 세 개의 Binder 도메인(framework/vnd/hw), ServiceManager, VINTF, 그리고 Zygote가 낳은 <code>system_server</code>의 내부 구조를 다룬다.</p>`,74)]))}const u=s(p,[["render",g],["__file","08-init-zygote.html.vue"]]),h=JSON.parse('{"path":"/study/android-internals/08-init-zygote.html","title":"CH8. init과 Zygote","lang":"en-US","frontmatter":{"title":"CH8. init과 Zygote","description":"PID 1 init의 역할과 ueventd, System Properties 심층 구조, rc 파일 문법 완전 정복(트리거·서비스 옵션), 커스텀 CAN 데몬 rc 예제, Zygote fork 모델, 그리고 안드로이드 네이티브 데몬 총람을 다룬다.","date":"2026-07-13T00:00:00.000Z","tags":["android","aosp","init","zygote","property","rc"]},"headers":[{"level":1,"title":"CH8. init과 Zygote","slug":"ch8-init과-zygote","link":"#ch8-init과-zygote","children":[{"level":2,"title":"학습 목표","slug":"학습-목표","link":"#학습-목표","children":[]},{"level":2,"title":"1. init의 역할 — PID 1이 짊어지는 것","slug":"_1-init의-역할-—-pid-1이-짊어지는-것","link":"#_1-init의-역할-—-pid-1이-짊어지는-것","children":[{"level":3,"title":"ueventd — /dev 노드와 콜드/핫플러그","slug":"ueventd-—-dev-노드와-콜드-핫플러그","link":"#ueventd-—-dev-노드와-콜드-핫플러그","children":[]}]},{"level":2,"title":"2. System Properties 심층","slug":"_2-system-properties-심층","link":"#_2-system-properties-심층","children":[{"level":3,"title":"네임스페이스 — 접두어가 곧 의미다","slug":"네임스페이스-—-접두어가-곧-의미다","link":"#네임스페이스-—-접두어가-곧-의미다","children":[]},{"level":3,"title":"prop 파일 로드 순서","slug":"prop-파일-로드-순서","link":"#prop-파일-로드-순서","children":[]},{"level":3,"title":"property store — 공유 메모리 구조","slug":"property-store-—-공유-메모리-구조","link":"#property-store-—-공유-메모리-구조","children":[]},{"level":3,"title":"실습 — getprop / setprop / watchprops","slug":"실습-—-getprop-setprop-watchprops","link":"#실습-—-getprop-setprop-watchprops","children":[]},{"level":3,"title":"property triggers","slug":"property-triggers","link":"#property-triggers","children":[]}]},{"level":2,"title":"3. rc 파일 문법 완전 정복","slug":"_3-rc-파일-문법-완전-정복","link":"#_3-rc-파일-문법-완전-정복","children":[{"level":3,"title":"import와 로드 위치","slug":"import와-로드-위치","link":"#import와-로드-위치","children":[]},{"level":3,"title":"on 트리거와 부트 단계","slug":"on-트리거와-부트-단계","link":"#on-트리거와-부트-단계","children":[]},{"level":3,"title":"명령어 세트","slug":"명령어-세트","link":"#명령어-세트","children":[]},{"level":3,"title":"service 정의 — 옵션 총정리","slug":"service-정의-—-옵션-총정리","link":"#service-정의-—-옵션-총정리","children":[]}]},{"level":2,"title":"4. 커스텀 서비스 rc 실전 — CAN 데몬","slug":"_4-커스텀-서비스-rc-실전-—-can-데몬","link":"#_4-커스텀-서비스-rc-실전-—-can-데몬","children":[]},{"level":2,"title":"5. Zygote — 왜 fork 모델인가","slug":"_5-zygote-—-왜-fork-모델인가","link":"#_5-zygote-—-왜-fork-모델인가","children":[]},{"level":2,"title":"6. 안드로이드 데몬 총람","slug":"_6-안드로이드-데몬-총람","link":"#_6-안드로이드-데몬-총람","children":[]},{"level":2,"title":"다음 챕터","slug":"다음-챕터","link":"#다음-챕터","children":[]}]}],"git":{},"filePathRelative":"_study/android-internals/08-init-zygote.md"}');export{u as comp,h as data};
