import{_ as s,c as n,e as a,o as t}from"./app-DncCSHO0.js";const l={};function c(i,e){return t(),n("div",null,e[0]||(e[0]=[a(`<h1 id="dns와-서비스-디스커버리" tabindex="-1"><a class="header-anchor" href="#dns와-서비스-디스커버리"><span>DNS와 서비스 디스커버리</span></a></h1><div class="hint-container info"><p class="hint-container-title">학습 목표</p><ul><li>CoreDNS가 클러스터 DNS로서 어떤 역할을 하는지 이해한다.</li><li>Service와 Pod의 DNS 레코드 명명 규칙을 정확히 안다.</li><li>dnsPolicy와 search 도메인이 짧은 이름 조회를 어떻게 가능하게 하는지 파악한다.</li><li>서비스 디스커버리 패턴(환경변수 vs DNS, headless)을 비교한다.</li></ul></div><h2 id="_1-coredns의-역할" tabindex="-1"><a class="header-anchor" href="#_1-coredns의-역할"><span>1. CoreDNS의 역할</span></a></h2><p>쿠버네티스에서 Service를 IP 대신 이름으로 부를 수 있는 것은 클러스터 내부 DNS 덕분이다. 현재 기본 구현은 <strong>CoreDNS</strong>로, <code>kube-system</code> 네임스페이스에 Deployment로 떠 있고 <code>kube-dns</code>라는 Service로 노출된다. 모든 Pod는 이 DNS 서버를 가리키도록 자동 설정된다.</p><p>CoreDNS는 API 서버를 감시하며 Service·엔드포인트 정보를 받아, 그에 대응하는 DNS 레코드를 동적으로 제공한다. Pod가 <code>backend</code>를 조회하면 CoreDNS가 해당 Service의 ClusterIP를 응답한다.</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line"><span class="token comment"># CoreDNS 확인</span></span>
<span class="line">kubectl get pods <span class="token parameter variable">-n</span> kube-system <span class="token parameter variable">-l</span> k8s-app<span class="token operator">=</span>kube-dns</span>
<span class="line">kubectl get svc <span class="token parameter variable">-n</span> kube-system kube-dns</span>
<span class="line"><span class="token comment"># NAME       TYPE        CLUSTER-IP   PORT(S)</span></span>
<span class="line"><span class="token comment"># kube-dns   ClusterIP   10.96.0.10   53/UDP,53/TCP</span></span>
<span class="line"></span>
<span class="line"><span class="token comment"># Pod의 resolv.conf는 이 DNS를 가리킨다</span></span>
<span class="line">kubectl <span class="token builtin class-name">exec</span> <span class="token parameter variable">-it</span> some-pod -- <span class="token function">cat</span> /etc/resolv.conf</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>CoreDNS의 동작은 <code>Corefile</code>이라는 설정(ConfigMap)으로 정의된다. <code>kubernetes</code> 플러그인이 클러스터 도메인을 처리하고, 그 외 외부 도메인은 업스트림 DNS로 포워딩한다.</p><pre class="mermaid">flowchart TB
    pod[&quot;Pod&quot;] -- &quot;backend 조회&quot; --&gt; cd[&quot;CoreDNS&lt;br&gt;(kube-dns 10.96.0.10)&quot;]
    cd -- &quot;kubernetes 플러그인&quot; --&gt; kapi[&quot;API 서버&lt;br&gt;Service/Endpoint 정보&quot;]
    cd -- &quot;외부 도메인&lt;br&gt;forward&quot; --&gt; up[&quot;업스트림 DNS&quot;]
    cd -- &quot;ClusterIP 응답&quot; --&gt; pod
</pre><p>전체 동작은 <a href="https://kubernetes.io/docs/concepts/services-networking/dns-pod-service/" target="_blank" rel="noopener noreferrer">DNS for Services and Pods 문서</a>에 정리돼 있다.</p><h2 id="_2-service의-dns-레코드-규칙" tabindex="-1"><a class="header-anchor" href="#_2-service의-dns-레코드-규칙"><span>2. Service의 DNS 레코드 규칙</span></a></h2><p>Service의 DNS 이름은 정해진 규칙을 따른다. 표준 형식은 다음과 같다.</p><div class="language-text line-numbers-mode" data-highlighter="prismjs" data-ext="text" data-title="text"><pre><code><span class="line">&lt;service-name&gt;.&lt;namespace&gt;.svc.&lt;cluster-domain&gt;</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>기본 클러스터 도메인은 <code>cluster.local</code>이므로, <code>default</code> 네임스페이스의 <code>backend</code> Service는 다음 FQDN을 가진다.</p><div class="language-text line-numbers-mode" data-highlighter="prismjs" data-ext="text" data-title="text"><pre><code><span class="line">backend.default.svc.cluster.local</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>핵심은 이름이 <strong>네임스페이스를 포함</strong>한다는 점이다. 같은 네임스페이스 안에서는 짧게 <code>backend</code>만 써도 되지만, 다른 네임스페이스의 Service를 부르려면 네임스페이스를 명시해야 한다.</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line"><span class="token comment"># 같은 네임스페이스: 짧은 이름</span></span>
<span class="line"><span class="token function">curl</span> http://backend:8080</span>
<span class="line"></span>
<span class="line"><span class="token comment"># 다른 네임스페이스(payment)의 Service</span></span>
<span class="line"><span class="token function">curl</span> http://gateway.payment:8080</span>
<span class="line"><span class="token function">curl</span> http://gateway.payment.svc.cluster.local:8080   <span class="token comment"># FQDN</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><table><thead><tr><th>대상</th><th>DNS 이름</th><th>반환</th></tr></thead><tbody><tr><td>일반 Service</td><td><code>svc.ns.svc.cluster.local</code></td><td>A 레코드 = ClusterIP</td></tr><tr><td>headless Service</td><td><code>svc.ns.svc.cluster.local</code></td><td>A 레코드 = 각 Pod IP들</td></tr><tr><td>명명된 포트</td><td><code>_http._tcp.svc.ns.svc.cluster.local</code></td><td>SRV 레코드</td></tr></tbody></table><h2 id="_3-pod의-dns-레코드" tabindex="-1"><a class="header-anchor" href="#_3-pod의-dns-레코드"><span>3. Pod의 DNS 레코드</span></a></h2><p>Pod에도 DNS 레코드를 만들 수 있다. 일반 Pod의 기본 A 레코드 형식은 IP의 점을 대시로 바꾼 형태다.</p><div class="language-text line-numbers-mode" data-highlighter="prismjs" data-ext="text" data-title="text"><pre><code><span class="line">&lt;pod-ip-dashes&gt;.&lt;namespace&gt;.pod.cluster.local</span>
<span class="line"># 예: 10.244.1.7 → 10-244-1-7.default.pod.cluster.local</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p>이 형태는 잘 쓰이지 않는다. 실무에서 의미 있는 것은 <strong>headless Service에 속한 Pod</strong>의 안정적인 이름이다. StatefulSet과 headless Service를 함께 쓰면 각 Pod가 다음 이름을 가진다.</p><div class="language-text line-numbers-mode" data-highlighter="prismjs" data-ext="text" data-title="text"><pre><code><span class="line">&lt;pod-hostname&gt;.&lt;headless-svc&gt;.&lt;namespace&gt;.svc.cluster.local</span>
<span class="line"># 예: db-0.db-headless.default.svc.cluster.local</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><pre class="mermaid">flowchart TB
    hs[&quot;headless Service&lt;br&gt;db-headless&quot;] --&gt; db0[&quot;db-0&quot;]
    hs --&gt; db1[&quot;db-1&quot;]
    hs --&gt; db2[&quot;db-2&quot;]
    db0 -. &quot;db-0.db-headless.ns.svc.cluster.local&quot; .-&gt; dns0[&quot;안정적 Pod DNS&quot;]
    db1 -. &quot;db-1.db-headless....&quot; .-&gt; dns1[&quot;안정적 Pod DNS&quot;]
</pre><p>이 안정적인 이름 덕분에 데이터베이스 클러스터 같은 스테이트풀 워크로드에서 각 멤버를 고정된 주소로 지칭할 수 있다.</p><h2 id="_4-dnspolicy와-search-도메인" tabindex="-1"><a class="header-anchor" href="#_4-dnspolicy와-search-도메인"><span>4. dnsPolicy와 search 도메인</span></a></h2><p><code>backend</code>처럼 짧은 이름이 어떻게 FQDN으로 해석되는지는 Pod의 <code>/etc/resolv.conf</code>에 있는 <strong>search 도메인</strong> 덕분이다.</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line">kubectl <span class="token builtin class-name">exec</span> <span class="token parameter variable">-it</span> some-pod -- <span class="token function">cat</span> /etc/resolv.conf</span>
<span class="line"><span class="token comment"># nameserver 10.96.0.10</span></span>
<span class="line"><span class="token comment"># search default.svc.cluster.local svc.cluster.local cluster.local</span></span>
<span class="line"><span class="token comment"># options ndots:5</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><code>backend</code>를 조회하면 리졸버가 search 도메인을 순서대로 붙여 <code>backend.default.svc.cluster.local</code>을 먼저 시도하고, 거기서 응답을 받는다. <code>ndots:5</code>는 점이 5개 미만인 이름은 우선 search 도메인을 붙여 시도하라는 의미다.</p><p>Pod의 DNS 동작은 <strong>dnsPolicy</strong>로 제어한다.</p><table><thead><tr><th>dnsPolicy</th><th>동작</th></tr></thead><tbody><tr><td>ClusterFirst</td><td>(기본) 클러스터 DNS를 먼저, 외부는 업스트림으로 포워딩</td></tr><tr><td>Default</td><td>노드의 resolv.conf를 그대로 상속(클러스터 DNS 미사용)</td></tr><tr><td>ClusterFirstWithHostNet</td><td>hostNetwork Pod에서 클러스터 DNS를 쓰려면 이걸로</td></tr><tr><td>None</td><td>모든 DNS 설정을 dnsConfig로 직접 지정</td></tr></tbody></table><p><code>None</code>을 쓰면 search·nameserver·options를 완전히 커스터마이즈할 수 있다.</p><div class="language-yaml line-numbers-mode" data-highlighter="prismjs" data-ext="yml" data-title="yml"><pre><code><span class="line"><span class="token key atrule">spec</span><span class="token punctuation">:</span></span>
<span class="line">  <span class="token key atrule">dnsPolicy</span><span class="token punctuation">:</span> <span class="token string">&quot;None&quot;</span></span>
<span class="line">  <span class="token key atrule">dnsConfig</span><span class="token punctuation">:</span></span>
<span class="line">    <span class="token key atrule">nameservers</span><span class="token punctuation">:</span></span>
<span class="line">    <span class="token punctuation">-</span> 10.96.0.10</span>
<span class="line">    <span class="token key atrule">searches</span><span class="token punctuation">:</span></span>
<span class="line">    <span class="token punctuation">-</span> custom.svc.cluster.local</span>
<span class="line">    <span class="token key atrule">options</span><span class="token punctuation">:</span></span>
<span class="line">    <span class="token punctuation">-</span> <span class="token key atrule">name</span><span class="token punctuation">:</span> ndots</span>
<span class="line">      <span class="token key atrule">value</span><span class="token punctuation">:</span> <span class="token string">&quot;2&quot;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><div class="hint-container warning"><p class="hint-container-title">ndots:5와 외부 도메인 조회</p><p><code>ndots:5</code> 때문에 <code>api.github.com</code>(점 2개)처럼 외부 도메인을 조회하면, 리졸버가 search 도메인을 먼저 붙여 여러 번 헛조회한 뒤에야 본래 이름을 시도한다. 외부 호출이 많은 워크로드에서 DNS 지연이 의심되면 FQDN(끝에 점, <code>api.github.com.</code>)을 쓰거나 ndots를 낮추는 것을 고려한다.</p></div><h2 id="_5-서비스-디스커버리-패턴" tabindex="-1"><a class="header-anchor" href="#_5-서비스-디스커버리-패턴"><span>5. 서비스 디스커버리 패턴</span></a></h2><p>쿠버네티스에서 다른 서비스를 찾는 방법은 크게 두 가지다.</p><p><strong>환경변수 방식.</strong> Pod가 생성될 때, 이미 존재하는 Service들의 정보가 환경변수로 주입된다(예: <code>BACKEND_SERVICE_HOST</code>, <code>BACKEND_SERVICE_PORT</code>). 단, <strong>Pod 생성 시점에 이미 있던 Service만</strong> 반영된다는 결정적 한계가 있다. 나중에 만들어진 Service는 보이지 않는다.</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line">kubectl <span class="token builtin class-name">exec</span> <span class="token parameter variable">-it</span> some-pod -- <span class="token function">env</span> <span class="token operator">|</span> <span class="token function">grep</span> SERVICE</span>
<span class="line"><span class="token comment"># BACKEND_SERVICE_HOST=10.96.0.42</span></span>
<span class="line"><span class="token comment"># BACKEND_SERVICE_PORT=8080</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><strong>DNS 방식.</strong> 위에서 본 것처럼 이름으로 조회한다. 생성 순서와 무관하게 항상 최신 상태를 반영하므로 <strong>권장되는 표준 방식</strong>이다.</p><pre class="mermaid">flowchart LR
    subgraph env[&quot;환경변수 방식&quot;]
        e1[&quot;Pod 시작 시점에&lt;br&gt;존재한 Service만 주입&quot;]
    end
    subgraph dns[&quot;DNS 방식 (권장)&quot;]
        d1[&quot;언제든 이름 조회&quot;] --&gt; d2[&quot;항상 최신 ClusterIP&quot;]
    end
</pre><p>일반 Service는 DNS가 ClusterIP를 주고 그 뒤를 kube-proxy가 로드밸런싱한다. 반면 headless Service는 DNS가 개별 Pod IP들을 직접 주므로, 클라이언트가 직접 멤버를 고르거나 모든 멤버에 연결하는 패턴(예: 분산 캐시, DB 클러스터)에 적합하다.</p><div class="hint-container tip"><p class="hint-container-title">핵심 정리</p><ul><li>CoreDNS는 클러스터 기본 DNS로, API 서버를 감시해 Service·Pod 레코드를 동적으로 제공한다.</li><li>Service의 표준 DNS 이름은 svc.namespace.svc.cluster.local이며, 다른 네임스페이스는 이름에 명시해야 한다.</li><li>headless Service + StatefulSet은 db-0.svc.ns.svc.cluster.local 같은 안정적인 Pod별 이름을 제공한다.</li><li>search 도메인과 ndots 덕분에 짧은 이름이 FQDN으로 해석되며, dnsPolicy로 DNS 동작을 제어한다.</li><li>서비스 디스커버리는 생성 순서 한계가 있는 환경변수보다 항상 최신을 반영하는 DNS 방식이 표준이다.</li></ul></div><h2 id="다음-챕터" tabindex="-1"><a class="header-anchor" href="#다음-챕터"><span>다음 챕터</span></a></h2><p>지금까지 Pod끼리 어떻게 찾고 통신하는지를 다뤘다. 그런데 모든 Pod가 서로 자유롭게 통신할 수 있다는 기본 상태는 보안상 위험하다. 다음 챕터 <a href="/study/kubernetes/29-network-policy">NetworkPolicy</a>에서는 트래픽을 명시적으로 허용·차단하는 방화벽 규칙을 깊게 다룬다.</p>`,43)]))}const r=s(l,[["render",c],["__file","28-dns-discovery.html.vue"]]),o=JSON.parse('{"path":"/study/kubernetes/28-dns-discovery.html","title":"DNS와 서비스 디스커버리","lang":"en-US","frontmatter":{"title":"DNS와 서비스 디스커버리","description":"클러스터 내부에서 Service를 이름으로 찾는 메커니즘 — CoreDNS의 역할, Service/Pod의 DNS 레코드 규칙(svc.namespace.svc.cluster.local), dnsPolicy와 search 도메인, 그리고 서비스 디스커버리 패턴을 깊게 다룬다.","date":"2026-06-15T00:00:00.000Z","tags":["Kubernetes","DNS","CoreDNS","Service Discovery","dnsPolicy"],"prev":"/study/kubernetes/27-ingress-gateway","next":"/study/kubernetes/29-network-policy"},"headers":[{"level":1,"title":"DNS와 서비스 디스커버리","slug":"dns와-서비스-디스커버리","link":"#dns와-서비스-디스커버리","children":[{"level":2,"title":"1. CoreDNS의 역할","slug":"_1-coredns의-역할","link":"#_1-coredns의-역할","children":[]},{"level":2,"title":"2. Service의 DNS 레코드 규칙","slug":"_2-service의-dns-레코드-규칙","link":"#_2-service의-dns-레코드-규칙","children":[]},{"level":2,"title":"3. Pod의 DNS 레코드","slug":"_3-pod의-dns-레코드","link":"#_3-pod의-dns-레코드","children":[]},{"level":2,"title":"4. dnsPolicy와 search 도메인","slug":"_4-dnspolicy와-search-도메인","link":"#_4-dnspolicy와-search-도메인","children":[]},{"level":2,"title":"5. 서비스 디스커버리 패턴","slug":"_5-서비스-디스커버리-패턴","link":"#_5-서비스-디스커버리-패턴","children":[]},{"level":2,"title":"다음 챕터","slug":"다음-챕터","link":"#다음-챕터","children":[]}]}],"git":{},"filePathRelative":"_study/kubernetes/28-dns-discovery.md"}');export{r as comp,o as data};
