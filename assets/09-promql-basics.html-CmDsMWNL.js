import{_ as s,c as a,f as e,o as t}from"./app-g85Ro3E6.js";const o="/images/study-observability/09-vector-types-light.png",c="/images/study-observability/09-vector-types-dark.png",p="/images/study-observability/09-counter-functions-light.png",l="/images/study-observability/09-counter-functions-dark.png",i={};function r(d,n){return t(),a("div",null,n[0]||(n[0]=[e(`<h1 id="promql-기초" tabindex="-1"><a class="header-anchor" href="#promql-기초"><span>PromQL 기초</span></a></h1><div class="hint-container info"><p class="hint-container-title">학습 목표</p><ul><li>instant vector와 range vector의 차이를 이해하고 언제 range selector <code>[5m]</code>가 필요한지 판단한다.</li><li>라벨 매처(<code>=</code>, <code>!=</code>, <code>=~</code>, <code>!~</code>)로 원하는 시계열만 정확히 골라낸다.</li><li>counter 타입에 <code>rate</code>/<code>irate</code>/<code>increase</code>를 써야 하는 이유와 rate의 흔한 함정을 파악한다.</li><li><code>sum</code>/<code>avg</code>/<code>max</code> 같은 집계 연산자를 <code>by</code>/<code>without</code>으로 원하는 카디널리티로 줄인다.</li><li>산술·비교·논리 연산자와 <code>histogram_quantile</code> 등 실무에서 자주 쓰는 함수를 익힌다.</li></ul></div><h2 id="_1-instant-vector와-range-vector" tabindex="-1"><a class="header-anchor" href="#_1-instant-vector와-range-vector"><span>1. Instant Vector와 Range Vector</span></a></h2><p>PromQL이 다루는 데이터 타입은 크게 네 가지(scalar, string, instant vector, range vector)지만, 실무에서 매일 쓰는 건 사실상 두 가지다.</p><p><strong>Instant vector</strong>는 특정 시점(기본은 쿼리 실행 시각) 기준으로, 라벨 집합이 같은 시계열마다 값 하나씩을 가진 벡터다. <code>http_requests_total</code>처럼 메트릭 이름만 쓰면 instant vector가 나온다.</p><p><strong>Range vector</strong>는 각 시계열에 대해 특정 구간의 샘플 목록을 통째로 담는다. 메트릭 이름 뒤에 <code>[5m]</code> 같은 range selector를 붙이면 만들어지며, <code>rate()</code>·<code>increase()</code>처럼 구간 전체를 입력으로 받는 함수의 재료로만 쓰인다. range vector 자체는 그래프로 그릴 수 없고, 반드시 range-to-instant 함수를 거쳐야 instant vector로 돌아온다.</p><div class="language-promql line-numbers-mode" data-highlighter="prismjs" data-ext="promql" data-title="promql"><pre><code><span class="line"><span class="token comment"># instant vector — 현재 시점의 값 하나</span></span>
<span class="line">http_requests_total<span class="token context-labels"><span class="token punctuation">{</span><span class="token label-key attr-name">job</span><span class="token punctuation">=</span><span class="token label-value attr-value">&quot;api&quot;</span><span class="token punctuation">}</span></span></span>
<span class="line"></span>
<span class="line"><span class="token comment"># range vector — 최근 5분간의 샘플들 (그 자체로는 그래프 불가)</span></span>
<span class="line">http_requests_total<span class="token context-labels"><span class="token punctuation">{</span><span class="token label-key attr-name">job</span><span class="token punctuation">=</span><span class="token label-value attr-value">&quot;api&quot;</span><span class="token punctuation">}</span></span><span class="token context-range"><span class="token punctuation">[</span><span class="token range-duration number">5m</span><span class="token punctuation">]</span></span></span>
<span class="line"></span>
<span class="line"><span class="token comment"># range vector를 rate()에 넣어야 instant vector가 된다</span></span>
<span class="line"><span class="token function">rate</span><span class="token punctuation">(</span>http_requests_total<span class="token context-labels"><span class="token punctuation">{</span><span class="token label-key attr-name">job</span><span class="token punctuation">=</span><span class="token label-value attr-value">&quot;api&quot;</span><span class="token punctuation">}</span></span><span class="token context-range"><span class="token punctuation">[</span><span class="token range-duration number">5m</span><span class="token punctuation">]</span></span><span class="token punctuation">)</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><img src="`+o+'" alt="메트릭 이름은 바로 Instant Vector가 되고, Range Selector [5m]를 붙이면 Range Vector가 되며 rate·increase 같은 함수를 거쳐야 다시 Instant Vector로 변환된다"><img src="'+c+`" alt="메트릭 이름은 바로 Instant Vector가 되고, Range Selector [5m]를 붙이면 Range Vector가 되며 rate·increase 같은 함수를 거쳐야 다시 Instant Vector로 변환된다"></p><p>range selector의 길이는 스크레이프 간격의 최소 2~4배로 잡는 게 안전하다. 스크레이프 주기가 15초인데 <code>[15s]</code>로 잡으면 스크레이프가 한 번이라도 밀리는 순간 구간 안에 샘플이 0~1개만 남아 <code>rate()</code>가 결측(gap)을 뱉는다.</p><h2 id="_2-셀렉터와-매처" tabindex="-1"><a class="header-anchor" href="#_2-셀렉터와-매처"><span>2. 셀렉터와 매처</span></a></h2><p>메트릭 이름 뒤 중괄호 안에 라벨 매처를 넣어 시계열을 좁힌다. 매처는 네 종류다.</p><table><thead><tr><th>연산자</th><th>의미</th><th>예시</th></tr></thead><tbody><tr><td><code>=</code></td><td>라벨 값이 정확히 일치</td><td><code>{job=&quot;api&quot;}</code></td></tr><tr><td><code>!=</code></td><td>라벨 값이 불일치</td><td><code>{job!=&quot;api&quot;}</code></td></tr><tr><td><code>=~</code></td><td>정규식 매치(RE2, 전체 문자열 anchor)</td><td><code>{status_code=~&quot;5..&quot;}</code></td></tr><tr><td><code>!~</code></td><td>정규식 불일치</td><td><code>{status_code!~&quot;2..|3..&quot;}</code></td></tr></tbody></table><div class="language-promql line-numbers-mode" data-highlighter="prismjs" data-ext="promql" data-title="promql"><pre><code><span class="line"><span class="token comment"># job이 api 또는 web인 시계열</span></span>
<span class="line">http_requests_total<span class="token context-labels"><span class="token punctuation">{</span><span class="token label-key attr-name">job</span><span class="token punctuation">=~</span><span class="token label-value attr-value">&quot;api|web&quot;</span><span class="token punctuation">}</span></span></span>
<span class="line"></span>
<span class="line"><span class="token comment"># 5xx 응답만</span></span>
<span class="line">http_requests_total<span class="token context-labels"><span class="token punctuation">{</span><span class="token label-key attr-name">status_code</span><span class="token punctuation">=~</span><span class="token label-value attr-value">&quot;5..&quot;</span><span class="token punctuation">}</span></span></span>
<span class="line"></span>
<span class="line"><span class="token comment"># 메트릭 이름 자체도 정규식으로 매칭 가능</span></span>
<span class="line"><span class="token context-labels"><span class="token punctuation">{</span><span class="token label-key attr-name">__name__</span><span class="token punctuation">=~</span><span class="token label-value attr-value">&quot;http_requests_.*&quot;</span><span class="token punctuation">}</span></span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><div class="hint-container warning"><p class="hint-container-title">정규식은 전체 문자열에 anchor된다</p><p><code>=~&quot;api&quot;</code>는 <code>job=&quot;api-gateway&quot;</code>에도 매치된다. <code>.*</code>가 암묵적으로 앞뒤에 붙지 않고 <strong>부분 문자열이 아니라 전체 매치</strong>를 시도하지만, <code>api</code>라는 패턴 자체가 <code>api-gateway</code> 전체와 매치되는 게 아니라 정규식 엔진이 문자열 전체를 이 패턴으로 설명할 수 있는지 본다. <code>job=&quot;api&quot;</code>처럼 완전히 같은 값이 아니면 <code>=~&quot;^api$&quot;</code>처럼 명시적으로 anchor를 걸어야 원하는 결과를 얻는다.</p></div><p>빈 값 매처(<code>{label=&quot;&quot;}</code>)는 그 라벨이 없는 시계열도 포함한다는 점도 기억해 둘 만하다. 반대로 <code>{label!=&quot;&quot;}</code>는 해당 라벨이 존재하고 비어있지 않은 시계열만 남긴다.</p><h2 id="_3-counter-다루기-—-rate-irate-increase" tabindex="-1"><a class="header-anchor" href="#_3-counter-다루기-—-rate-irate-increase"><span>3. Counter 다루기 — rate, irate, increase</span></a></h2><p>Prometheus의 counter는 단조 증가만 하는 누적값이다. 프로세스 재시작이나 pod 재배포로 값이 0으로 리셋될 수 있으므로, counter의 raw 값 자체는 거의 의미가 없고 항상 변화율로 바꿔서 본다.</p><p><strong><code>rate(v[t])</code></strong>는 구간 <code>[t]</code> 동안의 초당 평균 증가율을 계산한다. 내부적으로 구간의 첫 샘플과 마지막 샘플 차이를 시간으로 나누되, counter reset을 감지하면 리셋 전후 값을 보정해서 합산한다. 대시보드·알림에는 거의 항상 <code>rate</code>를 쓴다.</p><div class="language-promql line-numbers-mode" data-highlighter="prismjs" data-ext="promql" data-title="promql"><pre><code><span class="line"><span class="token comment"># 초당 요청 수 (5분 평균)</span></span>
<span class="line"><span class="token function">rate</span><span class="token punctuation">(</span>http_requests_total<span class="token context-range"><span class="token punctuation">[</span><span class="token range-duration number">5m</span><span class="token punctuation">]</span></span><span class="token punctuation">)</span></span>
<span class="line"></span>
<span class="line"><span class="token comment"># 5분간 총 증가량 (rate * 구간초)</span></span>
<span class="line"><span class="token function">increase</span><span class="token punctuation">(</span>http_requests_total<span class="token context-range"><span class="token punctuation">[</span><span class="token range-duration number">5m</span><span class="token punctuation">]</span></span><span class="token punctuation">)</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><strong><code>irate(v[t])</code></strong>는 구간 내 마지막 두 샘플만으로 순간 변화율을 계산한다. 급격한 스파이크를 더 민감하게 보여주지만, 노이즈가 심해 대시보드에서 지그재그가 심하다. 알림 조건에는 부적합하고, 짧은 구간의 순간 변화를 눈으로 확인할 때만 제한적으로 쓴다.</p><p><strong><code>increase(v[t])</code></strong>는 <code>rate(v[t]) * t</code>와 사실상 동일하다. &quot;5분간 몇 건 늘었나&quot;처럼 절대량이 궁금할 때 쓴다.</p><p><img src="`+p+'" alt="Counter 원시값을 rate(구간 평균 초당 증가율·리셋 보정)·irate(마지막 2개 샘플 순간 변화율)·increase(구간 총 증가량)로 변환해 각각 대시보드·순간 스파이크 관찰·절대 증가량 확인에 쓰는 흐름"><img src="'+l+`" alt="Counter 원시값을 rate(구간 평균 초당 증가율·리셋 보정)·irate(마지막 2개 샘플 순간 변화율)·increase(구간 총 증가량)로 변환해 각각 대시보드·순간 스파이크 관찰·절대 증가량 확인에 쓰는 흐름"></p><div class="hint-container warning"><p class="hint-container-title">rate의 흔한 함정</p><ul><li><strong>range가 너무 짧으면 결측이 생긴다.</strong> 스크레이프 간격 15초에 <code>rate(v[15s])</code>를 쓰면 구간에 샘플이 1개뿐일 때가 있어 <code>rate</code>가 값을 못 낸다. 최소 스크레이프 간격의 4배(<code>[1m]</code> 이상) 권장.</li><li><strong><code>rate</code> 전에 집계하지 않는다.</strong> <code>rate(sum(x)[5m])</code>처럼 먼저 합산한 뒤 rate를 걸면 개별 시계열의 counter reset이 뭉개져 잘못된 값이 나온다. 항상 <code>sum(rate(x[5m]))</code>처럼 rate를 먼저, 집계를 나중에 한다.</li><li><strong><code>rate</code>는 extrapolation(외삽)을 한다.</strong> 구간 경계와 실제 샘플 시각이 정확히 안 맞으면 Prometheus가 값을 추정해서 채운다. 짧은 구간일수록 추정 오차 비율이 커진다.</li><li><strong>gauge에는 <code>rate</code>를 쓰지 않는다.</strong> gauge는 증가·감소가 자유로운 값이라 <code>rate</code>의 counter reset 보정 로직이 의미를 갖지 못한다. gauge의 변화율이 필요하면 <code>deriv()</code>나 <code>delta()</code>를 쓴다.</li></ul></div><h2 id="_4-집계-연산자-—-sum-avg-max와-by-without" tabindex="-1"><a class="header-anchor" href="#_4-집계-연산자-—-sum-avg-max와-by-without"><span>4. 집계 연산자 — sum, avg, max와 by/without</span></a></h2><p>집계 연산자는 여러 시계열을 라벨 기준으로 묶어 값을 하나로 합친다. 자주 쓰는 것만 추리면 <code>sum</code>, <code>avg</code>, <code>min</code>, <code>max</code>, <code>count</code>, <code>topk</code>, <code>bottomk</code>, <code>stddev</code>, <code>quantile</code> 정도다.</p><div class="language-promql line-numbers-mode" data-highlighter="prismjs" data-ext="promql" data-title="promql"><pre><code><span class="line"><span class="token comment"># job별 요청 처리율 합계</span></span>
<span class="line"><span class="token keyword">sum</span><span class="token punctuation">(</span><span class="token function">rate</span><span class="token punctuation">(</span>http_requests_total<span class="token context-range"><span class="token punctuation">[</span><span class="token range-duration number">5m</span><span class="token punctuation">]</span></span><span class="token punctuation">)</span><span class="token punctuation">)</span> <span class="token keyword">by</span> <span class="token vector-match"><span class="token punctuation">(</span><span class="token label-key attr-name">job</span><span class="token punctuation">)</span></span></span>
<span class="line"></span>
<span class="line"><span class="token comment"># instance 라벨만 빼고 나머지 라벨은 유지</span></span>
<span class="line"><span class="token keyword">sum</span><span class="token punctuation">(</span><span class="token function">rate</span><span class="token punctuation">(</span>http_requests_total<span class="token context-range"><span class="token punctuation">[</span><span class="token range-duration number">5m</span><span class="token punctuation">]</span></span><span class="token punctuation">)</span><span class="token punctuation">)</span> <span class="token keyword">without</span> <span class="token vector-match"><span class="token punctuation">(</span><span class="token label-key attr-name">instance</span><span class="token punctuation">)</span></span></span>
<span class="line"></span>
<span class="line"><span class="token comment"># job, status_code 조합별 상위 5개</span></span>
<span class="line"><span class="token keyword">topk</span><span class="token punctuation">(</span><span class="token number">5</span><span class="token punctuation">,</span> <span class="token keyword">sum</span><span class="token punctuation">(</span><span class="token function">rate</span><span class="token punctuation">(</span>http_requests_total<span class="token context-range"><span class="token punctuation">[</span><span class="token range-duration number">5m</span><span class="token punctuation">]</span></span><span class="token punctuation">)</span><span class="token punctuation">)</span> <span class="token keyword">by</span> <span class="token vector-match"><span class="token punctuation">(</span><span class="token label-key attr-name">job</span><span class="token punctuation">,</span> <span class="token label-key attr-name">status_code</span><span class="token punctuation">)</span></span><span class="token punctuation">)</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><code>by (labels...)</code>는 지정한 라벨만 남기고 나머지는 버린 뒤 그룹핑한다. <code>without (labels...)</code>는 반대로 지정한 라벨만 버리고 나머지는 전부 유지한다. 결과에 남는 라벨 집합이 다르므로 둘은 대칭이 아니다. <code>instance</code>처럼 카디널리티가 높고 관심 없는 라벨을 뺄 때는 <code>without</code>이, <code>job</code>처럼 소수의 관심 라벨만 남기고 싶을 때는 <code>by</code>가 편하다.</p><div class="hint-container warning"><p class="hint-container-title">집계 후 라벨이 사라지면 join이 끊긴다</p><p><code>sum by (job)</code>를 거친 결과는 <code>job</code> 라벨만 남는다. 이후 다른 메트릭과 라벨 매칭 연산(10장에서 다룰 <code>on</code>/<code>ignoring</code>)을 하려면 필요한 라벨을 미리 <code>by</code>에 포함해 둬야 한다. 집계에서 라벨을 지워버리면 되돌릴 방법이 없다.</p></div><h2 id="_5-산술·비교·논리-연산자" tabindex="-1"><a class="header-anchor" href="#_5-산술·비교·논리-연산자"><span>5. 산술·비교·논리 연산자</span></a></h2><p>PromQL은 벡터끼리, 벡터와 스칼라 사이의 산술·비교·논리 연산을 지원한다.</p><div class="language-promql line-numbers-mode" data-highlighter="prismjs" data-ext="promql" data-title="promql"><pre><code><span class="line"><span class="token comment"># 산술: 메모리 사용률(%)</span></span>
<span class="line"><span class="token number">100</span> <span class="token operator">*</span> container_memory_working_set_bytes <span class="token operator">/</span> container_spec_memory_limit_bytes</span>
<span class="line"></span>
<span class="line"><span class="token comment"># 비교: 임계값을 넘는 시계열만 필터 (필터 목적으로는 bool 없이)</span></span>
<span class="line">node_load1 <span class="token operator">&gt;</span> <span class="token number">4</span></span>
<span class="line"></span>
<span class="line"><span class="token comment"># 비교 + bool: 조건 결과를 1/0으로 (알림식 안에서 산술에 쓸 때)</span></span>
<span class="line"><span class="token punctuation">(</span>node_load1 <span class="token operator">&gt;</span> bool <span class="token number">4</span><span class="token punctuation">)</span></span>
<span class="line"></span>
<span class="line"><span class="token comment"># 논리: and, or, unless (벡터 간 집합 연산)</span></span>
<span class="line">up<span class="token context-labels"><span class="token punctuation">{</span><span class="token label-key attr-name">job</span><span class="token punctuation">=</span><span class="token label-value attr-value">&quot;api&quot;</span><span class="token punctuation">}</span></span> <span class="token operator">==</span> <span class="token number">0</span></span>
<span class="line">  <span class="token operator">and</span> <span class="token keyword">on</span><span class="token vector-match"><span class="token punctuation">(</span><span class="token label-key attr-name">instance</span><span class="token punctuation">)</span></span> node_load1 <span class="token operator">&gt;</span> <span class="token number">4</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>비교 연산자(<code>==</code>, <code>!=</code>, <code>&gt;</code>, <code>&lt;</code>, <code>&gt;=</code>, <code>&lt;=</code>)를 벡터에 그냥 쓰면 조건을 만족하는 시계열만 필터링해서 반환하고, 값 자체는 원래 값을 유지한다. <code>bool</code> modifier를 붙이면 필터링 대신 조건 결과를 <code>1</code>(참)/<code>0</code>(거짓)으로 바꿔서 모든 시계열을 그대로 반환한다. 알림 룰에서는 필터링 동작을 그대로 쓰고, 스코어링처럼 산술에 결과를 활용할 때만 <code>bool</code>을 붙인다.</p><p>논리 연산자 <code>and</code>/<code>or</code>/<code>unless</code>는 라벨 집합이 일치하는 시계열끼리 집합 연산을 한다. <code>and</code>는 양쪽 다 존재하는 시계열만, <code>unless</code>는 왼쪽에서 오른쪽에 없는 시계열만 남긴다. 연산자 우선순위는 <code>^</code> &gt; <code>*, /, %, atan2</code> &gt; <code>+, -</code> &gt; 비교 연산자 &gt; <code>and, unless</code> &gt; <code>or</code> 순이며, 헷갈리면 괄호로 명시하는 편이 안전하다.</p><h2 id="_6-자주-쓰는-함수" tabindex="-1"><a class="header-anchor" href="#_6-자주-쓰는-함수"><span>6. 자주 쓰는 함수</span></a></h2><p><strong><code>histogram_quantile(φ, b)</code></strong>는 히스토그램의 <code>_bucket</code> 시계열로부터 분위수를 추정한다. 자세한 버킷 집계 순서와 함정은 <a href="/study/observability/10-promql-advanced">PromQL 심화</a>에서 다루지만, 기본형은 다음과 같다.</p><div class="language-promql line-numbers-mode" data-highlighter="prismjs" data-ext="promql" data-title="promql"><pre><code><span class="line"><span class="token comment"># p99 레이턴시 (le 라벨 기준 버킷 사용)</span></span>
<span class="line"><span class="token function">histogram_quantile</span><span class="token punctuation">(</span><span class="token number">0.99</span><span class="token punctuation">,</span> <span class="token keyword">sum</span><span class="token punctuation">(</span><span class="token function">rate</span><span class="token punctuation">(</span>http_request_duration_seconds_bucket<span class="token context-range"><span class="token punctuation">[</span><span class="token range-duration number">5m</span><span class="token punctuation">]</span></span><span class="token punctuation">)</span><span class="token punctuation">)</span> <span class="token keyword">by</span> <span class="token vector-match"><span class="token punctuation">(</span><span class="token label-key attr-name">le</span><span class="token punctuation">)</span></span><span class="token punctuation">)</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p><strong><code>absent(v)</code></strong>는 벡터 <code>v</code>에 시계열이 하나도 없으면 값 <code>1</code>짜리 벡터를, 하나라도 있으면 빈 벡터를 반환한다. &quot;타깃이 사라졌다&quot;류의 알림에 필수다.</p><div class="language-promql line-numbers-mode" data-highlighter="prismjs" data-ext="promql" data-title="promql"><pre><code><span class="line"><span class="token comment"># job=api의 up 시계열이 아예 없으면(스크레이프 자체가 안 됨) 알림</span></span>
<span class="line"><span class="token function">absent</span><span class="token punctuation">(</span>up<span class="token context-labels"><span class="token punctuation">{</span><span class="token label-key attr-name">job</span><span class="token punctuation">=</span><span class="token label-value attr-value">&quot;api&quot;</span><span class="token punctuation">}</span></span><span class="token punctuation">)</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p><strong><code>clamp(v, min, max)</code></strong>는 값을 <code>[min, max]</code> 범위로 자른다(구버전의 <code>clamp_min</code>/<code>clamp_max</code>를 통합). 이상치를 시각화에서 눌러줄 때 쓴다.</p><div class="language-promql line-numbers-mode" data-highlighter="prismjs" data-ext="promql" data-title="promql"><pre><code><span class="line"><span class="token function">clamp</span><span class="token punctuation">(</span>node_load1<span class="token punctuation">,</span> <span class="token number">0</span><span class="token punctuation">,</span> <span class="token number">10</span><span class="token punctuation">)</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p><strong><code>label_replace(v, dst, replacement, src, regex)</code></strong>는 라벨 값에 정규식을 적용해 새 라벨을 만들거나 기존 라벨을 바꾼다. 서로 다른 메트릭의 라벨 스키마를 맞춰 join하기 전 전처리로 자주 쓴다.</p><div class="language-promql line-numbers-mode" data-highlighter="prismjs" data-ext="promql" data-title="promql"><pre><code><span class="line"><span class="token comment"># instance=&quot;10.0.1.5:9100&quot; 에서 IP만 뽑아 host 라벨로 추가</span></span>
<span class="line"><span class="token function">label_replace</span><span class="token punctuation">(</span>node_load1<span class="token punctuation">,</span> &quot;host&quot;<span class="token punctuation">,</span> &quot;$<span class="token number">1</span>&quot;<span class="token punctuation">,</span> &quot;instance&quot;<span class="token punctuation">,</span> &quot;<span class="token punctuation">(</span><span class="token punctuation">[</span><span class="token operator">^</span>:<span class="token punctuation">]</span><span class="token operator">+</span><span class="token punctuation">)</span>:<span class="token punctuation">.</span><span class="token operator">*</span>&quot;<span class="token punctuation">)</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p>이 밖에 <code>abs</code>, <code>round</code>, <code>sort_desc</code>, <code>changes</code>, <code>resets</code>, <code>delta</code>, <code>deriv</code>, <code>predict_linear</code>도 운영 대시보드·알림에서 자주 등장하므로 <a href="https://prometheus.io/docs/prometheus/latest/querying/functions/" target="_blank" rel="noopener noreferrer">PromQL 함수 레퍼런스</a>를 한 번 훑어두면 좋다.</p><div class="hint-container tip"><p class="hint-container-title">핵심 정리</p><ul><li>instant vector는 시점 값, range vector는 구간 샘플 모음이며 range vector는 반드시 함수를 거쳐야 그래프로 그릴 수 있다.</li><li>라벨 매처는 <code>=</code>/<code>!=</code>/<code>=~</code>/<code>!~</code> 네 가지이고, 정규식은 전체 문자열 매치이므로 부분 매치가 필요하면 명시적으로 설계한다.</li><li>counter는 항상 <code>rate</code>로 변화율을 본다. <code>rate</code> 전에 집계하면 counter reset 보정이 깨지므로 순서를 지킨다.</li><li><code>by</code>는 지정 라벨만 남기고, <code>without</code>은 지정 라벨만 버린다 — 결과 라벨 집합이 다르므로 이후 join 가능 여부에 영향을 준다.</li><li>비교 연산자는 기본적으로 필터링이고, <code>bool</code> modifier를 붙여야 1/0 값으로 바뀐다.</li><li><code>histogram_quantile</code>, <code>absent</code>, <code>clamp</code>, <code>label_replace</code>는 실무 대시보드·알림에서 매일 쓰는 함수다.</li></ul></div><h2 id="다음-챕터" tabindex="-1"><a class="header-anchor" href="#다음-챕터"><span>다음 챕터</span></a></h2><p>기초 셀렉터와 함수를 익혔다면, 다음은 서로 다른 메트릭을 엮는 벡터 매칭과 히스토그램 쿼리의 세부 함정이다. <a href="/study/observability/10-promql-advanced">PromQL 심화</a>에서는 <code>on</code>/<code>ignoring</code>과 <code>group_left</code>/<code>group_right</code>를 이용한 join 패턴, 서브쿼리, native histogram 쿼리, <code>offset</code>과 <code>@</code> modifier, 그리고 고카디널리티 쿼리 같은 성능 안티패턴을 다룬다.</p>`,46)]))}const m=s(i,[["render",r],["__file","09-promql-basics.html.vue"]]),k=JSON.parse('{"path":"/study/observability/09-promql-basics.html","title":"PromQL 기초","lang":"en-US","frontmatter":{"title":"PromQL 기초","description":"PromQL의 두 가지 벡터 타입, 셀렉터와 매처, counter를 다루는 rate/irate/increase의 함정, 집계 연산자와 by/without, 연산자 우선순위, histogram_quantile 같은 필수 함수를 실전 예제로 정리한다.","date":"2026-07-02T00:00:00.000Z","tags":["Observability","Prometheus","PromQL"],"prev":"/study/observability/08-exporters-instrumentation","next":"/study/observability/10-promql-advanced"},"headers":[{"level":1,"title":"PromQL 기초","slug":"promql-기초","link":"#promql-기초","children":[{"level":2,"title":"1. Instant Vector와 Range Vector","slug":"_1-instant-vector와-range-vector","link":"#_1-instant-vector와-range-vector","children":[]},{"level":2,"title":"2. 셀렉터와 매처","slug":"_2-셀렉터와-매처","link":"#_2-셀렉터와-매처","children":[]},{"level":2,"title":"3. Counter 다루기 — rate, irate, increase","slug":"_3-counter-다루기-—-rate-irate-increase","link":"#_3-counter-다루기-—-rate-irate-increase","children":[]},{"level":2,"title":"4. 집계 연산자 — sum, avg, max와 by/without","slug":"_4-집계-연산자-—-sum-avg-max와-by-without","link":"#_4-집계-연산자-—-sum-avg-max와-by-without","children":[]},{"level":2,"title":"5. 산술·비교·논리 연산자","slug":"_5-산술·비교·논리-연산자","link":"#_5-산술·비교·논리-연산자","children":[]},{"level":2,"title":"6. 자주 쓰는 함수","slug":"_6-자주-쓰는-함수","link":"#_6-자주-쓰는-함수","children":[]},{"level":2,"title":"다음 챕터","slug":"다음-챕터","link":"#다음-챕터","children":[]}]}],"git":{},"filePathRelative":"_study/observability/09-promql-basics.md"}');export{m as comp,k as data};
