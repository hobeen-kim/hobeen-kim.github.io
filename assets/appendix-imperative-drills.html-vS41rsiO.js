import{_ as t,c as a,e as s,o as n}from"./app-DUkZoSdm.js";const d={};function l(r,e){return n(),a("div",null,e[0]||(e[0]=[s(`<h1 id="imperative-명령-드릴" tabindex="-1"><a class="header-anchor" href="#imperative-명령-드릴"><span>imperative 명령 드릴</span></a></h1><p>실무에서 장애 대응이나 임시 리소스 생성처럼 시간이 촉박한 상황에서는 선언형 YAML을 한 줄씩 손으로 쓰고 있을 여유가 없다. 이때 가장 빠른 방법은 <code>kubectl</code>의 imperative(명령형) 명령으로 리소스의 골격을 만들고, 필요하면 <code>--dry-run=client -o yaml</code>로 YAML을 뽑아 일부만 손으로 편집하는 것이다. 이 부록은 그 패턴을 반복 훈련해 속도를 끌어올리는 드릴이다.</p><p>공식 레퍼런스는 <a href="https://kubernetes.io/docs/reference/kubectl/" target="_blank" rel="noopener noreferrer">kubectl 명령어 레퍼런스</a>와 <a href="https://kubernetes.io/docs/reference/generated/kubectl/kubectl-commands" target="_blank" rel="noopener noreferrer">kubectl 전체 명령 목록</a>에 있다. 명령의 정확한 플래그가 헷갈리면 항상 이 두 문서를 1차 출처로 삼는다.</p><div class="hint-container info"><p class="hint-container-title">학습 목표</p><ul><li>명령형과 선언형을 언제 각각 써야 하는지 판단 기준을 세운다.</li><li><code>kubectl run</code>/<code>create</code>/<code>expose</code>로 리소스 골격을 즉시 만드는 패턴을 익힌다.</li><li><code>--dry-run=client -o yaml</code>로 YAML을 뽑아 최소 편집만 하는 워크플로우를 몸에 익힌다.</li><li>상황 설명만 보고 가장 빠른 명령을 즉시 떠올리는 속도를 훈련한다.</li></ul></div><h2 id="_1-명령형-vs-선언형" tabindex="-1"><a class="header-anchor" href="#_1-명령형-vs-선언형"><span>1. 명령형 vs 선언형</span></a></h2><p>명령형은 &quot;이 리소스를 지금 만들어라&quot;라고 행위를 직접 지시한다. 선언형은 &quot;최종 상태는 이래야 한다&quot;라고 원하는 상태를 파일로 선언하고 <code>kubectl apply</code>로 맞춘다. 둘 중 무엇을 쓸지는 다음 기준으로 갈린다.</p><pre class="mermaid">flowchart TD
    A[리소스 작업이 필요하다] --&gt; B{버전 관리·재현이&lt;br&gt;중요한가}
    B --&gt;|Yes| C[선언형: YAML 작성 후&lt;br&gt;kubectl apply -f]
    B --&gt;|No| D{일회성·임시·긴급&lt;br&gt;작업인가}
    D --&gt;|Yes| E[명령형: run / create / expose]
    D --&gt;|No| C
    E --&gt; F{필드 세부 조정이&lt;br&gt;필요한가}
    F --&gt;|No| G[그대로 생성]
    F --&gt;|Yes| H[--dry-run=client -o yaml로&lt;br&gt;골격 추출 후 편집]
    H --&gt; C
</pre><p>핵심 패턴은 명령형 명령에 <code>--dry-run=client -o yaml</code>을 붙여 클러스터에 적용하지 않고 YAML만 출력하는 것이다. 이걸 파일로 저장하면 손으로 처음부터 작성할 필요 없이 골격이 생긴다.</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line"><span class="token comment"># 클러스터에 만들지 않고 YAML 골격만 파일로 추출한다</span></span>
<span class="line">kubectl run nginx <span class="token parameter variable">--image</span><span class="token operator">=</span>nginx --dry-run<span class="token operator">=</span>client <span class="token parameter variable">-o</span> yaml <span class="token operator">&gt;</span> pod.yaml</span>
<span class="line"></span>
<span class="line"><span class="token comment"># 추출한 파일을 편집한 뒤 선언형으로 적용한다</span></span>
<span class="line">kubectl apply <span class="token parameter variable">-f</span> pod.yaml</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><div class="hint-container tip"><p class="hint-container-title">Tips</p><p><code>--dry-run=client</code>는 클라이언트 측에서만 검증하고 서버로 보내지 않는다. <code>--dry-run=server</code>는 실제로 서버에 보내 admission까지 거친 결과를 확인하되 영구 저장은 하지 않는다. YAML 골격을 뽑을 때는 <code>client</code>로 충분하다.</p></div><div class="hint-container warning"><p class="hint-container-title">Warning</p><p><code>kubectl run</code>은 Pod만 만든다. 예전 버전처럼 <code>--generator</code>로 Deployment를 만드는 기능은 제거됐다. Deployment가 필요하면 <code>kubectl create deployment</code>를 써야 한다.</p></div><h2 id="_2-리소스-빠른-생성" tabindex="-1"><a class="header-anchor" href="#_2-리소스-빠른-생성"><span>2. 리소스 빠른 생성</span></a></h2><p>리소스 종류별로 명령형 생성 명령이 정해져 있다. 상황별로 정리한다.</p><table><thead><tr><th>상황</th><th>명령</th></tr></thead><tbody><tr><td>nginx 이미지로 Pod 하나</td><td><code>kubectl run nginx --image=nginx</code></td></tr><tr><td>Deployment 생성</td><td><code>kubectl create deployment web --image=nginx</code></td></tr><tr><td>replica 3개로 Deployment</td><td><code>kubectl create deployment web --image=nginx --replicas=3</code></td></tr><tr><td>일회성 Job</td><td><code>kubectl create job hello --image=busybox -- echo hi</code></td></tr><tr><td>기존 CronJob에서 Job 즉시 실행</td><td><code>kubectl create job --from=cronjob/backup run-now</code></td></tr><tr><td>주기 실행 CronJob</td><td><code>kubectl create cronjob report --image=busybox --schedule=&quot;*/5 * * * *&quot; -- date</code></td></tr><tr><td>literal로 ConfigMap</td><td><code>kubectl create configmap cfg --from-literal=key=val</code></td></tr><tr><td>파일에서 ConfigMap</td><td><code>kubectl create configmap cfg --from-file=app.properties</code></td></tr><tr><td>generic Secret</td><td><code>kubectl create secret generic db --from-literal=password=1234</code></td></tr><tr><td>TLS Secret</td><td><code>kubectl create secret tls tls-secret --cert=tls.crt --key=tls.key</code></td></tr><tr><td>ClusterIP Service(Deployment 노출)</td><td><code>kubectl expose deployment web --port=80 --target-port=8080</code></td></tr><tr><td>NodePort Service</td><td><code>kubectl create service nodeport web --tcp=80:8080</code></td></tr><tr><td>ServiceAccount</td><td><code>kubectl create serviceaccount ci-bot</code></td></tr><tr><td>Role</td><td><code>kubectl create role reader --verb=get,list --resource=pods</code></td></tr><tr><td>RoleBinding</td><td><code>kubectl create rolebinding read-bind --role=reader --serviceaccount=default:ci-bot</code></td></tr><tr><td>ClusterRole</td><td><code>kubectl create clusterrole node-reader --verb=get,list --resource=nodes</code></td></tr><tr><td>Namespace</td><td><code>kubectl create namespace dev</code></td></tr></tbody></table><pre class="mermaid">flowchart LR
    A[상황 파악] --&gt; B[create/run 명령 선택]
    B --&gt; C{필드 조정&lt;br&gt;필요?}
    C --&gt;|아니오| D[즉시 생성]
    C --&gt;|예| E[--dry-run=client -o yaml]
    E --&gt; F[YAML 편집]
    F --&gt; G[kubectl apply -f]
    D --&gt; H[kubectl get으로 확인]
    G --&gt; H
</pre><div class="hint-container tip"><p class="hint-container-title">Tips</p><p><code>kubectl run</code>으로 임시 디버깅 Pod를 띄울 때는 <code>--rm -it --restart=Never</code>를 조합한다. 종료 시 Pod가 자동 삭제되고(<code>--rm</code>), 셸에 붙으며(<code>-it</code>), 재시작 정책이 Never라 Job 취급되지 않는다.</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line">kubectl run tmp <span class="token parameter variable">--image</span><span class="token operator">=</span>busybox <span class="token parameter variable">--rm</span> <span class="token parameter variable">-it</span> <span class="token parameter variable">--restart</span><span class="token operator">=</span>Never -- <span class="token function">sh</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div></div><h2 id="_3-빠른-수정·운영" tabindex="-1"><a class="header-anchor" href="#_3-빠른-수정·운영"><span>3. 빠른 수정·운영</span></a></h2><p>이미 떠 있는 리소스를 고칠 때도 YAML을 다시 쓰는 대신 명령형으로 바로 건드린다.</p><table><thead><tr><th>작업</th><th>명령</th></tr></thead><tbody><tr><td>이미지 교체(롤링 업데이트 유발)</td><td><code>kubectl set image deployment/web nginx=nginx:1.27</code></td></tr><tr><td>replica 수 조정</td><td><code>kubectl scale deployment/web --replicas=5</code></td></tr><tr><td>조건부 스케일</td><td><code>kubectl scale deployment/web --current-replicas=3 --replicas=5</code></td></tr><tr><td>Deployment를 Service로 노출</td><td><code>kubectl expose deployment web --port=80 --target-port=8080</code></td></tr><tr><td>라벨 추가</td><td><code>kubectl label pod nginx tier=frontend</code></td></tr><tr><td>라벨 덮어쓰기</td><td><code>kubectl label pod nginx tier=backend --overwrite</code></td></tr><tr><td>라벨 제거</td><td><code>kubectl label pod nginx tier-</code></td></tr><tr><td>어노테이션 추가</td><td><code>kubectl annotate pod nginx description=&quot;temp pod&quot;</code></td></tr><tr><td>롤아웃 상태 확인</td><td><code>kubectl rollout status deployment/web</code></td></tr><tr><td>롤아웃 히스토리</td><td><code>kubectl rollout history deployment/web</code></td></tr><tr><td>직전 버전으로 롤백</td><td><code>kubectl rollout undo deployment/web</code></td></tr><tr><td>특정 리비전으로 롤백</td><td><code>kubectl rollout undo deployment/web --to-revision=2</code></td></tr><tr><td>롤아웃 재시작(Pod 재생성)</td><td><code>kubectl rollout restart deployment/web</code></td></tr><tr><td>에디터로 직접 수정</td><td><code>kubectl edit deployment/web</code></td></tr><tr><td>특정 필드만 패치</td><td><code>kubectl patch deployment web -p &#39;{&quot;spec&quot;:{&quot;replicas&quot;:4}}&#39;</code></td></tr></tbody></table><div class="hint-container warning"><p class="hint-container-title">Warning</p><p><code>kubectl edit</code>나 <code>kubectl patch</code>로 라이브 리소스를 직접 고치면 원본 YAML 파일과 클러스터 상태가 어긋난다(구성 드리프트). 긴급 상황에는 빠르지만, 평상시 운영은 YAML을 고치고 <code>kubectl apply</code>하는 선언형으로 되돌리는 것이 안전하다.</p></div><h2 id="_4-시간-절약-세팅" tabindex="-1"><a class="header-anchor" href="#_4-시간-절약-세팅"><span>4. 시간 절약 세팅</span></a></h2><p>명령 자체를 빠르게 만들어 주는 환경 세팅과 조회 옵션을 모아둔다. 드릴 전에 깔아두면 모든 입력이 짧아진다.</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line"><span class="token comment"># 별칭 — kubectl을 k로 줄이고 자동완성까지 연결</span></span>
<span class="line"><span class="token builtin class-name">alias</span> <span class="token assign-left variable">k</span><span class="token operator">=</span>kubectl</span>
<span class="line"><span class="token builtin class-name">source</span> <span class="token operator">&lt;</span><span class="token punctuation">(</span>kubectl completion <span class="token function">bash</span><span class="token punctuation">)</span>        <span class="token comment"># zsh면 completion zsh</span></span>
<span class="line">complete <span class="token parameter variable">-o</span> default <span class="token parameter variable">-F</span> __start_kubectl k</span>
<span class="line"></span>
<span class="line"><span class="token comment"># 기본 네임스페이스 고정 — -n 반복 입력 제거</span></span>
<span class="line">kubectl config set-context <span class="token parameter variable">--current</span> <span class="token parameter variable">--namespace</span><span class="token operator">=</span>dev</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>조회·확인을 빠르게 하는 옵션은 다음과 같다.</p><table><thead><tr><th>작업</th><th>명령</th></tr></thead><tbody><tr><td>필드 구조·설명 확인</td><td><code>kubectl explain pod.spec.containers</code></td></tr><tr><td>모든 필드를 재귀로 펼쳐 보기</td><td><code>kubectl explain pod.spec --recursive</code></td></tr><tr><td>노드·IP까지 넓게 출력</td><td><code>kubectl get pods -o wide</code></td></tr><tr><td>상태로 필터(서버 측)</td><td><code>kubectl get pods --field-selector=status.phase=Running</code></td></tr><tr><td>특정 노드의 Pod만</td><td><code>kubectl get pods --field-selector=spec.nodeName=node1 -A</code></td></tr><tr><td>Pod IP만 뽑기</td><td><code>kubectl get pod nginx -o jsonpath=&#39;{.status.podIP}&#39;</code></td></tr><tr><td>모든 Pod 이름만 한 줄로</td><td><code>kubectl get pods -o jsonpath=&#39;{.items[*].metadata.name}&#39;</code></td></tr><tr><td>컨테이너 이미지만 추출</td><td><code>kubectl get pods -o jsonpath=&#39;{.items[*].spec.containers[*].image}&#39;</code></td></tr><tr><td>라벨로 필터</td><td><code>kubectl get pods -l app=web</code></td></tr></tbody></table><div class="hint-container tip"><p class="hint-container-title">Tips</p><p><code>kubectl explain</code>은 필드 이름이나 중첩 구조가 헷갈릴 때 문서를 열지 않고 터미널에서 즉시 확인하는 가장 빠른 방법이다. <code>--recursive</code>를 붙이면 하위 필드를 전부 펼쳐 한눈에 본다.</p></div><h2 id="_5-드릴-문제-모음" tabindex="-1"><a class="header-anchor" href="#_5-드릴-문제-모음"><span>5. 드릴 문제 모음</span></a></h2><p>상황 설명만 읽고 가장 빠른 명령을 머릿속으로 먼저 떠올린 다음, 정답을 펼쳐 비교한다. 명령을 손으로 다시 쳐 보면서 플래그를 몸에 익히는 것이 목적이다.</p><p><strong>문제 1.</strong> nginx 이미지로 Pod를 만들되, 클러스터에 적용하지 말고 YAML만 출력하라.</p><details class="hint-container details"><summary>정답</summary><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line">kubectl run nginx <span class="token parameter variable">--image</span><span class="token operator">=</span>nginx --dry-run<span class="token operator">=</span>client <span class="token parameter variable">-o</span> yaml</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p><code>--dry-run=client</code>로 서버에 보내지 않고, <code>-o yaml</code>로 골격만 출력한다. 뒤에 <code>&gt; pod.yaml</code>을 붙이면 파일로 저장해 편집할 수 있다.</p></details><p><strong>문제 2.</strong> <code>web</code>이라는 Deployment를 nginx 이미지·replica 3개로 만들고, 80번 포트로 노출하라.</p><details class="hint-container details"><summary>정답</summary><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line">kubectl create deployment web <span class="token parameter variable">--image</span><span class="token operator">=</span>nginx <span class="token parameter variable">--replicas</span><span class="token operator">=</span><span class="token number">3</span></span>
<span class="line">kubectl expose deployment web <span class="token parameter variable">--port</span><span class="token operator">=</span><span class="token number">80</span> --target-port<span class="token operator">=</span><span class="token number">80</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p><code>create deployment</code>로 워크로드를, <code>expose</code>로 ClusterIP Service를 만든다. 컨테이너 포트가 80이면 <code>--target-port</code>도 80이다.</p></details><p><strong>문제 3.</strong> <code>key1=val1</code>, <code>key2=val2</code> 두 항목을 가진 ConfigMap을 literal로 생성하라.</p><details class="hint-container details"><summary>정답</summary><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line">kubectl create configmap app-config --from-literal<span class="token operator">=</span>key1<span class="token operator">=</span>val1 --from-literal<span class="token operator">=</span>key2<span class="token operator">=</span>val2</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p><code>--from-literal</code>은 항목마다 반복해서 붙인다. 파일 통째로 넣을 때는 <code>--from-file</code>을 쓴다.</p></details><p><strong>문제 4.</strong> 비밀번호 <code>s3cr3t</code>를 담은 generic Secret을 만들어라.</p><details class="hint-container details"><summary>정답</summary><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line">kubectl create secret generic db-secret --from-literal<span class="token operator">=</span>password<span class="token operator">=</span>s3cr3t</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>generic 타입은 임의 key-value를 담는다. TLS 인증서는 <code>kubectl create secret tls</code>, 레지스트리 인증은 <code>kubectl create secret docker-registry</code>를 쓴다.</p></details><p><strong>문제 5.</strong> 디버깅용으로 busybox 셸을 임시로 띄우고, 종료하면 Pod가 자동 삭제되게 하라.</p><details class="hint-container details"><summary>정답</summary><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line">kubectl run tmp <span class="token parameter variable">--image</span><span class="token operator">=</span>busybox <span class="token parameter variable">--rm</span> <span class="token parameter variable">-it</span> <span class="token parameter variable">--restart</span><span class="token operator">=</span>Never -- <span class="token function">sh</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p><code>--rm</code>으로 종료 시 자동 삭제, <code>-it</code>로 셸 연결, <code>--restart=Never</code>로 Pod 단발 실행이 된다. 클러스터 내부에서 DNS·연결 테스트할 때 자주 쓴다.</p></details><p><strong>문제 6.</strong> <code>web</code> Deployment의 nginx 컨테이너 이미지를 <code>nginx:1.27</code>로 교체하고, 롤아웃이 끝날 때까지 지켜봐라.</p><details class="hint-container details"><summary>정답</summary><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line">kubectl <span class="token builtin class-name">set</span> image deployment/web <span class="token assign-left variable">nginx</span><span class="token operator">=</span>nginx:1.27</span>
<span class="line">kubectl rollout status deployment/web</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p><code>set image</code>는 <code>&lt;컨테이너이름&gt;=&lt;새이미지&gt;</code> 형식이다. 컨테이너 이름이 틀리면 아무 일도 일어나지 않으니 <code>kubectl get deploy web -o jsonpath=&#39;{.spec.template.spec.containers[*].name}&#39;</code>로 확인한다.</p></details><p><strong>문제 7.</strong> 방금 한 이미지 교체가 잘못됐다. 직전 버전으로 즉시 롤백하라.</p><details class="hint-container details"><summary>정답</summary><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line">kubectl rollout undo deployment/web</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>바로 이전 리비전으로 되돌린다. 특정 리비전을 지정하려면 <code>--to-revision=&lt;번호&gt;</code>를 붙인다. 리비전 번호는 <code>kubectl rollout history deployment/web</code>로 본다.</p></details><p><strong>문제 8.</strong> <code>node1</code> 노드를 새 Pod 스케줄 대상에서 제외하고, 그 위의 Pod를 안전하게 비워라(노드 점검 준비).</p><details class="hint-container details"><summary>정답</summary><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line">kubectl cordon node1</span>
<span class="line">kubectl drain node1 --ignore-daemonsets --delete-emptydir-data</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><p><code>cordon</code>으로 신규 스케줄을 막고, <code>drain</code>으로 기존 Pod를 다른 노드로 옮긴다. DaemonSet Pod는 옮길 수 없으므로 <code>--ignore-daemonsets</code>가 필요하고, emptyDir 데이터가 있으면 <code>--delete-emptydir-data</code>로 명시 동의한다. 점검이 끝나면 <code>kubectl uncordon node1</code>로 되돌린다.</p></details><p><strong>문제 9.</strong> <code>nginx</code> Pod에 설정된 환경변수를 명령 하나로 빠르게 확인하라.</p><details class="hint-container details"><summary>정답</summary><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line">kubectl <span class="token builtin class-name">exec</span> nginx -- <span class="token function">env</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>컨테이너 안에서 <code>env</code>를 실행해 실제 주입된 환경변수를 본다. spec에 정의된 값만 보려면 <code>kubectl get pod nginx -o jsonpath=&#39;{.spec.containers[*].env}&#39;</code>를 쓴다.</p></details><p><strong>문제 10.</strong> 전체 네임스페이스에서 <code>Running</code> 상태가 아닌 Pod만 빠르게 추려라.</p><details class="hint-container details"><summary>정답</summary><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line">kubectl get pods <span class="token parameter variable">-A</span> --field-selector<span class="token operator">=</span>status.phase<span class="token operator">!=</span>Running</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p><code>--field-selector</code>는 서버 측에서 필터링하므로 출력 후 <code>grep</code>하는 것보다 빠르고 정확하다. <code>!=</code>로 부정 조건을 건다.</p></details><p><strong>문제 11.</strong> <code>web</code> Deployment를 nginx로 만들되, replica를 5로 두고 메모리 limit을 추가해야 한다. 골격을 명령형으로 뽑은 뒤 편집해서 적용하는 흐름으로 처리하라.</p><details class="hint-container details"><summary>정답</summary><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line">kubectl create deployment web <span class="token parameter variable">--image</span><span class="token operator">=</span>nginx <span class="token parameter variable">--replicas</span><span class="token operator">=</span><span class="token number">5</span> <span class="token punctuation">\\</span></span>
<span class="line">  --dry-run<span class="token operator">=</span>client <span class="token parameter variable">-o</span> yaml <span class="token operator">&gt;</span> web.yaml</span>
<span class="line"><span class="token comment"># web.yaml의 containers 아래에 resources.limits.memory를 추가한 뒤</span></span>
<span class="line">kubectl apply <span class="token parameter variable">-f</span> web.yaml</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>replica처럼 명령형으로 되는 필드는 플래그로 넣고, <code>resources</code>처럼 플래그가 없는 필드는 YAML을 뽑아 편집한다. 명령형과 선언형을 섞어 쓰는 전형적인 패턴이다.</p></details><p><strong>문제 12.</strong> <code>default</code> 네임스페이스의 ServiceAccount <code>ci-bot</code>에게 Pod를 조회(get·list)할 수 있는 권한만 부여하라.</p><details class="hint-container details"><summary>정답</summary><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line">kubectl create serviceaccount ci-bot</span>
<span class="line">kubectl create role pod-reader <span class="token parameter variable">--verb</span><span class="token operator">=</span>get,list <span class="token parameter variable">--resource</span><span class="token operator">=</span>pods</span>
<span class="line">kubectl create rolebinding ci-bot-read <span class="token punctuation">\\</span></span>
<span class="line">  <span class="token parameter variable">--role</span><span class="token operator">=</span>pod-reader <span class="token parameter variable">--serviceaccount</span><span class="token operator">=</span>default:ci-bot</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>ServiceAccount, Role, RoleBinding 세 개를 모두 명령형으로 만들 수 있다. <code>--serviceaccount</code>는 <code>&lt;네임스페이스&gt;:&lt;이름&gt;</code> 형식이고, Role/RoleBinding은 네임스페이스 범위이므로 최소 권한 원칙에 맞는다.</p></details><div class="hint-container tip"><p class="hint-container-title">핵심 정리</p><ul><li>긴급·일회성 작업은 명령형, 버전 관리·재현이 필요한 작업은 선언형으로 가른다.</li><li><code>--dry-run=client -o yaml &gt; file.yaml</code>은 손으로 YAML을 쓰는 시간을 없애는 핵심 패턴이다. 플래그로 안 되는 필드만 편집한다.</li><li>Pod는 <code>run</code>, 그 밖의 워크로드·리소스는 <code>create</code>, 노출은 <code>expose</code>로 만든다.</li><li>수정·운영은 <code>set image</code>·<code>scale</code>·<code>rollout</code>·<code>label</code>로 라이브 리소스를 바로 건드리되, 평상시엔 선언형으로 되돌려 드리프트를 줄인다.</li><li>alias·completion·<code>explain</code>·<code>--field-selector</code>·<code>-o jsonpath</code>를 깔아두면 모든 명령이 짧아진다.</li></ul></div>`,53)]))}const o=t(d,[["render",l],["__file","appendix-imperative-drills.html.vue"]]),i=JSON.parse('{"path":"/study/kubernetes/appendix-imperative-drills.html","title":"imperative 명령 드릴","lang":"en-US","frontmatter":{"title":"imperative 명령 드릴","description":"kubectl 명령형으로 리소스를 빠르게 생성·편집하는 속도 훈련","date":"2026-06-15T00:00:00.000Z","tags":["kubectl","imperative","dry-run"],"prev":"/study/kubernetes/47-practice","next":"/study/kubernetes/appendix-kubectl-cheatsheet"},"headers":[{"level":1,"title":"imperative 명령 드릴","slug":"imperative-명령-드릴","link":"#imperative-명령-드릴","children":[{"level":2,"title":"1. 명령형 vs 선언형","slug":"_1-명령형-vs-선언형","link":"#_1-명령형-vs-선언형","children":[]},{"level":2,"title":"2. 리소스 빠른 생성","slug":"_2-리소스-빠른-생성","link":"#_2-리소스-빠른-생성","children":[]},{"level":2,"title":"3. 빠른 수정·운영","slug":"_3-빠른-수정·운영","link":"#_3-빠른-수정·운영","children":[]},{"level":2,"title":"4. 시간 절약 세팅","slug":"_4-시간-절약-세팅","link":"#_4-시간-절약-세팅","children":[]},{"level":2,"title":"5. 드릴 문제 모음","slug":"_5-드릴-문제-모음","link":"#_5-드릴-문제-모음","children":[]}]}],"git":{},"filePathRelative":"_study/kubernetes/appendix-imperative-drills.md"}');export{o as comp,i as data};
