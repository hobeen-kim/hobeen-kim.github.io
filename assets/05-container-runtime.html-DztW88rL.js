import{_ as s,c as e,e as t,o as a}from"./app-DUkZoSdm.js";const r={};function i(o,n){return a(),e("div",null,n[0]||(n[0]=[t(`<h1 id="컨테이너-런타임-심화" tabindex="-1"><a class="header-anchor" href="#컨테이너-런타임-심화"><span>컨테이너 런타임 심화</span></a></h1><div class="hint-container info"><p class="hint-container-title">학습 목표</p><ul><li>OCI 표준의 image-spec과 runtime-spec이 각각 무엇을 규정하는지 이해한다.</li><li>runc 같은 low-level 런타임과 containerd 같은 high-level 런타임의 역할 분담을 설명할 수 있다.</li><li>containerd 아키텍처(content store, snapshotter, shim)의 구성을 안다.</li><li>CRI(Container Runtime Interface)가 무엇이고 쿠버네티스가 런타임과 어떻게 연결되는지 이해한다.</li><li>gVisor·Kata 같은 샌드박스 런타임이 격리를 어떻게 강화하는지 설명할 수 있다.</li></ul></div><h2 id="_1-oci-표준-—-컨테이너의-공통-규약" tabindex="-1"><a class="header-anchor" href="#_1-oci-표준-—-컨테이너의-공통-규약"><span>1. OCI 표준 — 컨테이너의 공통 규약</span></a></h2><p>컨테이너 생태계가 도커에 종속되지 않고 발전할 수 있었던 이유는 <strong>OCI(Open Container Initiative)</strong> 표준 덕분이다. OCI는 컨테이너의 &quot;이미지 포맷&quot;과 &quot;실행 방식&quot;을 벤더 중립적으로 규정해, 어느 도구로 만든 이미지든 어느 런타임에서든 돌게 만든다.</p><p>OCI는 세 가지 명세로 구성된다.</p><ul><li><strong>image-spec</strong>: 이미지가 어떤 구조여야 하는지 규정한다. 레이어(tar), 설정(config JSON), 매니페스트, 다이제스트로 이뤄진 포맷이다. <code>docker build</code>로 만든 이미지든 buildah로 만든 이미지든 이 규격을 따르므로 서로 호환된다.</li><li><strong>runtime-spec</strong>: 풀어 놓은 파일시스템(rootfs)과 설정(<code>config.json</code>)을 받아 &quot;컨테이너를 어떻게 생성·시작·정지·삭제하는가&quot;의 표준 동작을 규정한다.</li><li><strong>distribution-spec</strong>: 레지스트리가 이미지를 주고받는 HTTP API를 규정한다.</li></ul><pre class="mermaid">flowchart TB
    subgraph OCI[&quot;OCI 표준&quot;]
        IMG[&quot;image-spec&lt;br&gt;(이미지 포맷)&quot;]
        RT[&quot;runtime-spec&lt;br&gt;(실행 동작)&quot;]
        DIST[&quot;distribution-spec&lt;br&gt;(레지스트리 API)&quot;]
    end
    IMG --&gt;|언팩| Bundle[&quot;OCI 번들&lt;br&gt;rootfs + config.json&quot;]
    Bundle --&gt;|입력| RT
    RT --&gt; Runner[&quot;OCI 런타임(runc 등)&quot;]
    DIST --&gt;|pull/push| Reg[&quot;레지스트리&quot;]
</pre><p>핵심은 <strong>&quot;이미지&quot;와 &quot;실행&quot;의 분리</strong>다. 이미지 빌드 도구와 런타임이 OCI라는 합의된 인터페이스로 만나므로, 도커가 아니어도 같은 이미지를 쓸 수 있다. 명세 원문은 <a href="https://opencontainers.org/" target="_blank" rel="noopener noreferrer">OCI 표준 저장소</a>에서 확인할 수 있다.</p><h2 id="_2-runc와-low-level-런타임" tabindex="-1"><a class="header-anchor" href="#_2-runc와-low-level-런타임"><span>2. runc와 low-level 런타임</span></a></h2><p><strong>runc</strong>는 OCI runtime-spec의 참조 구현이다. rootfs와 <code>config.json</code>이 담긴 <strong>OCI 번들(bundle)</strong>을 받아, <a href="/study/kubernetes/01-container-basics">namespace와 cgroup</a>을 설정하고 실제 프로세스를 띄운다. runc가 하는 일이 곧 1장에서 본 &quot;격리된 프로세스 만들기&quot;의 실체다.</p><p>runc는 한 컨테이너를 만들고 나면 빠진다(자기 역할만 하고 종료). 그래서 <strong>low-level(저수준) 런타임</strong>이라 부른다. 이미지 풀, 네트워크, 여러 컨테이너의 생명주기 관리 같은 상위 작업은 하지 않는다.</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line"><span class="token comment"># runc로 컨테이너를 직접 실행해 보기 (도커 없이)</span></span>
<span class="line"><span class="token function">mkdir</span> <span class="token parameter variable">-p</span> mycontainer/rootfs</span>
<span class="line"><span class="token comment"># alpine rootfs를 풀어 넣은 뒤</span></span>
<span class="line"><span class="token function">docker</span> <span class="token builtin class-name">export</span> <span class="token variable"><span class="token variable">$(</span><span class="token function">docker</span> create alpine<span class="token variable">)</span></span> <span class="token operator">|</span> <span class="token function">tar</span> <span class="token parameter variable">-C</span> mycontainer/rootfs <span class="token parameter variable">-xf</span> -</span>
<span class="line"></span>
<span class="line"><span class="token builtin class-name">cd</span> mycontainer</span>
<span class="line">runc spec                 <span class="token comment"># 기본 config.json 생성</span></span>
<span class="line">runc run demo             <span class="token comment"># OCI 번들로 컨테이너 실행</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>runc 외에도 OCI runtime-spec을 구현한 런타임이 여럿 있고, 이들은 같은 인터페이스(<code>runtime-spec</code>)를 따르므로 서로 교체 가능하다.</p><table><thead><tr><th>low-level 런타임</th><th>특징</th></tr></thead><tbody><tr><td><code>runc</code></td><td>표준 참조 구현. 네이티브 격리</td></tr><tr><td><code>crun</code></td><td>C로 작성, 더 가볍고 빠름</td></tr><tr><td><code>runsc</code> (gVisor)</td><td>사용자 공간 커널로 시스템 콜을 가로채 격리 강화</td></tr><tr><td><code>kata-runtime</code></td><td>경량 VM으로 각 컨테이너를 감쌈</td></tr></tbody></table><h2 id="_3-containerd-아키텍처-—-high-level-런타임" tabindex="-1"><a class="header-anchor" href="#_3-containerd-아키텍처-—-high-level-런타임"><span>3. containerd 아키텍처 — high-level 런타임</span></a></h2><p>runc가 컨테이너 하나를 &quot;만드는&quot; 손이라면, <strong>containerd</strong>는 그 위에서 이미지 관리·스토리지·다수 컨테이너의 생명주기를 총괄하는 <strong>high-level(고수준) 런타임</strong>이다. 도커도, 쿠버네티스도 그 아래에 containerd를 둔다.</p><pre class="mermaid">flowchart TB
    Client[&quot;docker / kubelet (CRI)&quot;] --&gt;|gRPC| CD[&quot;containerd&quot;]
    CD --&gt; CS[&quot;content store&lt;br&gt;(이미지 블롭 저장)&quot;]
    CD --&gt; SN[&quot;snapshotter&lt;br&gt;(레이어/스냅샷, OverlayFS)&quot;]
    CD --&gt; SHIM[&quot;containerd-shim&lt;br&gt;(컨테이너별 관리자)&quot;]
    SHIM --&gt; RUNC[&quot;runc (OCI 런타임)&quot;]
    RUNC --&gt; PROC[&quot;컨테이너 프로세스&quot;]
</pre><p>containerd의 주요 구성 요소는 다음과 같다.</p><ul><li><strong>content store</strong>: 레지스트리에서 받은 이미지 블롭(레이어, 설정)을 콘텐츠 주소(다이제스트) 기반으로 저장한다.</li><li><strong>snapshotter</strong>: 레이어로부터 컨테이너의 루트 파일시스템을 구성한다. <a href="/study/kubernetes/01-container-basics">OverlayFS</a> 같은 스냅샷터로 Copy-on-Write를 구현한다.</li><li><strong>containerd-shim</strong>: 각 컨테이너마다 하나씩 떠서 runc를 호출하고, 그 컨테이너의 생명주기를 책임진다. shim이 있어 <strong>containerd 데몬을 재시작해도 실행 중인 컨테이너는 죽지 않는다</strong> — 운영상 매우 중요한 설계다.</li></ul><p>이 분리 덕분에 containerd는 &quot;무엇을 어떻게 실행할지&quot; 정책만 관리하고, 실제 격리·실행은 runc(또는 다른 OCI 런타임)에 위임한다. 런타임을 바꾸면 같은 containerd 위에서 gVisor·Kata로 격리 수준을 갈아끼울 수 있다.</p><div class="hint-container tip"><p class="hint-container-title">도커 vs containerd, 쿠버네티스의 선택</p><p>쿠버네티스 1.24에서 dockershim이 제거됐다. 이는 &quot;도커 이미지를 못 쓴다&quot;가 아니라, kubelet이 도커 데몬을 거치지 않고 <strong>containerd(또는 CRI-O)를 직접</strong> 호출한다는 뜻이다. 이미지는 OCI 표준이라 그대로 동작한다.</p></div><h2 id="_4-cri와-쿠버네티스-연계" tabindex="-1"><a class="header-anchor" href="#_4-cri와-쿠버네티스-연계"><span>4. CRI와 쿠버네티스 연계</span></a></h2><p>쿠버네티스의 노드 에이전트인 <strong>kubelet</strong>은 특정 런타임에 종속되지 않도록 <strong>CRI(Container Runtime Interface)</strong>라는 gRPC 인터페이스를 통해 런타임과 대화한다. CRI는 &quot;파드/컨테이너를 만들고, 이미지를 풀고, 상태를 보고하라&quot;는 표준 호출 규약이다. 공식 설명은 <a href="https://kubernetes.io/docs/concepts/architecture/cri/" target="_blank" rel="noopener noreferrer">Container Runtimes (CRI)</a>에 있다.</p><p>CRI를 구현한 런타임이 kubelet에 꽂힌다.</p><ul><li><strong>containerd</strong>: CRI 플러그인을 내장해 kubelet과 직접 통신한다. 가장 널리 쓰인다.</li><li><strong>CRI-O</strong>: 쿠버네티스 전용으로 설계된 가벼운 CRI 런타임.</li></ul><pre class="mermaid">sequenceDiagram
    participant API as kube-apiserver
    participant Kubelet as kubelet
    participant CRI as CRI (gRPC)
    participant CD as containerd
    participant RUNC as runc
    API-&gt;&gt;Kubelet: Pod 스펙 전달
    Kubelet-&gt;&gt;CRI: RunPodSandbox()
    CRI-&gt;&gt;CD: 샌드박스(파드 네트워크 ns) 생성
    Kubelet-&gt;&gt;CRI: PullImage() + CreateContainer()
    CD-&gt;&gt;RUNC: 컨테이너 생성
    RUNC--&gt;&gt;CD: 실행 중
    CD--&gt;&gt;Kubelet: 컨테이너 상태 보고
    Kubelet--&gt;&gt;API: Pod Running
</pre><p>흐름을 한 줄로 요약하면 이렇다. <strong>kube-apiserver → kubelet → CRI → containerd → runc → 컨테이너</strong>. 위로 갈수록 &quot;무엇을 원하는가(선언)&quot;에 가깝고, 아래로 갈수록 &quot;어떻게 만드는가(실행)&quot;에 가깝다. 이 계층 분리가 쿠버네티스가 다양한 런타임을 품을 수 있는 근거다.</p><p>CRI에는 &quot;파드 샌드박스(PodSandbox)&quot;라는 개념이 등장하는데, 이는 파드 내 컨테이너들이 공유할 네트워크 namespace 등을 먼저 만드는 단계다. 이 부분은 <a href="/study/kubernetes/07-cluster-architecture">클러스터 아키텍처</a>와 <a href="/study/kubernetes/15-pod">Pod</a> 챕터에서 이어진다.</p><h2 id="_5-샌드박스-런타임-—-gvisor와-kata" tabindex="-1"><a class="header-anchor" href="#_5-샌드박스-런타임-—-gvisor와-kata"><span>5. 샌드박스 런타임 — gVisor와 Kata</span></a></h2><p>1장에서 본 컨테이너의 한계, 즉 <strong>커널 공유로 인한 격리 약화</strong>를 보완하려는 것이 <strong>샌드박스 런타임</strong>이다. 멀티테넌트 환경(신뢰할 수 없는 코드를 같은 노드에서 돌릴 때)에서 특히 중요하다. 둘은 접근이 다르다.</p><pre class="mermaid">flowchart TB
    subgraph Normal[&quot;일반 컨테이너 (runc)&quot;]
        AppN[&quot;앱&quot;] --&gt; KN[&quot;호스트 커널 직접 호출&quot;]
    end
    subgraph GV[&quot;gVisor (runsc)&quot;]
        AppG[&quot;앱&quot;] --&gt; Sentry[&quot;Sentry&lt;br&gt;(사용자 공간 커널)&quot;]
        Sentry --&gt;|제한된 syscall| KG[&quot;호스트 커널&quot;]
    end
    subgraph KT[&quot;Kata Containers&quot;]
        AppK[&quot;앱&quot;] --&gt; GK[&quot;게스트 커널&lt;br&gt;(경량 VM)&quot;]
        GK --&gt; HV[&quot;하이퍼바이저&quot;] --&gt; KK[&quot;호스트 커널&quot;]
    end
</pre><ul><li><strong>gVisor</strong>: 구글이 만든 런타임으로, <strong>Sentry</strong>라는 사용자 공간 커널이 컨테이너의 시스템 콜을 가로채 처리한다. 앱이 호스트 커널을 직접 부르지 못하게 하므로, 커널 공격 표면이 극적으로 줄어든다. OCI 런타임 <code>runsc</code>로 동작해 containerd에 꽂을 수 있다. 대신 시스템 콜을 중계하는 오버헤드가 있다.</li><li><strong>Kata Containers</strong>: 각 컨테이너(또는 파드)를 <strong>경량 VM</strong>으로 감싼다. 게스트 커널과 하이퍼바이저 경계가 한 겹 더 생겨 VM 수준의 격리를 얻는다. 컨테이너의 사용 편의성과 VM의 강한 격리를 절충한 형태다.</li></ul><p>쿠버네티스에서는 <a href="https://kubernetes.io/docs/concepts/containers/runtime-class/" target="_blank" rel="noopener noreferrer">RuntimeClass</a>로 워크로드별 런타임을 선택할 수 있다. 신뢰도가 낮은 워크로드만 gVisor로 돌리는 식이다.</p><div class="language-yaml line-numbers-mode" data-highlighter="prismjs" data-ext="yml" data-title="yml"><pre><code><span class="line"><span class="token key atrule">apiVersion</span><span class="token punctuation">:</span> node.k8s.io/v1</span>
<span class="line"><span class="token key atrule">kind</span><span class="token punctuation">:</span> RuntimeClass</span>
<span class="line"><span class="token key atrule">metadata</span><span class="token punctuation">:</span></span>
<span class="line">  <span class="token key atrule">name</span><span class="token punctuation">:</span> gvisor</span>
<span class="line"><span class="token key atrule">handler</span><span class="token punctuation">:</span> runsc        <span class="token comment"># containerd에 등록된 gVisor 핸들러</span></span>
<span class="line"><span class="token punctuation">---</span></span>
<span class="line"><span class="token key atrule">apiVersion</span><span class="token punctuation">:</span> v1</span>
<span class="line"><span class="token key atrule">kind</span><span class="token punctuation">:</span> Pod</span>
<span class="line"><span class="token key atrule">metadata</span><span class="token punctuation">:</span></span>
<span class="line">  <span class="token key atrule">name</span><span class="token punctuation">:</span> untrusted<span class="token punctuation">-</span>app</span>
<span class="line"><span class="token key atrule">spec</span><span class="token punctuation">:</span></span>
<span class="line">  <span class="token key atrule">runtimeClassName</span><span class="token punctuation">:</span> gvisor   <span class="token comment"># 이 파드만 샌드박스 런타임으로 실행</span></span>
<span class="line">  <span class="token key atrule">containers</span><span class="token punctuation">:</span></span>
<span class="line">    <span class="token punctuation">-</span> <span class="token key atrule">name</span><span class="token punctuation">:</span> app</span>
<span class="line">      <span class="token key atrule">image</span><span class="token punctuation">:</span> untrusted/app<span class="token punctuation">:</span><span class="token number">1.0</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><div class="hint-container warning"><p class="hint-container-title">격리에는 비용이 따른다</p><p>샌드박스 런타임은 안전하지만 시스템 콜 중계(gVisor)나 VM 부팅·메모리(Kata)로 인한 성능·자원 오버헤드가 있다. 모든 워크로드에 적용하기보다, 신뢰할 수 없는 코드나 강한 테넌트 격리가 필요한 곳에 선별 적용하는 것이 실무적이다.</p></div><div class="hint-container tip"><p class="hint-container-title">핵심 정리</p><ul><li><strong>OCI</strong>는 image-spec(포맷)·runtime-spec(실행)·distribution-spec(레지스트리)으로 이미지와 실행을 표준화해 벤더 종속을 끊었다.</li><li><strong>runc</strong>는 OCI 번들을 받아 namespace·cgroup을 설정하는 low-level 런타임이고, <strong>containerd</strong>는 이미지·스토리지·생명주기를 총괄하는 high-level 런타임이다. shim 덕에 containerd 재시작에도 컨테이너는 산다.</li><li>쿠버네티스는 <strong>CRI</strong>로 런타임과 대화하며 흐름은 kubelet → CRI → containerd → runc → 컨테이너로 이어진다. dockershim 제거는 이미지 호환을 깨지 않는다.</li><li><strong>gVisor·Kata</strong>는 커널 공유의 한계를 보완하는 샌드박스 런타임이며, RuntimeClass로 워크로드별로 선택 적용한다.</li></ul></div><h2 id="다음-챕터" tabindex="-1"><a class="header-anchor" href="#다음-챕터"><span>다음 챕터</span></a></h2><p>여기까지로 컨테이너의 원리부터 실행 엔진까지 모두 살펴봤다. 이제 이 컨테이너들을 여러 노드에 걸쳐 자동으로 운영하는 오케스트레이터로 넘어간다. <a href="/study/kubernetes/06-what-is-k8s">쿠버네티스란</a>에서 오케스트레이션의 필요성과 선언적 모델, 등장 배경을 다룬다.</p>`,38)]))}const c=s(r,[["render",i],["__file","05-container-runtime.html.vue"]]),p=JSON.parse('{"path":"/study/kubernetes/05-container-runtime.html","title":"컨테이너 런타임 심화","lang":"en-US","frontmatter":{"title":"컨테이너 런타임 심화","description":"OCI 표준(image-spec/runtime-spec), runc와 low-level 런타임, containerd 아키텍처, CRI와 쿠버네티스 연계, gVisor·Kata 샌드박스 런타임까지 컨테이너 실행 계층을 파고든다.","date":"2026-06-15T00:00:00.000Z","tags":["OCI","runc","containerd","CRI","gVisor","Kata"],"prev":"/study/kubernetes/04-container-network-volume","next":"/study/kubernetes/06-what-is-k8s"},"headers":[{"level":1,"title":"컨테이너 런타임 심화","slug":"컨테이너-런타임-심화","link":"#컨테이너-런타임-심화","children":[{"level":2,"title":"1. OCI 표준 — 컨테이너의 공통 규약","slug":"_1-oci-표준-—-컨테이너의-공통-규약","link":"#_1-oci-표준-—-컨테이너의-공통-규약","children":[]},{"level":2,"title":"2. runc와 low-level 런타임","slug":"_2-runc와-low-level-런타임","link":"#_2-runc와-low-level-런타임","children":[]},{"level":2,"title":"3. containerd 아키텍처 — high-level 런타임","slug":"_3-containerd-아키텍처-—-high-level-런타임","link":"#_3-containerd-아키텍처-—-high-level-런타임","children":[]},{"level":2,"title":"4. CRI와 쿠버네티스 연계","slug":"_4-cri와-쿠버네티스-연계","link":"#_4-cri와-쿠버네티스-연계","children":[]},{"level":2,"title":"5. 샌드박스 런타임 — gVisor와 Kata","slug":"_5-샌드박스-런타임-—-gvisor와-kata","link":"#_5-샌드박스-런타임-—-gvisor와-kata","children":[]},{"level":2,"title":"다음 챕터","slug":"다음-챕터","link":"#다음-챕터","children":[]}]}],"git":{},"filePathRelative":"_study/kubernetes/05-container-runtime.md"}');export{c as comp,p as data};
