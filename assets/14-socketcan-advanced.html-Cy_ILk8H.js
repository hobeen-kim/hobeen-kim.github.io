import{_ as s,c as a,e,o as t}from"./app-DwqdxajT.js";const c={};function p(o,n){return t(),a("div",null,n[0]||(n[0]=[e(`<h1 id="ch14-socketcan-고급" tabindex="-1"><a class="header-anchor" href="#ch14-socketcan-고급"><span>CH14. SocketCAN 고급</span></a></h1><div class="hint-container info"><p class="hint-container-title">학습 목표</p><ul><li>Virtual CAN(vcan)으로 하드웨어 없이 CAN 애플리케이션을 개발·테스트할 수 있다.</li><li>cangw로 커널 내에서 두 CAN 인터페이스를 라우팅·필터링·변환할 수 있다.</li><li>can-isotp 커널 모듈로 ISO 15765-2 segmentation을 커널에 맡기는 방법을 안다.</li><li>Broadcast Manager(BCM)로 주기 송신과 타임아웃 감지를 효율적으로 구현한다.</li><li>CAN FD를 SocketCAN에서 올리고, python-can의 다양한 백엔드와 asyncio 패턴을 쓸 수 있다.</li></ul></div><h2 id="_0-이전-장과-이-장의-차이" tabindex="-1"><a class="header-anchor" href="#_0-이전-장과-이-장의-차이"><span>0. 이전 장과 이 장의 차이</span></a></h2><p>CH13에서는 <code>can0</code> 인터페이스를 올리고 <code>candump</code>·<code>cansend</code>로 프레임을 보내는 기초를 다뤘다. 이 장은 그 위에서 실전 시스템을 만들 때 필요한 기법을 모은다. 같은 SocketCAN이지만, 가상 인터페이스로 하드웨어 종속성을 끊고, 커널 내 게이트웨이로 라우팅을 구성하고, ISO-TP·BCM으로 애플리케이션 복잡도를 줄이는 <strong>엔지니어링 도구함</strong>이다. 차량 개발에서 &quot;CAN을 잘 쓴다&quot;는 말이 이 장의 내용에 얼마나 익숙한가에 달려 있다 해도 과언이 아니다.</p><h2 id="_1-socketcan의-레이어드-관점" tabindex="-1"><a class="header-anchor" href="#_1-socketcan의-레이어드-관점"><span>1. SocketCAN의 레이어드 관점</span></a></h2><p>CH13에서 본 <code>can0</code>는 빙산의 일각이다. SocketCAN은 리눅스 네트워크 스택 위에 CAN 전용 프로토콜 패밀리(<strong>PF_CAN</strong>)를 얹은 구조라서, 그 위에 다양한 커널 모듈이 붙는다. 실제 하드웨어가 없어도 동작하는 <strong>vcan</strong>, 두 버스를 잇는 <strong>cangw</strong>, 전송 계층을 구현하는 <strong>can-isotp</strong>, 주기 송신을 커널이 대신 해주는 <strong>BCM</strong>이 모두 같은 레이어 위에 올라앉는다.</p><pre class="mermaid">flowchart TB
    APP[User App&lt;br&gt;python-can, candump, isotpsend]
    subgraph KERNEL[Linux Kernel]
        CAN_RAW[CAN_RAW socket]
        CAN_BCM[CAN_BCM socket]
        CAN_ISOTP[can-isotp module]
        CAN_GW[cangw / can-gw]
        CAN_CORE[AF_CAN core]
        DRV[Driver layer&lt;br&gt;can-dev, mcp251x, flexcan]
    end
    HW0[can0 HW]
    HW1[can1 HW]
    VCAN[vcan0 virtual]

    APP --&gt; CAN_RAW
    APP --&gt; CAN_BCM
    APP --&gt; CAN_ISOTP
    CAN_RAW --&gt; CAN_CORE
    CAN_BCM --&gt; CAN_CORE
    CAN_ISOTP --&gt; CAN_CORE
    CAN_GW --&gt; CAN_CORE
    CAN_CORE --&gt; DRV
    DRV --&gt; HW0
    DRV --&gt; HW1
    CAN_CORE --&gt; VCAN
</pre><p>위 그림이 앞으로의 모든 이야기의 뼈대다. 각 블록이 무엇이고 어떻게 조립되는지 하나씩 풀어낸다.</p><p>이 구조의 장점은 <strong>프로토콜별 소켓 타입</strong>이 분리돼 있다는 데 있다. <code>CAN_RAW</code>는 원시 프레임을 그대로 주고받고, <code>CAN_BCM</code>은 주기 송신과 타임아웃 감지를 커널 타이머 위에 올리며, <code>CAN_ISOTP</code>는 ISO 15765-2의 segmentation까지 커널이 책임진다. 즉 애플리케이션 개발자는 &quot;어떤 레벨에서 CAN을 다룰 것인가&quot;를 소켓 타입 하나로 결정한다. 또한 <code>cangw</code>는 소켓이 아니라 netlink 기반 커널 기능이므로 유저 프로세스가 죽어도 라우팅이 유지된다. 이 점은 <strong>상시 동작 게이트웨이</strong>를 만들 때 결정적이다.</p><h2 id="_2-virtual-can-—-하드웨어-없이-테스트" tabindex="-1"><a class="header-anchor" href="#_2-virtual-can-—-하드웨어-없이-테스트"><span>2. Virtual CAN — 하드웨어 없이 테스트</span></a></h2><p><code>vcan</code>은 <strong>가상 CAN 인터페이스</strong>를 제공하는 커널 모듈이다. 물리 트랜시버 없이 커널 내부에서 루프백처럼 동작해 개발·CI 환경에서 특히 유용하다.</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line"><span class="token function">sudo</span> modprobe vcan</span>
<span class="line"><span class="token function">sudo</span> <span class="token function">ip</span> <span class="token function">link</span> <span class="token function">add</span> dev vcan0 <span class="token builtin class-name">type</span> vcan</span>
<span class="line"><span class="token function">sudo</span> <span class="token function">ip</span> <span class="token function">link</span> <span class="token builtin class-name">set</span> vcan0 up</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>이것만으로 <code>candump vcan0</code>, <code>cansend vcan0 123#DEADBEEF</code>가 즉시 돌아간다. 여러 개가 필요하면 <code>vcan1</code>, <code>vcan2</code>를 추가로 만들면 된다. 재부팅 후에도 유지하려면 systemd-networkd의 netdev 파일에 <code>Kind=vcan</code>으로 선언하거나, <code>ip link</code>를 호출하는 단순 init 스크립트를 <code>/etc/systemd/system/</code>에 두는 방식이 보편적이다.</p><div class="hint-container tip"><p class="hint-container-title">vcan의 쓰임새</p><ul><li>로컬 개발 — 트랜시버·어댑터를 꽂지 않고 애플리케이션 로직만 검증.</li><li>유닛 테스트 — pytest·GoogleTest에서 <code>vcan0</code>를 세팅하고 송수신 assertion.</li><li>CI 파이프라인 — GitHub Actions runner에 <code>modprobe vcan</code> 한 줄만 추가하면 E2E 테스트 가능.</li><li>프로토콜 설계 검증 — DBC·ISO-TP·UDS 시뮬레이션을 실기 없이 먼저 끝낸다.</li></ul></div><p>vcan에는 타이밍 오류·오류 프레임·bus-off가 없다는 점을 기억해야 한다. 오류 시나리오를 재현하려면 별도의 수단이 필요하고, 이때는 실기·Vector CANoe·specialized fault injector를 써야 한다.</p><p>또한 vcan은 본질적으로 루프백이라, 한 프로세스가 송신하면 같은 인터페이스에 바인드된 모든 소켓이 수신한다. 송신자가 자기 프레임을 다시 받는 &quot;echo&quot;를 원치 않으면 <code>CAN_RAW_RECV_OWN_MSGS</code> 소켓 옵션을 끄면 된다. python-can에서는 <code>receive_own_messages=False</code>가 기본값이다. 이 동작은 실제 CAN 버스와 달라 단위 테스트에서 <strong>자기 송신 루프</strong>를 잘못 탐지하는 원인이 되기도 한다.</p><p>대역폭 제한도 없다. 실제 500kbps·1Mbps 물리 버스의 버스 부하·지연을 검증하려면 vcan은 부족하고, <code>tc qdisc</code>나 CAN traffic generator 어댑터를 써야 한다. 그러나 <strong>로직 정합성</strong>만 본다면 vcan만으로도 프로젝트의 80% 이상 테스트가 커버된다.</p><p>유틸리티 중 <strong>vxcan</strong>도 알아두면 좋다. 두 개의 가상 인터페이스가 paired 형태로 생성돼, 한쪽에서 보낸 프레임이 다른 쪽에서 수신되는 구조다. 서로 다른 netns(network namespace)에 각각 배치하면 격리된 두 ECU 시뮬레이션을 한 머신에서 깔끔히 구현할 수 있다. 컨테이너·샌드박스 테스트에 특히 유용하다.</p><h2 id="_3-cangw-—-커널-내-can-라우팅" tabindex="-1"><a class="header-anchor" href="#_3-cangw-—-커널-내-can-라우팅"><span>3. cangw — 커널 내 CAN 라우팅</span></a></h2><p><strong>cangw</strong>는 <code>can-gw</code> 커널 모듈을 제어하는 유저 툴이다. 두 개 이상의 CAN 인터페이스 사이에서 프레임을 복사·필터링·변형하는 <strong>브리지/게이트웨이</strong> 역할을 한다. 유저 공간을 거치지 않고 커널 내부에서 처리되므로 지연이 작다.</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line"><span class="token comment"># can0 -&gt; can1로 모든 프레임 복사(echo 활성)</span></span>
<span class="line">cangw <span class="token parameter variable">-A</span> <span class="token parameter variable">-s</span> can0 <span class="token parameter variable">-d</span> can1 <span class="token parameter variable">-e</span></span>
<span class="line"></span>
<span class="line"><span class="token comment"># ID 필터: 0x123만 통과</span></span>
<span class="line">cangw <span class="token parameter variable">-A</span> <span class="token parameter variable">-s</span> can0 <span class="token parameter variable">-d</span> can1 <span class="token parameter variable">-f</span> <span class="token number">123</span>:C00007FF</span>
<span class="line"></span>
<span class="line"><span class="token comment"># 페이로드 수정: 8바이트 중 특정 바이트를 덮어씀</span></span>
<span class="line">cangw <span class="token parameter variable">-A</span> <span class="token parameter variable">-s</span> can0 <span class="token parameter variable">-d</span> can1 <span class="token parameter variable">-m</span> SET:IL:0.0.8.0xDEADBEEF00</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><code>-s</code>·<code>-d</code>는 source·destination, <code>-e</code>는 자기 인터페이스에서도 echo를 유지하라는 뜻이다. <code>-f</code>는 ID:MASK 형식 필터, <code>-m</code>은 modifier 규칙이다. <code>cangw -L</code>로 현재 규칙을 나열하고 <code>-D</code>로 삭제한다.</p><p>modifier는 순서가 중요하다. <code>AND</code>·<code>OR</code>·<code>XOR</code>·<code>SET</code> 연산이 있고, 대상은 ID·DLC·페이로드·타임스탬프 모두 가능하다. 예를 들어 <code>SET:IL:0.0.8.0xDEADBEEF00</code>에서 <code>IL</code>은 Identifier·Length, 그 뒤 숫자들은 적용 바이트 위치·길이다. 규칙 하나로 ID 변환·페이로드 덮어쓰기·DLC 조정까지 할 수 있으므로 <strong>게이트웨이 로직을 유저 코드 한 줄 없이</strong> 구성하는 것이 가능하다.</p><p>실전 규칙 세트는 다음과 같은 모양이 많다.</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line"><span class="token comment"># 민감 메시지 차단(0x7F0~0x7FF 진단 범위는 통과시키지 않음)</span></span>
<span class="line">cangw <span class="token parameter variable">-A</span> <span class="token parameter variable">-s</span> can0 <span class="token parameter variable">-d</span> can1 <span class="token parameter variable">-f</span> 000:FFFF    <span class="token comment"># 일단 전체 통과</span></span>
<span class="line">cangw <span class="token parameter variable">-A</span> <span class="token parameter variable">-s</span> can0 <span class="token parameter variable">-d</span> can1 <span class="token parameter variable">-f</span> 7F0:FFF0 <span class="token parameter variable">-i</span> <span class="token comment"># 0x7F0~0x7FF는 inverse로 제외</span></span>
<span class="line"></span>
<span class="line"><span class="token comment"># ID 재매핑(섀시 0x200 -&gt; 파워트레인 0x300)</span></span>
<span class="line">cangw <span class="token parameter variable">-A</span> <span class="token parameter variable">-s</span> can0 <span class="token parameter variable">-d</span> can1 <span class="token parameter variable">-f</span> <span class="token number">200</span>:7FF <span class="token parameter variable">-m</span> SET:I:0x300</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>규칙은 커널 내부 링크드 리스트로 관리되므로 수십 개도 성능에 거의 영향이 없다. 다만 모든 소스·목적 쌍에 대해 규칙이 선형 탐색되므로, 한 인터페이스에 수백 개를 붙이면 지연이 생길 수 있다.</p><div class="hint-container info"><p class="hint-container-title">cangw의 실전 활용</p><ul><li><strong>브리지</strong>: 차량 섀시 CAN과 파워트레인 CAN을 게이트웨이로 연결하면서 민감 메시지는 필터링.</li><li><strong>ECU 에뮬레이션</strong>: 미완성 ECU의 자리에 vcan 채널을 만들고, 일부 메시지만 실기로 전달.</li><li><strong>로드맵 재구성</strong>: 기존 메시지 ID를 프로젝트 새 규약으로 변환(ID remap + 페이로드 수정).</li><li><strong>분석용 tap</strong>: 모든 프레임을 별도 인터페이스로 복제해 <code>candump</code>·Wireshark로 관찰.</li><li><strong>테스트 더블</strong>: 실차에서 들어온 프레임을 변조해 시뮬레이션 버스로 넘김. 고장 주입 시나리오에 유용.</li></ul></div><p>cangw는 <strong>경계 방어 장치</strong>로도 쓰인다. 차량 네트워크에 진단 포트를 노출해야 하는 상황에서, 진단 전용 ID만 통과시키고 나머지는 차단하는 규칙을 걸어두면 외부 공격 벡터를 크게 줄일 수 있다. CH22·CH23의 보안 관점과 자연스럽게 이어진다.</p><h2 id="_4-can-isotp-—-iso-15765-2를-커널로" tabindex="-1"><a class="header-anchor" href="#_4-can-isotp-—-iso-15765-2를-커널로"><span>4. can-isotp — ISO 15765-2를 커널로</span></a></h2><p>UDS·OBD-II는 8바이트(또는 FD 64바이트)를 넘는 페이로드를 보내기 위해 <strong>ISO 15765-2(ISO-TP)</strong>라는 전송 계층을 쓴다. 이를 유저 공간에서 구현하면 Flow Control·Block Size·ST_min 타이밍에 민감해 버그가 잘 생긴다. <code>can-isotp</code> 모듈은 이 segmentation·reassembly를 커널에서 처리해주는 공식 구현이다.</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line"><span class="token function">sudo</span> modprobe can-isotp</span>
<span class="line">isotpsend <span class="token parameter variable">-s</span> 7E0 <span class="token parameter variable">-d</span> 7E8 can0 <span class="token operator">&lt;</span> payload.bin</span>
<span class="line">isotprecv <span class="token parameter variable">-s</span> 7E8 <span class="token parameter variable">-d</span> 7E0 can0</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><code>-s</code>는 송신 ID(tester가 ECU에게 보낼 요청 ID), <code>-d</code>는 수신 ID(ECU가 tester에게 보낼 응답 ID). 위 예는 UDS 관례에 맞춘 표준 페어다. 파이썬에서는 <code>SocketcanIsotp</code> backend나 <code>can-isotp</code> Python binding으로 동일 인터페이스를 쓴다.</p><div class="language-python line-numbers-mode" data-highlighter="prismjs" data-ext="py" data-title="py"><pre><code><span class="line"><span class="token keyword">import</span> socket</span>
<span class="line">s <span class="token operator">=</span> socket<span class="token punctuation">.</span>socket<span class="token punctuation">(</span>socket<span class="token punctuation">.</span>AF_CAN<span class="token punctuation">,</span> socket<span class="token punctuation">.</span>SOCK_DGRAM<span class="token punctuation">,</span> socket<span class="token punctuation">.</span>CAN_ISOTP<span class="token punctuation">)</span></span>
<span class="line">s<span class="token punctuation">.</span>bind<span class="token punctuation">(</span><span class="token punctuation">(</span><span class="token string">&#39;can0&#39;</span><span class="token punctuation">,</span> <span class="token number">0x7E0</span><span class="token punctuation">,</span> <span class="token number">0x7E8</span><span class="token punctuation">)</span><span class="token punctuation">)</span></span>
<span class="line">s<span class="token punctuation">.</span>send<span class="token punctuation">(</span><span class="token string">b&#39;\\x22\\xF1\\x90&#39;</span><span class="token punctuation">)</span>  <span class="token comment"># UDS ReadDataByIdentifier</span></span>
<span class="line">resp <span class="token operator">=</span> s<span class="token punctuation">.</span>recv<span class="token punctuation">(</span><span class="token number">4095</span><span class="token punctuation">)</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>세부 프로토콜(SF/FF/CF, FC의 Block Size·ST_min, padding)은 CH19에서 상세히 다룬다. 여기서는 &quot;커널 인터페이스로 바로 풀 페이로드를 주고받을 수 있다&quot;는 점만 짚어두면 충분하다.</p><p><code>can-isotp</code>의 이점은 세 가지다. 첫째, 타이밍이 정확하다. ST_min을 지키는 것은 고빈도 주기 일을 가진 유저 공간에서는 쉽지 않은데, 커널 타이머는 편차가 작다. 둘째, Flow Control의 응답을 놓쳐 segment가 뒤엉키는 버그가 없다. 셋째, 플랫폼 간 일관된 인터페이스를 얻는다. Windows의 PCAN-ISO-TP API나 Vector XL의 TP 레이어와 동일한 개념으로 쓸 수 있다.</p><p>단점은 인터페이스당 하나의 송수신 쌍만 bind 가능한 제약이다. 여러 ECU와 동시에 통신하려면 추가 소켓을 열어야 한다. 또한 padding·extended addressing 같은 옵션은 <code>setsockopt</code>로 세밀히 조정해야 하며, 이를 모르면 &quot;왜 ECU가 응답을 안 하지&quot;에서 몇 시간을 허비한다.</p><h2 id="_5-broadcast-manager-bcm-—-주기-송신의-효율판" tabindex="-1"><a class="header-anchor" href="#_5-broadcast-manager-bcm-—-주기-송신의-효율판"><span>5. Broadcast Manager(BCM) — 주기 송신의 효율판</span></a></h2><p>BCM은 <code>CAN_BCM</code> 프로토콜로 열리는 소켓을 통해 <strong>주기 송신과 수신 타임아웃 감지</strong>를 커널이 대행하는 장치다. <code>cansend</code> 루프를 유저 공간에서 돌리면 스케줄링 지터·컨텍스트 스위치가 개입하지만, BCM은 커널 타이머에 직접 올라가므로 더 정밀하다.</p><div class="language-python line-numbers-mode" data-highlighter="prismjs" data-ext="py" data-title="py"><pre><code><span class="line"><span class="token keyword">import</span> socket<span class="token punctuation">,</span> struct</span>
<span class="line">s <span class="token operator">=</span> socket<span class="token punctuation">.</span>socket<span class="token punctuation">(</span>socket<span class="token punctuation">.</span>AF_CAN<span class="token punctuation">,</span> socket<span class="token punctuation">.</span>SOCK_DGRAM<span class="token punctuation">,</span> socket<span class="token punctuation">.</span>CAN_BCM<span class="token punctuation">)</span></span>
<span class="line">s<span class="token punctuation">.</span>connect<span class="token punctuation">(</span><span class="token punctuation">(</span><span class="token string">&#39;can0&#39;</span><span class="token punctuation">,</span><span class="token punctuation">)</span><span class="token punctuation">)</span></span>
<span class="line"><span class="token comment"># TX_SETUP: 100ms 주기로 0x123 메시지 반복 송신</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>BCM은 수신 측에서도 강력하다. &quot;특정 ID가 500ms 동안 오지 않으면 이벤트 발생&quot; 같은 <strong>timeout monitor</strong>를 걸어두면, 유저가 폴링할 필요 없이 커널이 통보한다. Network Management(CH17)에서 타임아웃 감지가 핵심이 될 때 다시 등장한다.</p><p>BCM의 또 다른 쓸모는 <strong>조건부 변경 감지</strong>다. 동일 ID의 페이로드가 이전과 달라진 순간만 이벤트를 올려주는 모드가 있다. 대시보드 갱신·텔레매틱스 업로드처럼 &quot;값이 바뀔 때만 올려라&quot; 요구에 정확히 부합한다. 바이트 단위·비트 단위 마스크로 비교 범위를 좁힐 수도 있어, 신호 하나가 변할 때만 반응하는 저전력 로직도 커널에서 해결된다.</p><p>단점도 있다. BCM 소켓 하나당 관리할 수 있는 룰 수에 한계가 있고(보통 수백 개), 메시지 ID가 수천 개인 대형 차량 네트워크에서는 소켓을 분할해야 한다. 또한 BCM은 주기 송신 사이사이에 <strong>동적 페이로드 교체</strong>가 가능하지만, 이 때문에 race condition이 나기도 한다. 커널 제공 API(<code>TX_SEND</code> vs <code>TX_SETUP</code>)를 정확히 구분해 쓰는 연습이 필요하다.</p><div class="hint-container tip"><p class="hint-container-title">cyclic 송신의 세 선택지</p><ul><li><code>cangen -g 10</code> — 간단한 스트레스 테스트.</li><li><code>cansend</code> 루프 — 빠른 스크립트.</li><li>BCM — 정밀한 주기·타임아웃이 필요한 ECU 시뮬레이션. 실전 rest-bus simulation에 적합.</li></ul></div><h2 id="_6-can-fd-on-socketcan" tabindex="-1"><a class="header-anchor" href="#_6-can-fd-on-socketcan"><span>6. CAN FD on SocketCAN</span></a></h2><p>CAN FD는 SocketCAN에서 <code>ip</code> 명령으로 간단히 올린다.</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line"><span class="token function">sudo</span> <span class="token function">ip</span> <span class="token function">link</span> <span class="token builtin class-name">set</span> can0 up <span class="token builtin class-name">type</span> can <span class="token punctuation">\\</span></span>
<span class="line">    bitrate <span class="token number">500000</span> dbitrate <span class="token number">2000000</span> fd on <span class="token punctuation">\\</span></span>
<span class="line">    sample-point <span class="token number">0.75</span> dsample-point <span class="token number">0.75</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><code>bitrate</code>는 arbitration phase 속도, <code>dbitrate</code>는 data phase 속도다. <code>fd on</code>을 빠뜨리면 드라이버는 classical CAN 모드로만 동작한다. 송신 시 <code>cangen -x can0</code>처럼 <code>-x</code> 옵션을 주면 CAN FD 프레임을 생성하고, <code>cansend can0 123##1DEADBEEF...</code> 형식(<code>##</code>가 FD flag, 그 뒤 1자리 hex가 ESI/BRS)으로 수동 발생도 가능하다.</p><p>python-can에서는 <code>Bus(interface=&#39;socketcan&#39;, channel=&#39;can0&#39;, fd=True)</code>만 쓰면 된다. 프레임 길이는 0·1·2·3·4·5·6·7·8·12·16·20·24·32·48·64 바이트 중에서 선택한다.</p><p>FD 링크를 올릴 때 제일 먼저 확인할 값은 <strong>sample-point와 dsample-point</strong>다. arbitration phase와 data phase의 샘플 포인트를 분리 지정해야 안정성이 나오는데, 많은 현장 매뉴얼이 classical CAN의 경험칙(75%)을 그대로 쓰다가 고속 data phase에서 비트 오류가 쏟아지는 사고를 겪는다. CH4·CH7에서 설명한 비트 타이밍 규칙을 상기하며, FD에서는 data phase 샘플 포인트를 올리기보다 <strong>SJW를 넉넉히 잡아</strong> 오실레이터 편차를 흡수하는 쪽이 안전하다.</p><p>또한 <code>ip -details link show can0</code>로 현재 비트율·샘플 포인트·FD 여부를 확인하는 습관이 중요하다. 드라이버가 요청한 비트율과 실제 가능한 비트율 사이에 차이가 있을 수 있고, 드라이버는 근사 값을 조용히 채택한다.</p><h2 id="_7-python-can-백엔드-확장" tabindex="-1"><a class="header-anchor" href="#_7-python-can-백엔드-확장"><span>7. python-can 백엔드 확장</span></a></h2><p>python-can은 <strong>어댑터 추상화 레이어</strong>다. 같은 코드가 다른 하드웨어에서 돌게 해준다.</p><table><thead><tr><th>backend</th><th>플랫폼</th><th>비고</th></tr></thead><tbody><tr><td><code>socketcan</code></td><td>Linux</td><td>기본. vcan/cangw/isotp과 궁합 최고</td></tr><tr><td><code>pcan</code></td><td>Win/Lin/macOS</td><td>PEAK PCAN-USB, PCAN-Basic API 필요</td></tr><tr><td><code>vector</code></td><td>Windows</td><td>Vector CANcase/VN1600, XL Driver Library 필요</td></tr><tr><td><code>kvaser</code></td><td>Win/Lin/macOS</td><td>Kvaser CANlib 필요</td></tr><tr><td><code>slcan</code></td><td>모든 OS</td><td>SLCAN 호환 어댑터(candleLight 일부)</td></tr><tr><td><code>gs_usb</code></td><td>모든 OS</td><td>candleLight 펌웨어의 USB CAN(오픈소스)</td></tr><tr><td><code>seeedstudio</code></td><td>모든 OS</td><td>Seeed USB-CAN-B 시리얼 프로토콜</td></tr><tr><td><code>neovi</code></td><td>Win/Lin</td><td>Intrepid ValueCAN/neoVI</td></tr></tbody></table><p>선택은 코드 한 줄이다.</p><div class="language-python line-numbers-mode" data-highlighter="prismjs" data-ext="py" data-title="py"><pre><code><span class="line"><span class="token keyword">import</span> can</span>
<span class="line">bus <span class="token operator">=</span> can<span class="token punctuation">.</span>Bus<span class="token punctuation">(</span>interface<span class="token operator">=</span><span class="token string">&#39;pcan&#39;</span><span class="token punctuation">,</span> channel<span class="token operator">=</span><span class="token string">&#39;PCAN_USBBUS1&#39;</span><span class="token punctuation">,</span> bitrate<span class="token operator">=</span><span class="token number">500000</span><span class="token punctuation">)</span></span>
<span class="line">bus <span class="token operator">=</span> can<span class="token punctuation">.</span>Bus<span class="token punctuation">(</span>interface<span class="token operator">=</span><span class="token string">&#39;vector&#39;</span><span class="token punctuation">,</span> app_name<span class="token operator">=</span><span class="token string">&#39;CANalyzer&#39;</span><span class="token punctuation">,</span> channel<span class="token operator">=</span><span class="token number">0</span><span class="token punctuation">,</span> bitrate<span class="token operator">=</span><span class="token number">500000</span><span class="token punctuation">)</span></span>
<span class="line">bus <span class="token operator">=</span> can<span class="token punctuation">.</span>Bus<span class="token punctuation">(</span>interface<span class="token operator">=</span><span class="token string">&#39;socketcan&#39;</span><span class="token punctuation">,</span> channel<span class="token operator">=</span><span class="token string">&#39;can0&#39;</span><span class="token punctuation">,</span> fd<span class="token operator">=</span><span class="token boolean">True</span><span class="token punctuation">)</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>동일한 <code>Bus</code> 객체에 <code>recv()</code>·<code>send()</code>·<code>Notifier</code>를 걸면 된다. 대부분의 백엔드가 CAN FD·error frame·filter를 지원하지만, 세부 기능은 벤더 문서와 python-can 호환 표를 반드시 확인해야 한다.</p><p>백엔드가 바뀌면 타임스탬프의 기준도 달라진다. socketcan은 리눅스 커널의 <code>SO_TIMESTAMPING</code>을 사용해 <strong>하드웨어 타임스탬프</strong>를 잡을 수 있지만, 일부 저가 어댑터는 유저 공간에 도착한 시각을 쓴다. 정밀한 지연 측정·bus-load 계산에서는 이 차이가 중요하므로, &quot;어느 시점의 타임스탬프인가&quot;를 명시하는 문서를 프로젝트에 남기는 것이 좋다.</p><p>또한 필터(<code>can_filters=[{&quot;can_id&quot;: 0x123, &quot;can_mask&quot;: 0x7FF}]</code>)는 socketcan에서는 커널에서 걸러지므로 유저 공간에 오는 프레임 수가 줄어든다. 벤더 백엔드는 유저 공간 필터인 경우가 많아, 고부하 버스에서는 <strong>CPU 사용률 차이</strong>가 체감된다.</p><h2 id="_8-asyncio와-notifier-—-이벤트-기반-처리" tabindex="-1"><a class="header-anchor" href="#_8-asyncio와-notifier-—-이벤트-기반-처리"><span>8. asyncio와 Notifier — 이벤트 기반 처리</span></a></h2><p>대량 프레임을 처리할 때 <code>while True: bus.recv()</code> 패턴은 빠르게 한계에 부딪힌다. python-can의 <code>Notifier</code>는 내부 스레드에서 수신을 돌리며 등록된 Listener에 디스패치한다. asyncio와 결합하면 단일 이벤트 루프에서 수백 ID를 깔끔히 다룰 수 있다.</p><div class="language-python line-numbers-mode" data-highlighter="prismjs" data-ext="py" data-title="py"><pre><code><span class="line"><span class="token keyword">import</span> asyncio<span class="token punctuation">,</span> can</span>
<span class="line"></span>
<span class="line"><span class="token keyword">class</span> <span class="token class-name">MyListener</span><span class="token punctuation">(</span>can<span class="token punctuation">.</span>Listener<span class="token punctuation">)</span><span class="token punctuation">:</span></span>
<span class="line">    <span class="token keyword">def</span> <span class="token function">on_message_received</span><span class="token punctuation">(</span>self<span class="token punctuation">,</span> msg<span class="token punctuation">)</span><span class="token punctuation">:</span></span>
<span class="line">        <span class="token keyword">print</span><span class="token punctuation">(</span>msg<span class="token punctuation">.</span>arbitration_id<span class="token punctuation">,</span> msg<span class="token punctuation">.</span>data<span class="token punctuation">)</span></span>
<span class="line"></span>
<span class="line"><span class="token keyword">async</span> <span class="token keyword">def</span> <span class="token function">main</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">:</span></span>
<span class="line">    bus <span class="token operator">=</span> can<span class="token punctuation">.</span>Bus<span class="token punctuation">(</span>interface<span class="token operator">=</span><span class="token string">&#39;socketcan&#39;</span><span class="token punctuation">,</span> channel<span class="token operator">=</span><span class="token string">&#39;can0&#39;</span><span class="token punctuation">)</span></span>
<span class="line">    loop <span class="token operator">=</span> asyncio<span class="token punctuation">.</span>get_running_loop<span class="token punctuation">(</span><span class="token punctuation">)</span></span>
<span class="line">    notifier <span class="token operator">=</span> can<span class="token punctuation">.</span>Notifier<span class="token punctuation">(</span>bus<span class="token punctuation">,</span> <span class="token punctuation">[</span>MyListener<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">]</span><span class="token punctuation">,</span> loop<span class="token operator">=</span>loop<span class="token punctuation">)</span></span>
<span class="line">    <span class="token keyword">await</span> asyncio<span class="token punctuation">.</span>sleep<span class="token punctuation">(</span><span class="token number">10</span><span class="token punctuation">)</span></span>
<span class="line">    notifier<span class="token punctuation">.</span>stop<span class="token punctuation">(</span><span class="token punctuation">)</span></span>
<span class="line">    bus<span class="token punctuation">.</span>shutdown<span class="token punctuation">(</span><span class="token punctuation">)</span></span>
<span class="line"></span>
<span class="line">asyncio<span class="token punctuation">.</span>run<span class="token punctuation">(</span>main<span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Notifier가 소켓 I/O를 이벤트 루프에 위임하는 흐름은 다음과 같다.</p><pre class="mermaid">sequenceDiagram
    participant APP as asyncio loop
    participant NOTI as can.Notifier
    participant BUS as can.Bus(socketcan)
    participant KER as Linux kernel
    APP-&gt;&gt;NOTI: register(Listener)
    NOTI-&gt;&gt;BUS: poll/recv
    BUS-&gt;&gt;KER: read(can_raw socket)
    KER--&gt;&gt;BUS: CAN frame
    BUS--&gt;&gt;NOTI: Message
    NOTI--&gt;&gt;APP: on_message_received()
    APP-&gt;&gt;APP: await next tick
    APP-&gt;&gt;NOTI: stop()
    NOTI-&gt;&gt;BUS: shutdown
</pre><p>Listener는 여러 개를 체인할 수 있다. 로깅 리스너 + 디코더 리스너 + 매칭 리스너를 한 버스에 붙이는 구성이 실전에서 가장 깔끔하다.</p><p><strong>AsyncBufferedReader</strong>와 <strong>BufferedReader</strong>는 Listener와 짝을 이루는 큐 리더다. 코루틴 안에서 <code>await reader.get_message()</code>로 단일 메시지를 받거나, 이벤트 단위로 처리 순서를 보장할 수 있다. 이렇게 꾸리면 신호 디코딩·UDS 상태 기계·상위 애플리케이션을 각각 다른 코루틴에 둘 수 있어 <strong>테스트와 유지보수</strong>가 훨씬 쉬워진다.</p><p>스레드 기반 Notifier와 asyncio Notifier는 혼합해서는 안 된다. 하나의 Bus에 두 종류를 동시에 얹으면 프레임을 한쪽이 먼저 소비해 다른 쪽이 굶는다. 로깅 파이프라인을 이원화할 때는 <strong>Bus 인스턴스 자체를 분리</strong>하거나, 로깅용 별도 커널 소켓을 붙이는 구성이 안전하다.</p><h2 id="_9-dbc-연동-preview" tabindex="-1"><a class="header-anchor" href="#_9-dbc-연동-preview"><span>9. DBC 연동 preview</span></a></h2><p>python-can 자체는 바이트열만 다루지만, <strong>cantools</strong>와 결합하면 signal 단위 인코딩·디코딩이 가능해진다.</p><div class="language-python line-numbers-mode" data-highlighter="prismjs" data-ext="py" data-title="py"><pre><code><span class="line"><span class="token keyword">import</span> can<span class="token punctuation">,</span> cantools</span>
<span class="line">db <span class="token operator">=</span> cantools<span class="token punctuation">.</span>database<span class="token punctuation">.</span>load_file<span class="token punctuation">(</span><span class="token string">&#39;vehicle.dbc&#39;</span><span class="token punctuation">)</span></span>
<span class="line">msg <span class="token operator">=</span> db<span class="token punctuation">.</span>get_message_by_name<span class="token punctuation">(</span><span class="token string">&#39;EngineData&#39;</span><span class="token punctuation">)</span></span>
<span class="line">data <span class="token operator">=</span> msg<span class="token punctuation">.</span>encode<span class="token punctuation">(</span><span class="token punctuation">{</span><span class="token string">&#39;RPM&#39;</span><span class="token punctuation">:</span> <span class="token number">1000.0</span><span class="token punctuation">,</span> <span class="token string">&#39;Temp&#39;</span><span class="token punctuation">:</span> <span class="token number">80.0</span><span class="token punctuation">}</span><span class="token punctuation">)</span></span>
<span class="line">bus <span class="token operator">=</span> can<span class="token punctuation">.</span>Bus<span class="token punctuation">(</span>interface<span class="token operator">=</span><span class="token string">&#39;socketcan&#39;</span><span class="token punctuation">,</span> channel<span class="token operator">=</span><span class="token string">&#39;can0&#39;</span><span class="token punctuation">)</span></span>
<span class="line">bus<span class="token punctuation">.</span>send<span class="token punctuation">(</span>can<span class="token punctuation">.</span>Message<span class="token punctuation">(</span>arbitration_id<span class="token operator">=</span>msg<span class="token punctuation">.</span>frame_id<span class="token punctuation">,</span> data<span class="token operator">=</span>data<span class="token punctuation">,</span> is_extended_id<span class="token operator">=</span><span class="token boolean">False</span><span class="token punctuation">)</span><span class="token punctuation">)</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>DBC 파싱·Intel/Motorola 바이트 오더·Factor/Offset 계산은 CH16에서 상세히 다룬다. 여기서는 &quot;python-can + cantools 조합이면 신호 수준으로 작업할 수 있다&quot;만 기억하면 된다.</p><p>cantools는 <code>candump</code> 로그 파일도 바로 해석한다. 몇 시간짜리 필드 로그를 CLI 한 줄로 신호 CSV로 변환해 두면, pandas·Jupyter에서 시각화·분석이 즉시 가능하다. 이 흐름이 실전 필드 분석의 기본기다.</p><h2 id="_10-docker-ci-통합" tabindex="-1"><a class="header-anchor" href="#_10-docker-ci-통합"><span>10. Docker/CI 통합</span></a></h2><p>vcan만 있으면 E2E 테스트 컨테이너를 꾸밀 수 있다. 핵심은 두 가지: <strong>privileged 모드</strong>와 <strong>커널 모듈 로딩</strong>이다. 컨테이너 자체가 커널을 공유하므로, 호스트에서 <code>modprobe vcan</code>을 해두면 컨테이너 안에서 <code>ip link add</code>만으로 인터페이스가 뜬다.</p><div class="language-yaml line-numbers-mode" data-highlighter="prismjs" data-ext="yml" data-title="yml"><pre><code><span class="line"><span class="token comment"># docker-compose.yml 단편</span></span>
<span class="line"><span class="token key atrule">services</span><span class="token punctuation">:</span></span>
<span class="line">  <span class="token key atrule">can-tests</span><span class="token punctuation">:</span></span>
<span class="line">    <span class="token key atrule">image</span><span class="token punctuation">:</span> python<span class="token punctuation">:</span><span class="token number">3.12</span></span>
<span class="line">    <span class="token key atrule">privileged</span><span class="token punctuation">:</span> <span class="token boolean important">true</span></span>
<span class="line">    <span class="token key atrule">command</span><span class="token punctuation">:</span> <span class="token punctuation">&gt;</span><span class="token scalar string"></span>
<span class="line">      bash -c &quot;ip link add dev vcan0 type vcan &amp;&amp;</span>
<span class="line">               ip link set vcan0 up &amp;&amp;</span>
<span class="line">               pytest tests/&quot;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>GitHub Actions에서는 Linux runner에 <code>sudo modprobe vcan</code>을 두면 충분하다. 단, macOS·Windows runner에는 vcan이 없으므로 테스트 매트릭스를 OS별로 분기해야 한다.</p><p>컨테이너 이미지에는 <code>can-utils</code>·<code>iproute2</code>·<code>python3-can</code>·<code>python3-cantools</code>를 포함시키는 것이 편하다. 이 네 패키지만 있으면 <code>candump</code>·<code>cansend</code>·<code>isotpsend</code>·<code>cantools decode</code>를 모두 돌릴 수 있고, CI 스크립트가 간결해진다. 이미지를 한 번 캐시해두면 워크플로우 실행마다 설치 시간이 사라진다.</p><div class="hint-container warning"><p class="hint-container-title">실기 회귀 테스트는 따로</p><p>vcan은 논리적 검증에 강하지만 물리·타이밍·오류 동작은 재현하지 못한다. CI는 vcan으로, 야간·주간 실기 회귀는 Kvaser·PCAN과 연결된 별도 러너로 이원화하는 구성이 현실적이다.</p></div><h2 id="_11-운영-체크리스트" tabindex="-1"><a class="header-anchor" href="#_11-운영-체크리스트"><span>11. 운영 체크리스트</span></a></h2><p>SocketCAN을 실전 배포할 때 빠뜨리지 말아야 할 항목을 모았다.</p><ol><li><strong>링크 자동 복구</strong> — 버스 오프 복구는 드라이버가 자동으로 해주지 않는다. <code>ip link set can0 type can restart-ms 100</code>으로 지연 시간을 설정하거나, systemd 서비스에서 <code>restart-now</code>를 주기적으로 찌르는 구성이 보편적이다.</li><li><strong>큐 튜닝</strong> — <code>ip link set can0 txqueuelen 1000</code> 등으로 송신 큐를 넉넉히 잡아둔다. 기본값이 10인 경우가 있어 고부하에서 드랍이 발생한다.</li><li><strong>타임스탬프 정확도</strong> — <code>SO_TIMESTAMPING</code>의 하드웨어 모드 지원 여부를 드라이버별로 확인하고, 진단 툴에 명시한다.</li><li><strong>권한 관리</strong> — CAN 소켓 bind는 CAP_NET_RAW가 필요하다. 서비스 유저에 capability를 부여하거나, systemd의 <code>AmbientCapabilities=CAP_NET_RAW</code>로 제한을 건다.</li><li><strong>로깅·디스크 용량</strong> — <code>candump -l can0</code>는 대용량 로그를 만든다. logrotate·디스크 모니터링을 반드시 붙인다.</li><li><strong>vcan 정리 자동화</strong> — 테스트 종료 시 <code>ip link delete vcan0</code>로 인터페이스를 정리해야 다음 러너가 깨끗한 상태로 시작한다.</li></ol><h2 id="다음-챕터" tabindex="-1"><a class="header-anchor" href="#다음-챕터"><span>다음 챕터</span></a></h2><p>SocketCAN의 가상·브리지·ISO-TP·FD·백엔드·asyncio·Docker까지 훑었다. 이제는 실기 쪽 장비를 본다. CH15에서는 <strong>Vector·Kvaser·PEAK</strong> 같은 상용 하드웨어와 <strong>BusMaster·SavvyCAN·Wireshark</strong> 같은 소프트웨어 툴을 비교하고 상황별 선택 기준을 제시한다.</p><div class="hint-container tip"><p class="hint-container-title">핵심 정리</p><ul><li>vcan은 하드웨어 없이 CAN 애플리케이션을 개발·CI 테스트하게 해준다. 오류 시나리오는 재현 불가.</li><li>cangw는 커널 내에서 두 CAN 인터페이스 사이를 복사·필터·변조하는 브리지를 만든다.</li><li>can-isotp 모듈은 ISO 15765-2 segmentation을 커널에 맡겨 UDS·OBD-II 구현을 단순화한다.</li><li>BCM은 정밀한 주기 송신과 타임아웃 감지를 커널 타이머 기반으로 대행한다.</li><li>CAN FD는 <code>ip link set ... dbitrate fd on</code> 한 줄이면 SocketCAN에서 바로 동작한다.</li><li>python-can은 socketcan·pcan·vector·kvaser 등 다수 백엔드를 같은 API로 추상화하며, Notifier + asyncio 조합이 실전 패턴이다.</li></ul></div>`,83)]))}const l=s(c,[["render",p],["__file","14-socketcan-advanced.html.vue"]]),r=JSON.parse('{"path":"/study/can/14-socketcan-advanced.html","title":"CH14. SocketCAN 고급","lang":"en-US","frontmatter":{"title":"CH14. SocketCAN 고급","description":"Virtual CAN, 브리지/게이트웨이(cangw), ISO-TP 커널 모듈, CAN FD 운용, python-can 백엔드 확장까지 SocketCAN의 진짜 힘을 꺼낸다.","date":"2026-04-20T00:00:00.000Z","tags":["CAN","SocketCAN","vcan","cangw","isotp"],"prev":"/study/can/13-socketcan-basics","next":"/study/can/15-hardware-tools"},"headers":[{"level":1,"title":"CH14. SocketCAN 고급","slug":"ch14-socketcan-고급","link":"#ch14-socketcan-고급","children":[{"level":2,"title":"0. 이전 장과 이 장의 차이","slug":"_0-이전-장과-이-장의-차이","link":"#_0-이전-장과-이-장의-차이","children":[]},{"level":2,"title":"1. SocketCAN의 레이어드 관점","slug":"_1-socketcan의-레이어드-관점","link":"#_1-socketcan의-레이어드-관점","children":[]},{"level":2,"title":"2. Virtual CAN — 하드웨어 없이 테스트","slug":"_2-virtual-can-—-하드웨어-없이-테스트","link":"#_2-virtual-can-—-하드웨어-없이-테스트","children":[]},{"level":2,"title":"3. cangw — 커널 내 CAN 라우팅","slug":"_3-cangw-—-커널-내-can-라우팅","link":"#_3-cangw-—-커널-내-can-라우팅","children":[]},{"level":2,"title":"4. can-isotp — ISO 15765-2를 커널로","slug":"_4-can-isotp-—-iso-15765-2를-커널로","link":"#_4-can-isotp-—-iso-15765-2를-커널로","children":[]},{"level":2,"title":"5. Broadcast Manager(BCM) — 주기 송신의 효율판","slug":"_5-broadcast-manager-bcm-—-주기-송신의-효율판","link":"#_5-broadcast-manager-bcm-—-주기-송신의-효율판","children":[]},{"level":2,"title":"6. CAN FD on SocketCAN","slug":"_6-can-fd-on-socketcan","link":"#_6-can-fd-on-socketcan","children":[]},{"level":2,"title":"7. python-can 백엔드 확장","slug":"_7-python-can-백엔드-확장","link":"#_7-python-can-백엔드-확장","children":[]},{"level":2,"title":"8. asyncio와 Notifier — 이벤트 기반 처리","slug":"_8-asyncio와-notifier-—-이벤트-기반-처리","link":"#_8-asyncio와-notifier-—-이벤트-기반-처리","children":[]},{"level":2,"title":"9. DBC 연동 preview","slug":"_9-dbc-연동-preview","link":"#_9-dbc-연동-preview","children":[]},{"level":2,"title":"10. Docker/CI 통합","slug":"_10-docker-ci-통합","link":"#_10-docker-ci-통합","children":[]},{"level":2,"title":"11. 운영 체크리스트","slug":"_11-운영-체크리스트","link":"#_11-운영-체크리스트","children":[]},{"level":2,"title":"다음 챕터","slug":"다음-챕터","link":"#다음-챕터","children":[]}]}],"git":{},"filePathRelative":"_study/can/14-socketcan-advanced.md"}');export{l as comp,r as data};
