import{_ as s,c as a,e,o as t}from"./app-Dji5cKJR.js";const o="/images/study-observability/10-vector-matching-light.png",p="/images/study-observability/10-vector-matching-dark.png",c="/images/study-observability/10-antipattern-decision-light.png",l="/images/study-observability/10-antipattern-decision-dark.png",i={};function r(u,n){return t(),a("div",null,n[0]||(n[0]=[e(`<h1 id="promql-심화" tabindex="-1"><a class="header-anchor" href="#promql-심화"><span>PromQL 심화</span></a></h1><div class="hint-container info"><p class="hint-container-title">학습 목표</p><ul><li>라벨 집합이 다른 두 메트릭을 <code>on</code>/<code>ignoring</code>과 <code>group_left</code>/<code>group_right</code>로 정확히 조인한다.</li><li>서브쿼리로 range vector 함수의 결과를 다시 range로 감싸는 패턴을 익힌다.</li><li>classic histogram의 <code>le</code> 버킷 집계 순서 함정과 native histogram 쿼리의 차이를 이해한다.</li><li><code>offset</code>과 <code>@</code> modifier로 과거 시점을 참조하는 쿼리를 작성한다.</li><li>고카디널리티 쿼리·<code>rate</code> 이전 집계 같은 성능 안티패턴을 피한다.</li></ul></div><h2 id="_1-벡터-매칭-—-on-ignoring-group-left-group-right" tabindex="-1"><a class="header-anchor" href="#_1-벡터-매칭-—-on-ignoring-group-left-group-right"><span>1. 벡터 매칭 — on/ignoring, group_left/group_right</span></a></h2><p>두 instant vector 사이에 이항 연산(산술·비교)을 하려면 Prometheus가 어떤 시계열끼리 짝을 지을지 알아야 한다. 기본 규칙은 <strong>양쪽 라벨 집합이 완전히 같은 시계열끼리</strong> 매칭하는 것(one-to-one)이다. 라벨 스키마가 다른 메트릭끼리는 대부분 이 기본 규칙으로 매칭되지 않는다.</p><div class="language-promql line-numbers-mode" data-highlighter="prismjs" data-ext="promql" data-title="promql"><pre><code><span class="line"><span class="token comment"># 기본 매칭: 두 메트릭의 라벨 집합이 동일해야 매치된다</span></span>
<span class="line"><span class="token comment"># method, path, status_code가 양쪽에 다 있어야 성공</span></span>
<span class="line">http_requests_total<span class="token context-labels"><span class="token punctuation">{</span><span class="token label-key attr-name">status_code</span><span class="token punctuation">=</span><span class="token label-value attr-value">&quot;500&quot;</span><span class="token punctuation">}</span></span> <span class="token operator">/</span> http_requests_total<span class="token context-labels"><span class="token punctuation">{</span><span class="token label-key attr-name">status_code</span><span class="token punctuation">=~</span><span class="token label-value attr-value">&quot;.*&quot;</span><span class="token punctuation">}</span></span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><strong><code>on(labels...)</code></strong>은 지정한 라벨만 기준으로 매칭한다. <strong><code>ignoring(labels...)</code></strong>은 반대로 지정한 라벨을 제외한 나머지 전부를 기준으로 매칭한다.</p><div class="language-promql line-numbers-mode" data-highlighter="prismjs" data-ext="promql" data-title="promql"><pre><code><span class="line"><span class="token comment"># instance 라벨만 기준으로 매칭 (나머지 라벨 차이는 무시)</span></span>
<span class="line">node_memory_MemAvailable_bytes</span>
<span class="line">  <span class="token operator">/</span> <span class="token keyword">on</span><span class="token vector-match"><span class="token punctuation">(</span><span class="token label-key attr-name">instance</span><span class="token punctuation">)</span></span> node_memory_MemTotal_bytes</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>한쪽 시계열 하나에 다른 쪽 시계열 여러 개가 매칭되는 many-to-one 상황(예: Deployment 정보 하나에 Pod 여러 개)에서는 <code>on</code>/<code>ignoring</code>만으로 부족하다. Prometheus가 &quot;어느 쪽이 many인가&quot;를 명시하라고 에러를 낸다. 이때 <strong><code>group_left(extra_labels...)</code></strong> 또는 <strong><code>group_right(extra_labels...)</code></strong>를 붙인다. <code>group_left</code>는 왼쪽이 &quot;one&quot;, 오른쪽이 &quot;many&quot;라는 뜻이고, <code>group_right</code>는 그 반대다. 괄호 안에는 many 쪽에서 결과에 끌어오고 싶은 추가 라벨을 나열한다.</p><div class="language-promql line-numbers-mode" data-highlighter="prismjs" data-ext="promql" data-title="promql"><pre><code><span class="line"><span class="token comment"># kube_pod_info(many: pod마다 1개, node 라벨 보유)와</span></span>
<span class="line"><span class="token comment"># cAdvisor 메모리 사용량(마찬가지로 pod 단위)을 조인해</span></span>
<span class="line"><span class="token comment"># node 라벨을 결과에 끌어오는 예 — pod 기준 one-to-one이므로 group 불필요</span></span>
<span class="line">container_memory_working_set_bytes</span>
<span class="line">  <span class="token operator">*</span> <span class="token keyword">on</span><span class="token vector-match"><span class="token punctuation">(</span><span class="token label-key attr-name">pod</span><span class="token punctuation">)</span></span> <span class="token keyword">group_left</span><span class="token vector-match"><span class="token punctuation">(</span><span class="token label-key attr-name">node</span><span class="token punctuation">)</span></span> kube_pod_info</span>
<span class="line"></span>
<span class="line"><span class="token comment"># Deployment 단위 replica 부족 여부(one: deployment당 1개)에</span></span>
<span class="line"><span class="token comment"># 해당 deployment 소속 Pod들의 CPU 사용량(many: pod마다 여러 개)을 곱해</span></span>
<span class="line"><span class="token comment"># &quot;replica 부족한 deployment의 pod cpu&quot;를 남기는 예</span></span>
<span class="line">kube_deployment_status_replicas_available</span>
<span class="line">  <span class="token operator">/</span> <span class="token keyword">on</span><span class="token vector-match"><span class="token punctuation">(</span><span class="token label-key attr-name">deployment</span><span class="token punctuation">)</span></span> <span class="token keyword">group_right</span><span class="token vector-match"><span class="token punctuation">(</span><span class="token punctuation">)</span></span></span>
<span class="line">  <span class="token keyword">sum</span><span class="token punctuation">(</span><span class="token function">rate</span><span class="token punctuation">(</span>container_cpu_usage_seconds_total<span class="token context-range"><span class="token punctuation">[</span><span class="token range-duration number">5m</span><span class="token punctuation">]</span></span><span class="token punctuation">)</span><span class="token punctuation">)</span> <span class="token keyword">by</span> <span class="token vector-match"><span class="token punctuation">(</span><span class="token label-key attr-name">deployment</span><span class="token punctuation">)</span></span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><img src="`+o+'" alt="벡터 매칭 — on/group_left로 왼쪽(one) 벡터의 값을 기준으로, 오른쪽(many) 벡터의 extra 라벨을 결과 벡터에 끌어와 덧붙이는 조인 구조"><img src="'+p+`" alt="벡터 매칭 — on/group_left로 왼쪽(one) 벡터의 값을 기준으로, 오른쪽(many) 벡터의 extra 라벨을 결과 벡터에 끌어와 덧붙이는 조인 구조"></p><p>가장 흔한 실무 패턴은 <code>kube_pod_info</code>, <code>kube_pod_labels</code>처럼 <strong>정보성(info) 메트릭</strong>을 <code>group_left</code>로 끌어와 사용량 메트릭에 라벨을 덧붙이는 것이다. info 메트릭은 값 자체는 항상 <code>1</code>이고 라벨에만 의미가 있다.</p><div class="language-promql line-numbers-mode" data-highlighter="prismjs" data-ext="promql" data-title="promql"><pre><code><span class="line"><span class="token comment"># 팀 라벨을 실제 사용량에 붙여서 팀별로 집계</span></span>
<span class="line"><span class="token keyword">sum</span><span class="token punctuation">(</span></span>
<span class="line">  <span class="token function">rate</span><span class="token punctuation">(</span>container_cpu_usage_seconds_total<span class="token context-range"><span class="token punctuation">[</span><span class="token range-duration number">5m</span><span class="token punctuation">]</span></span><span class="token punctuation">)</span></span>
<span class="line">  <span class="token operator">*</span> <span class="token keyword">on</span><span class="token vector-match"><span class="token punctuation">(</span><span class="token label-key attr-name">namespace</span><span class="token punctuation">)</span></span> <span class="token keyword">group_left</span><span class="token vector-match"><span class="token punctuation">(</span><span class="token label-key attr-name">team</span><span class="token punctuation">)</span></span> kube_namespace_labels<span class="token context-labels"><span class="token punctuation">{</span><span class="token label-key attr-name">label_team</span><span class="token punctuation">!=</span><span class="token label-value attr-value">&quot;&quot;</span><span class="token punctuation">}</span></span></span>
<span class="line"><span class="token punctuation">)</span> <span class="token keyword">by</span> <span class="token vector-match"><span class="token punctuation">(</span><span class="token label-key attr-name">team</span><span class="token punctuation">)</span></span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><div class="hint-container warning"><p class="hint-container-title">many-to-many는 group_left/group_right로도 못 푼다</p><p>양쪽 다 매칭 키에 여러 시계열이 걸리는 many-to-many 상황은 애초에 벡터 매칭으로 표현할 수 없는 연산이다. 먼저 <code>sum by (...)</code> 등으로 한쪽을 one으로 만든 뒤에 조인해야 한다.</p></div><h2 id="_2-서브쿼리" tabindex="-1"><a class="header-anchor" href="#_2-서브쿼리"><span>2. 서브쿼리</span></a></h2><p>서브쿼리는 range vector 함수의 <strong>결과</strong>를 다시 range로 감싸서 또 다른 range 함수에 넘기는 문법이다. <code>expr[range:resolution]</code> 형태로 쓰며, <code>resolution</code>을 생략하면 Prometheus가 전역 evaluation interval을 기본값으로 쓴다.</p><div class="language-promql line-numbers-mode" data-highlighter="prismjs" data-ext="promql" data-title="promql"><pre><code><span class="line"><span class="token comment"># 지난 1시간 동안, 1분 해상도로 계산한 rate의 최댓값</span></span>
<span class="line"><span class="token function">max_over_time</span><span class="token punctuation">(</span></span>
<span class="line">  <span class="token function">rate</span><span class="token punctuation">(</span>http_requests_total<span class="token context-range"><span class="token punctuation">[</span><span class="token range-duration number">5m</span><span class="token punctuation">]</span></span><span class="token punctuation">)</span><span class="token context-range"><span class="token punctuation">[</span><span class="token range-duration number">1h</span><span class="token punctuation">:</span><span class="token range-duration number">1m</span><span class="token punctuation">]</span></span></span>
<span class="line"><span class="token punctuation">)</span></span>
<span class="line"></span>
<span class="line"><span class="token comment"># 지난 6시간 동안 5분 rate가 임계값을 넘은 비율</span></span>
<span class="line"><span class="token function">avg_over_time</span><span class="token punctuation">(</span></span>
<span class="line">  <span class="token punctuation">(</span><span class="token function">rate</span><span class="token punctuation">(</span>http_requests_total<span class="token context-labels"><span class="token punctuation">{</span><span class="token label-key attr-name">status_code</span><span class="token punctuation">=~</span><span class="token label-value attr-value">&quot;5..&quot;</span><span class="token punctuation">}</span></span><span class="token context-range"><span class="token punctuation">[</span><span class="token range-duration number">5m</span><span class="token punctuation">]</span></span><span class="token punctuation">)</span> <span class="token operator">&gt;</span> bool <span class="token number">0.05</span><span class="token punctuation">)</span><span class="token context-range"><span class="token punctuation">[</span><span class="token range-duration number">6h</span><span class="token punctuation">:</span><span class="token range-duration number">1m</span><span class="token punctuation">]</span></span></span>
<span class="line"><span class="token punctuation">)</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>서브쿼리는 &quot;이미 range 함수를 한 번 거친 결과를 또 range로 다뤄야 할 때&quot;만 필요하다. 단순히 <code>rate(v[1h])</code> 한 번으로 되는 걸 굳이 서브쿼리로 만들면 안 된다. 서브쿼리는 내부적으로 지정한 resolution마다 쿼리를 재평가하기 때문에 계산 비용이 크다 — 특히 짧은 resolution·긴 range 조합은 Prometheus 서버에 상당한 부하를 준다. Recording Rule로 미리 계산해두는 편이 나을 때가 많다(<a href="/study/observability/11-recording-alerting-rules">Recording·Alerting Rule</a> 참고).</p><h2 id="_3-히스토그램-쿼리-심화" tabindex="-1"><a class="header-anchor" href="#_3-히스토그램-쿼리-심화"><span>3. 히스토그램 쿼리 심화</span></a></h2><p>classic histogram은 <code>_bucket{le=&quot;...&quot;}</code> (누적 카운트), <code>_sum</code>, <code>_count</code> 세 종류의 시계열로 구성된다. <code>histogram_quantile(φ, b)</code>는 <code>le</code> 라벨을 가진 버킷 시계열 집합을 받아 선형 보간으로 분위수를 추정한다.</p><div class="language-promql line-numbers-mode" data-highlighter="prismjs" data-ext="promql" data-title="promql"><pre><code><span class="line"><span class="token comment"># p95 레이턴시 — le별로 sum한 뒤에 quantile을 계산해야 한다</span></span>
<span class="line"><span class="token function">histogram_quantile</span><span class="token punctuation">(</span></span>
<span class="line">  <span class="token number">0.95</span><span class="token punctuation">,</span></span>
<span class="line">  <span class="token keyword">sum</span><span class="token punctuation">(</span><span class="token function">rate</span><span class="token punctuation">(</span>http_request_duration_seconds_bucket<span class="token context-range"><span class="token punctuation">[</span><span class="token range-duration number">5m</span><span class="token punctuation">]</span></span><span class="token punctuation">)</span><span class="token punctuation">)</span> <span class="token keyword">by</span> <span class="token vector-match"><span class="token punctuation">(</span><span class="token label-key attr-name">le</span><span class="token punctuation">,</span> <span class="token label-key attr-name">job</span><span class="token punctuation">)</span></span></span>
<span class="line"><span class="token punctuation">)</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><div class="hint-container warning"><p class="hint-container-title">aggregation 순서가 결과를 바꾼다</p><p><code>sum by (le, job)</code>처럼 <strong><code>le</code>를 반드시 group-by 라벨에 포함</strong>해야 한다. <code>le</code>를 빼고 집계하면 서로 다른 버킷 경계의 카운트가 하나로 뭉개져 <code>histogram_quantile</code>이 엉뚱한 값을 낸다. 그리고 <code>rate()</code>는 반드시 <code>histogram_quantile</code> <strong>안쪽</strong>, <code>sum by</code>보다 <strong>먼저</strong> 적용해야 한다 — counter인 버킷 값을 먼저 합산한 뒤 rate를 걸면 개별 인스턴스의 리셋 보정이 깨지는 것은 일반 counter와 동일한 함정이다. 올바른 순서는 <code>histogram_quantile(φ, sum by (le, ...) (rate(..._bucket[5m])))</code>다.</p></div><p>버킷 경계(<code>le</code> 값)는 클라이언트 라이브러리에서 미리 정의하므로, 버킷 사이 구간에서는 균등 분포를 가정한 <strong>선형 보간</strong>일 뿐 정확한 값이 아니다. 버킷 경계가 성기면 오차가 커진다. p99처럼 꼬리 분위수를 정확히 봐야 한다면 버킷 경계를 촘촘히 설계하거나 native histogram으로 전환하는 게 낫다.</p><h2 id="_4-native-histogram-쿼리" tabindex="-1"><a class="header-anchor" href="#_4-native-histogram-쿼리"><span>4. Native Histogram 쿼리</span></a></h2><p><strong>Native histogram</strong>은 버킷 각각을 별도 시계열로 노출하던 classic 방식과 달리, 하나의 시계열 안에 지수 간격 버킷 전체를 구조화된 값으로 담는다. 클라이언트가 native histogram으로 계측하면 <code>_bucket</code>/<code>_sum</code>/<code>_count</code> 시계열 폭발 없이 훨씬 높은 해상도의 버킷을 저비용으로 유지할 수 있다.</p><div class="language-promql line-numbers-mode" data-highlighter="prismjs" data-ext="promql" data-title="promql"><pre><code><span class="line"><span class="token comment"># native histogram은 le로 sum by 할 필요가 없다 — 시계열 자체가 히스토그램 값이다</span></span>
<span class="line"><span class="token function">histogram_quantile</span><span class="token punctuation">(</span><span class="token number">0.95</span><span class="token punctuation">,</span> <span class="token keyword">sum</span><span class="token punctuation">(</span><span class="token function">rate</span><span class="token punctuation">(</span>http_request_duration_seconds<span class="token context-range"><span class="token punctuation">[</span><span class="token range-duration number">5m</span><span class="token punctuation">]</span></span><span class="token punctuation">)</span><span class="token punctuation">)</span> <span class="token keyword">by</span> <span class="token vector-match"><span class="token punctuation">(</span><span class="token label-key attr-name">job</span><span class="token punctuation">)</span></span><span class="token punctuation">)</span></span>
<span class="line"></span>
<span class="line"><span class="token comment"># 히스토그램 시계열에서 평균값을 직접 추출</span></span>
<span class="line"><span class="token function">histogram_avg</span><span class="token punctuation">(</span><span class="token function">rate</span><span class="token punctuation">(</span>http_request_duration_seconds<span class="token context-range"><span class="token punctuation">[</span><span class="token range-duration number">5m</span><span class="token punctuation">]</span></span><span class="token punctuation">)</span><span class="token punctuation">)</span></span>
<span class="line"></span>
<span class="line"><span class="token comment"># 총 관측 횟수 / 총 합</span></span>
<span class="line"><span class="token function">histogram_count</span><span class="token punctuation">(</span><span class="token function">rate</span><span class="token punctuation">(</span>http_request_duration_seconds<span class="token context-range"><span class="token punctuation">[</span><span class="token range-duration number">5m</span><span class="token punctuation">]</span></span><span class="token punctuation">)</span><span class="token punctuation">)</span></span>
<span class="line"><span class="token function">histogram_sum</span><span class="token punctuation">(</span><span class="token function">rate</span><span class="token punctuation">(</span>http_request_duration_seconds<span class="token context-range"><span class="token punctuation">[</span><span class="token range-duration number">5m</span><span class="token punctuation">]</span></span><span class="token punctuation">)</span><span class="token punctuation">)</span></span>
<span class="line"></span>
<span class="line"><span class="token comment"># 특정 구간에 속하는 비율 (lower, upper]</span></span>
<span class="line">histogram_fraction<span class="token vector-match"><span class="token punctuation">(</span><span class="token label-key attr-name">0.1</span><span class="token punctuation">,</span> <span class="token label-key attr-name">0.5</span><span class="token punctuation">,</span> <span class="token label-key attr-name">rate(http_request_duration_seconds[5m</span>]<span class="token punctuation">)</span></span><span class="token punctuation">)</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><code>histogram_quantile</code>은 native histogram 시계열을 그대로 받아도 동작하도록 확장되어 있어, classic histogram과 같은 함수를 쓰지만 <strong><code>le</code> group-by가 필요 없다</strong>는 점이 가장 큰 차이다. <code>histogram_avg</code>, <code>histogram_count</code>, <code>histogram_sum</code>, <code>histogram_fraction</code>은 native histogram 전용 함수로 classic histogram에는 쓸 수 없다.</p><div class="hint-container warning"><p class="hint-container-title">native histogram은 활성화와 버전 확인이 먼저다</p><p>native histogram은 클라이언트 라이브러리가 네이티브 포맷으로 노출해야 하고, Prometheus 서버에서도 수신·저장 기능이 최신 버전 기준으로 안정화된 기능이다. 스크레이프 설정에서 native histogram 수신이 켜져 있는지, 클라이언트 계측 라이브러리가 이를 지원하는 버전인지 먼저 확인해야 한다.</p></div><h2 id="_5-offset과-modifier-—-시간-이동" tabindex="-1"><a class="header-anchor" href="#_5-offset과-modifier-—-시간-이동"><span>5. offset과 @ modifier — 시간 이동</span></a></h2><p><strong><code>offset</code></strong>은 쿼리 평가 시각을 과거로 밀어서 그 시점 기준 데이터를 가져온다.</p><div class="language-promql line-numbers-mode" data-highlighter="prismjs" data-ext="promql" data-title="promql"><pre><code><span class="line"><span class="token comment"># 24시간 전 같은 시각의 요청률과 비교</span></span>
<span class="line"><span class="token function">rate</span><span class="token punctuation">(</span>http_requests_total<span class="token context-range"><span class="token punctuation">[</span><span class="token range-duration number">5m</span><span class="token punctuation">]</span></span><span class="token punctuation">)</span></span>
<span class="line">  <span class="token operator">/</span> <span class="token function">rate</span><span class="token punctuation">(</span>http_requests_total<span class="token context-range"><span class="token punctuation">[</span><span class="token range-duration number">5m</span><span class="token punctuation">]</span></span> <span class="token keyword">offset</span> <span class="token context-range"><span class="token range-duration number">1d</span></span><span class="token punctuation">)</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><strong><code>@</code> modifier</strong>는 쿼리 평가 시각을 특정 Unix timestamp로 고정한다. <code>offset</code>이 상대 이동이라면 <code>@</code>는 절대 고정이다. 둘은 함께 쓸 수도 있다.</p><div class="language-promql line-numbers-mode" data-highlighter="prismjs" data-ext="promql" data-title="promql"><pre><code><span class="line"><span class="token comment"># 특정 시각(유닉스 타임스탬프) 기준으로 고정 평가</span></span>
<span class="line">http_requests_total @ <span class="token number">1700000000</span></span>
<span class="line"></span>
<span class="line"><span class="token comment"># 쿼리 range의 시작/끝 시각에 고정 (대시보드 변수와 조합 시 유용)</span></span>
<span class="line"><span class="token function">rate</span><span class="token punctuation">(</span>http_requests_total<span class="token context-range"><span class="token punctuation">[</span><span class="token range-duration number">5m</span><span class="token punctuation">]</span></span> @ <span class="token function">end</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><code>@</code>는 대시보드에서 &quot;항상 특정 배포 시점 기준으로 비교&quot;하는 패널이나, 알림 룰에서 특정 사건 시점을 고정 기준점으로 잡는 용도에 쓴다. 일반적인 상대 비교는 <code>offset</code> 하나로 충분하다.</p><h2 id="_6-안티패턴과-성능" tabindex="-1"><a class="header-anchor" href="#_6-안티패턴과-성능"><span>6. 안티패턴과 성능</span></a></h2><p><strong>고카디널리티 쿼리.</strong> 라벨 조합이 매우 많은 메트릭(요청 ID, 사용자 ID처럼 값이 사실상 무한한 라벨)에 와일드카드 매처를 걸면 쿼리가 수백만 개 시계열을 훑는다. 원인은 대개 계측 단계에서부터 고카디널리티 라벨을 붙인 것이므로, 쿼리를 최적화하기 전에 <a href="/study/observability/34-cardinality-cost">카디널리티 관리</a>에서 라벨 설계 자체를 점검해야 한다.</p><p><strong><code>rate</code> 이전 집계 금지.</strong> <a href="/study/observability/09-promql-basics">PromQL 기초</a>에서 다룬 것과 동일한 함정이 심화 쿼리에서도 반복된다. <code>sum(x[5m])</code>처럼 range vector를 바로 합산하는 문법 자체가 없으므로 실수하기 쉽지 않지만, <code>rate(sum_over_time(x[5m]))</code>류의 조합도 counter reset 보정을 깨뜨리므로 피한다.</p><p><strong>불필요한 정규식 매처.</strong> <code>job=~&quot;api&quot;</code>처럼 <code>=</code>로 충분한 곳에 <code>=~</code>를 쓰면 정규식 엔진 오버헤드가 매 쿼리마다 붙는다. 값이 고정된 매칭은 항상 <code>=</code>/<code>!=</code>를 우선한다.</p><p><strong>넓은 range의 즉석 집계 반복.</strong> <code>histogram_quantile</code>이나 <code>topk</code> 같은 무거운 연산을 긴 range·짧은 step으로 대시보드에서 반복 실행하면 매 새로고침마다 Prometheus에 부하가 걸린다. 자주 조회되는 무거운 쿼리는 Recording Rule로 미리 계산해두는 것이 정석이다.</p><p><img src="`+c+'" alt="느린·비싼 쿼리 진단 결정 트리 — 고카디널리티 라벨 매칭이면 라벨 설계 재검토, rate 전 집계면 순서 재배치, 반복 조회되는 무거운 집계면 Recording Rule 사전 계산, 아니면 스크레이프 간격·타임아웃 점검"><img src="'+l+'" alt="느린·비싼 쿼리 진단 결정 트리 — 고카디널리티 라벨 매칭이면 라벨 설계 재검토, rate 전 집계면 순서 재배치, 반복 조회되는 무거운 집계면 Recording Rule 사전 계산, 아니면 스크레이프 간격·타임아웃 점검"></p><div class="hint-container tip"><p class="hint-container-title">핵심 정리</p><ul><li>벡터 매칭은 기본적으로 라벨 전체 일치이며, <code>on</code>/<code>ignoring</code>으로 매칭 기준을, <code>group_left</code>/<code>group_right</code>로 many 쪽 방향을 명시한다.</li><li>서브쿼리(<code>[range:resolution]</code>)는 range 함수 결과를 다시 range로 감쌀 때만 쓰고, 비용이 크므로 남용하지 않는다.</li><li>classic histogram은 <code>le</code>를 group-by에 포함하고 rate를 먼저 적용해야 <code>histogram_quantile</code>이 정확하다.</li><li>native histogram은 <code>le</code> group-by 없이 <code>histogram_quantile</code>을 쓸 수 있고, <code>histogram_avg</code>/<code>histogram_count</code>/<code>histogram_sum</code>/<code>histogram_fraction</code> 전용 함수를 제공한다.</li><li><code>offset</code>은 상대 시간 이동, <code>@</code>는 절대 시점 고정이며 함께 쓸 수 있다.</li><li>고카디널리티 매칭, rate 전 집계, 불필요한 정규식, 반복되는 무거운 쿼리는 대표적인 성능 안티패턴이다.</li></ul></div><h2 id="다음-챕터" tabindex="-1"><a class="header-anchor" href="#다음-챕터"><span>다음 챕터</span></a></h2><p>쿼리를 매번 즉석에서 계산하는 대신 미리 계산해두고, 조건을 만족하면 자동으로 알림을 발화하는 장치가 필요하다. <a href="/study/observability/11-recording-alerting-rules">Recording·Alerting Rule</a>에서는 사전 계산 명명 규칙, <code>for</code>를 이용한 pending/firing 상태 전이, 룰 그룹 평가 비용, <code>promtool test rules</code>를 이용한 룰 테스트까지 다룬다.</p>',42)]))}const m=s(i,[["render",r],["__file","10-promql-advanced.html.vue"]]),k=JSON.parse('{"path":"/study/observability/10-promql-advanced.html","title":"PromQL 심화","lang":"en-US","frontmatter":{"title":"PromQL 심화","description":"on/ignoring과 group_left/group_right를 이용한 벡터 매칭 join 패턴, 서브쿼리, classic·native 히스토그램 쿼리의 함정, offset과 @ modifier를 이용한 시간 이동, 고카디널리티 쿼리 같은 성능 안티패턴을 실전 예제로 정리한다.","date":"2026-07-02T00:00:00.000Z","tags":["Observability","Prometheus","PromQL"],"prev":"/study/observability/09-promql-basics","next":"/study/observability/11-recording-alerting-rules"},"headers":[{"level":1,"title":"PromQL 심화","slug":"promql-심화","link":"#promql-심화","children":[{"level":2,"title":"1. 벡터 매칭 — on/ignoring, group_left/group_right","slug":"_1-벡터-매칭-—-on-ignoring-group-left-group-right","link":"#_1-벡터-매칭-—-on-ignoring-group-left-group-right","children":[]},{"level":2,"title":"2. 서브쿼리","slug":"_2-서브쿼리","link":"#_2-서브쿼리","children":[]},{"level":2,"title":"3. 히스토그램 쿼리 심화","slug":"_3-히스토그램-쿼리-심화","link":"#_3-히스토그램-쿼리-심화","children":[]},{"level":2,"title":"4. Native Histogram 쿼리","slug":"_4-native-histogram-쿼리","link":"#_4-native-histogram-쿼리","children":[]},{"level":2,"title":"5. offset과 @ modifier — 시간 이동","slug":"_5-offset과-modifier-—-시간-이동","link":"#_5-offset과-modifier-—-시간-이동","children":[]},{"level":2,"title":"6. 안티패턴과 성능","slug":"_6-안티패턴과-성능","link":"#_6-안티패턴과-성능","children":[]},{"level":2,"title":"다음 챕터","slug":"다음-챕터","link":"#다음-챕터","children":[]}]}],"git":{},"filePathRelative":"_study/observability/10-promql-advanced.md"}');export{m as comp,k as data};
