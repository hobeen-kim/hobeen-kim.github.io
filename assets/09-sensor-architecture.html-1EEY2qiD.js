import{_ as e,c as a,e as s,o as t}from"./app-DyNxEAnD.js";const l={};function o(c,n){return t(),a("div",null,n[0]||(n[0]=[s(`<h1 id="ch9-센서-드라이버-아키텍처" tabindex="-1"><a class="header-anchor" href="#ch9-센서-드라이버-아키텍처"><span>CH9. 센서 드라이버 아키텍처</span></a></h1><div class="hint-container info"><p class="hint-container-title">학습 목표</p><ul><li>센서 드라이버를 Frontend/Backend로 나누는 이유를 설명할 수 있다.</li><li>Frontend <code>AP_InertialSensor</code>가 싱글턴으로 상위 코드에 단일 API를 제공하는 구조를 이해한다.</li><li>Backend의 순수 가상함수 계약(<code>update()</code>, <code>accumulate()</code>)을 코드로 확인할 수 있다.</li><li>칩별 드라이버가 Backend를 상속받아 <code>update()</code>를 구현하는 계층을 이해한다.</li><li>백엔드가 수집한 원시 샘플이 <code>_notify_new_*_raw_sample</code> → <code>_publish_*</code> 경로로 프론트엔드에 전달되는 흐름을 따라갈 수 있다.</li><li>Baro·GPS도 동일한 Frontend/Backend 패턴을 따른다는 사실을 안다.</li></ul></div><h2 id="_1-왜-frontend-backend로-나누나" tabindex="-1"><a class="header-anchor" href="#_1-왜-frontend-backend로-나누나"><span>1. 왜 Frontend/Backend로 나누나</span></a></h2><h3 id="상위-코드가-알아야-하는-것" tabindex="-1"><a class="header-anchor" href="#상위-코드가-알아야-하는-것"><span>상위 코드가 알아야 하는 것</span></a></h3><p>비행 제어기(Copter, Plane 등)의 자세 추정 코드가 매 루프마다 필요로 하는 건 단 하나다. &quot;지금 기체가 얼마나 빠르게 회전하고 있는가&quot; — 즉 자이로 값이다. 이 코드는 자이로 값을 얻기 위해 다음 한 줄만 알면 된다.</p><div class="language-cpp line-numbers-mode" data-highlighter="prismjs" data-ext="cpp" data-title="cpp"><pre><code><span class="line">Vector3f gyro <span class="token operator">=</span> ins<span class="token punctuation">.</span><span class="token function">get_gyro</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>상위 코드는 그 자이로 값이 InvenSense의 칩에서 왔는지, Bosch의 칩에서 왔는지, SPI로 읽었는지 I2C로 읽었는지 전혀 알 필요가 없다. 이렇게 &quot;무엇을 제공하는가(인터페이스)&quot;와 &quot;어떻게 만드는가(구현)&quot;를 분리하는 것이 Frontend/Backend 설계의 핵심이다.</p><ul><li><strong>Frontend</strong>: 상위 코드가 보는 단일 창구. 칩 종류와 무관한 추상 API를 제공한다.</li><li><strong>Backend</strong>: 실제 칩과 대화하는 계층. 레지스터를 읽고 버스를 다룬다.</li></ul><h3 id="새-칩-추가가-쉬워진다" tabindex="-1"><a class="header-anchor" href="#새-칩-추가가-쉬워진다"><span>새 칩 추가가 쉬워진다</span></a></h3><p>새로운 IMU 칩을 지원해야 한다고 하자. Frontend/Backend가 분리돼 있으면 새 칩을 위한 Backend 클래스 하나만 추가하면 된다. 상위 비행 제어 코드는 한 줄도 바뀌지 않는다. 칩의 차이는 전부 Backend 안에 캡슐화돼 있기 때문이다.</p><h3 id="멀티-인스턴스를-투명하게-관리한다" tabindex="-1"><a class="header-anchor" href="#멀티-인스턴스를-투명하게-관리한다"><span>멀티 인스턴스를 투명하게 관리한다</span></a></h3><p>상용 드론은 신뢰성을 위해 IMU를 2~3개 탑재한다. 하나가 진동으로 망가져도 다른 IMU로 비행을 유지하기 위해서다. Frontend는 이 여러 개의 백엔드를 내부 배열로 관리하고, 상위 코드에는 마치 하나의 센서처럼 보이게 한다. 상위 코드는 &quot;주(primary) 자이로 값&quot;만 요청하면 되고, 어느 물리 칩이 주로 선택됐는지는 Frontend가 알아서 결정한다.</p><pre class="mermaid">classDiagram
    class AP_InertialSensor {
        +get_gyro() Vector3f
        +get_accel() Vector3f
        -_backends[] AP_InertialSensor_Backend
    }
    class AP_InertialSensor_Backend {
        &lt;&lt;abstract&gt;&gt;
        +update() bool
        +accumulate() void
        #_notify_new_gyro_raw_sample()
        #_publish_gyro()
    }
    class AP_InertialSensor_Invensensev3 {
        +update() bool
        -read_fifo() void
    }
    class AP_InertialSensor_BMI088 {
        +update() bool
    }
    AP_InertialSensor &quot;1&quot; --&gt; &quot;1..*&quot; AP_InertialSensor_Backend : manages
    AP_InertialSensor_Backend &lt;|-- AP_InertialSensor_Invensensev3
    AP_InertialSensor_Backend &lt;|-- AP_InertialSensor_BMI088
</pre><h2 id="_2-frontend-ap-inertialsensor" tabindex="-1"><a class="header-anchor" href="#_2-frontend-ap-inertialsensor"><span>2. Frontend: AP_InertialSensor</span></a></h2><p>Frontend 클래스는 <code>AP_InertialSensor</code>다.</p><div class="language-cpp line-numbers-mode" data-highlighter="prismjs" data-ext="cpp" data-title="cpp"><pre><code><span class="line"><span class="token keyword">class</span> <span class="token class-name">AP_InertialSensor</span> <span class="token operator">:</span> AP_AccelCal_Client</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>(libraries/AP_InertialSensor/AP_InertialSensor.h:50)</p><p>이 객체는 싱글턴이다. 시스템 전체에 IMU Frontend는 단 하나만 존재하고, 상위 코드는 항상 이 객체를 통해서만 관성 센서에 접근한다. 자세 추정·제어 어디서든 동일한 <code>ins</code> 객체를 호출하므로, 센서 접근 지점이 한 곳으로 모인다.</p><h3 id="get-gyro-get-accel" tabindex="-1"><a class="header-anchor" href="#get-gyro-get-accel"><span>get_gyro() / get_accel()</span></a></h3><p>상위 코드가 쓰는 두 가지 핵심 API다.</p><div class="language-cpp line-numbers-mode" data-highlighter="prismjs" data-ext="cpp" data-title="cpp"><pre><code><span class="line"><span class="token keyword">const</span> Vector3f <span class="token operator">&amp;</span><span class="token function">get_gyro</span><span class="token punctuation">(</span><span class="token keyword">void</span><span class="token punctuation">)</span> <span class="token keyword">const</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>(libraries/AP_InertialSensor/AP_InertialSensor.h:111)</p><div class="language-cpp line-numbers-mode" data-highlighter="prismjs" data-ext="cpp" data-title="cpp"><pre><code><span class="line"><span class="token keyword">const</span> Vector3f <span class="token operator">&amp;</span><span class="token function">get_accel</span><span class="token punctuation">(</span><span class="token keyword">void</span><span class="token punctuation">)</span> <span class="token keyword">const</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>(libraries/AP_InertialSensor/AP_InertialSensor.h:134)</p><p>두 함수 모두 <code>Vector3f</code>(x, y, z 3축 벡터)를 반환한다. <code>get_gyro()</code>는 각속도(rad/s), <code>get_accel()</code>은 가속도(m/s²)다. 반환형이 <code>const &amp;</code>인 점에 주목하자. 매 호출마다 값을 새로 계산하는 게 아니라, Frontend 내부에 이미 확정돼 저장된 값을 참조로 돌려준다. 즉 이 함수는 &quot;센서를 읽는&quot; 함수가 아니라 &quot;이미 읽혀서 가공된 값을 꺼내는&quot; 함수다. 실제 칩 읽기는 Backend가 별도로 수행한다.</p><h3 id="내부에-백엔드-배열을-보유한다" tabindex="-1"><a class="header-anchor" href="#내부에-백엔드-배열을-보유한다"><span>내부에 백엔드 배열을 보유한다</span></a></h3><p>Frontend는 내부에 <code>_backends[]</code> 배열을 들고 있다. 부팅 시점에 보드에 연결된 IMU 칩들을 탐지하고, 각 칩에 맞는 Backend 객체를 생성해 이 배열에 등록한다. 이후 Frontend의 <code>update()</code>가 호출될 때마다, 배열의 모든 백엔드를 순회하며 각 백엔드의 <code>update()</code>를 호출한다. 이렇게 멀티 인스턴스가 하나의 Frontend 뒤로 숨는다.</p><h2 id="_3-backend-가상함수-계약" tabindex="-1"><a class="header-anchor" href="#_3-backend-가상함수-계약"><span>3. Backend 가상함수 계약</span></a></h2><p>Backend의 기반 클래스는 <code>AP_InertialSensor_Backend</code>다.</p><div class="language-cpp line-numbers-mode" data-highlighter="prismjs" data-ext="cpp" data-title="cpp"><pre><code><span class="line"><span class="token keyword">class</span> <span class="token class-name">AP_InertialSensor_Backend</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>(libraries/AP_InertialSensor/AP_InertialSensor_Backend.h:39)</p><p>이 클래스는 모든 IMU 칩 드라이버가 따라야 하는 **계약(contract)**을 순수 가상함수로 정의한다. 계약이란 &quot;이 함수들을 반드시 구현하라&quot;는 약속이다. Frontend는 구체적인 칩이 뭔지 몰라도, 이 계약만 믿고 백엔드를 호출할 수 있다.</p><h3 id="update-—-순수-가상함수" tabindex="-1"><a class="header-anchor" href="#update-—-순수-가상함수"><span>update() — 순수 가상함수</span></a></h3><div class="language-cpp line-numbers-mode" data-highlighter="prismjs" data-ext="cpp" data-title="cpp"><pre><code><span class="line"><span class="token keyword">virtual</span> <span class="token keyword">bool</span> <span class="token function">update</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token operator">=</span> <span class="token number">0</span><span class="token punctuation">;</span> <span class="token comment">/* front end */</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>(libraries/AP_InertialSensor/AP_InertialSensor_Backend.h:54)</p><p><code>= 0</code>이 붙은 순수 가상함수다. 기반 클래스에는 구현이 없고, 상속받는 칩 드라이버가 <strong>반드시</strong> 구현해야 한다. 구현하지 않으면 그 클래스는 인스턴스화조차 되지 않는다. 주석의 <code>/* front end */</code>는 이 함수가 Frontend 컨텍스트(메인 루프)에서 호출된다는 뜻이다. <code>update()</code>는 그동안 백엔드가 모아둔 데이터를 Frontend의 출력 버퍼로 확정(publish)하는 역할을 한다.</p><h3 id="accumulate-—-빈-기본-구현-가상함수" tabindex="-1"><a class="header-anchor" href="#accumulate-—-빈-기본-구현-가상함수"><span>accumulate() — 빈 기본 구현 가상함수</span></a></h3><div class="language-cpp line-numbers-mode" data-highlighter="prismjs" data-ext="cpp" data-title="cpp"><pre><code><span class="line"><span class="token keyword">virtual</span> <span class="token keyword">void</span> <span class="token function">accumulate</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token punctuation">{</span><span class="token punctuation">}</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>(libraries/AP_InertialSensor/AP_InertialSensor_Backend.h:68)</p><p>이쪽은 <code>= 0</code>이 없는 일반 가상함수이고, 기본 구현이 빈 본문 <code>{}</code>이다. 즉 구현이 <strong>선택</strong>이다. 필요한 칩만 오버라이드하면 되고, 필요 없는 칩은 무시해도 컴파일된다. <code>accumulate()</code>는 보통 메인 루프와 별개로 센서 샘플을 누적할 때 쓰인다.</p><p>여기서 순수 가상함수와 기본 구현 가상함수의 설계 의도 차이가 드러난다. <code>update()</code>처럼 모든 칩이 반드시 해야 하는 일은 순수 가상함수로 강제하고, <code>accumulate()</code>처럼 칩에 따라 선택적인 일은 빈 기본 구현으로 둔다.</p><h3 id="같은-패턴-baro와-gps" tabindex="-1"><a class="header-anchor" href="#같은-패턴-baro와-gps"><span>같은 패턴: Baro와 GPS</span></a></h3><p>이 가상함수 계약 패턴은 IMU만의 것이 아니다. 다른 센서 백엔드도 똑같다.</p><div class="language-cpp line-numbers-mode" data-highlighter="prismjs" data-ext="cpp" data-title="cpp"><pre><code><span class="line"><span class="token keyword">class</span> <span class="token class-name">AP_Baro_Backend</span></span>
<span class="line"><span class="token comment">// ...</span></span>
<span class="line"><span class="token keyword">virtual</span> <span class="token keyword">void</span> <span class="token function">update</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token operator">=</span> <span class="token number">0</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>(libraries/AP_Baro/AP_Baro_Backend.h:5, libraries/AP_Baro/AP_Baro_Backend.h:13)</p><div class="language-cpp line-numbers-mode" data-highlighter="prismjs" data-ext="cpp" data-title="cpp"><pre><code><span class="line"><span class="token keyword">class</span> <span class="token class-name">AP_GPS_Backend</span></span>
<span class="line"><span class="token comment">// ...</span></span>
<span class="line"><span class="token keyword">virtual</span> <span class="token keyword">bool</span> <span class="token function">read</span><span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token operator">=</span> <span class="token number">0</span><span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>(libraries/AP_GPS/GPS_Backend.h:49, libraries/AP_GPS/GPS_Backend.h:61)</p><p>기압계(Baro)는 <code>update()</code>, GPS는 <code>read()</code>라는 이름으로 순수 가상함수 계약을 둔다. 함수 이름만 센서 특성에 맞게 다를 뿐, &quot;기반 클래스가 계약을 선언하고 칩 드라이버가 구현한다&quot;는 구조는 완전히 동일하다.</p><h2 id="_4-칩별-드라이버-계층" tabindex="-1"><a class="header-anchor" href="#_4-칩별-드라이버-계층"><span>4. 칩별 드라이버 계층</span></a></h2><p><code>AP_InertialSensor_Backend</code>는 추상 기반 클래스다. 실제로 칩과 대화하는 코드는 이걸 상속받은 칩별 드라이버에 들어간다.</p><pre class="mermaid">classDiagram
    class AP_InertialSensor_Backend {
        &lt;&lt;abstract&gt;&gt;
        +update() bool
    }
    class AP_InertialSensor_Invensensev3 {
        +update() bool
        -read_fifo() void
    }
    class AP_InertialSensor_BMI088 {
        +update() bool
    }
    class AP_InertialSensor_LSM9DS1 {
        +update() bool
    }
    AP_InertialSensor_Backend &lt;|-- AP_InertialSensor_Invensensev3
    AP_InertialSensor_Backend &lt;|-- AP_InertialSensor_BMI088
    AP_InertialSensor_Backend &lt;|-- AP_InertialSensor_LSM9DS1
</pre><p>예를 들어 InvenSense의 ICM-42688 계열 칩은 <code>AP_InertialSensor_Invensensev3</code> 드라이버가 담당하고, Bosch의 BMI088 칩은 <code>AP_InertialSensor_BMI088</code> 드라이버가 담당한다. 각 드라이버는 다음을 안다.</p><ul><li>칩 고유의 레지스터 주소와 초기화 시퀀스</li><li>통신 버스(SPI vs I2C)와 클럭 설정</li><li>FIFO(센서 내부 버퍼) 구조와 데이터 포맷</li></ul><p>그리고 각 드라이버는 <code>update()</code>를 자기 칩에 맞게 구현한다. InvenSensev3 드라이버라면 FIFO에서 샘플을 읽어들이는 <code>read_fifo()</code> 같은 내부 함수를 호출하고, 거기서 얻은 원시 데이터를 다음 절의 경로로 Frontend에 넘긴다. 이 모든 칩 의존적 디테일이 드라이버 안에 갇혀 있기 때문에, Frontend와 상위 코드는 깨끗하게 유지된다.</p><h2 id="_5-백엔드-→-프론트엔드-데이터-전달" tabindex="-1"><a class="header-anchor" href="#_5-백엔드-→-프론트엔드-데이터-전달"><span>5. 백엔드 → 프론트엔드 데이터 전달</span></a></h2><p>이제 핵심 질문이다. 백엔드가 칩에서 읽은 숫자가 어떻게 <code>get_gyro()</code>로 나오는가. 두 단계로 나뉜다. <strong>(1) 원시 샘플 통지</strong>와 **(2) 확정(publish)**이다.</p><h3 id="_1-원시-샘플-통지" tabindex="-1"><a class="header-anchor" href="#_1-원시-샘플-통지"><span>(1) 원시 샘플 통지</span></a></h3><p>칩 드라이버가 FIFO에서 새 샘플을 읽으면, 기반 클래스가 제공하는 헬퍼 함수로 그 샘플을 Frontend 쪽에 통지한다.</p><div class="language-cpp line-numbers-mode" data-highlighter="prismjs" data-ext="cpp" data-title="cpp"><pre><code><span class="line"><span class="token keyword">void</span> <span class="token function">_notify_new_gyro_raw_sample</span><span class="token punctuation">(</span><span class="token keyword">uint8_t</span> instance<span class="token punctuation">,</span></span>
<span class="line">                                 <span class="token keyword">const</span> Vector3f <span class="token operator">&amp;</span>accel<span class="token punctuation">,</span></span>
<span class="line">                                 <span class="token keyword">uint64_t</span> sample_us<span class="token operator">=</span><span class="token number">0</span><span class="token punctuation">)</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>(libraries/AP_InertialSensor/AP_InertialSensor_Backend.h:212)</p><div class="language-cpp line-numbers-mode" data-highlighter="prismjs" data-ext="cpp" data-title="cpp"><pre><code><span class="line"><span class="token keyword">void</span> <span class="token function">_notify_new_accel_raw_sample</span><span class="token punctuation">(</span><span class="token keyword">uint8_t</span> instance<span class="token punctuation">,</span></span>
<span class="line">                                  <span class="token keyword">const</span> Vector3f <span class="token operator">&amp;</span>accel<span class="token punctuation">,</span></span>
<span class="line">                                  <span class="token keyword">uint64_t</span> sample_us<span class="token operator">=</span><span class="token number">0</span><span class="token punctuation">,</span></span>
<span class="line">                                  <span class="token keyword">bool</span> fsync_set<span class="token operator">=</span><span class="token boolean">false</span><span class="token punctuation">)</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>(libraries/AP_InertialSensor/AP_InertialSensor_Backend.h:226)</p><p><code>instance</code>는 몇 번째 IMU인지(0, 1, 2…), <code>sample_us</code>는 샘플이 찍힌 시각이다. 이 함수들은 보통 <strong>타이머 인터럽트(또는 고속 백그라운드 스레드)</strong> 안에서 호출된다. 칩의 샘플 주파수(수 kHz)는 메인 루프 주파수(수백 Hz)보다 훨씬 높기 때문에, 메인 루프를 기다리지 않고 인터럽트가 부지런히 샘플을 받아 누적한다.</p><h3 id="필터-적용" tabindex="-1"><a class="header-anchor" href="#필터-적용"><span>필터 적용</span></a></h3><p>원시 샘플은 그대로 쓰지 않는다. 모터·프레임 진동이 섞인 노이즈를 걸러내야 한다. 그래서 통지 경로 중간에 필터가 적용된다.</p><div class="language-cpp line-numbers-mode" data-highlighter="prismjs" data-ext="cpp" data-title="cpp"><pre><code><span class="line"><span class="token keyword">void</span> <span class="token function">apply_gyro_filters</span><span class="token punctuation">(</span><span class="token keyword">const</span> <span class="token keyword">uint8_t</span> instance<span class="token punctuation">,</span> <span class="token keyword">const</span> Vector3f <span class="token operator">&amp;</span>gyro<span class="token punctuation">)</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>(libraries/AP_InertialSensor/AP_InertialSensor_Backend.h:203)</p><p>여기서 로우패스 필터, 노치 필터 등이 적용돼 진동 성분이 제거된 깨끗한 자이로 값이 만들어진다.</p><h3 id="_2-확정-publish" tabindex="-1"><a class="header-anchor" href="#_2-확정-publish"><span>(2) 확정(publish)</span></a></h3><p>인터럽트가 누적·필터링한 값은 아직 Frontend의 공개 버퍼에 들어가지 않은 &quot;중간 상태&quot;다. 이 값을 상위 코드가 읽을 수 있는 최종 값으로 확정하는 단계가 publish다.</p><div class="language-cpp line-numbers-mode" data-highlighter="prismjs" data-ext="cpp" data-title="cpp"><pre><code><span class="line"><span class="token keyword">void</span> <span class="token function">_publish_gyro</span><span class="token punctuation">(</span><span class="token keyword">uint8_t</span> instance<span class="token punctuation">,</span> <span class="token keyword">const</span> Vector3f <span class="token operator">&amp;</span>gyro<span class="token punctuation">)</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>(libraries/AP_InertialSensor/AP_InertialSensor_Backend.h:200)</p><div class="language-cpp line-numbers-mode" data-highlighter="prismjs" data-ext="cpp" data-title="cpp"><pre><code><span class="line"><span class="token keyword">void</span> <span class="token function">_publish_accel</span><span class="token punctuation">(</span><span class="token keyword">uint8_t</span> instance<span class="token punctuation">,</span> <span class="token keyword">const</span> Vector3f <span class="token operator">&amp;</span>accel<span class="token punctuation">)</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>(libraries/AP_InertialSensor/AP_InertialSensor_Backend.h:218)</p><p><code>_publish_gyro</code>/<code>_publish_accel</code>는 칩 드라이버의 <code>update()</code> 안에서(즉 메인 루프 컨텍스트에서) 호출된다. 이 함수가 호출되고 나서야 <code>get_gyro()</code>/<code>get_accel()</code>이 반환하는 값이 갱신된다. 이렇게 &quot;인터럽트는 모으고, 메인 루프가 확정한다&quot;는 분리 덕분에, 상위 코드는 루프 한 사이클 동안 일관된 센서 값을 본다. 값이 읽는 도중에 바뀌어 찢어지는(tearing) 일을 막는다.</p><pre class="mermaid">flowchart LR
    A[&quot;IMU 칩 하드웨어&lt;br&gt;(ICM-42688 등)&quot;] --&gt;|SPI / I2C| B[&quot;Backend 드라이버&lt;br&gt;read_fifo()&quot;]
    B --&gt; C[&quot;_notify_new_gyro&lt;br&gt;_raw_sample()&quot;]
    C --&gt; D[&quot;apply_gyro_filters()&lt;br&gt;(노이즈 제거)&quot;]
    D --&gt; E[&quot;인터럽트 누적 버퍼&quot;]
    E --&gt;|메인 루프 update()| F[&quot;_publish_gyro()&quot;]
    F --&gt; G[&quot;Frontend 출력 버퍼&quot;]
    G --&gt;|get_gyro()| H[&quot;비행 제어&lt;br&gt;(자세 추정·PID)&quot;]
</pre><p>이 흐름을 한 문장으로 요약하면 이렇다. <strong>칩이 SPI/I2C로 원시 샘플을 내보내면, 백엔드가 인터럽트에서 받아 필터링해 누적하고, 메인 루프의 <code>update()</code>가 그것을 publish해 Frontend 버퍼에 확정하며, 상위 코드는 <code>get_gyro()</code>로 그 확정값을 꺼내 비행 제어에 쓴다.</strong></p><h2 id="_6-gps와-baro도-같은-패턴" tabindex="-1"><a class="header-anchor" href="#_6-gps와-baro도-같은-패턴"><span>6. GPS와 Baro도 같은 패턴</span></a></h2><p>3절에서 봤듯이 Baro·GPS 백엔드도 순수 가상함수 계약을 갖는다. 데이터 흐름의 큰 그림도 IMU와 같다.</p><ul><li><strong>Baro</strong>: <code>AP_Baro</code>(Frontend) ← <code>AP_Baro_Backend</code>(추상) ← 칩별 드라이버(MS5611 등). 칩 드라이버가 <code>update()</code>로 기압·온도를 읽어 Frontend에 넘기고, 상위 고도 추정 코드는 Frontend API만 호출한다.</li><li><strong>GPS</strong>: <code>AP_GPS</code>(Frontend) ← <code>AP_GPS_Backend</code>(추상) ← 프로토콜별 드라이버(u-blox, NMEA 등). 드라이버가 <code>read()</code>로 위성 메시지를 파싱해 위치를 채운다.</li></ul><p>세 센서 모두 &quot;Frontend 싱글턴 + Backend 추상 계약 + 칩별 구현&quot;이라는 같은 골격을 공유한다. 이 일관성 덕분에 ArduPilot은 수십 종의 센서 칩을 지원하면서도 상위 코드를 깨끗하게 유지한다. 한 패턴을 이해하면 나머지 센서 코드도 같은 눈으로 읽을 수 있다.</p><div class="hint-container tip"><p class="hint-container-title">핵심 정리</p><ul><li>센서 드라이버는 **Frontend(추상 API)**와 **Backend(칩 구현)**로 분리된다. 상위 코드는 Frontend만 호출하고 칩 종류를 모른다.</li><li><code>AP_InertialSensor</code>는 싱글턴 Frontend로, <code>get_gyro()</code>/<code>get_accel()</code>로 확정된 센서 값을 참조 반환한다. 내부에 <code>_backends[]</code> 배열로 멀티 인스턴스를 관리한다.</li><li><code>AP_InertialSensor_Backend</code>는 <code>update()</code>(순수 가상, 필수)와 <code>accumulate()</code>(빈 기본 구현, 선택)로 칩 드라이버 계약을 정의한다.</li><li>칩별 드라이버(<code>AP_InertialSensor_Invensensev3</code>, <code>AP_InertialSensor_BMI088</code> 등)가 이 백엔드를 상속해 <code>update()</code>를 구현한다.</li><li>데이터는 <code>_notify_new_*_raw_sample</code>(인터럽트에서 통지·필터링) → <code>_publish_*</code>(메인 루프에서 확정) 두 단계로 Frontend에 전달된다.</li><li>Baro(<code>update()</code>), GPS(<code>read()</code>)도 동일한 Frontend/Backend 패턴을 따른다.</li></ul></div><h2 id="다음-챕터" tabindex="-1"><a class="header-anchor" href="#다음-챕터"><span>다음 챕터</span></a></h2><p>다음 챕터에서는 IMU 자체를 더 깊이 파고든다. 가속도계와 자이로스코프가 물리적으로 어떻게 동작하는지, 그리고 ArduPilot이 이 두 센서를 어떻게 캘리브레이션하고 융합하는지 살펴본다.</p><p>→ <a href="/study/ardupilot/10-imu">CH10. IMU: 가속도계와 자이로스코프</a></p>`,85)]))}const r=e(l,[["render",o],["__file","09-sensor-architecture.html.vue"]]),i=JSON.parse('{"path":"/study/ardupilot/09-sensor-architecture.html","title":"CH9. 센서 드라이버 아키텍처","lang":"en-US","frontmatter":{"title":"CH9. 센서 드라이버 아키텍처","description":"ArduPilot 센서 드라이버의 Frontend/Backend 분리 설계를 분석한다. AP_InertialSensor(frontend)와 AP_InertialSensor_Backend(backend), 칩별 드라이버까지 가상함수 계약, 데이터 전달 경로, 멀티 인스턴스 관리를 코드로 따라간다.","date":"2026-06-22T00:00:00.000Z","tags":["ArduPilot","센서","IMU","Frontend","Backend","드라이버","임베디드"],"prev":"/study/ardupilot/08-realtime-scheduling","next":"/study/ardupilot/10-imu"},"headers":[{"level":1,"title":"CH9. 센서 드라이버 아키텍처","slug":"ch9-센서-드라이버-아키텍처","link":"#ch9-센서-드라이버-아키텍처","children":[{"level":2,"title":"1. 왜 Frontend/Backend로 나누나","slug":"_1-왜-frontend-backend로-나누나","link":"#_1-왜-frontend-backend로-나누나","children":[{"level":3,"title":"상위 코드가 알아야 하는 것","slug":"상위-코드가-알아야-하는-것","link":"#상위-코드가-알아야-하는-것","children":[]},{"level":3,"title":"새 칩 추가가 쉬워진다","slug":"새-칩-추가가-쉬워진다","link":"#새-칩-추가가-쉬워진다","children":[]},{"level":3,"title":"멀티 인스턴스를 투명하게 관리한다","slug":"멀티-인스턴스를-투명하게-관리한다","link":"#멀티-인스턴스를-투명하게-관리한다","children":[]}]},{"level":2,"title":"2. Frontend: AP_InertialSensor","slug":"_2-frontend-ap-inertialsensor","link":"#_2-frontend-ap-inertialsensor","children":[{"level":3,"title":"get_gyro() / get_accel()","slug":"get-gyro-get-accel","link":"#get-gyro-get-accel","children":[]},{"level":3,"title":"내부에 백엔드 배열을 보유한다","slug":"내부에-백엔드-배열을-보유한다","link":"#내부에-백엔드-배열을-보유한다","children":[]}]},{"level":2,"title":"3. Backend 가상함수 계약","slug":"_3-backend-가상함수-계약","link":"#_3-backend-가상함수-계약","children":[{"level":3,"title":"update() — 순수 가상함수","slug":"update-—-순수-가상함수","link":"#update-—-순수-가상함수","children":[]},{"level":3,"title":"accumulate() — 빈 기본 구현 가상함수","slug":"accumulate-—-빈-기본-구현-가상함수","link":"#accumulate-—-빈-기본-구현-가상함수","children":[]},{"level":3,"title":"같은 패턴: Baro와 GPS","slug":"같은-패턴-baro와-gps","link":"#같은-패턴-baro와-gps","children":[]}]},{"level":2,"title":"4. 칩별 드라이버 계층","slug":"_4-칩별-드라이버-계층","link":"#_4-칩별-드라이버-계층","children":[]},{"level":2,"title":"5. 백엔드 → 프론트엔드 데이터 전달","slug":"_5-백엔드-→-프론트엔드-데이터-전달","link":"#_5-백엔드-→-프론트엔드-데이터-전달","children":[{"level":3,"title":"(1) 원시 샘플 통지","slug":"_1-원시-샘플-통지","link":"#_1-원시-샘플-통지","children":[]},{"level":3,"title":"필터 적용","slug":"필터-적용","link":"#필터-적용","children":[]},{"level":3,"title":"(2) 확정(publish)","slug":"_2-확정-publish","link":"#_2-확정-publish","children":[]}]},{"level":2,"title":"6. GPS와 Baro도 같은 패턴","slug":"_6-gps와-baro도-같은-패턴","link":"#_6-gps와-baro도-같은-패턴","children":[]},{"level":2,"title":"다음 챕터","slug":"다음-챕터","link":"#다음-챕터","children":[]}]}],"git":{},"filePathRelative":"_study/ardupilot/09-sensor-architecture.md"}');export{r as comp,i as data};
