import{_ as t,c as s,e as a,o as n}from"./app-Dlu9Fjob.js";const o="/images/study-observability/06-timeseries-id-light.png",i="/images/study-observability/06-timeseries-id-dark.png",r="/images/study-observability/06-histogram-vs-summary-light.png",c="/images/study-observability/06-histogram-vs-summary-dark.png",d="/images/study-observability/06-native-histogram-light.png",l="/images/study-observability/06-native-histogram-dark.png",u={};function p(m,e){return n(),s("div",null,e[0]||(e[0]=[a(`<h1 id="데이터-모델과-시계열" tabindex="-1"><a class="header-anchor" href="#데이터-모델과-시계열"><span>데이터 모델과 시계열</span></a></h1><div class="hint-container info"><p class="hint-container-title">학습 목표</p><ul><li>metric name과 라벨 집합이 어떻게 하나의 시계열(sample stream)을 유일하게 식별하는지 이해한다.</li><li>Counter·Gauge·Histogram·Summary 네 메트릭 타입의 동작 방식과 쓰임을 구분한다.</li><li>Histogram과 Summary의 client-side/server-side 계산 차이와 집계 가능성 트레이드오프를 판단한다.</li><li>Native histogram의 동작 원리와 기존 histogram 대비 카디널리티 이점을 안다.</li><li>노출 형식(text exposition, OpenMetrics)과 실전 라벨 설계 원칙을 익힌다.</li></ul></div><h2 id="_1-시계열-데이터-모델" tabindex="-1"><a class="header-anchor" href="#_1-시계열-데이터-모델"><span>1. 시계열 데이터 모델</span></a></h2><p>Prometheus에서 모든 데이터는 <strong>시계열(time series)</strong>로 저장된다. 하나의 시계열은 <strong>metric name과 라벨(label) 집합의 조합</strong>으로 유일하게 식별되고, 그 아래에 <code>(timestamp, float64 value)</code> 샘플이 시간순으로 쌓인다.</p><div class="language-text line-numbers-mode" data-highlighter="prismjs" data-ext="text" data-title="text"><pre><code><span class="line">http_requests_total{method=&quot;GET&quot;, status=&quot;200&quot;, job=&quot;api&quot;, instance=&quot;10.0.0.5:8080&quot;}</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>이 한 줄이 하나의 시계열 식별자다. <code>method</code>, <code>status</code>, <code>job</code>, <code>instance</code> 라벨 값 중 하나라도 다르면 완전히 별개의 시계열이 된다. 내부적으로는 metric name조차 <code>__name__</code>이라는 예약 라벨일 뿐이다. 즉 위 시계열은 실제로는 <code>{__name__=&quot;http_requests_total&quot;, method=&quot;GET&quot;, status=&quot;200&quot;, job=&quot;api&quot;, instance=&quot;10.0.0.5:8080&quot;}</code>라는 순수한 라벨 집합으로 취급된다. <code>{__name__=&quot;http_requests_total&quot;}</code>처럼 라벨 매처만으로 조회하는 것이 가능한 이유가 여기 있다.</p><p><img src="`+o+'" alt="시계열 식별 구조 — metric name(name)과 method·status·job·instance 라벨들이 결합해 유일한 시계열 ID(라벨 집합 조합)를 이루고, 그 아래에 (t1,v1)(t2,v2)… 샘플 스트림이 append되는 관계"><img src="'+i+`" alt="시계열 식별 구조 — metric name(name)과 method·status·job·instance 라벨들이 결합해 유일한 시계열 ID(라벨 집합 조합)를 이루고, 그 아래에 (t1,v1)(t2,v2)… 샘플 스트림이 append되는 관계"></p><p>라벨 값을 하나 바꾸는 것은 새 시계열을 만드는 것과 같다. 예컨대 <code>status=&quot;200&quot;</code>과 <code>status=&quot;404&quot;</code>는 서로 다른 두 시계열이며, <code>method</code>, <code>status</code>의 카디널리티(가능한 값의 조합 수)만큼 시계열 개수가 곱셈으로 늘어난다. 이 폭발 메커니즘은 <a href="/study/observability/04-pull-push-cardinality">04장</a>에서 다룬 카디널리티 문제의 근본 원인이다.</p><h2 id="_2-메트릭-타입" tabindex="-1"><a class="header-anchor" href="#_2-메트릭-타입"><span>2. 메트릭 타입</span></a></h2><p>Prometheus는 네 가지 메트릭 타입을 정의한다. 타입 정보는 클라이언트 라이브러리와 노출 형식(<code># TYPE</code>)에만 존재하고, TSDB 저장 단계에서는 결국 모두 동일한 실수 시계열로 저장된다는 점이 중요하다 — 타입은 <strong>클라이언트의 계약이자 PromQL 함수 선택의 힌트</strong>이지, 저장 포맷 차이가 아니다.</p><ul><li><strong>Counter</strong>는 단조 증가만 하는 누적 카운터다. 재시작하면 0으로 리셋된다. 값 자체를 보는 것이 아니라 항상 <code>rate()</code>나 <code>increase()</code>로 감싸서 증가율을 본다. Prometheus 클라이언트는 리셋을 자동 감지해 <code>rate()</code> 계산 시 보정한다.<div class="language-promql line-numbers-mode" data-highlighter="prismjs" data-ext="promql" data-title="promql"><pre><code><span class="line"><span class="token function">rate</span><span class="token punctuation">(</span>http_requests_total<span class="token context-labels"><span class="token punctuation">{</span><span class="token label-key attr-name">status</span><span class="token punctuation">=</span><span class="token label-value attr-value">&quot;500&quot;</span><span class="token punctuation">}</span></span><span class="token context-range"><span class="token punctuation">[</span><span class="token range-duration number">5m</span><span class="token punctuation">]</span></span><span class="token punctuation">)</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div></li><li><strong>Gauge</strong>는 오르내릴 수 있는 순간값이다. 현재 메모리 사용량, 큐 길이, 동시 연결 수처럼 &quot;지금 값이 얼마인가&quot;를 나타낸다. <code>rate()</code>를 쓰지 않고 값 자체나 <code>delta()</code>, <code>deriv()</code>를 쓴다.</li><li><strong>Histogram</strong>은 관측값을 미리 정의된 버킷(bucket)에 누적 카운트하는 타입이다. <code>_bucket{le=&quot;...&quot;}</code>, <code>_sum</code>, <code>_count</code> 세 종류의 시계열을 함께 노출한다. 버킷 경계를 넘는 값의 누적 개수를 세는 방식이라 <strong>client-side에서는 버킷 카운팅만</strong> 하고, 분위수(quantile) 계산은 쿼리 시점에 PromQL의 <code>histogram_quantile()</code>이 담당한다.</li><li><strong>Summary</strong>는 분위수를 <strong>client-side에서 직접 계산</strong>해 φ-quantile로 노출한다(<code>{quantile=&quot;0.5&quot;}</code>, <code>{quantile=&quot;0.9&quot;}</code> 등). <code>_sum</code>, <code>_count</code>도 함께 노출하지만 <code>_bucket</code>은 없다.</li></ul><h2 id="_3-histogram-vs-summary-—-client-side와-server-side-그리고-집계-가능성" tabindex="-1"><a class="header-anchor" href="#_3-histogram-vs-summary-—-client-side와-server-side-그리고-집계-가능성"><span>3. Histogram vs Summary — client-side와 server-side, 그리고 집계 가능성</span></a></h2><p>둘 다 분포와 지연시간류 지표를 다루지만, <strong>어디서 분위수를 계산하는가</strong>가 근본적인 차이를 만든다.</p><p><img src="`+r+'" alt="Histogram vs Summary 비교 — Histogram은 클라이언트가 버킷 카운트만 누적하고 여러 인스턴스 버킷을 sum by (le)로 합산한 뒤 PromQL histogram_quantile()로 쿼리 시점에 분위수를 계산해 집계 가능하지만, Summary는 클라이언트가 분위수를 직접 계산해 노출하므로 인스턴스별 값이 확정되어 여러 인스턴스 quantile을 평균·합산해도 의미 없어 집계 불가"><img src="'+c+`" alt="Histogram vs Summary 비교 — Histogram은 클라이언트가 버킷 카운트만 누적하고 여러 인스턴스 버킷을 sum by (le)로 합산한 뒤 PromQL histogram_quantile()로 쿼리 시점에 분위수를 계산해 집계 가능하지만, Summary는 클라이언트가 분위수를 직접 계산해 노출하므로 인스턴스별 값이 확정되어 여러 인스턴스 quantile을 평균·합산해도 의미 없어 집계 불가"></p><table><thead><tr><th>구분</th><th>Histogram</th><th>Summary</th></tr></thead><tbody><tr><td>분위수 계산 위치</td><td>서버(PromQL, 쿼리 시점)</td><td>클라이언트(계측 시점)</td></tr><tr><td>노출되는 것</td><td><code>_bucket{le}</code>, <code>_sum</code>, <code>_count</code></td><td><code>{quantile}</code>, <code>_sum</code>, <code>_count</code></td></tr><tr><td>다중 인스턴스 집계</td><td>가능 — 버킷을 <code>sum by (le)</code>로 합친 뒤 <code>histogram_quantile()</code></td><td>불가능 — quantile 값끼리 평균 내면 수학적으로 틀린다</td></tr><tr><td>정확도</td><td>버킷 경계 사이 선형 보간 → 근사치</td><td>스트리밍 알고리즘 기반 정확한 값(인스턴스 단위)</td></tr><tr><td>버킷/분위수 설계</td><td>배포 전 버킷 경계를 미리 정해야 함(도메인 지식 필요)</td><td>배포 시점에 원하는 quantile 목록 지정</td></tr><tr><td>클라이언트 비용</td><td>낮음(카운터 증가만)</td><td>상대적으로 높음(슬라이딩 윈도 계산)</td></tr></tbody></table><p><strong>집계 가능성</strong>이 실무에서 가장 크게 갈리는 지점이다. Kubernetes 환경처럼 Pod가 여러 개인 서비스의 p99 레이턴시를 보고 싶다면 Histogram을 써야 한다. 각 Pod가 노출한 <code>_bucket{le}</code> 시계열을 <code>sum by (le) (rate(...))</code>로 먼저 합산한 뒤 <code>histogram_quantile()</code>을 적용하면 서비스 전체의 분위수를 낼 수 있다.</p><div class="language-promql line-numbers-mode" data-highlighter="prismjs" data-ext="promql" data-title="promql"><pre><code><span class="line"><span class="token function">histogram_quantile</span><span class="token punctuation">(</span><span class="token number">0.99</span><span class="token punctuation">,</span></span>
<span class="line">  <span class="token keyword">sum</span> <span class="token keyword">by</span> <span class="token vector-match"><span class="token punctuation">(</span><span class="token label-key attr-name">le</span><span class="token punctuation">)</span></span> <span class="token punctuation">(</span><span class="token function">rate</span><span class="token punctuation">(</span>http_request_duration_seconds_bucket<span class="token context-range"><span class="token punctuation">[</span><span class="token range-duration number">5m</span><span class="token punctuation">]</span></span><span class="token punctuation">)</span><span class="token punctuation">)</span></span>
<span class="line"><span class="token punctuation">)</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Summary는 이 합산이 원천적으로 불가능하다. 인스턴스 A의 p99가 200ms, 인스턴스 B의 p99가 50ms라고 해서 전체 p99가 그 평균인 125ms가 되는 것이 아니다. 분위수는 분포 전체를 알아야 계산되는 값이라 사후에 합칠 수 없다. 이런 이유로 실무에서는 <strong>여러 인스턴스를 집계해야 하는 서비스 레벨 지표는 거의 항상 Histogram을 택한다.</strong> Summary는 인스턴스 단위로만 정확한 값이 필요하고 집계가 필요 없는 특수한 경우(예: 단일 프로세스 GC pause 시간)에 한정해 쓴다.</p><h2 id="_4-native-histograms" tabindex="-1"><a class="header-anchor" href="#_4-native-histograms"><span>4. Native histograms</span></a></h2><p>기존 Histogram의 가장 큰 실무 약점은 <strong>카디널리티</strong>다. 버킷 경계마다 별도 시계열이 생기므로 <code>le</code> 값이 10개면 시계열도 10배가 된다. 여기에 다른 라벨(엔드포인트, 상태 코드 등)까지 곱해지면 카디널리티가 빠르게 불어난다.</p><p><strong>Native histogram</strong>(최신 Prometheus 기준 기능)은 이 문제를 근본적으로 다르게 푼다. 버킷 경계를 사용자가 미리 정의하는 대신, <strong>지수적으로 증가하는 버킷 스킴</strong>을 자동 적용하고 그 버킷 카운트 전체를 <strong>하나의 시계열 안에 sparse 표현으로 압축</strong>해 저장한다. 즉 <code>le</code>별로 시계열을 쪼개지 않고, 단일 시계열의 값 자체가 &quot;버킷 카운트 벡터&quot;를 담는다.</p><p><img src="`+d+'" alt="Classic vs Native Histogram 카디널리티 비교 — Classic은 le=0.1·0.5·1·+Inf 버킷 경계마다 별도 시계열이 생겨 버킷 수만큼 시계열이 배수로 증가하지만, Native는 값 자체가 sparse 버킷 벡터인 단일 시계열에 지수 스킴 버킷을 담아 버킷 수와 무관하게 시계열 하나로 고해상도를 유지"><img src="'+l+`" alt="Classic vs Native Histogram 카디널리티 비교 — Classic은 le=0.1·0.5·1·+Inf 버킷 경계마다 별도 시계열이 생겨 버킷 수만큼 시계열이 배수로 증가하지만, Native는 값 자체가 sparse 버킷 벡터인 단일 시계열에 지수 스킴 버킷을 담아 버킷 수와 무관하게 시계열 하나로 고해상도를 유지"></p><p>이점은 두 가지다. 첫째, <strong>카디널리티가 버킷 개수와 무관</strong>해진다 — 버킷이 몇 개든 시계열은 여전히 하나다. 둘째, <strong>해상도가 훨씬 높다.</strong> 클래식 히스토그램은 배포 전에 정한 소수의 버킷 경계에 갇히지만, native histogram은 지수 스킴 덕분에 사실상 연속에 가까운 해상도로 분포를 표현한다. PromQL 쪽에서는 <code>histogram_quantile()</code>을 그대로 쓸 수 있어 마이그레이션 부담이 적다.</p><p>다만 아직 진화 중인 기능이라 주의가 필요하다. 서버에서 <code>--enable-feature=native-histograms</code> 같은 기능 플래그가 필요하고, 클라이언트 라이브러리도 native histogram을 지원하는 버전이어야 한다. 원격 저장(remote_write) 프로토콜과 다운스트림(Grafana, Mimir 등) 지원 범위도 계속 확장 중이므로, 도입 전 각 컴포넌트의 버전과 지원 상태를 확인해야 한다.</p><h2 id="_5-노출-형식-—-text-exposition과-openmetrics" tabindex="-1"><a class="header-anchor" href="#_5-노출-형식-—-text-exposition과-openmetrics"><span>5. 노출 형식 — text exposition과 OpenMetrics</span></a></h2><p>Prometheus가 스크레이프하는 <code>/metrics</code> 응답은 사람이 읽을 수 있는 텍스트 형식이다.</p><div class="language-text line-numbers-mode" data-highlighter="prismjs" data-ext="text" data-title="text"><pre><code><span class="line"># HELP http_requests_total Total number of HTTP requests</span>
<span class="line"># TYPE http_requests_total counter</span>
<span class="line">http_requests_total{method=&quot;GET&quot;,status=&quot;200&quot;} 27364</span>
<span class="line">http_requests_total{method=&quot;POST&quot;,status=&quot;500&quot;} 12</span>
<span class="line"></span>
<span class="line"># HELP http_request_duration_seconds Request latency</span>
<span class="line"># TYPE http_request_duration_seconds histogram</span>
<span class="line">http_request_duration_seconds_bucket{le=&quot;0.1&quot;} 24054</span>
<span class="line">http_request_duration_seconds_bucket{le=&quot;0.5&quot;} 27301</span>
<span class="line">http_request_duration_seconds_bucket{le=&quot;1&quot;} 27350</span>
<span class="line">http_request_duration_seconds_bucket{le=&quot;+Inf&quot;} 27376</span>
<span class="line">http_request_duration_seconds_sum 8734.2</span>
<span class="line">http_request_duration_seconds_count 27376</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><code># HELP</code>로 설명, <code># TYPE</code>으로 타입을 선언하고, 이후 라인이 실제 샘플이다. 이 포맷을 Prometheus 진영에서는 <strong>text-based exposition format</strong>이라 부른다.</p><p><strong>OpenMetrics</strong>는 이 포맷을 CNCF 표준으로 정식화한 상위 호환 규격이다. 몇 가지가 다르다.</p><ul><li>응답 끝에 <code># EOF</code> 라인을 강제해 응답이 잘리지 않았음을 보장한다.</li><li><strong>Exemplar</strong>를 지원한다. 히스토그램 버킷 라인 끝에 <code># {trace_id=&quot;...&quot;} 값 타임스탬프</code> 형태로 특정 관측값을 만든 트레이스를 매달 수 있어, 메트릭에서 트레이스로 바로 점프하는 상관관계(<a href="/study/observability/32-signal-correlation">32장</a> 참고)의 기반이 된다.</li><li>Counter에 <code>_total</code> 접미사를 명세 차원에서 강제한다.</li></ul><p>Prometheus는 스크레이프 시 타깃이 <code>Content-Type: application/openmetrics-text</code>로 응답하면 OpenMetrics로 파싱하고, 그렇지 않으면 기존 text 포맷으로 처리한다. 최신 클라이언트 라이브러리는 대부분 OpenMetrics 노출도 지원한다. 공식 스펙은 <a href="https://openmetrics.io/" target="_blank" rel="noopener noreferrer">OpenMetrics 프로젝트</a>에 정리돼 있다.</p><h2 id="_6-라벨-설계-원칙" tabindex="-1"><a class="header-anchor" href="#_6-라벨-설계-원칙"><span>6. 라벨 설계 원칙</span></a></h2><p>라벨 설계는 나중에 바꾸기 어렵다. 대시보드·알림 규칙·다른 팀의 쿼리가 라벨 이름에 의존하기 시작하면 변경 비용이 눈덩이처럼 불어난다. <a href="https://prometheus.io/docs/practices/naming/" target="_blank" rel="noopener noreferrer">Prometheus 네이밍 가이드</a>가 정리한 원칙을 실전 기준으로 요약한다.</p><ul><li><strong>metric name은 <code>snake_case</code>, base unit, <code>_total</code> 접미사</strong> 세 가지를 지킨다. <code>http_request_duration_seconds</code>처럼 밀리초가 아닌 초 단위(base unit)를 쓰고, Counter에는 <code>_total</code>을 붙인다(<code>http_requests_total</code>). 단위를 이름 끝에 명시하면(<code>_bytes</code>, <code>_seconds</code>) 쿼리 작성자가 변환 실수를 하지 않는다.</li><li><strong><code>__</code>(더블 언더스코어)로 시작하는 라벨은 Prometheus 내부 예약</strong>이다. <code>__name__</code>, <code>__address__</code>, 그리고 relabeling 단계에서만 보이는 <code>__meta_*</code> 라벨들이 여기 속한다. 사용자가 직접 이 네임스페이스에 라벨을 만들면 안 된다 — 실제로 만들어도 최종 relabel 단계에서 <code>__</code>로 시작하는 라벨은 자동으로 버려진다(내부용이므로).</li><li><strong><code>job</code>과 <code>instance</code>는 Prometheus가 자동으로 붙이는 예약 라벨</strong>이다. <code>job</code>은 <code>scrape_config</code>의 <code>job_name</code>, 즉 &quot;무슨 서비스인가&quot;를 나타낸다. <code>instance</code>는 <code>__address__</code>(보통 <code>host:port</code>), 즉 &quot;그 서비스의 어느 개별 타깃인가&quot;를 나타낸다. 이 둘의 조합이 사실상 &quot;어떤 프로세스에서 온 데이터인가&quot;를 결정한다.</li><li><strong>카디널리티가 무한할 수 있는 값은 라벨로 쓰지 않는다.</strong> 사용자 ID, 이메일, 요청 URL의 쿼리 파라미터, 타임스탬프 등을 라벨에 넣으면 시계열이 무한 증식한다. 이 값들이 필요하면 로그(<a href="/study/observability/16-loki-architecture">16장</a> 이후) 쪽에 남기는 것이 맞다. 04장에서 다룬 카디널리티 폭발의 실제 진입점이 바로 이 실수다.</li><li>라벨은 <strong>측정 대상을 분해하는 차원(dimension)</strong>이어야 한다. <code>status</code>, <code>method</code>, <code>region</code>처럼 값의 종류가 유한하고 예측 가능한 것만 라벨로 승격시킨다.</li></ul><div class="hint-container tip"><p class="hint-container-title">핵심 정리</p><ul><li>시계열은 metric name(<code>__name__</code>)과 라벨 집합의 조합으로 유일하게 식별되며, 라벨 값 하나가 바뀌면 새 시계열이 된다.</li><li>Counter는 <code>rate()</code>로, Gauge는 값 자체로, Histogram은 버킷 카운트로, Summary는 client-side 분위수로 다룬다.</li><li>Histogram은 버킷을 서버(PromQL)에서 집계·계산하므로 다중 인스턴스 합산이 가능하지만, Summary의 quantile은 사후 집계가 불가능하다.</li><li>Native histogram은 지수 버킷과 sparse 표현으로 버킷 수와 무관하게 카디널리티를 낮게 유지하면서 해상도를 높인다.</li><li>OpenMetrics는 text exposition format의 CNCF 표준화 버전으로, EOF 마커와 exemplar를 추가로 지원한다.</li><li>라벨 설계는 base unit·<code>_total</code> 접미사·예약 네임스페이스(<code>__</code>)·유한한 카디널리티 원칙을 지켜야 나중에 비용이 커지지 않는다.</li></ul></div><h2 id="다음-챕터" tabindex="-1"><a class="header-anchor" href="#다음-챕터"><span>다음 챕터</span></a></h2><p><a href="/study/observability/07-scraping-service-discovery">스크레이핑과 서비스 디스커버리</a>에서는 scrape_config의 실제 구조, kubernetes_sd를 비롯한 서비스 디스커버리 메커니즘, 그리고 relabeling으로 타깃과 라벨을 정교하게 통제하는 방법을 다룬다.</p>`,37)]))}const h=t(u,[["render",p],["__file","06-data-model.html.vue"]]),g=JSON.parse('{"path":"/study/observability/06-data-model.html","title":"데이터 모델과 시계열","lang":"en-US","frontmatter":{"title":"데이터 모델과 시계열","description":"metric name과 라벨 조합이 시계열을 식별하는 방식, Counter·Gauge·Histogram·Summary 네 메트릭 타입의 차이와 집계 가능성, native histogram과 노출 형식, 라벨 설계 원칙까지 Prometheus 데이터 모델을 정리한다.","date":"2026-07-02T00:00:00.000Z","tags":["Prometheus","Metrics","Histogram"],"prev":"/study/observability/05-prometheus-architecture","next":"/study/observability/07-scraping-service-discovery"},"headers":[{"level":1,"title":"데이터 모델과 시계열","slug":"데이터-모델과-시계열","link":"#데이터-모델과-시계열","children":[{"level":2,"title":"1. 시계열 데이터 모델","slug":"_1-시계열-데이터-모델","link":"#_1-시계열-데이터-모델","children":[]},{"level":2,"title":"2. 메트릭 타입","slug":"_2-메트릭-타입","link":"#_2-메트릭-타입","children":[]},{"level":2,"title":"3. Histogram vs Summary — client-side와 server-side, 그리고 집계 가능성","slug":"_3-histogram-vs-summary-—-client-side와-server-side-그리고-집계-가능성","link":"#_3-histogram-vs-summary-—-client-side와-server-side-그리고-집계-가능성","children":[]},{"level":2,"title":"4. Native histograms","slug":"_4-native-histograms","link":"#_4-native-histograms","children":[]},{"level":2,"title":"5. 노출 형식 — text exposition과 OpenMetrics","slug":"_5-노출-형식-—-text-exposition과-openmetrics","link":"#_5-노출-형식-—-text-exposition과-openmetrics","children":[]},{"level":2,"title":"6. 라벨 설계 원칙","slug":"_6-라벨-설계-원칙","link":"#_6-라벨-설계-원칙","children":[]},{"level":2,"title":"다음 챕터","slug":"다음-챕터","link":"#다음-챕터","children":[]}]}],"git":{},"filePathRelative":"_study/observability/06-data-model.md"}');export{h as comp,g as data};
