import{_ as s,c as n,e as a,o as i}from"./app-DW00K3Mq.js";const d="/images/study-android-internals/19-register-vs-stack-light.png",l="/images/study-android-internals/19-register-vs-stack-dark.png",t="/images/study-android-internals/19-dex-format-light.png",o="/images/study-android-internals/19-dex-format-dark.png",c={};function r(p,e){return i(),n("div",null,e[0]||(e[0]=[a('<h1 id="ch19-dalvik과-dex" tabindex="-1"><a class="header-anchor" href="#ch19-dalvik과-dex"><span>CH19. Dalvik과 DEX</span></a></h1><p>Dalvik VM 자체는 <a href="/study/android-internals/20-art-internals">ART</a>로 대체돼 역사 속으로 사라졌지만, 그 유산인 <strong>DEX 파일 포맷</strong>은 지금도 모든 안드로이드 앱의 실행 단위로 그대로 남아 있다. 이 챕터는 역사보다 현재도 유효한 DEX 포맷에 비중을 둔다. 레지스터 기반 설계가 왜 나왔는지, DEX 파일이 어떻게 생겼는지, 그리고 바이트코드를 손으로 읽는 법을 익힌다. 디컴파일·최적화·보안 분석의 바닥에 깔린 지식이다.</p><h2 id="학습-목표" tabindex="-1"><a class="header-anchor" href="#학습-목표"><span>학습 목표</span></a></h2><ul><li>레지스터 기반 Dalvik과 스택 기반 JVM의 차이, 그리고 별도 VM을 만든 이유를 이해한다.</li><li>DEX 파일 포맷의 header·인덱스 테이블·code_item 구조를 파악한다.</li><li>65536 메서드 한계가 어디서 오는지와 multidex 해법을 안다.</li><li>주요 DEX opcode와 smali 문법으로 간단한 메서드를 읽는다.</li><li>dexdump/dexlayout 같은 도구로 DEX를 검사하는 법을 익힌다.</li></ul><h2 id="dalvik-vs-jvm" tabindex="-1"><a class="header-anchor" href="#dalvik-vs-jvm"><span>Dalvik vs JVM</span></a></h2><p>Dalvik은 2008년 안드로이드와 함께 등장한 가상 머신이다. 자바 소스를 컴파일한 <code>.class</code>(JVM 바이트코드)를 그대로 쓰지 않고, <code>dx</code>(현재는 <code>d8</code>)로 변환한 <strong>DEX(Dalvik Executable)</strong>를 실행했다. 왜 굳이 별도 VM과 포맷을 만들었을까.</p><p>가장 근본적인 차이는 <strong>레지스터 기반 vs 스택 기반</strong>이다.</p><p><img src="'+d+'" alt="스택 기반 JVM과 레지스터 기반 DEX의 바이트코드 대비 — 같은 덧셈을 스택은 4개 명령으로 레지스터는 1개 명령으로 표현"><img src="'+l+'" alt="스택 기반 JVM과 레지스터 기반 DEX의 바이트코드 대비 — 같은 덧셈을 스택은 4개 명령으로 레지스터는 1개 명령으로 표현"></p><p>JVM은 <strong>스택 기반</strong>이다. <code>a = b + c</code>를 하려면 b를 스택에 push(<code>iload</code>), c를 push(<code>iload</code>), 더해서(<code>iadd</code>) 결과를 스택에 남기고, 다시 변수에 저장(<code>istore</code>)한다—명령 4개. 각 명령은 짧지만 피연산자를 스택에서 매번 넣고 빼야 해 명령 수가 많고, 그만큼 명령 인출(fetch/decode) 오버헤드가 크다.</p><p>Dalvik은 <strong>레지스터 기반</strong>이다. 메서드마다 여러 가상 레지스터(<code>v0</code>, <code>v1</code>, ...)가 있고, <code>add-int v0, v1, v2</code>처럼 피연산자와 목적지를 명령 하나에 직접 담는다—명령 1개. 명령 수가 줄고 명령당 정보량이 많아, 인터프리터가 명령을 인출·디코드하는 횟수가 줄어든다. 초기 모바일처럼 인터프리터 오버헤드가 크리티컬한 환경에서 유리한 선택이었다.</p><p>DEX를 만든 다른 이유는 <strong>크기와 공유</strong>다. 앱은 여러 <code>.class</code>로 나뉘지만 DEX는 모든 클래스를 <strong>하나의 파일</strong>로 묶고, 문자열·타입·메서드 이름 같은 상수를 <strong>공유 풀</strong>에 한 번만 저장한다. 여러 <code>.class</code>가 각자 상수 풀을 갖는 JVM 대비 중복이 크게 줄어 APK 크기가 작아진다.</p><p><strong>Dalvik에서 ART로의 전환</strong>은 간결히만 짚는다. Dalvik은 JIT(Just-In-Time) 인터프리터였다. Android 4.4에서 실험적으로 ART가 등장하고, <strong>5.0에서 ART가 Dalvik을 완전히 대체</strong>했다. ART는 초기엔 순수 AOT(설치 시 전체 컴파일)였다가 7.0에서 다시 인터프리터+JIT+프로파일 기반 AOT 하이브리드로 진화한다. 이 이야기는 <a href="/study/android-internals/20-art-internals">CH20</a>에서 다룬다. 중요한 것은 <strong>실행 엔진은 바뀌었어도 입력 포맷인 DEX는 그대로</strong>라는 점이다.</p><h2 id="dex-파일-포맷-심층" tabindex="-1"><a class="header-anchor" href="#dex-파일-포맷-심층"><span>DEX 파일 포맷 심층</span></a></h2><p><code>classes.dex</code>는 잘 정의된 바이너리 구조다. 크게 <strong>header → 인덱스 테이블들 → data</strong> 순으로 배치된다.</p><p><img src="'+t+'" alt="classes.dex의 섹션 순서(header·string_ids·type_ids·proto_ids·field_ids·method_ids·class_defs·data)와 method_id가 type_id·proto_id·string_id를 참조하는 관계"><img src="'+o+`" alt="classes.dex의 섹션 순서(header·string_ids·type_ids·proto_ids·field_ids·method_ids·class_defs·data)와 method_id가 type_id·proto_id·string_id를 참조하는 관계"></p><ul><li><strong>header</strong>는 파일 시작에 온다. 매직(<code>dex\\n035\\0</code> 등 버전), 파일 전체의 Adler-32 <strong>checksum</strong>, SHA-1 <strong>signature</strong>, 파일 크기, 그리고 뒤따르는 각 섹션의 개수와 오프셋이 담긴다. 런타임은 checksum·signature로 무결성을 먼저 확인한다.</li><li><strong>string_ids</strong>는 파일 안 모든 문자열 상수(클래스명·메서드명·리터럴)의 인덱스다. 실제 문자열 바이트는 data 영역에 있고 여기엔 오프셋만 있다.</li><li><strong>type_ids</strong>는 타입(클래스·인터페이스·기본형)의 인덱스로, 각 항목은 string_ids를 가리켜 타입 이름을 참조한다.</li><li><strong>proto_ids</strong>는 메서드 <strong>시그니처(프로토타입)</strong>—반환 타입과 파라미터 타입 목록—의 인덱스다.</li><li><strong>field_ids</strong>는 필드(정의 클래스·타입·이름)의 인덱스, <strong>method_ids</strong>는 메서드(정의 클래스·시그니처·이름)의 인덱스다. 그림처럼 method_id 하나가 type_id(정의 클래스)·proto_id(시그니처)·string_id(메서드명)를 동시에 참조한다.</li><li><strong>class_defs</strong>는 클래스 정의다. 슈퍼클래스, 구현 인터페이스, 필드·메서드 목록, 그리고 각 메서드의 코드로 이어지는 링크를 담는다.</li><li><strong>data / code_item</strong>은 실제 본문이다. 문자열 바이트, 그리고 각 메서드의 <strong>code_item</strong>—사용 레지스터 수, 인자 개수, 바이트코드 명령 배열, try/catch 정보, 디버그 정보—이 여기 들어간다.</li></ul><p>이 구조 덕에 여러 DEX가 문자열·타입 풀을 공유하는 효과를 내고, 인덱스 참조로 중복을 없앤다. 포맷을 눈으로 확인하려면 <code>dexdump</code>를 쓴다.</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line"><span class="token comment"># APK에서 DEX 추출</span></span>
<span class="line"><span class="token function">unzip</span> app.apk classes.dex</span>
<span class="line"></span>
<span class="line"><span class="token comment"># 헤더·클래스·메서드 덤프</span></span>
<span class="line">dexdump <span class="token parameter variable">-f</span> classes.dex <span class="token operator">|</span> <span class="token function">sed</span> <span class="token parameter variable">-n</span> <span class="token string">&#39;1,30p&#39;</span>     <span class="token comment"># 헤더(-f)</span></span>
<span class="line">dexdump <span class="token parameter variable">-d</span> classes.dex <span class="token operator">|</span> <span class="token function">sed</span> <span class="token parameter variable">-n</span> <span class="token string">&#39;1,60p&#39;</span>     <span class="token comment"># 바이트코드 디스어셈블(-d)</span></span>
<span class="line"></span>
<span class="line"><span class="token comment"># dexlayout: 프로파일 기반 재배치·통계</span></span>
<span class="line">dexlayout <span class="token parameter variable">-o</span> /tmp <span class="token parameter variable">-w</span> /tmp classes.dex</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><code>dexlayout</code>은 DEX를 재배치(hot 메서드를 앞으로 모으는 등)하거나 구조 통계를 뽑는 도구로, ART의 CDEX 최적화와도 관련된다.</p><h2 id="_65536-메서드-한계와-multidex" tabindex="-1"><a class="header-anchor" href="#_65536-메서드-한계와-multidex"><span>65536 메서드 한계와 multidex</span></a></h2><p>DEX에서 method_ids를 참조하는 명령(<code>invoke-*</code>)의 메서드 인덱스는 <strong>16비트</strong>다. 그래서 하나의 DEX가 참조할 수 있는 메서드는 <strong>2^16 = 65536개</strong>가 상한이다. 이 숫자는 앱이 <em>정의한</em> 메서드뿐 아니라 <strong>참조하는 모든 메서드</strong>(프레임워크·라이브러리 포함)를 합친 값이라, 의존성이 많은 앱은 의외로 쉽게 넘어선다. 넘으면 빌드가 그 유명한 <code>Too many field/method references</code> 오류로 실패한다.</p><p>해법이 <strong>multidex</strong>다. 클래스를 여러 DEX 파일(<code>classes.dex</code>, <code>classes2.dex</code>, <code>classes3.dex</code>, ...)로 나눠 각 DEX가 자신의 64K 한계를 따로 갖게 한다. 각 DEX가 자체 인덱스 공간을 가지므로 전체 메서드 수는 사실상 제한이 없어진다.</p><div class="language-text line-numbers-mode" data-highlighter="prismjs" data-ext="text" data-title="text"><pre><code><span class="line">app.apk</span>
<span class="line"> ├─ classes.dex     ← 첫 64K 메서드 참조</span>
<span class="line"> ├─ classes2.dex    ← 다음 묶음</span>
<span class="line"> └─ classes3.dex    ← ...</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>Android 5.0(ART) 이상에서는 런타임이 모든 <code>classesN.dex</code>를 기본으로 로드하므로 multidex가 투명하게 동작한다. 5.0 이전 Dalvik에서는 부팅 시 첫 DEX만 자동 로드돼, <code>MultiDex.install()</code>을 앱 초기화에서 명시적으로 불러 나머지를 로드해야 했다. <code>field_ids</code> 참조도 같은 16비트 제약을 받는다.</p><h2 id="dex-바이트코드-읽기" tabindex="-1"><a class="header-anchor" href="#dex-바이트코드-읽기"><span>DEX 바이트코드 읽기</span></a></h2><p>바이트코드를 직접 읽을 수 있으면 디컴파일 결과를 검증하고 최적화·난독화를 이해하는 데 큰 도움이 된다. 사람이 읽기 쉬운 형태가 <strong>smali</strong>(baksmali가 만드는 어셈블리 표기)다.</p><p>먼저 자주 보는 opcode 부류를 알아둔다.</p><ul><li><strong>이동/상수:</strong> <code>move v0, v1</code>(레지스터 복사), <code>const/4 v0, 0x1</code>·<code>const-string v0, &quot;hi&quot;</code>(상수 로드).</li><li><strong>연산:</strong> <code>add-int v0, v1, v2</code>, <code>mul-int/lit8 v0, v1, 0x2</code>(리터럴 곱), <code>sub-int</code> 등. 접미사(<code>-int</code>, <code>-long</code>, <code>-float</code>)가 타입을 지정한다.</li><li><strong>필드 접근:</strong> <code>iget</code>/<code>iput</code>(인스턴스 필드), <code>sget</code>/<code>sput</code>(정적 필드).</li><li><strong>메서드 호출:</strong> <code>invoke-virtual</code>(가상 디스패치), <code>invoke-static</code>, <code>invoke-direct</code>(생성자·private), <code>invoke-super</code>. 반환값은 뒤이은 <code>move-result</code>로 받는다.</li><li><strong>분기/반환:</strong> <code>if-eqz v0, :label</code>(0이면 점프), <code>goto</code>, <code>return-void</code>, <code>return v0</code>.</li></ul><p>이제 간단한 메서드를 smali로 읽어보자. 자바 원본은 다음과 같다.</p><div class="language-java line-numbers-mode" data-highlighter="prismjs" data-ext="java" data-title="java"><pre><code><span class="line"><span class="token keyword">int</span> <span class="token function">square</span><span class="token punctuation">(</span><span class="token keyword">int</span> x<span class="token punctuation">)</span> <span class="token punctuation">{</span></span>
<span class="line">    <span class="token keyword">return</span> x <span class="token operator">*</span> x<span class="token punctuation">;</span></span>
<span class="line"><span class="token punctuation">}</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>이를 baksmali로 풀면 대략 이렇게 나온다.</p><div class="language-text line-numbers-mode" data-highlighter="prismjs" data-ext="text" data-title="text"><pre><code><span class="line">.method square(I)I</span>
<span class="line">    .registers 3            # v0, v1(=this는 p0), 파라미터 포함 총 3개 레지스터</span>
<span class="line">    .param p1, &quot;x&quot;          # p1 = 첫 번째 파라미터 x</span>
<span class="line"></span>
<span class="line">    mul-int v0, p1, p1      # v0 = x * x</span>
<span class="line">    return v0               # v0 반환</span>
<span class="line">.end method</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>읽는 요령은 이렇다. <code>.registers</code>는 이 메서드가 쓰는 레지스터 총수이고, <code>p0</code>는 인스턴스 메서드의 <code>this</code>, <code>p1</code>부터가 실제 파라미터다(파라미터 레지스터 <code>pN</code>은 지역 레지스터 <code>vN</code> 뒤에 매핑된다). <code>mul-int v0, p1, p1</code>은 &quot;p1과 p1을 곱해 v0에 넣어라&quot;, <code>return v0</code>은 v0을 반환하라는 뜻이다. 시그니처 <code>(I)I</code>는 proto—<code>int</code> 하나를 받아 <code>int</code>를 반환—를 나타낸다.</p><p>조금 더 복잡한 조건 분기도 패턴은 같다.</p><div class="language-text line-numbers-mode" data-highlighter="prismjs" data-ext="text" data-title="text"><pre><code><span class="line">.method isPositive(I)Z</span>
<span class="line">    .registers 2</span>
<span class="line">    if-lez p1, :not_pos     # p1 &lt;= 0 이면 :not_pos로 점프</span>
<span class="line">    const/4 v0, 0x1         # v0 = true</span>
<span class="line">    return v0</span>
<span class="line">    :not_pos</span>
<span class="line">    const/4 v0, 0x0         # v0 = false</span>
<span class="line">    return v0</span>
<span class="line">.end method</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><code>if-lez</code>(less-than-or-equal-zero)로 분기하고, 레이블(<code>:not_pos</code>)로 흐름이 갈린다. 이 정도 문법만 익혀도 jadx가 자바로 복원하지 못한 난독화 코드나 최적화 흔적을 바이트코드 수준에서 따라갈 수 있다. smali를 만들고 되돌리는 도구는 <a href="/study/android-internals/18-app-anatomy">CH18의 디컴파일 절</a>에서 소개한 apktool/baksmali다.</p><h2 id="dalvik-jni와-레거시" tabindex="-1"><a class="header-anchor" href="#dalvik-jni와-레거시"><span>Dalvik JNI와 레거시</span></a></h2><p>Dalvik 시절의 <a href="/study/android-internals/18-app-anatomy">JNI</a> 구현도 간결히만 언급한다. Dalvik은 네이티브 메서드 호출 시 인디렉트 레퍼런스 테이블로 로컬/글로벌 레퍼런스를 관리했고, 이 기본 규약은 ART에서도 이어진다. Dalvik 고유의 <code>dexopt</code>(설치 시 DEX를 최적화한 <code>odex</code> 생성)와 verification 단계는 ART의 dex2oat·VDEX로 대체됐다. 즉 <strong>개념적 뼈대는 유지되고 구현이 현대화</strong>됐다고 보면 된다. 오늘날 실무에서 순수 Dalvik을 만날 일은 없지만, DEX 포맷과 JNI 규약이라는 유산은 여전히 매일 마주치는 대상이다.</p><div class="hint-container tip"><p class="hint-container-title">핵심 정리</p><ul><li>Dalvik은 레지스터 기반 VM으로, 스택 기반 JVM보다 명령 수가 적어 인터프리터 오버헤드를 줄이려는 설계였고, DEX는 상수 풀 공유로 앱 크기를 줄인다.</li><li>DEX는 header → string/type/proto/field/method_ids → class_defs → data(code_item) 구조이며, method_id가 type/proto/string_id를 참조한다.</li><li>invoke 명령의 메서드 인덱스가 16비트라 DEX당 65536 메서드 한계가 생기고, multidex(classesN.dex)로 이를 넘는다.</li><li>DEX 바이트코드는 레지스터 명령(add-int v0, v1, v2 등)이며, smali 표기로 .registers·pN·invoke-<em>·if-</em> 패턴을 읽을 수 있다.</li><li>dexdump/dexlayout으로 DEX 구조를 검사하며, Dalvik의 dexopt·JNI 규약은 ART에서 현대화돼 이어진다.</li></ul></div><h2 id="다음-챕터" tabindex="-1"><a class="header-anchor" href="#다음-챕터"><span>다음 챕터</span></a></h2><p><a href="/study/android-internals/20-art-internals">CH20. ART 내부 구조</a>에서는 com.android.art APEX 구성, OAT/ART/VDEX/CDEX 파일의 역할, dex2oat 컴파일 전략과 compiler filter, JIT·프로파일·CC GC, 그리고 임베디드 기기의 first-boot dexopt 최적화를 다룬다.</p>`,41)]))}const m=s(c,[["render",r],["__file","19-dalvik-dex.html.vue"]]),u=JSON.parse('{"path":"/study/android-internals/19-dalvik-dex.html","title":"CH19. Dalvik과 DEX","lang":"en-US","frontmatter":{"title":"CH19. Dalvik과 DEX","description":"레지스터 기반 Dalvik VM이 스택 기반 JVM과 무엇이 다른지, DEX 파일 포맷의 헤더와 인덱스 테이블·code_item 구조, 65536 메서드 한계와 multidex, 그리고 DEX 바이트코드와 smali를 손으로 읽는 법까지 안드로이드 실행 포맷의 근본을 파고든다.","date":"2026-07-13T00:00:00.000Z","tags":["android","aosp","dalvik","dex","bytecode"]},"headers":[{"level":1,"title":"CH19. Dalvik과 DEX","slug":"ch19-dalvik과-dex","link":"#ch19-dalvik과-dex","children":[{"level":2,"title":"학습 목표","slug":"학습-목표","link":"#학습-목표","children":[]},{"level":2,"title":"Dalvik vs JVM","slug":"dalvik-vs-jvm","link":"#dalvik-vs-jvm","children":[]},{"level":2,"title":"DEX 파일 포맷 심층","slug":"dex-파일-포맷-심층","link":"#dex-파일-포맷-심층","children":[]},{"level":2,"title":"65536 메서드 한계와 multidex","slug":"_65536-메서드-한계와-multidex","link":"#_65536-메서드-한계와-multidex","children":[]},{"level":2,"title":"DEX 바이트코드 읽기","slug":"dex-바이트코드-읽기","link":"#dex-바이트코드-읽기","children":[]},{"level":2,"title":"Dalvik JNI와 레거시","slug":"dalvik-jni와-레거시","link":"#dalvik-jni와-레거시","children":[]},{"level":2,"title":"다음 챕터","slug":"다음-챕터","link":"#다음-챕터","children":[]}]}],"git":{},"filePathRelative":"_study/android-internals/19-dalvik-dex.md"}');export{m as comp,u as data};
